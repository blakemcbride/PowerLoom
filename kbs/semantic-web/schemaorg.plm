;;; -*- Mode: Lisp; Package: STELLA; Syntax: COMMON-LISP; Base: 10 -*-

;;; schemaorg.rdf

#|
;;; This is a translation of the schema.org ontology v3.2
;;; (see http://schema.org/ and https://github.com/schemaorg/schemaorg for more information).

;;; Dependencies: webcore.plm, rdfx.plm, owl.plm, dcelements.plm

;;; Translation process:
STELLA(2): (load-configuration-file "~/Projects/powerloom/sources/kagg/config/TAC-KBP-2015-coldstart-lisp-test.dat")
STELLA(3): (load "PL:sources;kagg;scripts;TAC-KBP;TAC-KBP-initialize.plm")
STELLA(4): (load "dcelements")
STELLA(5): (DEFMODULE "SCHEMA.ORG"
    :documentation "Module that defines the Schema.org vocabulary."
    :includes ("WEB-CORE") :case-sensitive? TRUE)
STELLA(6): (set-property "schema.orgMap"
  (semweb/define-namespace-map
    :base-map (semweb/define-namespace-map)
    ;; we are mapping DCT onto DC here for a smaller import requirement footprint:
    (quote (:namespace "dct" :uri "http://purl.org/dc/terms/" :module "/XMLNS/DC" :style :bare-name))
    (quote (:namespace "schema" :uri "http://schema.org/" :module "SCHEMA.ORG" :style :bare-name :default? TRUE))))
STELLA(7): (eval (semweb/owl-to-powerloom
 "PL:kbs;semantic-web;schemaorg.rdf"
 "PL:kbs;semantic-web;schemaorg.plm"
 (get-property "schema.orgMap")
 null
 (get SCHEMA.ORG)))
STELLA(8): (eval (semweb/owl-to-powerloom
 "PL:kbs;semantic-web;schemaorg-ext-meta.rdf"
 "PL:kbs;semantic-web;schemaorg-ext-meta.plm"
 (get-property "schema.orgMap")
 null
 (get SCHEMA.ORG)))

;;; minor post-editing for module definition, inclusion of ext-meta definitions, and commenting of redundant info
|#

(CL:IN-PACKAGE "STELLA")

(DEFMODULE "SCHEMA.ORG"
    :documentation "Module that defines the Schema.org vocabulary."
    :includes ("WEB-CORE") :case-sensitive? TRUE)

(IN-MODULE "SCHEMA.ORG")


;;; merged-in definitions from schemaorg-ext-meta.rdf:

(DEFRELATION supersededBy (?X ?Y)
  :DOCUMENTATION "Relates a term (i.e. a property, class or enumeration) to one that supersedes it."
  ;:BINARY-RELATION TRUE
  :AXIOMS (isPartOf supersededBy |http://meta.schema.org/|)
  :AXIOMS (rangeIncludes supersededBy Property)
  :AXIOMS (domainIncludes supersededBy Class)
  :AXIOMS (rangeIncludes supersededBy Enumeration)
  :AXIOMS (domainIncludes supersededBy Property)
  :AXIOMS (rangeIncludes supersededBy Class)
  :AXIOMS (/XMLNS/RDFS/label supersededBy "supersededBy")
  :AXIOMS (domainIncludes supersededBy Enumeration))

(DEFRELATION inverseOf (?X ?Y)
  :DOCUMENTATION "Relates a property to a property that is its inverse. Inverse properties relate the same pairs of items to each other, but in reversed direction. For example, the 'alumni' and 'alumniOf' properties are inverseOf each other. Some properties don't have explicit inverses; in these situations RDFa and JSON-LD syntax for reverse properties can be used."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label inverseOf "inverseOf")
  :AXIOMS (isPartOf inverseOf |http://meta.schema.org/|)
  :AXIOMS (rangeIncludes inverseOf Property)
  :AXIOMS (domainIncludes inverseOf Property))

(DEFRELATION rangeIncludes (?X ?Y)
  :DOCUMENTATION "Relates a property to a class that constitutes (one of) the expected type(s) for values of the property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes rangeIncludes Class)
  :AXIOMS (domainIncludes rangeIncludes Property)
  :AXIOMS (/XMLNS/RDFS/label rangeIncludes "rangeIncludes")
  :AXIOMS (isPartOf rangeIncludes |http://meta.schema.org/|))

(DEFRELATION domainIncludes (?X ?Y)
  :DOCUMENTATION "Relates a property to a class that is (one of) the type(s) the property is expected to be used on."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label domainIncludes "domainIncludes")
  :AXIOMS (domainIncludes domainIncludes Property)
  :AXIOMS (rangeIncludes domainIncludes Class)
  :AXIOMS (isPartOf domainIncludes |http://meta.schema.org/|))

;;; additional allowed domains:
(ASSERT (domainIncludes category Property))
(ASSERT (domainIncludes category Class))

(DEFCONCEPT Class (?X)
  :DOCUMENTATION "A class, also often called a 'Type'; equivalent to rdfs:Class."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Class "Class")
  :AXIOMS (isPartOf Class |http://meta.schema.org/|))

(DEFCONCEPT Property (?X)
  :DOCUMENTATION "A property, used to indicate attributes and relationships of some Thing; equivalent to rdf:Property."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Property "Property")
  :AXIOMS (isPartOf Property |http://meta.schema.org/|))


;;; schemaorg.rdf

(DEFCONCEPT CafeOrCoffeeShop (?X)
  :DOCUMENTATION "A cafe or coffee shop."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label CafeOrCoffeeShop "CafeOrCoffeeShop"))

(DEFRELATION upvoteCount (?X ?Y)
  :DOCUMENTATION "The number of upvotes this question, answer or comment has received from the community."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes upvoteCount Integer)
  :AXIOMS (domainIncludes upvoteCount Question)
  :AXIOMS (/XMLNS/RDFS/label upvoteCount "upvoteCount")
  :AXIOMS (domainIncludes upvoteCount Comment))

(DEFRELATION owns (?X ?Y)
  :DOCUMENTATION "Products owned by the organization or person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source owns |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes owns Organization)
  :AXIOMS (rangeIncludes owns Product)
  :AXIOMS (/XMLNS/RDFS/label owns "owns")
  :AXIOMS (domainIncludes owns Person)
  :AXIOMS (rangeIncludes owns OwnershipInfo))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|
   "This term <a href=\"http://blog.schema.org/2012/11/good-relations-and-schemaorg.html\">uses</a> terminology from the GoodRelations Vocabulary for E-Commerce, created by Martin Hepp. GoodRelations is a data model for sharing e-commerce data on the Web. More information about GoodRelations can be found at <a href=\"http://purl.org/goodrelations/\">http://purl.org/goodrelations/</a>.")
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms| "GoodRelationsTerms")))

(DEFCONCEPT PerformingArtsTheater (?X)
  :DOCUMENTATION "A theater or other performing art center."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label PerformingArtsTheater "PerformingArtsTheater"))

(DEFRELATION broadcastChannelId (?X ?Y)
  :DOCUMENTATION "The unique address by which the BroadcastService can be identified in a provider lineup. In US, this is typically a number."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label broadcastChannelId "broadcastChannelId")
  :AXIOMS (rangeIncludes broadcastChannelId Text)
  :AXIOMS (domainIncludes broadcastChannelId BroadcastChannel))

(DEFRELATION productionCompany (?X ?Y)
  :DOCUMENTATION "The production company or studio responsible for the item e.g. series, video game, episode etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes productionCompany MovieSeries)
  :AXIOMS (domainIncludes productionCompany RadioSeries)
  :AXIOMS (domainIncludes productionCompany Movie)
  :AXIOMS (domainIncludes productionCompany TVSeries)
  :AXIOMS (/XMLNS/RDFS/label productionCompany "productionCompany")
  :AXIOMS (domainIncludes productionCompany MediaObject)
  :AXIOMS (domainIncludes productionCompany Episode)
  :AXIOMS (domainIncludes productionCompany CreativeWorkSeason)
  :AXIOMS (domainIncludes productionCompany VideoGameSeries)
  :AXIOMS (rangeIncludes productionCompany Organization))

(DEFRELATION temporal (?X ?Y)
  :DOCUMENTATION "The range of temporal applicability of a dataset, e.g. for a 2011 census dataset, the year 2011 (in ISO 8601 time interval format)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy temporal temporalCoverage)
  :AXIOMS (domainIncludes temporal Dataset)
  :AXIOMS (/XMLNS/RDFS/label temporal "temporal")
  :AXIOMS (rangeIncludes temporal DateTime))

(DEFRELATION temporalCoverage (?X ?Y)
  :DOCUMENTATION "The temporalCoverage of a CreativeWork indicates the period that the content applies to, i.e. that it describes, either as a DateTime or as a textual string indicating a time period in <a href=\"https://en.wikipedia.org/wiki/ISO_8601#Time_intervals\">ISO 8601 time interval format</a>. In
      the case of a Dataset it will typically indicate the relevant time period in a precise notation (e.g. for a 2011 census dataset, the year 2011 would be written \"2011/2012\"). Other forms of content e.g. ScholarlyArticle, Book, TVSeries or TVEpisode may indicate their temporalCoverage in broader terms - textually or via well-known URL.
      Written works such as books may sometimes have precise temporal coverage too, e.g. a work set in 1939 - 1945 can be indicated in ISO 8601 interval format format via \"1939/1945\"."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes temporalCoverage CreativeWork)
  :AXIOMS (rangeIncludes temporalCoverage Text)
  :AXIOMS (/XMLNS/RDFS/label temporalCoverage "temporalCoverage")
  :AXIOMS (rangeIncludes temporalCoverage URL)
  :AXIOMS (/XMLNS/OWL/equivalentProperty temporalCoverage /XMLNS/DC/temporal)
  :AXIOMS (rangeIncludes temporalCoverage DateTime))

(DEFRELATION hasDigitalDocumentPermission (?X ?Y)
  :DOCUMENTATION "A permission related to the access to this document (e.g. permission to read or write an electronic document). For a public document, specify a grantee with an Audience with audienceType equal to \"public\"."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes hasDigitalDocumentPermission DigitalDocumentPermission)
  :AXIOMS (domainIncludes hasDigitalDocumentPermission DigitalDocument)
  :AXIOMS (/XMLNS/RDFS/label hasDigitalDocumentPermission "hasDigitalDocumentPermission"))

(DEFRELATION seeks (?X ?Y)
  :DOCUMENTATION "A pointer to products or services sought by the organization or person (demand)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source seeks |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes seeks Person)
  :AXIOMS (rangeIncludes seeks Demand)
  :AXIOMS (/XMLNS/RDFS/label seeks "seeks")
  :AXIOMS (domainIncludes seeks Organization))

(DEFCONCEPT DrinkAction (?X)
  :DOCUMENTATION "The act of swallowing liquids."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label DrinkAction "DrinkAction"))

(DEFRELATION passengerPriorityStatus (?X ?Y)
  :DOCUMENTATION "The priority status assigned to a passenger for security or boarding (e.g. FastTrack or Priority)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes passengerPriorityStatus QualitativeValue)
  :AXIOMS (domainIncludes passengerPriorityStatus FlightReservation)
  :AXIOMS (/XMLNS/RDFS/label passengerPriorityStatus "passengerPriorityStatus")
  :AXIOMS (rangeIncludes passengerPriorityStatus Text))

(DEFRELATION additionalNumberOfGuests (?X ?Y)
  :DOCUMENTATION "If responding yes, the number of guests who will attend in addition to the invitee."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label additionalNumberOfGuests "additionalNumberOfGuests")
  :AXIOMS (rangeIncludes additionalNumberOfGuests Number)
  :AXIOMS (domainIncludes additionalNumberOfGuests RsvpAction))

(DEFRELATION email (?X ?Y)
  :DOCUMENTATION "Email address."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes email Text)
  :AXIOMS (domainIncludes email Organization)
  :AXIOMS (domainIncludes email Person)
  :AXIOMS (/XMLNS/RDFS/label email "email")
  :AXIOMS (domainIncludes email ContactPoint))

(DEFRELATION followee (?X ?Y)
  :DOCUMENTATION "A sub property of object. The person or organization being followed."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (rangeIncludes followee Organization)
  :AXIOMS (domainIncludes followee FollowAction)
  :AXIOMS (/XMLNS/RDFS/label followee "followee")
  :AXIOMS (rangeIncludes followee Person))

(DEFRELATION object (?X ?Y)
  :DOCUMENTATION "The object upon which the action is carried out, whose state is kept intact or changed. Also known as the semantic roles patient, affected or undergoer (which change their state) or theme (which doesn't). e.g. John read <em>a book</em>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes object Thing)
  :AXIOMS (/XMLNS/RDFS/label object "object")
  :AXIOMS (domainIncludes object Action))

(DEFRELATION remainingAttendeeCapacity (?X ?Y)
  :DOCUMENTATION "The number of attendee places for an event that remain unallocated."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes remainingAttendeeCapacity Integer)
  :AXIOMS (/XMLNS/RDFS/label remainingAttendeeCapacity "remainingAttendeeCapacity")
  :AXIOMS (domainIncludes remainingAttendeeCapacity Event))

(DEFCONCEPT AboutPage (?X)
  :DOCUMENTATION "Web page type: About page."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label AboutPage "AboutPage"))

(DEFRELATION numberOfBeds (?X ?Y)
  :DOCUMENTATION "The quantity of the given bed type available in the HotelRoom, Suite, House, or Apartment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source numberOfBeds |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (domainIncludes numberOfBeds BedDetails)
  :AXIOMS (rangeIncludes numberOfBeds Number)
  :AXIOMS (/XMLNS/RDFS/label numberOfBeds "numberOfBeds"))

(ASSERT
 (AND (Organization |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  (/XMLNS/RDFS/label |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology| "STI Accommodation Ontology")
  (DOCUMENTATION |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|
   "This element is based on the STI Accommodation Ontology, see <a href=\"http://ontologies.sti-innsbruck.at/acco/ns.html\">http://ontologies.sti-innsbruck.at/acco/ns.html</a> for details.
    Many class and property definitions are inspired by or based on abstracts from Wikipedia, the free encyclopedia.")))

(DEFRELATION catalogNumber (?X ?Y)
  :DOCUMENTATION "The catalog number for the release."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes catalogNumber Text)
  :AXIOMS (/XMLNS/DC/source catalogNumber |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label catalogNumber "catalogNumber")
  :AXIOMS (domainIncludes catalogNumber MusicRelease))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|
   "This vocabulary was improved through collaboration with the MusicBrainz project
    (<a href=\"http://www.musicbrainz.org\">www.musicbrainz.org</a>), and is partially inspired by the MusicBrainz and
    <a href=\"http://musicontology.com/docs/getting-started.html\">Music Ontology</a> schemas.")
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ| "MBZ")))

(DEFRELATION orderDelivery (?X ?Y)
  :DOCUMENTATION "The delivery of the parcel related to this order or order item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label orderDelivery "orderDelivery")
  :AXIOMS (rangeIncludes orderDelivery ParcelDelivery)
  :AXIOMS (domainIncludes orderDelivery Order)
  :AXIOMS (domainIncludes orderDelivery OrderItem))

(DEFCONCEPT SaleEvent (?X)
  :DOCUMENTATION "Event type: Sales event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label SaleEvent "SaleEvent"))

(DEFCONCEPT Sculpture (?X)
  :DOCUMENTATION "A piece of sculpture."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Sculpture "Sculpture"))

(DEFCONCEPT GovernmentPermit (?X)
  :DOCUMENTATION "A permit issued by a government agency."
  :SUBSET-OF Permit
  :AXIOMS (/XMLNS/RDFS/label GovernmentPermit "GovernmentPermit"))

(DEFRELATION partOfTVSeries (?X ?Y)
  :DOCUMENTATION "The TV series to which this episode or season belongs."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF isPartOf
  :AXIOMS (/XMLNS/RDFS/label partOfTVSeries "partOfTVSeries")
  :AXIOMS (supersededBy partOfTVSeries partOfSeries)
  :AXIOMS (rangeIncludes partOfTVSeries TVSeries)
  :AXIOMS (domainIncludes partOfTVSeries TVEpisode)
  :AXIOMS (domainIncludes partOfTVSeries TVClip)
  :AXIOMS (domainIncludes partOfTVSeries TVSeason))

(DEFRELATION partOfSeries (?X ?Y)
  :DOCUMENTATION "The series to which this episode or season belongs."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF isPartOf
  :AXIOMS (domainIncludes partOfSeries Clip)
  :AXIOMS (/XMLNS/RDFS/label partOfSeries "partOfSeries")
  :AXIOMS (domainIncludes partOfSeries CreativeWorkSeason)
  :AXIOMS (rangeIncludes partOfSeries CreativeWorkSeries)
  :AXIOMS (domainIncludes partOfSeries Episode))

(DEFRELATION isPartOf (?X ?Y)
  :DOCUMENTATION "Indicates a CreativeWork that this CreativeWork is (in some sense) part of."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label isPartOf "isPartOf")
  :AXIOMS (rangeIncludes isPartOf CreativeWork)
  :AXIOMS (inverseOf isPartOf hasPart)
  :AXIOMS (domainIncludes isPartOf CreativeWork))

(DEFRELATION quest (?X ?Y)
  :DOCUMENTATION "The task that a player-controlled character, or group of characters may complete in order to gain a reward."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes quest VideoGameSeries)
  :AXIOMS (/XMLNS/RDFS/label quest "quest")
  :AXIOMS (domainIncludes quest Game)
  :AXIOMS (rangeIncludes quest Thing))

(DEFRELATION minValue (?X ?Y)
  :DOCUMENTATION "The lower value of some characteristic or property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes minValue QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label minValue "minValue")
  :AXIOMS (domainIncludes minValue PropertyValueSpecification)
  :AXIOMS (/XMLNS/DC/source minValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes minValue Number)
  :AXIOMS (domainIncludes minValue PropertyValue)
  :AXIOMS (domainIncludes minValue MonetaryAmount))

(DEFRELATION totalTime (?X ?Y)
  :DOCUMENTATION "The total time it takes to prepare and cook the recipe, in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 duration format</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label totalTime "totalTime")
  :AXIOMS (rangeIncludes totalTime Duration)
  :AXIOMS (domainIncludes totalTime Recipe))

(DEFRELATION events (?X ?Y)
  :DOCUMENTATION "Upcoming or past events associated with this place or organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes events Event)
  :AXIOMS (supersededBy events event)
  :AXIOMS (/XMLNS/RDFS/label events "events")
  :AXIOMS (domainIncludes events Place)
  :AXIOMS (domainIncludes events Organization))

(DEFRELATION event (?X ?Y)
  :DOCUMENTATION "Upcoming or past event associated with this place, organization, or action."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes event LeaveAction)
  :AXIOMS (domainIncludes event InviteAction)
  :AXIOMS (/XMLNS/RDFS/label event "event")
  :AXIOMS (domainIncludes event Organization)
  :AXIOMS (rangeIncludes event Event)
  :AXIOMS (domainIncludes event InformAction)
  :AXIOMS (domainIncludes event PlayAction)
  :AXIOMS (domainIncludes event Place)
  :AXIOMS (domainIncludes event JoinAction))

(DEFRELATION dependencies (?X ?Y)
  :DOCUMENTATION "Prerequisites needed to fulfill steps in article."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dependencies TechArticle)
  :AXIOMS (rangeIncludes dependencies Text)
  :AXIOMS (/XMLNS/RDFS/label dependencies "dependencies"))

(DEFRELATION musicBy (?X ?Y)
  :DOCUMENTATION "The composer of the soundtrack."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes musicBy TVSeries)
  :AXIOMS (/XMLNS/RDFS/label musicBy "musicBy")
  :AXIOMS (domainIncludes musicBy RadioSeries)
  :AXIOMS (domainIncludes musicBy MovieSeries)
  :AXIOMS (rangeIncludes musicBy MusicGroup)
  :AXIOMS (domainIncludes musicBy VideoGame)
  :AXIOMS (domainIncludes musicBy Clip)
  :AXIOMS (domainIncludes musicBy VideoGameSeries)
  :AXIOMS (domainIncludes musicBy Episode)
  :AXIOMS (rangeIncludes musicBy Person)
  :AXIOMS (domainIncludes musicBy Movie)
  :AXIOMS (domainIncludes musicBy VideoObject))

(DEFRELATION checkoutTime (?X ?Y)
  :DOCUMENTATION "The latest someone may check out of a lodging establishment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes checkoutTime LodgingBusiness)
  :AXIOMS (rangeIncludes checkoutTime DateTime)
  :AXIOMS (domainIncludes checkoutTime LodgingReservation)
  :AXIOMS (/XMLNS/RDFS/label checkoutTime "checkoutTime"))

(DEFCONCEPT Hospital (?X)
  :DOCUMENTATION "A hospital."
  :SUBSET-OF CivicStructure
  :SUBSET-OF MedicalOrganization
  :SUBSET-OF EmergencyService
  :AXIOMS (/XMLNS/RDFS/label Hospital "Hospital"))

(DEFCONCEPT ChildrensEvent (?X)
  :DOCUMENTATION "Event type: Children's event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label ChildrensEvent "ChildrensEvent"))

(DEFRELATION resultComment (?X ?Y)
  :DOCUMENTATION "A sub property of result. The Comment created or sent as a result of this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF result
  :AXIOMS (/XMLNS/RDFS/label resultComment "resultComment")
  :AXIOMS (domainIncludes resultComment ReplyAction)
  :AXIOMS (rangeIncludes resultComment Comment)
  :AXIOMS (domainIncludes resultComment CommentAction))

(DEFRELATION result (?X ?Y)
  :DOCUMENTATION "The result produced in the action. e.g. John wrote <em>a book</em>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes result Action)
  :AXIOMS (/XMLNS/RDFS/label result "result")
  :AXIOMS (rangeIncludes result Thing))

(DEFRELATION applicationCategory (?X ?Y)
  :DOCUMENTATION "Type of software application, e.g. 'Game, Multimedia'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label applicationCategory "applicationCategory")
  :AXIOMS (domainIncludes applicationCategory SoftwareApplication)
  :AXIOMS (rangeIncludes applicationCategory Text)
  :AXIOMS (rangeIncludes applicationCategory URL))

(DEFRELATION honorificSuffix (?X ?Y)
  :DOCUMENTATION "An honorific suffix preceding a Person's name such as M.D. /PhD/MSCSW."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label honorificSuffix "honorificSuffix")
  :AXIOMS (rangeIncludes honorificSuffix Text)
  :AXIOMS (domainIncludes honorificSuffix Person))

(DEFRELATION editor (?X ?Y)
  :DOCUMENTATION "Specifies the Person who edited the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label editor "editor")
  :AXIOMS (rangeIncludes editor Person)
  :AXIOMS (domainIncludes editor CreativeWork))

(DEFRELATION aircraft (?X ?Y)
  :DOCUMENTATION "The kind of aircraft (e.g., \"Boeing 747\")."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes aircraft Text)
  :AXIOMS (domainIncludes aircraft Flight)
  :AXIOMS (/XMLNS/RDFS/label aircraft "aircraft")
  :AXIOMS (rangeIncludes aircraft Vehicle))

(DEFCONCEPT Cemetery (?X)
  :DOCUMENTATION "A graveyard."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Cemetery "Cemetery"))

(DEFRELATION lyrics (?X ?Y)
  :DOCUMENTATION "The words in the song."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source lyrics |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label lyrics "lyrics")
  :AXIOMS (rangeIncludes lyrics CreativeWork)
  :AXIOMS (domainIncludes lyrics MusicComposition))

(DEFCONCEPT ClothingStore (?X)
  :DOCUMENTATION "A clothing store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label ClothingStore "ClothingStore"))

(DEFRELATION option (?X ?Y)
  :DOCUMENTATION "A sub property of object. The options subject to this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (domainIncludes option ChooseAction)
  :AXIOMS (rangeIncludes option Thing)
  :AXIOMS (/XMLNS/RDFS/label option "option")
  :AXIOMS (rangeIncludes option Text)
  :AXIOMS (supersededBy option actionOption))

(DEFRELATION actionOption (?X ?Y)
  :DOCUMENTATION "A sub property of object. The options subject to this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (rangeIncludes actionOption Text)
  :AXIOMS (domainIncludes actionOption ChooseAction)
  :AXIOMS (rangeIncludes actionOption Thing)
  :AXIOMS (/XMLNS/RDFS/label actionOption "actionOption"))

(ASSERT
 (AND (DigitalDocumentPermissionType WritePermission) (DOCUMENTATION WritePermission "Permission to write or edit the document.")
  (/XMLNS/RDFS/label WritePermission "WritePermission")))

(DEFRELATION serviceOperator (?X ?Y)
  :DOCUMENTATION "The operating organization, if different from the provider.  This enables the representation of services that are provided by an organization, but operated by another organization like a subcontractor."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serviceOperator Organization)
  :AXIOMS (domainIncludes serviceOperator GovernmentService)
  :AXIOMS (/XMLNS/RDFS/label serviceOperator "serviceOperator"))

(DEFRELATION numberOfEpisodes (?X ?Y)
  :DOCUMENTATION "The number of episodes in this season or series."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes numberOfEpisodes TVSeries)
  :AXIOMS (domainIncludes numberOfEpisodes CreativeWorkSeason)
  :AXIOMS (domainIncludes numberOfEpisodes VideoGameSeries)
  :AXIOMS (rangeIncludes numberOfEpisodes Integer)
  :AXIOMS (domainIncludes numberOfEpisodes RadioSeries)
  :AXIOMS (/XMLNS/RDFS/label numberOfEpisodes "numberOfEpisodes"))

(DEFRELATION programName (?X ?Y)
  :DOCUMENTATION "The program providing the membership."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label programName "programName")
  :AXIOMS (domainIncludes programName ProgramMembership)
  :AXIOMS (rangeIncludes programName Text))

(DEFRELATION targetDescription (?X ?Y)
  :DOCUMENTATION "The description of a node in an established educational framework."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label targetDescription "targetDescription")
  :AXIOMS (domainIncludes targetDescription AlignmentObject)
  :AXIOMS (rangeIncludes targetDescription Text))

(DEFCONCEPT ElementarySchool (?X)
  :DOCUMENTATION "An elementary school."
  :SUBSET-OF EducationalOrganization
  :AXIOMS (/XMLNS/RDFS/label ElementarySchool "ElementarySchool"))

(DEFRELATION parents (?X ?Y)
  :DOCUMENTATION "A parents of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy parents parent)
  :AXIOMS (domainIncludes parents Person)
  :AXIOMS (/XMLNS/RDFS/label parents "parents")
  :AXIOMS (rangeIncludes parents Person))

(DEFRELATION parent (?X ?Y)
  :DOCUMENTATION "A parent of this person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes parent Person)
  :AXIOMS (/XMLNS/RDFS/label parent "parent")
  :AXIOMS (domainIncludes parent Person))

(DEFRELATION amenityFeature (?X ?Y)
  :DOCUMENTATION "An amenity feature (e.g. a characteristic or service) of the Accommodation. This generic property does not make a statement about whether the feature is included in an offer for the main accommodation or available at extra costs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source amenityFeature |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (rangeIncludes amenityFeature LocationFeatureSpecification)
  :AXIOMS (domainIncludes amenityFeature Accommodation)
  :AXIOMS (domainIncludes amenityFeature Place)
  :AXIOMS (domainIncludes amenityFeature LodgingBusiness)
  :AXIOMS (/XMLNS/RDFS/label amenityFeature "amenityFeature"))

(DEFRELATION endDate (?X ?Y)
  :DOCUMENTATION "The end date and time of the item (in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 date format</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes endDate DateTime)
  :AXIOMS (domainIncludes endDate DatedMoneySpecification)
  :AXIOMS (domainIncludes endDate Role)
  :AXIOMS (rangeIncludes endDate Date)
  :AXIOMS (domainIncludes endDate Event)
  :AXIOMS (domainIncludes endDate CreativeWorkSeason)
  :AXIOMS (domainIncludes endDate CreativeWorkSeries)
  :AXIOMS (/XMLNS/RDFS/label endDate "endDate"))

(DEFRELATION coach (?X ?Y)
  :DOCUMENTATION "A person that acts in a coaching role for a sports team."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes coach SportsTeam)
  :AXIOMS (rangeIncludes coach Person)
  :AXIOMS (/XMLNS/RDFS/label coach "coach"))

(DEFCONCEPT DeleteAction (?X)
  :DOCUMENTATION "The act of editing a recipient by removing one of its objects."
  :SUBSET-OF UpdateAction
  :AXIOMS (/XMLNS/RDFS/label DeleteAction "DeleteAction"))

(DEFRELATION pagination (?X ?Y)
  :DOCUMENTATION "Any description of pages that is not separated into pageStart and pageEnd; for example, \"1-6, 9, 55\" or \"10-12, 46-49\"."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes pagination Article)
  :AXIOMS (rangeIncludes pagination Text)
  :AXIOMS (domainIncludes pagination PublicationIssue)
  :AXIOMS (/XMLNS/DC/source pagination |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label pagination "pagination")
  :AXIOMS (domainIncludes pagination PublicationVolume)
  :AXIOMS (/XMLNS/OWL/equivalentProperty pagination |http://purl.org/ontology/bibo/pages|))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex| "BibExTerm")
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|
   "The W3C <a href=\"http://www.w3.org/community/schemabibex/\">Schema Bib Extend</a> (BibEx) group led the work to improve schema.org for bibliographic information, including terms for periodicals, articles and multi-volume works. The design was inspired in places (e.g. <a class=\"localLink\" href=\"http://schema.org/pageStart\">pageStart</a>, <a class=\"localLink\" href=\"http://schema.org/pageEnd\">pageEnd</a>, <a class=\"localLink\" href=\"http://schema.org/pagination\">pagination</a>) by the <a href=\"http://bibliontology.com/\">Bibliographic Ontology</a>, 'bibo'.")))

(DEFCONCEPT OfficeEquipmentStore (?X)
  :DOCUMENTATION "An office equipment store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label OfficeEquipmentStore "OfficeEquipmentStore"))

(DEFCONCEPT GroceryStore (?X)
  :DOCUMENTATION "A grocery store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label GroceryStore "GroceryStore"))

(DEFRELATION acceptedPaymentMethod (?X ?Y)
  :DOCUMENTATION "The payment method(s) accepted by seller for this offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes acceptedPaymentMethod PaymentMethod)
  :AXIOMS (domainIncludes acceptedPaymentMethod Demand)
  :AXIOMS (/XMLNS/DC/source acceptedPaymentMethod |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes acceptedPaymentMethod Offer)
  :AXIOMS (rangeIncludes acceptedPaymentMethod LoanOrCredit)
  :AXIOMS (/XMLNS/RDFS/label acceptedPaymentMethod "acceptedPaymentMethod"))

(DEFCONCEPT AutoRepair (?X)
  :DOCUMENTATION "Car repair business."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label AutoRepair "AutoRepair"))

(DEFRELATION supportingData (?X ?Y)
  :DOCUMENTATION "Supporting data for a SoftwareApplication."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes supportingData DataFeed)
  :AXIOMS (domainIncludes supportingData SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label supportingData "supportingData"))

(DEFRELATION feesAndCommissionsSpecification (?X ?Y)
  :DOCUMENTATION "Description of fees, commissions, and other terms applied either to a class of financial product, or by a financial service organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes feesAndCommissionsSpecification URL)
  :AXIOMS (/XMLNS/DC/source feesAndCommissionsSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (domainIncludes feesAndCommissionsSpecification FinancialService)
  :AXIOMS (rangeIncludes feesAndCommissionsSpecification Text)
  :AXIOMS (domainIncludes feesAndCommissionsSpecification FinancialProduct)
  :AXIOMS (/XMLNS/RDFS/label feesAndCommissionsSpecification "feesAndCommissionsSpecification"))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO| "FIBO")
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|
   "This element is based on the work of the Financial Industry Business Ontology project (see <a href=\"http://www.fibo.org/schema\">http://www.fibo.org/schema</a> for details), in support of the W3C Financial Industry Business Ontology Community Group (<a href=\"http://www.fibo.org/community\">http://www.fibo.org/community</a>). Many class and property definitions are inspired by or based on <a href=\"http://www.fibo.org\">http://www.fibo.org</a>.")))

(DEFRELATION ownedThrough (?X ?Y)
  :DOCUMENTATION "The date and time of giving up ownership on the product."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes ownedThrough OwnershipInfo)
  :AXIOMS (/XMLNS/DC/source ownedThrough |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes ownedThrough DateTime)
  :AXIOMS (/XMLNS/RDFS/label ownedThrough "ownedThrough"))

(DEFRELATION isGift (?X ?Y)
  :DOCUMENTATION "Was the offer accepted as a gift for someone other than the buyer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label isGift "isGift")
  :AXIOMS (rangeIncludes isGift Boolean)
  :AXIOMS (domainIncludes isGift Order))

(DEFCONCEPT Synagogue (?X)
  :DOCUMENTATION "A synagogue."
  :SUBSET-OF PlaceOfWorship
  :AXIOMS (/XMLNS/RDFS/label Synagogue "Synagogue"))

(ASSERT
 (AND (MusicAlbumReleaseType SingleRelease) (/XMLNS/DC/source SingleRelease |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (DOCUMENTATION SingleRelease "SingleRelease.") (/XMLNS/RDFS/label SingleRelease "SingleRelease")))

(DEFRELATION productSupported (?X ?Y)
  :DOCUMENTATION "The product or service this support contact point is related to (such as product support for a particular product line). This can be a specific product or product line (e.g. \"iPhone\") or a general category of products or services (e.g. \"smartphones\")."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes productSupported Text)
  :AXIOMS (domainIncludes productSupported ContactPoint)
  :AXIOMS (rangeIncludes productSupported Product)
  :AXIOMS (/XMLNS/RDFS/label productSupported "productSupported"))

(DEFRELATION gender (?X ?Y)
  :DOCUMENTATION "Gender of the person. While http://schema.org/Male and http://schema.org/Female may be used, text strings are also acceptable for people who do not identify as a binary gender."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label gender "gender")
  :AXIOMS (domainIncludes gender Person)
  :AXIOMS (rangeIncludes gender GenderType)
  :AXIOMS (rangeIncludes gender Text))

(DEFRELATION itemReviewed (?X ?Y)
  :DOCUMENTATION "The item that is being reviewed/rated."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label itemReviewed "itemReviewed")
  :AXIOMS (rangeIncludes itemReviewed Thing)
  :AXIOMS (domainIncludes itemReviewed AggregateRating)
  :AXIOMS (domainIncludes itemReviewed Review))

(DEFCONCEPT PresentationDigitalDocument (?X)
  :DOCUMENTATION "A file containing slides or used for a presentation."
  :SUBSET-OF DigitalDocument
  :AXIOMS (/XMLNS/RDFS/label PresentationDigitalDocument "PresentationDigitalDocument"))

(DEFCONCEPT CheckOutAction (?X)
  :DOCUMENTATION "<p>The act of an agent communicating (service provider, social media, etc) their departure of a previously reserved service (e.g. flight check in) or place (e.g. hotel).</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/CheckInAction\">CheckInAction</a>: The antonym of CheckOutAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/DepartAction\">DepartAction</a>: Unlike DepartAction, CheckOutAction implies that the agent is informing/confirming the end of a previously reserved service.</li>
<li><a class=\"localLink\" href=\"http://schema.org/CancelAction\">CancelAction</a>: Unlike CancelAction, CheckOutAction implies that the agent is informing/confirming the end of a previously reserved service.</li>
</ul>
"
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label CheckOutAction "CheckOutAction"))

(DEFRELATION citation (?X ?Y)
  :DOCUMENTATION "A citation or reference to another creative work, such as another publication, web page, scholarly article, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label citation "citation")
  :AXIOMS (domainIncludes citation CreativeWork)
  :AXIOMS (rangeIncludes citation Text)
  :AXIOMS (rangeIncludes citation CreativeWork))

(ASSERT
 (AND (MusicReleaseFormatType VinylFormat) (/XMLNS/RDFS/label VinylFormat "VinylFormat") (DOCUMENTATION VinylFormat "VinylFormat.")
  (/XMLNS/DC/source VinylFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION birthDate (?X ?Y)
  :DOCUMENTATION "Date of birth."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes birthDate Person)
  :AXIOMS (/XMLNS/RDFS/label birthDate "birthDate")
  :AXIOMS (rangeIncludes birthDate Date))

(DEFRELATION closes (?X ?Y)
  :DOCUMENTATION "The closing hour of the place or service on the given day(s) of the week."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes closes Time)
  :AXIOMS (/XMLNS/RDFS/label closes "closes")
  :AXIOMS (/XMLNS/DC/source closes |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes closes OpeningHoursSpecification))

(DEFRELATION partOfSeason (?X ?Y)
  :DOCUMENTATION "The season to which this episode belongs."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF isPartOf
  :AXIOMS (rangeIncludes partOfSeason CreativeWorkSeason)
  :AXIOMS (domainIncludes partOfSeason Clip)
  :AXIOMS (domainIncludes partOfSeason Episode)
  :AXIOMS (/XMLNS/RDFS/label partOfSeason "partOfSeason"))

(DEFRELATION vendor (?X ?Y)
  :DOCUMENTATION "'vendor' is an earlier term for 'seller'."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (supersededBy vendor seller)
  :AXIOMS (rangeIncludes vendor Organization)
  :AXIOMS (rangeIncludes vendor Person)
  :AXIOMS (/XMLNS/RDFS/label vendor "vendor")
  :AXIOMS (domainIncludes vendor BuyAction))

(DEFRELATION seller (?X ?Y)
  :DOCUMENTATION "An entity which offers (sells / leases / lends / loans) the services / goods.  A seller may also be a provider."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (rangeIncludes seller Person)
  :AXIOMS (rangeIncludes seller Organization)
  :AXIOMS (domainIncludes seller Demand)
  :AXIOMS (/XMLNS/RDFS/label seller "seller")
  :AXIOMS (domainIncludes seller Offer)
  :AXIOMS (domainIncludes seller BuyAction)
  :AXIOMS (domainIncludes seller Order)
  :AXIOMS (domainIncludes seller Flight))

(DEFRELATION participant (?X ?Y)
  :DOCUMENTATION "Other co-agents that participated in the action indirectly. e.g. John wrote a book with <em>Steve</em>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes participant Organization)
  :AXIOMS (rangeIncludes participant Person)
  :AXIOMS (/XMLNS/RDFS/label participant "participant")
  :AXIOMS (domainIncludes participant Action))

(DEFCONCEPT MiddleSchool (?X)
  :DOCUMENTATION "A middle school (typically for children aged around 11-14, although this varies somewhat)."
  :SUBSET-OF EducationalOrganization
  :AXIOMS (/XMLNS/RDFS/label MiddleSchool "MiddleSchool"))

(DEFCONCEPT OutletStore (?X)
  :DOCUMENTATION "An outlet store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label OutletStore "OutletStore"))

(DEFRELATION gameTip (?X ?Y)
  :DOCUMENTATION "Links to tips, tactics, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label gameTip "gameTip")
  :AXIOMS (domainIncludes gameTip VideoGame)
  :AXIOMS (rangeIncludes gameTip CreativeWork))

(DEFCONCEPT FastFoodRestaurant (?X)
  :DOCUMENTATION "A fast-food restaurant."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label FastFoodRestaurant "FastFoodRestaurant"))

(ASSERT
 (AND (OrderStatus OrderInTransit) (/XMLNS/RDFS/label OrderInTransit "OrderInTransit")
  (DOCUMENTATION OrderInTransit "OrderStatus representing that an order is in transit.")))

(DEFRELATION bed (?X ?Y)
  :DOCUMENTATION "The type of bed or beds included in the accommodation. For the single case of just one bed of a certain type, you use bed directly with a text.
      If you want to indicate the quantity of a certain kind of bed, use an instance of BedDetails. For more detailed information, use the amenityFeature property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source bed |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label bed "bed")
  :AXIOMS (domainIncludes bed Suite)
  :AXIOMS (rangeIncludes bed Text)
  :AXIOMS (domainIncludes bed HotelRoom)
  :AXIOMS (rangeIncludes bed BedDetails))

(DEFRELATION numberOfPreviousOwners (?X ?Y)
  :DOCUMENTATION "The number of owners of the vehicle, including the current one.</p>

<p>Typical unit code(s): C62"
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numberOfPreviousOwners QuantitativeValue)
  :AXIOMS (domainIncludes numberOfPreviousOwners Vehicle)
  :AXIOMS (/XMLNS/RDFS/label numberOfPreviousOwners "numberOfPreviousOwners")
  :AXIOMS (/XMLNS/DC/source numberOfPreviousOwners |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes numberOfPreviousOwners Number))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group| "AutomotiveOntologyWGClass")
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|
   "This element is based on the work of the Automotive Ontology Working Group, see <a href=\"http://www.automotive-ontology.org\">www.automotive-ontology.org</a> for details. Many class and property definitions are inspired by or based on abstracts from Wikipedia, the free encyclopedia.")))

(DEFCONCEPT PetStore (?X)
  :DOCUMENTATION "A pet store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label PetStore "PetStore"))

(DEFCONCEPT HVACBusiness (?X)
  :DOCUMENTATION "A business that provide Heating, Ventilation and Air Conditioning services."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label HVACBusiness "HVACBusiness"))

(DEFRELATION contentSize (?X ?Y)
  :DOCUMENTATION "File size in (mega/kilo) bytes."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes contentSize Text)
  :AXIOMS (/XMLNS/RDFS/label contentSize "contentSize")
  :AXIOMS (domainIncludes contentSize MediaObject))

(DEFRELATION mileageFromOdometer (?X ?Y)
  :DOCUMENTATION "The total distance travelled by the particular vehicle since its initial production, as read from its odometer.</p>

<p>Typical unit code(s): KMT for kilometers, SMI for statute miles"
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source mileageFromOdometer |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes mileageFromOdometer QuantitativeValue)
  :AXIOMS (domainIncludes mileageFromOdometer Vehicle)
  :AXIOMS (/XMLNS/RDFS/label mileageFromOdometer "mileageFromOdometer"))

(DEFRELATION endorsee (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The person/organization being supported."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (rangeIncludes endorsee Person)
  :AXIOMS (domainIncludes endorsee EndorseAction)
  :AXIOMS (/XMLNS/RDFS/label endorsee "endorsee")
  :AXIOMS (rangeIncludes endorsee Organization))

(DEFRELATION seasons (?X ?Y)
  :DOCUMENTATION "A season in a media series."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes seasons TVSeries)
  :AXIOMS (domainIncludes seasons VideoGameSeries)
  :AXIOMS (/XMLNS/RDFS/label seasons "seasons")
  :AXIOMS (domainIncludes seasons RadioSeries)
  :AXIOMS (rangeIncludes seasons CreativeWorkSeason)
  :AXIOMS (supersededBy seasons season))

(DEFRELATION season (?X ?Y)
  :DOCUMENTATION "A season in a media series."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF hasPart
  :AXIOMS (supersededBy season containsSeason)
  :AXIOMS (domainIncludes season TVSeries)
  :AXIOMS (rangeIncludes season CreativeWorkSeason)
  :AXIOMS (/XMLNS/RDFS/label season "season")
  :AXIOMS (domainIncludes season RadioSeries)
  :AXIOMS (domainIncludes season VideoGameSeries))

(DEFCONCEPT RegisterAction (?X)
  :DOCUMENTATION "<p>The act of registering to be a user of a service, product or web page.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/JoinAction\">JoinAction</a>: Unlike JoinAction, RegisterAction implies you are registering to be a user of a service, <em>not</em> a group/team of people.</li>
<li>[FollowAction]]: Unlike FollowAction, RegisterAction doesn't imply that the agent is expecting to poll for updates from the object.</li>
<li><a class=\"localLink\" href=\"http://schema.org/SubscribeAction\">SubscribeAction</a>: Unlike SubscribeAction, RegisterAction doesn't imply that the agent is expecting updates from the object.</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label RegisterAction "RegisterAction"))

(DEFRELATION defaultValue (?X ?Y)
  :DOCUMENTATION "The default value of the input.  For properties that expect a literal, the default is a literal value, for properties that expect an object, it's an ID reference to one of the current values."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label defaultValue "defaultValue")
  :AXIOMS (rangeIncludes defaultValue Thing)
  :AXIOMS (rangeIncludes defaultValue Text)
  :AXIOMS (domainIncludes defaultValue PropertyValueSpecification))

(DEFRELATION providesBroadcastService (?X ?Y)
  :DOCUMENTATION "The BroadcastService offered on this channel."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label providesBroadcastService "providesBroadcastService")
  :AXIOMS (domainIncludes providesBroadcastService BroadcastChannel)
  :AXIOMS (rangeIncludes providesBroadcastService BroadcastService))

(DEFRELATION softwareVersion (?X ?Y)
  :DOCUMENTATION "Version of the software instance."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes softwareVersion SoftwareApplication)
  :AXIOMS (rangeIncludes softwareVersion Text)
  :AXIOMS (/XMLNS/RDFS/label softwareVersion "softwareVersion"))

(DEFRELATION paymentUrl (?X ?Y)
  :DOCUMENTATION "The URL for sending a payment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes paymentUrl URL)
  :AXIOMS (domainIncludes paymentUrl Order)
  :AXIOMS (/XMLNS/RDFS/label paymentUrl "paymentUrl"))

(ASSERT
 (AND (DriveWheelConfigurationValue FrontWheelDriveConfiguration)
  (/XMLNS/DC/source FrontWheelDriveConfiguration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (/XMLNS/RDFS/label FrontWheelDriveConfiguration "FrontWheelDriveConfiguration")
  (DOCUMENTATION FrontWheelDriveConfiguration "Front-wheel drive is a transmission layout where the engine drives the front wheels.")))

(DEFRELATION price (?X ?Y)
  :DOCUMENTATION "<p>The offer price of a product, or of a price component when attached to PriceSpecification and its subtypes.</p>

<p>Usage guidelines:</p>

<ul>
<li>Use the <a class=\"localLink\" href=\"http://schema.org/priceCurrency\">priceCurrency</a> property (with <a href=\"http://en.wikipedia.org/wiki/ISO_4217#Active_codes\">ISO 4217 codes</a> e.g. \"USD\") instead of
  including <a href=\"http://en.wikipedia.org/wiki/Dollar_sign#Currencies_that_use_the_dollar_or_peso_sign\">ambiguous symbols</a> such as '$' in the value.</li>
<li>Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator.</li>
<li>Note that both <a href=\"http://www.w3.org/TR/xhtml-rdfa-primer/#using-the-content-attribute\">RDFa</a> and Microdata syntax allow the use of a \"content=\" attribute for publishing simple machine-readable values alongside more human-friendly formatting.</li>
<li>Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similiar Unicode symbols.</li>
</ul>
"
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes price Text)
  :AXIOMS (domainIncludes price Offer)
  :AXIOMS (domainIncludes price TradeAction)
  :AXIOMS (domainIncludes price PriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label price "price")
  :AXIOMS (rangeIncludes price Number))

(DEFRELATION sourceOrganization (?X ?Y)
  :DOCUMENTATION "The Organization on whose behalf the creator was working."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label sourceOrganization "sourceOrganization")
  :AXIOMS (domainIncludes sourceOrganization CreativeWork)
  :AXIOMS (rangeIncludes sourceOrganization Organization))

(DEFRELATION borrower (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The person that borrows the object being lent."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (domainIncludes borrower LendAction)
  :AXIOMS (rangeIncludes borrower Person)
  :AXIOMS (/XMLNS/RDFS/label borrower "borrower"))

(DEFRELATION characterName (?X ?Y)
  :DOCUMENTATION "The name of a character played in some acting or performing role, i.e. in a PerformanceRole."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label characterName "characterName")
  :AXIOMS (rangeIncludes characterName Text)
  :AXIOMS (domainIncludes characterName PerformanceRole))

(DEFRELATION numberOfRooms (?X ?Y)
  :DOCUMENTATION "The number of rooms (excluding bathrooms and closets) of the acccommodation or lodging business.
Typical unit code(s): ROM for room or C62 for no unit. The type of room can be put in the unitText property of the QuantitativeValue."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes numberOfRooms SingleFamilyResidence)
  :AXIOMS (rangeIncludes numberOfRooms Number)
  :AXIOMS (domainIncludes numberOfRooms Apartment)
  :AXIOMS (domainIncludes numberOfRooms Accommodation)
  :AXIOMS (/XMLNS/DC/source numberOfRooms |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (domainIncludes numberOfRooms House)
  :AXIOMS (rangeIncludes numberOfRooms QuantitativeValue)
  :AXIOMS (domainIncludes numberOfRooms Suite)
  :AXIOMS (/XMLNS/RDFS/label numberOfRooms "numberOfRooms"))

(DEFRELATION fuelType (?X ?Y)
  :DOCUMENTATION "The type of fuel suitable for the engine or engines of the vehicle. If the vehicle has only one engine, this property can be attached directly to the vehicle."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes fuelType Text)
  :AXIOMS (domainIncludes fuelType EngineSpecification)
  :AXIOMS (domainIncludes fuelType Vehicle)
  :AXIOMS (/XMLNS/DC/source fuelType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes fuelType URL)
  :AXIOMS (rangeIncludes fuelType QualitativeValue)
  :AXIOMS (/XMLNS/RDFS/label fuelType "fuelType"))

(DEFCONCEPT LiquorStore (?X)
  :DOCUMENTATION "A shop that sells alcoholic drinks such as wine, beer, whisky and other spirits."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label LiquorStore "LiquorStore"))

(DEFRELATION numberedPosition (?X ?Y)
  :DOCUMENTATION "A number associated with a role in an organization, for example, the number on an athlete's jersey."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numberedPosition Number)
  :AXIOMS (/XMLNS/RDFS/label numberedPosition "numberedPosition")
  :AXIOMS (domainIncludes numberedPosition OrganizationRole))

(DEFRELATION softwareAddOn (?X ?Y)
  :DOCUMENTATION "Additional content for a software application."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label softwareAddOn "softwareAddOn")
  :AXIOMS (rangeIncludes softwareAddOn SoftwareApplication)
  :AXIOMS (domainIncludes softwareAddOn SoftwareApplication))

(DEFRELATION characterAttribute (?X ?Y)
  :DOCUMENTATION "A piece of data that represents a particular aspect of a fictional character (skill, power, character points, advantage, disadvantage)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label characterAttribute "characterAttribute")
  :AXIOMS (domainIncludes characterAttribute VideoGameSeries)
  :AXIOMS (rangeIncludes characterAttribute Thing)
  :AXIOMS (domainIncludes characterAttribute Game))

(DEFRELATION seatSection (?X ?Y)
  :DOCUMENTATION "The section location of the reserved seat (e.g. Orchestra)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes seatSection Text)
  :AXIOMS (/XMLNS/RDFS/label seatSection "seatSection")
  :AXIOMS (domainIncludes seatSection Seat))

(DEFRELATION underName (?X ?Y)
  :DOCUMENTATION "The person or organization the reservation or ticket is for."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes underName Person)
  :AXIOMS (rangeIncludes underName Organization)
  :AXIOMS (domainIncludes underName Reservation)
  :AXIOMS (domainIncludes underName Ticket)
  :AXIOMS (/XMLNS/RDFS/label underName "underName"))

(DEFRELATION leiCode (?X ?Y)
  :DOCUMENTATION "An organization identifier that uniquely identifies a legal entity as defined in ISO 17442."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label leiCode "leiCode")
  :AXIOMS (rangeIncludes leiCode Text)
  :AXIOMS (domainIncludes leiCode Organization)
  :AXIOMS (/XMLNS/DC/source leiCode |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|))

(ASSERT
 (AND (RsvpResponseType RsvpResponseYes) (/XMLNS/RDFS/label RsvpResponseYes "RsvpResponseYes") (DOCUMENTATION RsvpResponseYes "The invitee will attend.")))

(DEFRELATION requiredMaxAge (?X ?Y)
  :DOCUMENTATION "Audiences defined by a person's maximum age."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes requiredMaxAge Integer)
  :AXIOMS (/XMLNS/RDFS/label requiredMaxAge "requiredMaxAge")
  :AXIOMS (domainIncludes requiredMaxAge PeopleAudience))

(DEFRELATION wordCount (?X ?Y)
  :DOCUMENTATION "The number of words in the text of the Article."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes wordCount Integer)
  :AXIOMS (/XMLNS/RDFS/label wordCount "wordCount")
  :AXIOMS (domainIncludes wordCount Article))

(DEFCONCEPT Hotel (?X)
  :DOCUMENTATION "A hotel is an establishment that provides lodging paid on a short-term basis (Source: Wikipedia, the free encyclopedia, see http://en.wikipedia.org/wiki/Hotel).
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF LodgingBusiness
  :AXIOMS (/XMLNS/RDFS/label Hotel "Hotel")
  :AXIOMS (/XMLNS/DC/source Hotel |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFCONCEPT ImageGallery (?X)
  :DOCUMENTATION "Web page type: Image gallery page."
  :SUBSET-OF CollectionPage
  :AXIOMS (/XMLNS/RDFS/label ImageGallery "ImageGallery"))

(DEFRELATION alternateName (?X ?Y)
  :DOCUMENTATION "An alias for the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label alternateName "alternateName")
  :AXIOMS (domainIncludes alternateName Thing)
  :AXIOMS (rangeIncludes alternateName Text))

(DEFRELATION pageStart (?X ?Y)
  :DOCUMENTATION "The page on which the work starts; for example \"135\" or \"xiii\"."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes pageStart Article)
  :AXIOMS (rangeIncludes pageStart Text)
  :AXIOMS (/XMLNS/OWL/equivalentProperty pageStart |http://purl.org/ontology/bibo/pageStart|)
  :AXIOMS (domainIncludes pageStart PublicationIssue)
  :AXIOMS (/XMLNS/DC/source pageStart |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (rangeIncludes pageStart Integer)
  :AXIOMS (domainIncludes pageStart PublicationVolume)
  :AXIOMS (/XMLNS/RDFS/label pageStart "pageStart"))

(DEFRELATION stepValue (?X ?Y)
  :DOCUMENTATION "The stepValue attribute indicates the granularity that is expected (and required) of the value in a PropertyValueSpecification."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes stepValue Number)
  :AXIOMS (/XMLNS/RDFS/label stepValue "stepValue")
  :AXIOMS (domainIncludes stepValue PropertyValueSpecification))

(DEFCONCEPT PaymentService (?X)
  :DOCUMENTATION "A Service to transfer funds from a person or organization to a beneficiary person or organization."
  :SUBSET-OF FinancialProduct
  :AXIOMS (/XMLNS/DC/source PaymentService |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label PaymentService "PaymentService"))

(DEFRELATION learningResourceType (?X ?Y)
  :DOCUMENTATION "The predominant type or kind characterizing the learning resource. For example, 'presentation', 'handout'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes learningResourceType CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label learningResourceType "learningResourceType")
  :AXIOMS (rangeIncludes learningResourceType Text))

(DEFRELATION warrantyScope (?X ?Y)
  :DOCUMENTATION "The scope of the warranty promise."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes warrantyScope WarrantyScope)
  :AXIOMS (/XMLNS/RDFS/label warrantyScope "warrantyScope")
  :AXIOMS (domainIncludes warrantyScope WarrantyPromise)
  :AXIOMS (/XMLNS/DC/source warrantyScope |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFCONCEPT BuddhistTemple (?X)
  :DOCUMENTATION "A Buddhist temple."
  :SUBSET-OF PlaceOfWorship
  :AXIOMS (/XMLNS/RDFS/label BuddhistTemple "BuddhistTemple"))

(DEFRELATION spouse (?X ?Y)
  :DOCUMENTATION "The person's spouse."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes spouse Person)
  :AXIOMS (/XMLNS/RDFS/label spouse "spouse")
  :AXIOMS (domainIncludes spouse Person))

(DEFRELATION paymentStatus (?X ?Y)
  :DOCUMENTATION "The status of payment; whether the invoice has been paid or not."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label paymentStatus "paymentStatus")
  :AXIOMS (domainIncludes paymentStatus Invoice)
  :AXIOMS (rangeIncludes paymentStatus Text)
  :AXIOMS (rangeIncludes paymentStatus PaymentStatusType))

(DEFRELATION caption (?X ?Y)
  :DOCUMENTATION "The caption for this object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes caption VideoObject)
  :AXIOMS (rangeIncludes caption Text)
  :AXIOMS (/XMLNS/RDFS/label caption "caption")
  :AXIOMS (domainIncludes caption ImageObject))

(DEFRELATION educationRequirements (?X ?Y)
  :DOCUMENTATION "Educational background needed for the position."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes educationRequirements JobPosting)
  :AXIOMS (rangeIncludes educationRequirements Text)
  :AXIOMS (/XMLNS/RDFS/label educationRequirements "educationRequirements"))

(DEFRELATION bitrate (?X ?Y)
  :DOCUMENTATION "The bitrate of the media object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label bitrate "bitrate")
  :AXIOMS (domainIncludes bitrate MediaObject)
  :AXIOMS (rangeIncludes bitrate Text))

(DEFRELATION honorificPrefix (?X ?Y)
  :DOCUMENTATION "An honorific prefix preceding a Person's name such as Dr/Mrs/Mr."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label honorificPrefix "honorificPrefix")
  :AXIOMS (domainIncludes honorificPrefix Person)
  :AXIOMS (rangeIncludes honorificPrefix Text))

(DEFRELATION trailer (?X ?Y)
  :DOCUMENTATION "The trailer of a movie or tv/radio series, season, episode, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes trailer VideoObject)
  :AXIOMS (domainIncludes trailer RadioSeries)
  :AXIOMS (domainIncludes trailer Movie)
  :AXIOMS (domainIncludes trailer VideoGame)
  :AXIOMS (domainIncludes trailer TVSeries)
  :AXIOMS (domainIncludes trailer CreativeWorkSeason)
  :AXIOMS (domainIncludes trailer Episode)
  :AXIOMS (domainIncludes trailer MovieSeries)
  :AXIOMS (/XMLNS/RDFS/label trailer "trailer")
  :AXIOMS (domainIncludes trailer VideoGameSeries))

(DEFCONCEPT EatAction (?X)
  :DOCUMENTATION "The act of swallowing solid objects."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label EatAction "EatAction"))

(DEFRELATION vehicleTransmission (?X ?Y)
  :DOCUMENTATION "The type of component used for transmitting the power from a rotating power source to the wheels or other relevant component(s) (\"gearbox\" for cars)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes vehicleTransmission Text)
  :AXIOMS (/XMLNS/RDFS/label vehicleTransmission "vehicleTransmission")
  :AXIOMS (rangeIncludes vehicleTransmission QualitativeValue)
  :AXIOMS (rangeIncludes vehicleTransmission URL)
  :AXIOMS (domainIncludes vehicleTransmission Vehicle)
  :AXIOMS (/XMLNS/DC/source vehicleTransmission |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFRELATION billingIncrement (?X ?Y)
  :DOCUMENTATION "This property specifies the minimal quantity and rounding increment that will be the basis for the billing. The unit of measurement is specified by the unitCode property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source billingIncrement |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes billingIncrement UnitPriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label billingIncrement "billingIncrement")
  :AXIOMS (rangeIncludes billingIncrement Number))

(DEFRELATION branchOf (?X ?Y)
  :DOCUMENTATION "The larger organization that this local business is a branch of, if any. Not to be confused with (anatomical)<a class=\"localLink\" href=\"http://schema.org/branch\">branch</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes branchOf LocalBusiness)
  :AXIOMS (rangeIncludes branchOf Organization)
  :AXIOMS (supersededBy branchOf parentOrganization)
  :AXIOMS (/XMLNS/RDFS/label branchOf "branchOf"))

(DEFRELATION parentOrganization (?X ?Y)
  :DOCUMENTATION "The larger organization that this local business is a branch of, if any."
  ;:BINARY-RELATION TRUE
  :AXIOMS (inverseOf parentOrganization subOrganization)
  :AXIOMS (/XMLNS/RDFS/label parentOrganization "parentOrganization")
  :AXIOMS (rangeIncludes parentOrganization Organization)
  :AXIOMS (domainIncludes parentOrganization Organization))

(DEFCONCEPT RoofingContractor (?X)
  :DOCUMENTATION "A roofing contractor."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label RoofingContractor "RoofingContractor"))

(DEFRELATION genre (?X ?Y)
  :DOCUMENTATION "Genre of the creative work or group."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label genre "genre")
  :AXIOMS (rangeIncludes genre URL)
  :AXIOMS (domainIncludes genre MusicGroup)
  :AXIOMS (domainIncludes genre CreativeWork)
  :AXIOMS (rangeIncludes genre Text))

(DEFRELATION version (?X ?Y)
  :DOCUMENTATION "The version of the CreativeWork embodied by a specified resource."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label version "version")
  :AXIOMS (rangeIncludes version Text)
  :AXIOMS (rangeIncludes version Number)
  :AXIOMS (domainIncludes version CreativeWork))

(DEFCONCEPT StadiumOrArena (?X)
  :DOCUMENTATION "A stadium."
  :SUBSET-OF CivicStructure
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label StadiumOrArena "StadiumOrArena"))

(DEFCONCEPT AutoRental (?X)
  :DOCUMENTATION "A car rental business."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label AutoRental "AutoRental"))

(ASSERT
 (AND (ItemListOrderType ItemListUnordered) (/XMLNS/RDFS/label ItemListUnordered "ItemListUnordered")
  (DOCUMENTATION ItemListUnordered "An ItemList ordered with no explicit order.")))

(ASSERT
 (AND (GameServerStatus OnlineFull)
  (DOCUMENTATION OnlineFull "Game server status: OnlineFull. Server is online but unavailable. The maximum number of players has reached.")
  (/XMLNS/RDFS/label OnlineFull "OnlineFull")))

(DEFRELATION unsaturatedFatContent (?X ?Y)
  :DOCUMENTATION "The number of grams of unsaturated fat."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes unsaturatedFatContent Mass)
  :AXIOMS (domainIncludes unsaturatedFatContent NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label unsaturatedFatContent "unsaturatedFatContent"))

(DEFRELATION numberOfSeasons (?X ?Y)
  :DOCUMENTATION "The number of seasons in this series."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numberOfSeasons Integer)
  :AXIOMS (domainIncludes numberOfSeasons TVSeries)
  :AXIOMS (/XMLNS/RDFS/label numberOfSeasons "numberOfSeasons")
  :AXIOMS (domainIncludes numberOfSeasons VideoGameSeries)
  :AXIOMS (domainIncludes numberOfSeasons RadioSeries))

(DEFRELATION coverageStartTime (?X ?Y)
  :DOCUMENTATION "The time when the live blog will begin covering the Event. Note that coverage may begin before the Event's start time. The LiveBlogPosting may also be created before coverage begins."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes coverageStartTime DateTime)
  :AXIOMS (/XMLNS/RDFS/label coverageStartTime "coverageStartTime")
  :AXIOMS (domainIncludes coverageStartTime LiveBlogPosting))

(DEFRELATION isAccessoryOrSparePartFor (?X ?Y)
  :DOCUMENTATION "A pointer to another product (or multiple products) for which this product is an accessory or spare part."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source isAccessoryOrSparePartFor |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label isAccessoryOrSparePartFor "isAccessoryOrSparePartFor")
  :AXIOMS (rangeIncludes isAccessoryOrSparePartFor Product)
  :AXIOMS (domainIncludes isAccessoryOrSparePartFor Product))

(DEFRELATION coverageEndTime (?X ?Y)
  :DOCUMENTATION "The time when the live blog will stop covering the Event. Note that coverage may continue after the Event concludes."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes coverageEndTime DateTime)
  :AXIOMS (domainIncludes coverageEndTime LiveBlogPosting)
  :AXIOMS (/XMLNS/RDFS/label coverageEndTime "coverageEndTime"))

(DEFCONCEPT FurnitureStore (?X)
  :DOCUMENTATION "A furniture store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label FurnitureStore "FurnitureStore"))

(DEFRELATION bookingAgent (?X ?Y)
  :DOCUMENTATION "'bookingAgent' is an out-dated term indicating a 'broker' that serves as a booking agent."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy bookingAgent broker)
  :AXIOMS (/XMLNS/RDFS/label bookingAgent "bookingAgent")
  :AXIOMS (rangeIncludes bookingAgent Person)
  :AXIOMS (domainIncludes bookingAgent Reservation)
  :AXIOMS (rangeIncludes bookingAgent Organization))

(DEFRELATION broker (?X ?Y)
  :DOCUMENTATION "An entity that arranges for an exchange between a buyer and a seller.  In most cases a broker never acquires or releases ownership of a product or service involved in an exchange.  If it is not clear whether an entity is a broker, seller, or buyer, the latter two terms are preferred."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes broker Person)
  :AXIOMS (domainIncludes broker Reservation)
  :AXIOMS (rangeIncludes broker Organization)
  :AXIOMS (/XMLNS/RDFS/label broker "broker")
  :AXIOMS (domainIncludes broker Order)
  :AXIOMS (domainIncludes broker Invoice))

(DEFRELATION encodesCreativeWork (?X ?Y)
  :DOCUMENTATION "The CreativeWork encoded by this media object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label encodesCreativeWork "encodesCreativeWork")
  :AXIOMS (rangeIncludes encodesCreativeWork CreativeWork)
  :AXIOMS (domainIncludes encodesCreativeWork MediaObject))

(DEFCONCEPT TieAction (?X)
  :DOCUMENTATION "The act of reaching a draw in a competitive activity."
  :SUBSET-OF AchieveAction
  :AXIOMS (/XMLNS/RDFS/label TieAction "TieAction"))

(DEFRELATION steeringPosition (?X ?Y)
  :DOCUMENTATION "The position of the steering wheel or similar device (mostly for cars)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes steeringPosition Vehicle)
  :AXIOMS (rangeIncludes steeringPosition SteeringPositionValue)
  :AXIOMS (/XMLNS/RDFS/label steeringPosition "steeringPosition")
  :AXIOMS (/XMLNS/DC/source steeringPosition |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFRELATION installUrl (?X ?Y)
  :DOCUMENTATION "URL at which the app may be installed, if different from the URL of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label installUrl "installUrl")
  :AXIOMS (domainIncludes installUrl SoftwareApplication)
  :AXIOMS (rangeIncludes installUrl URL))

(DEFRELATION episodeNumber (?X ?Y)
  :DOCUMENTATION "Position of the episode within an ordered group of episodes."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF position
  :AXIOMS (rangeIncludes episodeNumber Text)
  :AXIOMS (/XMLNS/RDFS/label episodeNumber "episodeNumber")
  :AXIOMS (rangeIncludes episodeNumber Integer)
  :AXIOMS (domainIncludes episodeNumber Episode))

(DEFRELATION position (?X ?Y)
  :DOCUMENTATION "The position of an item in a series or sequence of items."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes position ListItem)
  :AXIOMS (rangeIncludes position Integer)
  :AXIOMS (rangeIncludes position Text)
  :AXIOMS (/XMLNS/RDFS/label position "position")
  :AXIOMS (domainIncludes position CreativeWork))

(DEFRELATION additionalProperty (?X ?Y)
  :DOCUMENTATION "A property-value pair representing an additional characteristics of the entitity, e.g. a product feature or another characteristic for which there is no matching property in schema.org.</p>

<p>Note: Publishers should be aware that applications designed to use specific schema.org properties (e.g. http://schema.org/width, http://schema.org/color, http://schema.org/gtin13, ...) will typically expect such data to be provided using those properties, rather than using the generic property/value mechanism."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes additionalProperty QualitativeValue)
  :AXIOMS (rangeIncludes additionalProperty PropertyValue)
  :AXIOMS (domainIncludes additionalProperty Place)
  :AXIOMS (domainIncludes additionalProperty QuantitativeValue)
  :AXIOMS (domainIncludes additionalProperty Product)
  :AXIOMS (/XMLNS/RDFS/label additionalProperty "additionalProperty"))

(ASSERT
 (AND (DayOfWeek PublicHolidays)
  (DOCUMENTATION PublicHolidays
   "This stands for any day that is a public holiday; it is a placeholder for all official public holidays in some particular location. While not technically a \"day of the week\", it can be used with <a class=\"localLink\" href=\"http://schema.org/OpeningHoursSpecification\">OpeningHoursSpecification</a>. In the context of an opening hours specification it can be used to indicate opening hours on public holidays, overriding general opening hours for the day of the week on which a public holiday occurs.")
  (/XMLNS/DC/source PublicHolidays |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  (/XMLNS/RDFS/label PublicHolidays "PublicHolidays")))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|
   "This class is derived from the GoodRelations Vocabulary for E-Commerce, created by Martin Hepp. GoodRelations is a data model for sharing e-commerce data on the Web that can be expressed in a variety of syntaxes, including RDFa and HTML5 Microdata. More information about GoodRelations can be found at <a href=\"http://purl.org/goodrelations/\">http://purl.org/goodrelations/</a>.")
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass| "GoodRelationsClass")))

(DEFRELATION reservationFor (?X ?Y)
  :DOCUMENTATION "The thing -- flight, event, restaurant,etc. being reserved."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label reservationFor "reservationFor")
  :AXIOMS (domainIncludes reservationFor Reservation)
  :AXIOMS (rangeIncludes reservationFor Thing))

(DEFCONCEPT Painting (?X)
  :DOCUMENTATION "A painting."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Painting "Painting"))

(DEFRELATION floorSize (?X ?Y)
  :DOCUMENTATION "The size of the accommodation, e.g. in square meter or squarefoot.
Typical unit code(s): MTK for square meter, FTK for square foot, or YDK for square yard"
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes floorSize QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label floorSize "floorSize")
  :AXIOMS (/XMLNS/DC/source floorSize |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (domainIncludes floorSize Accommodation))

(DEFCONCEPT SiteNavigationElement (?X)
  :DOCUMENTATION "A navigation element of the page."
  :SUBSET-OF WebPageElement
  :AXIOMS (/XMLNS/RDFS/label SiteNavigationElement "SiteNavigationElement"))

(DEFRELATION transcript (?X ?Y)
  :DOCUMENTATION "If this MediaObject is an AudioObject or VideoObject, the transcript of that object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes transcript AudioObject)
  :AXIOMS (/XMLNS/RDFS/label transcript "transcript")
  :AXIOMS (domainIncludes transcript VideoObject)
  :AXIOMS (rangeIncludes transcript Text))

(DEFCONCEPT LakeBodyOfWater (?X)
  :DOCUMENTATION "A lake (for example, Lake Pontrachain)."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label LakeBodyOfWater "LakeBodyOfWater"))

(DEFRELATION orderQuantity (?X ?Y)
  :DOCUMENTATION "The number of the item ordered. If the property is not set, assume the quantity is one."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label orderQuantity "orderQuantity")
  :AXIOMS (domainIncludes orderQuantity OrderItem)
  :AXIOMS (rangeIncludes orderQuantity Number))

(DEFCONCEPT Volcano (?X)
  :DOCUMENTATION "A volcano, like Fuji san."
  :SUBSET-OF Landform
  :AXIOMS (/XMLNS/RDFS/label Volcano "Volcano"))

(DEFRELATION applicationSuite (?X ?Y)
  :DOCUMENTATION "The name of the application suite to which the application belongs (e.g. Excel belongs to Office)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes applicationSuite Text)
  :AXIOMS (domainIncludes applicationSuite SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label applicationSuite "applicationSuite"))

(DEFRELATION saturatedFatContent (?X ?Y)
  :DOCUMENTATION "The number of grams of saturated fat."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes saturatedFatContent Mass)
  :AXIOMS (domainIncludes saturatedFatContent NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label saturatedFatContent "saturatedFatContent"))

(DEFRELATION hiringOrganization (?X ?Y)
  :DOCUMENTATION "Organization offering the job position."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label hiringOrganization "hiringOrganization")
  :AXIOMS (rangeIncludes hiringOrganization Organization)
  :AXIOMS (domainIncludes hiringOrganization JobPosting))

(DEFRELATION bestRating (?X ?Y)
  :DOCUMENTATION "The highest value allowed in this rating system. If bestRating is omitted, 5 is assumed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label bestRating "bestRating")
  :AXIOMS (domainIncludes bestRating Rating)
  :AXIOMS (rangeIncludes bestRating Text)
  :AXIOMS (rangeIncludes bestRating Number))

(DEFRELATION broadcaster (?X ?Y)
  :DOCUMENTATION "The organization owning or operating the broadcast service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes broadcaster Organization)
  :AXIOMS (domainIncludes broadcaster BroadcastService)
  :AXIOMS (/XMLNS/RDFS/label broadcaster "broadcaster"))

(DEFRELATION ingredients (?X ?Y)
  :DOCUMENTATION "A single ingredient used in the recipe, e.g. sugar, flour or garlic."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes ingredients Text)
  :AXIOMS (domainIncludes ingredients Recipe)
  :AXIOMS (supersededBy ingredients recipeIngredient)
  :AXIOMS (/XMLNS/RDFS/label ingredients "ingredients"))

(DEFRELATION recipeIngredient (?X ?Y)
  :DOCUMENTATION "A single ingredient used in the recipe, e.g. sugar, flour or garlic."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes recipeIngredient Recipe)
  :AXIOMS (/XMLNS/RDFS/label recipeIngredient "recipeIngredient")
  :AXIOMS (rangeIncludes recipeIngredient Text))

(DEFRELATION appliesToDeliveryMethod (?X ?Y)
  :DOCUMENTATION "The delivery method(s) to which the delivery charge or payment charge specification applies."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label appliesToDeliveryMethod "appliesToDeliveryMethod")
  :AXIOMS (domainIncludes appliesToDeliveryMethod PaymentChargeSpecification)
  :AXIOMS (rangeIncludes appliesToDeliveryMethod DeliveryMethod)
  :AXIOMS (domainIncludes appliesToDeliveryMethod DeliveryChargeSpecification)
  :AXIOMS (/XMLNS/DC/source appliesToDeliveryMethod |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION issuedBy (?X ?Y)
  :DOCUMENTATION "The organization issuing the ticket or permit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes issuedBy Organization)
  :AXIOMS (domainIncludes issuedBy Permit)
  :AXIOMS (domainIncludes issuedBy Ticket)
  :AXIOMS (/XMLNS/RDFS/label issuedBy "issuedBy"))

(DEFRELATION highPrice (?X ?Y)
  :DOCUMENTATION "The highest price of all offers available."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes highPrice AggregateOffer)
  :AXIOMS (rangeIncludes highPrice Text)
  :AXIOMS (/XMLNS/RDFS/label highPrice "highPrice")
  :AXIOMS (rangeIncludes highPrice Number))

(DEFRELATION deliveryMethod (?X ?Y)
  :DOCUMENTATION "A sub property of instrument. The method of delivery."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF instrument
  :AXIOMS (rangeIncludes deliveryMethod DeliveryMethod)
  :AXIOMS (domainIncludes deliveryMethod SendAction)
  :AXIOMS (domainIncludes deliveryMethod OrderAction)
  :AXIOMS (domainIncludes deliveryMethod TrackAction)
  :AXIOMS (domainIncludes deliveryMethod ReceiveAction)
  :AXIOMS (/XMLNS/RDFS/label deliveryMethod "deliveryMethod"))

(DEFRELATION instrument (?X ?Y)
  :DOCUMENTATION "The object that helped the agent perform the action. e.g. John wrote a book with <em>a pen</em>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes instrument Action)
  :AXIOMS (/XMLNS/RDFS/label instrument "instrument")
  :AXIOMS (rangeIncludes instrument Thing))

(DEFRELATION recipient (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The participant who is at the receiving end of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (domainIncludes recipient PayAction)
  :AXIOMS (domainIncludes recipient CommunicateAction)
  :AXIOMS (rangeIncludes recipient Audience)
  :AXIOMS (rangeIncludes recipient Organization)
  :AXIOMS (domainIncludes recipient TipAction)
  :AXIOMS (domainIncludes recipient ReturnAction)
  :AXIOMS (domainIncludes recipient GiveAction)
  :AXIOMS (domainIncludes recipient AuthorizeAction)
  :AXIOMS (domainIncludes recipient Message)
  :AXIOMS (/XMLNS/RDFS/label recipient "recipient")
  :AXIOMS (domainIncludes recipient SendAction)
  :AXIOMS (rangeIncludes recipient Person)
  :AXIOMS (domainIncludes recipient DonateAction))

(DEFCONCEPT Resort (?X)
  :DOCUMENTATION "A resort is a place used for relaxation or recreation, attracting visitors for holidays or vacations. Resorts are places, towns or sometimes commercial establishment operated by a single company (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/Resort\">http://en.wikipedia.org/wiki/Resort</a>).
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF LodgingBusiness
  :AXIOMS (/XMLNS/DC/source Resort |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label Resort "Resort"))

(DEFRELATION priceValidUntil (?X ?Y)
  :DOCUMENTATION "The date after which the price is no longer available."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label priceValidUntil "priceValidUntil")
  :AXIOMS (domainIncludes priceValidUntil Offer)
  :AXIOMS (rangeIncludes priceValidUntil Date))

(DEFRELATION yearsInOperation (?X ?Y)
  :DOCUMENTATION "The age of the business."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label yearsInOperation "yearsInOperation")
  :AXIOMS (rangeIncludes yearsInOperation QuantitativeValue)
  :AXIOMS (domainIncludes yearsInOperation BusinessAudience))

(DEFRELATION sugarContent (?X ?Y)
  :DOCUMENTATION "The number of grams of sugar."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes sugarContent NutritionInformation)
  :AXIOMS (rangeIncludes sugarContent Mass)
  :AXIOMS (/XMLNS/RDFS/label sugarContent "sugarContent"))

(DEFCONCEPT State (?X)
  :DOCUMENTATION "A state or province of a country."
  :SUBSET-OF AdministrativeArea
  :AXIOMS (/XMLNS/RDFS/label State "State"))

(DEFRELATION gameLocation (?X ?Y)
  :DOCUMENTATION "Real or fictional location of the game (or part of game)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes gameLocation PostalAddress)
  :AXIOMS (rangeIncludes gameLocation URL)
  :AXIOMS (rangeIncludes gameLocation Place)
  :AXIOMS (domainIncludes gameLocation VideoGameSeries)
  :AXIOMS (domainIncludes gameLocation Game)
  :AXIOMS (/XMLNS/RDFS/label gameLocation "gameLocation"))

(DEFCONCEPT DisagreeAction (?X)
  :DOCUMENTATION "The act of expressing a difference of opinion with the object. An agent disagrees to/about an object (a proposition, topic or theme) with participants."
  :SUBSET-OF ReactAction
  :AXIOMS (/XMLNS/RDFS/label DisagreeAction "DisagreeAction"))

(DEFRELATION incentives (?X ?Y)
  :DOCUMENTATION "Description of bonus and commission compensation aspects of the job."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label incentives "incentives")
  :AXIOMS (supersededBy incentives incentiveCompensation)
  :AXIOMS (domainIncludes incentives JobPosting)
  :AXIOMS (rangeIncludes incentives Text))

(DEFRELATION incentiveCompensation (?X ?Y)
  :DOCUMENTATION "Description of bonus and commission compensation aspects of the job."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label incentiveCompensation "incentiveCompensation")
  :AXIOMS (domainIncludes incentiveCompensation JobPosting)
  :AXIOMS (rangeIncludes incentiveCompensation Text))

(DEFCONCEPT UserBlocks (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (supersededBy UserBlocks InteractionCounter)
  :AXIOMS (/XMLNS/RDFS/label UserBlocks "UserBlocks"))

(DEFRELATION tickerSymbol (?X ?Y)
  :DOCUMENTATION "The exchange traded instrument associated with a Corporation object. The tickerSymbol is expressed as an exchange and an instrument name separated by a space character. For the exchange component of the tickerSymbol attribute, we reccommend using the controlled vocaulary of Market Identifier Codes (MIC) specified in ISO15022."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label tickerSymbol "tickerSymbol")
  :AXIOMS (rangeIncludes tickerSymbol Text)
  :AXIOMS (domainIncludes tickerSymbol Corporation))

(DEFCONCEPT Canal (?X)
  :DOCUMENTATION "A canal, like the Panama Canal."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label Canal "Canal"))

(DEFRELATION homeTeam (?X ?Y)
  :DOCUMENTATION "The home team in a sports event."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF competitor
  :AXIOMS (rangeIncludes homeTeam Person)
  :AXIOMS (rangeIncludes homeTeam SportsTeam)
  :AXIOMS (domainIncludes homeTeam SportsEvent)
  :AXIOMS (/XMLNS/RDFS/label homeTeam "homeTeam"))

(DEFRELATION competitor (?X ?Y)
  :DOCUMENTATION "A competitor in a sports event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes competitor SportsEvent)
  :AXIOMS (/XMLNS/RDFS/label competitor "competitor")
  :AXIOMS (rangeIncludes competitor Person)
  :AXIOMS (rangeIncludes competitor SportsTeam))

(ASSERT
 (AND (ReservationStatusType ReservationHold)
  (DOCUMENTATION ReservationHold "The status of a reservation on hold pending an update like credit card number or flight changes.")
  (/XMLNS/RDFS/label ReservationHold "ReservationHold")))

(DEFRELATION sku (?X ?Y)
  :DOCUMENTATION "The Stock Keeping Unit (SKU), i.e. a merchant-specific identifier for a product or service, or the product to which the offer refers."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes sku Demand)
  :AXIOMS (rangeIncludes sku Text)
  :AXIOMS (domainIncludes sku Offer)
  :AXIOMS (domainIncludes sku Product)
  :AXIOMS (/XMLNS/RDFS/label sku "sku")
  :AXIOMS (/XMLNS/DC/source sku |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION reviewedBy (?X ?Y)
  :DOCUMENTATION "People or organizations that have reviewed the content on this web page for accuracy and/or completeness."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes reviewedBy Organization)
  :AXIOMS (rangeIncludes reviewedBy Person)
  :AXIOMS (domainIncludes reviewedBy WebPage)
  :AXIOMS (/XMLNS/RDFS/label reviewedBy "reviewedBy"))

(DEFRELATION billingPeriod (?X ?Y)
  :DOCUMENTATION "The time interval used to compute the invoice."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes billingPeriod Invoice)
  :AXIOMS (rangeIncludes billingPeriod Duration)
  :AXIOMS (/XMLNS/RDFS/label billingPeriod "billingPeriod"))

(DEFRELATION numberOfPlayers (?X ?Y)
  :DOCUMENTATION "Indicate how many people can play this game (minimum, maximum, or range)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numberOfPlayers QuantitativeValue)
  :AXIOMS (domainIncludes numberOfPlayers Game)
  :AXIOMS (/XMLNS/RDFS/label numberOfPlayers "numberOfPlayers")
  :AXIOMS (domainIncludes numberOfPlayers VideoGameSeries))

(DEFRELATION typeOfGood (?X ?Y)
  :DOCUMENTATION "The product that this structured value is referring to."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source typeOfGood |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label typeOfGood "typeOfGood")
  :AXIOMS (domainIncludes typeOfGood OwnershipInfo)
  :AXIOMS (rangeIncludes typeOfGood Product)
  :AXIOMS (rangeIncludes typeOfGood Service)
  :AXIOMS (domainIncludes typeOfGood TypeAndQuantityNode))

(DEFCONCEPT WPSideBar (?X)
  :DOCUMENTATION "A sidebar section of the page."
  :SUBSET-OF WebPageElement
  :AXIOMS (/XMLNS/RDFS/label WPSideBar "WPSideBar"))

(DEFRELATION founders (?X ?Y)
  :DOCUMENTATION "A person who founded this organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label founders "founders")
  :AXIOMS (domainIncludes founders Organization)
  :AXIOMS (supersededBy founders founder)
  :AXIOMS (rangeIncludes founders Person))

(DEFRELATION founder (?X ?Y)
  :DOCUMENTATION "A person who founded this organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes founder Person)
  :AXIOMS (/XMLNS/RDFS/label founder "founder")
  :AXIOMS (domainIncludes founder Organization))

(DEFRELATION inPlaylist (?X ?Y)
  :DOCUMENTATION "The playlist to which this recording belongs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes inPlaylist MusicPlaylist)
  :AXIOMS (/XMLNS/RDFS/label inPlaylist "inPlaylist")
  :AXIOMS (domainIncludes inPlaylist MusicRecording))

(DEFCONCEPT Notary (?X)
  :DOCUMENTATION "A notary."
  :SUBSET-OF LegalService
  :AXIOMS (/XMLNS/RDFS/label Notary "Notary"))

(DEFRELATION includesObject (?X ?Y)
  :DOCUMENTATION "This links to a node or nodes indicating the exact quantity of the products included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes includesObject TypeAndQuantityNode)
  :AXIOMS (/XMLNS/RDFS/label includesObject "includesObject")
  :AXIOMS (domainIncludes includesObject Offer)
  :AXIOMS (/XMLNS/DC/source includesObject |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes includesObject Demand))

(DEFRELATION securityScreening (?X ?Y)
  :DOCUMENTATION "The type of security screening the passenger is subject to."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes securityScreening Text)
  :AXIOMS (domainIncludes securityScreening FlightReservation)
  :AXIOMS (/XMLNS/RDFS/label securityScreening "securityScreening"))

(DEFRELATION cargoVolume (?X ?Y)
  :DOCUMENTATION "The available volume for cargo or luggage. For automobiles, this is usually the trunk volume.</p>

<p>Typical unit code(s): LTR for liters, FTQ for cubic foot/feet</p>

<p>Note: You can use <a class=\"localLink\" href=\"http://schema.org/minValue\">minValue</a> and <a class=\"localLink\" href=\"http://schema.org/maxValue\">maxValue</a> to indicate ranges."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes cargoVolume QuantitativeValue)
  :AXIOMS (/XMLNS/DC/source cargoVolume |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (domainIncludes cargoVolume Vehicle)
  :AXIOMS (/XMLNS/RDFS/label cargoVolume "cargoVolume"))

(DEFRELATION unitText (?X ?Y)
  :DOCUMENTATION "A string or text indicating the unit of measurement. Useful if you cannot provide a standard unit code for
<a href='unitCode'>unitCode</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes unitText QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label unitText "unitText")
  :AXIOMS (domainIncludes unitText PropertyValue)
  :AXIOMS (domainIncludes unitText TypeAndQuantityNode)
  :AXIOMS (rangeIncludes unitText Text)
  :AXIOMS (domainIncludes unitText UnitPriceSpecification))

(DEFRELATION productID (?X ?Y)
  :DOCUMENTATION "The product identifier, such as ISBN. For example: <code>meta itemprop=\"productID\" content=\"isbn:123-456-789\"</code>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes productID Text)
  :AXIOMS (domainIncludes productID Product)
  :AXIOMS (/XMLNS/RDFS/label productID "productID"))

(DEFRELATION produces (?X ?Y)
  :DOCUMENTATION "The tangible thing generated by the service, e.g. a passport, permit, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes produces Thing)
  :AXIOMS (domainIncludes produces Service)
  :AXIOMS (/XMLNS/RDFS/label produces "produces")
  :AXIOMS (supersededBy produces serviceOutput))

(DEFRELATION serviceOutput (?X ?Y)
  :DOCUMENTATION "The tangible thing generated by the service, e.g. a passport, permit, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serviceOutput Thing)
  :AXIOMS (/XMLNS/RDFS/label serviceOutput "serviceOutput")
  :AXIOMS (domainIncludes serviceOutput Service))

(DEFRELATION recipe (?X ?Y)
  :DOCUMENTATION "A sub property of instrument. The recipe/instructions used to perform the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF instrument
  :AXIOMS (domainIncludes recipe CookAction)
  :AXIOMS (rangeIncludes recipe Recipe)
  :AXIOMS (/XMLNS/RDFS/label recipe "recipe"))

(DEFRELATION albumReleaseType (?X ?Y)
  :DOCUMENTATION "The kind of release which this album is: single, EP or album."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source albumReleaseType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label albumReleaseType "albumReleaseType")
  :AXIOMS (domainIncludes albumReleaseType MusicAlbum)
  :AXIOMS (rangeIncludes albumReleaseType MusicAlbumReleaseType))

(DEFRELATION faxNumber (?X ?Y)
  :DOCUMENTATION "The fax number."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes faxNumber Person)
  :AXIOMS (domainIncludes faxNumber Place)
  :AXIOMS (domainIncludes faxNumber Organization)
  :AXIOMS (rangeIncludes faxNumber Text)
  :AXIOMS (domainIncludes faxNumber ContactPoint)
  :AXIOMS (/XMLNS/RDFS/label faxNumber "faxNumber"))

(DEFRELATION about (?X ?Y)
  :DOCUMENTATION "The subject matter of the content."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes about CommunicateAction)
  :AXIOMS (/XMLNS/RDFS/label about "about")
  :AXIOMS (domainIncludes about CreativeWork)
  :AXIOMS (rangeIncludes about Thing))

(DEFRELATION members (?X ?Y)
  :DOCUMENTATION "A member of this organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label members "members")
  :AXIOMS (supersededBy members member)
  :AXIOMS (domainIncludes members ProgramMembership)
  :AXIOMS (rangeIncludes members Organization)
  :AXIOMS (rangeIncludes members Person)
  :AXIOMS (domainIncludes members Organization))

(DEFRELATION member (?X ?Y)
  :DOCUMENTATION "A member of an Organization or a ProgramMembership. Organizations can be members of organizations; ProgramMembership is typically for individuals."
  ;:BINARY-RELATION TRUE
  :AXIOMS (inverseOf member memberOf)
  :AXIOMS (rangeIncludes member Organization)
  :AXIOMS (rangeIncludes member Person)
  :AXIOMS (domainIncludes member Organization)
  :AXIOMS (domainIncludes member ProgramMembership)
  :AXIOMS (/XMLNS/RDFS/label member "member"))

(DEFRELATION mentions (?X ?Y)
  :DOCUMENTATION "Indicates that the CreativeWork contains a reference to, but is not necessarily about a concept."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label mentions "mentions")
  :AXIOMS (rangeIncludes mentions Thing)
  :AXIOMS (domainIncludes mentions CreativeWork))

(DEFRELATION photos (?X ?Y)
  :DOCUMENTATION "Photographs of this place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label photos "photos")
  :AXIOMS (supersededBy photos photo)
  :AXIOMS (rangeIncludes photos Photograph)
  :AXIOMS (domainIncludes photos Place)
  :AXIOMS (rangeIncludes photos ImageObject))

(DEFRELATION photo (?X ?Y)
  :DOCUMENTATION "A photograph of this place."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF image
  :AXIOMS (/XMLNS/RDFS/label photo "photo")
  :AXIOMS (domainIncludes photo Place)
  :AXIOMS (rangeIncludes photo Photograph)
  :AXIOMS (rangeIncludes photo ImageObject))

(ASSERT
 (AND (OfferItemCondition DamagedCondition) (/XMLNS/RDFS/label DamagedCondition "DamagedCondition")
  (DOCUMENTATION DamagedCondition "Indicates that the item is damaged.")))

(DEFRELATION requiredGender (?X ?Y)
  :DOCUMENTATION "Audiences defined by a person's gender."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes requiredGender PeopleAudience)
  :AXIOMS (/XMLNS/RDFS/label requiredGender "requiredGender")
  :AXIOMS (rangeIncludes requiredGender Text))

(DEFCONCEPT CurrencyConversionService (?X)
  :DOCUMENTATION "A service to convert funds from one currency to another currency."
  :SUBSET-OF FinancialProduct
  :AXIOMS (/XMLNS/DC/source CurrencyConversionService |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label CurrencyConversionService "CurrencyConversionService"))

(DEFCONCEPT MusicVideoObject (?X)
  :DOCUMENTATION "A music video file."
  :SUBSET-OF MediaObject
  :AXIOMS (/XMLNS/RDFS/label MusicVideoObject "MusicVideoObject"))

(DEFRELATION video (?X ?Y)
  :DOCUMENTATION "An embedded video object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes video VideoObject)
  :AXIOMS (/XMLNS/RDFS/label video "video")
  :AXIOMS (domainIncludes video CreativeWork))

(DEFRELATION prepTime (?X ?Y)
  :DOCUMENTATION "The length of time it takes to prepare the recipe, in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 duration format</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label prepTime "prepTime")
  :AXIOMS (domainIncludes prepTime Recipe)
  :AXIOMS (rangeIncludes prepTime Duration))

(DEFRELATION pageEnd (?X ?Y)
  :DOCUMENTATION "The page on which the work ends; for example \"138\" or \"xvi\"."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes pageEnd Integer)
  :AXIOMS (/XMLNS/OWL/equivalentProperty pageEnd |http://purl.org/ontology/bibo/pageEnd|)
  :AXIOMS (domainIncludes pageEnd PublicationIssue)
  :AXIOMS (rangeIncludes pageEnd Text)
  :AXIOMS (/XMLNS/DC/source pageEnd |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label pageEnd "pageEnd")
  :AXIOMS (domainIncludes pageEnd Article)
  :AXIOMS (domainIncludes pageEnd PublicationVolume))

(DEFCONCEPT ConfirmAction (?X)
  :DOCUMENTATION "<p>The act of notifying someone that a future event/action is going to happen as expected.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/CancelAction\">CancelAction</a>: The antonym of ConfirmAction.</li>
</ul>
"
  :SUBSET-OF InformAction
  :AXIOMS (/XMLNS/RDFS/label ConfirmAction "ConfirmAction"))

(DEFRELATION loser (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The loser of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (/XMLNS/RDFS/label loser "loser")
  :AXIOMS (domainIncludes loser WinAction)
  :AXIOMS (rangeIncludes loser Person))

(DEFRELATION encodingFormat (?X ?Y)
  :DOCUMENTATION "mp3, mpeg4, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes encodingFormat Text)
  :AXIOMS (domainIncludes encodingFormat MediaObject)
  :AXIOMS (/XMLNS/RDFS/label encodingFormat "encodingFormat"))

(DEFCONCEPT City (?X)
  :DOCUMENTATION "A city or town."
  :SUBSET-OF AdministrativeArea
  :AXIOMS (/XMLNS/RDFS/label City "City"))

(DEFCONCEPT NGO (?X)
  :DOCUMENTATION "Organization: Non-governmental Organization."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label NGO "NGO"))

(DEFRELATION departurePlatform (?X ?Y)
  :DOCUMENTATION "The platform from which the train departs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes departurePlatform TrainTrip)
  :AXIOMS (/XMLNS/RDFS/label departurePlatform "departurePlatform")
  :AXIOMS (rangeIncludes departurePlatform Text))

(ASSERT
 (AND (PaymentStatusType PaymentDeclined) (/XMLNS/RDFS/label PaymentDeclined "PaymentDeclined")
  (DOCUMENTATION PaymentDeclined "The payee received the payment, but it was declined for some reason.")))

(DEFRELATION agent (?X ?Y)
  :DOCUMENTATION "The direct performer or driver of the action (animate or inanimate). e.g. <em>John</em> wrote a book."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes agent Action)
  :AXIOMS (rangeIncludes agent Person)
  :AXIOMS (rangeIncludes agent Organization)
  :AXIOMS (/XMLNS/RDFS/label agent "agent"))

(DEFRELATION knownVehicleDamages (?X ?Y)
  :DOCUMENTATION "A textual description of known damages, both repaired and unrepaired."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source knownVehicleDamages |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (domainIncludes knownVehicleDamages Vehicle)
  :AXIOMS (rangeIncludes knownVehicleDamages Text)
  :AXIOMS (/XMLNS/RDFS/label knownVehicleDamages "knownVehicleDamages"))

(DEFRELATION playerType (?X ?Y)
  :DOCUMENTATION "Player type required&#x2014;for example, Flash or Silverlight."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label playerType "playerType")
  :AXIOMS (domainIncludes playerType MediaObject)
  :AXIOMS (rangeIncludes playerType Text))

(DEFRELATION reviewBody (?X ?Y)
  :DOCUMENTATION "The actual body of the review."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes reviewBody Review)
  :AXIOMS (/XMLNS/RDFS/label reviewBody "reviewBody")
  :AXIOMS (rangeIncludes reviewBody Text))

(DEFRELATION valueName (?X ?Y)
  :DOCUMENTATION "Indicates the name of the PropertyValueSpecification to be used in URL templates and form encoding in a manner analogous to HTML's input@name."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes valueName PropertyValueSpecification)
  :AXIOMS (rangeIncludes valueName Text)
  :AXIOMS (/XMLNS/RDFS/label valueName "valueName"))

(DEFRELATION memoryRequirements (?X ?Y)
  :DOCUMENTATION "Minimum memory requirements."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label memoryRequirements "memoryRequirements")
  :AXIOMS (rangeIncludes memoryRequirements URL)
  :AXIOMS (rangeIncludes memoryRequirements Text)
  :AXIOMS (domainIncludes memoryRequirements SoftwareApplication))

(DEFRELATION discussionUrl (?X ?Y)
  :DOCUMENTATION "A link to the page containing the comments of the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes discussionUrl CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label discussionUrl "discussionUrl")
  :AXIOMS (rangeIncludes discussionUrl URL))

(DEFRELATION assembly (?X ?Y)
  :DOCUMENTATION "Library file name e.g., mscorlib.dll, system.web.dll."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label assembly "assembly")
  :AXIOMS (domainIncludes assembly APIReference)
  :AXIOMS (supersededBy assembly executableLibraryName)
  :AXIOMS (rangeIncludes assembly Text))

(DEFRELATION executableLibraryName (?X ?Y)
  :DOCUMENTATION "Library file name e.g., mscorlib.dll, system.web.dll."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes executableLibraryName Text)
  :AXIOMS (/XMLNS/RDFS/label executableLibraryName "executableLibraryName")
  :AXIOMS (domainIncludes executableLibraryName APIReference))

(DEFRELATION baseSalary (?X ?Y)
  :DOCUMENTATION "The base salary of the job or of an employee in an EmployeeRole."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label baseSalary "baseSalary")
  :AXIOMS (rangeIncludes baseSalary MonetaryAmount)
  :AXIOMS (domainIncludes baseSalary JobPosting)
  :AXIOMS (rangeIncludes baseSalary PriceSpecification)
  :AXIOMS (rangeIncludes baseSalary Number)
  :AXIOMS (domainIncludes baseSalary EmployeeRole))

(DEFRELATION circle (?X ?Y)
  :DOCUMENTATION "A circle is the circular region of a specified radius centered at a specified latitude and longitude. A circle is expressed as a pair followed by a radius in meters."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes circle Text)
  :AXIOMS (/XMLNS/RDFS/label circle "circle")
  :AXIOMS (domainIncludes circle GeoShape))

(DEFRELATION commentCount (?X ?Y)
  :DOCUMENTATION "The number of comments this CreativeWork (e.g. Article, Question or Answer) has received. This is most applicable to works published in Web sites with commenting system; additional comments may exist elsewhere."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes commentCount Integer)
  :AXIOMS (/XMLNS/RDFS/label commentCount "commentCount")
  :AXIOMS (domainIncludes commentCount CreativeWork))

(DEFRELATION actionStatus (?X ?Y)
  :DOCUMENTATION "Indicates the current disposition of the Action."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes actionStatus ActionStatusType)
  :AXIOMS (domainIncludes actionStatus Action)
  :AXIOMS (/XMLNS/RDFS/label actionStatus "actionStatus"))

(DEFRELATION acquiredFrom (?X ?Y)
  :DOCUMENTATION "The organization or person from which the product was acquired."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label acquiredFrom "acquiredFrom")
  :AXIOMS (rangeIncludes acquiredFrom Organization)
  :AXIOMS (domainIncludes acquiredFrom OwnershipInfo)
  :AXIOMS (rangeIncludes acquiredFrom Person)
  :AXIOMS (/XMLNS/DC/source acquiredFrom |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION performers (?X ?Y)
  :DOCUMENTATION "The main performer or performers of the event&#x2014;for example, a presenter, musician, or actor."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy performers performer)
  :AXIOMS (rangeIncludes performers Person)
  :AXIOMS (/XMLNS/RDFS/label performers "performers")
  :AXIOMS (rangeIncludes performers Organization)
  :AXIOMS (domainIncludes performers Event))

(DEFRELATION performer (?X ?Y)
  :DOCUMENTATION "A performer at the event&#x2014;for example, a presenter, musician, musical group or actor."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes performer Person)
  :AXIOMS (rangeIncludes performer Organization)
  :AXIOMS (domainIncludes performer Event)
  :AXIOMS (/XMLNS/RDFS/label performer "performer"))

(ASSERT
 (AND (ItemAvailability InStoreOnly) (/XMLNS/RDFS/label InStoreOnly "InStoreOnly")
  (DOCUMENTATION InStoreOnly "Indicates that the item is available only at physical locations.")))

(DEFRELATION occupancy (?X ?Y)
  :DOCUMENTATION "The allowed total occupancy for the accommodation in persons (including infants etc). For individual accommodations, this is not necessarily the legal maximum but defines the permitted usage as per the contractual agreement (e.g. a double room used by a single person).
Typical unit code(s): C62 for person"
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes occupancy SingleFamilyResidence)
  :AXIOMS (domainIncludes occupancy HotelRoom)
  :AXIOMS (domainIncludes occupancy Suite)
  :AXIOMS (domainIncludes occupancy Apartment)
  :AXIOMS (/XMLNS/RDFS/label occupancy "occupancy")
  :AXIOMS (rangeIncludes occupancy QuantitativeValue)
  :AXIOMS (/XMLNS/DC/source occupancy |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFRELATION sharedContent (?X ?Y)
  :DOCUMENTATION "A CreativeWork such as an image, video, or audio clip shared as part of this posting."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label sharedContent "sharedContent")
  :AXIOMS (domainIncludes sharedContent SocialMediaPosting)
  :AXIOMS (rangeIncludes sharedContent CreativeWork))

(DEFCONCEPT RejectAction (?X)
  :DOCUMENTATION "<p>The act of rejecting to/adopting an object.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/AcceptAction\">AcceptAction</a>: The antonym of RejectAction.</li>
</ul>
"
  :SUBSET-OF AllocateAction
  :AXIOMS (/XMLNS/RDFS/label RejectAction "RejectAction"))

(DEFRELATION license (?X ?Y)
  :DOCUMENTATION "A license document that applies to this content, typically indicated by URL."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes license CreativeWork)
  :AXIOMS (rangeIncludes license URL)
  :AXIOMS (domainIncludes license CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label license "license"))

(DEFRELATION vehicleConfiguration (?X ?Y)
  :DOCUMENTATION "A short text indicating the configuration of the vehicle, e.g. '5dr hatchback ST 2.5 MT 225 hp' or 'limited edition'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label vehicleConfiguration "vehicleConfiguration")
  :AXIOMS (domainIncludes vehicleConfiguration Vehicle)
  :AXIOMS (rangeIncludes vehicleConfiguration Text)
  :AXIOMS (/XMLNS/DC/source vehicleConfiguration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFRELATION reviewRating (?X ?Y)
  :DOCUMENTATION "The rating given in this review. Note that reviews can themselves be rated. The <code>reviewRating</code> applies to rating given by the review. The <a class=\"localLink\" href=\"http://schema.org/aggregateRating\">aggregateRating</a> property applies to the review itself, as a creative work."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes reviewRating Review)
  :AXIOMS (/XMLNS/RDFS/label reviewRating "reviewRating")
  :AXIOMS (rangeIncludes reviewRating Rating))

(DEFRELATION dateIssued (?X ?Y)
  :DOCUMENTATION "The date the ticket was issued."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateIssued Ticket)
  :AXIOMS (rangeIncludes dateIssued DateTime)
  :AXIOMS (/XMLNS/RDFS/label dateIssued "dateIssued"))

(DEFRELATION deathDate (?X ?Y)
  :DOCUMENTATION "Date of death."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes deathDate Person)
  :AXIOMS (/XMLNS/RDFS/label deathDate "deathDate")
  :AXIOMS (rangeIncludes deathDate Date))

(DEFRELATION isicV4 (?X ?Y)
  :DOCUMENTATION "The International Standard of Industrial Classification of All Economic Activities (ISIC), Revision 4 code for a particular organization, business person, or place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source isicV4 |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes isicV4 Place)
  :AXIOMS (domainIncludes isicV4 Person)
  :AXIOMS (domainIncludes isicV4 Organization)
  :AXIOMS (rangeIncludes isicV4 Text)
  :AXIOMS (/XMLNS/RDFS/label isicV4 "isicV4"))

(DEFRELATION givenName (?X ?Y)
  :DOCUMENTATION "Given name. In the U.S., the first name of a Person. This can be used along with familyName instead of the name property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label givenName "givenName")
  :AXIOMS (domainIncludes givenName Person)
  :AXIOMS (rangeIncludes givenName Text))

(DEFRELATION expectedArrivalUntil (?X ?Y)
  :DOCUMENTATION "The latest date the package may arrive."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes expectedArrivalUntil ParcelDelivery)
  :AXIOMS (rangeIncludes expectedArrivalUntil DateTime)
  :AXIOMS (/XMLNS/RDFS/label expectedArrivalUntil "expectedArrivalUntil"))

(DEFRELATION partySize (?X ?Y)
  :DOCUMENTATION "Number of people the reservation should accommodate."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes partySize QuantitativeValue)
  :AXIOMS (domainIncludes partySize TaxiReservation)
  :AXIOMS (domainIncludes partySize FoodEstablishmentReservation)
  :AXIOMS (/XMLNS/RDFS/label partySize "partySize")
  :AXIOMS (rangeIncludes partySize Integer))

(DEFRELATION propertyID (?X ?Y)
  :DOCUMENTATION "A commonly used identifier for the characteristic represented by the property, e.g. a manufacturer or a standard code for a property. propertyID can be
(1) a prefixed string, mainly meant to be used with standards for product properties; (2) a site-specific, non-prefixed string (e.g. the primary key of the property or the vendor-specific id of the property), or (3)
a URL indicating the type of the property, either pointing to an external vocabulary, or a Web resource that describes the property (e.g. a glossary entry).
Standards bodies should promote a standard prefix for the identifiers of properties from their standards."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label propertyID "propertyID")
  :AXIOMS (rangeIncludes propertyID URL)
  :AXIOMS (domainIncludes propertyID PropertyValue)
  :AXIOMS (rangeIncludes propertyID Text))

(DEFCONCEPT EventReservation (?X)
  :DOCUMENTATION "A reservation for an event like a concert, sporting event, or lecture.</p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use <a class=\"localLink\" href=\"http://schema.org/Offer\">Offer</a>."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label EventReservation "EventReservation"))

(DEFRELATION athlete (?X ?Y)
  :DOCUMENTATION "A person that acts as performing member of a sports team; a player as opposed to a coach."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes athlete SportsTeam)
  :AXIOMS (/XMLNS/RDFS/label athlete "athlete")
  :AXIOMS (rangeIncludes athlete Person))

(DEFRELATION mapType (?X ?Y)
  :DOCUMENTATION "Indicates the kind of Map, from the MapCategoryType Enumeration."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes mapType Map)
  :AXIOMS (/XMLNS/RDFS/label mapType "mapType")
  :AXIOMS (rangeIncludes mapType MapCategoryType))

(DEFCONCEPT WatchAction (?X)
  :DOCUMENTATION "The act of consuming dynamic/moving visual content."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label WatchAction "WatchAction"))

(DEFRELATION color (?X ?Y)
  :DOCUMENTATION "The color of the product."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label color "color")
  :AXIOMS (/XMLNS/DC/source color |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes color Text)
  :AXIOMS (domainIncludes color Product))

(DEFCONCEPT InternetCafe (?X)
  :DOCUMENTATION "An internet cafe."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label InternetCafe "InternetCafe"))

(DEFRELATION totalPaymentDue (?X ?Y)
  :DOCUMENTATION "The total amount due."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes totalPaymentDue Invoice)
  :AXIOMS (rangeIncludes totalPaymentDue PriceSpecification)
  :AXIOMS (rangeIncludes totalPaymentDue MonetaryAmount)
  :AXIOMS (/XMLNS/RDFS/label totalPaymentDue "totalPaymentDue"))

(DEFRELATION distribution (?X ?Y)
  :DOCUMENTATION "A downloadable form of this dataset, at a specific location, in a specific format."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes distribution DataDownload)
  :AXIOMS (/XMLNS/RDFS/label distribution "distribution")
  :AXIOMS (domainIncludes distribution Dataset))

(DEFRELATION specialOpeningHoursSpecification (?X ?Y)
  :DOCUMENTATION "The special opening hours of a certain place.</p>

<p>Use this to explicitly override general opening hours brought in scope by <a class=\"localLink\" href=\"http://schema.org/openingHoursSpecification\">openingHoursSpecification</a> or <a class=\"localLink\" href=\"http://schema.org/openingHours\">openingHours</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes specialOpeningHoursSpecification OpeningHoursSpecification)
  :AXIOMS (/XMLNS/RDFS/label specialOpeningHoursSpecification "specialOpeningHoursSpecification")
  :AXIOMS (domainIncludes specialOpeningHoursSpecification Place))

(DEFRELATION timeRequired (?X ?Y)
  :DOCUMENTATION "Approximate or typical time it takes to work with or through this learning resource for the typical intended target audience, e.g. 'P30M', 'P1H25M'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes timeRequired CreativeWork)
  :AXIOMS (rangeIncludes timeRequired Duration)
  :AXIOMS (/XMLNS/RDFS/label timeRequired "timeRequired"))

(DEFRELATION dropoffTime (?X ?Y)
  :DOCUMENTATION "When a rental car can be dropped off."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes dropoffTime DateTime)
  :AXIOMS (/XMLNS/RDFS/label dropoffTime "dropoffTime")
  :AXIOMS (domainIncludes dropoffTime RentalCarReservation))

(DEFRELATION availabilityEnds (?X ?Y)
  :DOCUMENTATION "The end of the availability of the product or service included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source availabilityEnds |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes availabilityEnds Demand)
  :AXIOMS (domainIncludes availabilityEnds Offer)
  :AXIOMS (rangeIncludes availabilityEnds DateTime)
  :AXIOMS (/XMLNS/RDFS/label availabilityEnds "availabilityEnds"))

(DEFRELATION firstPerformance (?X ?Y)
  :DOCUMENTATION "The date and place the work was first performed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes firstPerformance MusicComposition)
  :AXIOMS (/XMLNS/DC/source firstPerformance |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label firstPerformance "firstPerformance")
  :AXIOMS (rangeIncludes firstPerformance Event))

(DEFRELATION successorOf (?X ?Y)
  :DOCUMENTATION "A pointer from a newer variant of a product  to its previous, often discontinued predecessor."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source successorOf |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label successorOf "successorOf")
  :AXIOMS (rangeIncludes successorOf ProductModel)
  :AXIOMS (domainIncludes successorOf ProductModel))

(DEFRELATION buyer (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The participant/person/organization that bought the object."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (domainIncludes buyer SellAction)
  :AXIOMS (/XMLNS/RDFS/label buyer "buyer")
  :AXIOMS (rangeIncludes buyer Person))

(DEFRELATION addOn (?X ?Y)
  :DOCUMENTATION "An additional offer that can only be obtained in combination with the first base offer (e.g. supplements and extensions that are available for a surcharge)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes addOn Offer)
  :AXIOMS (/XMLNS/DC/source addOn |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes addOn Offer)
  :AXIOMS (/XMLNS/RDFS/label addOn "addOn"))

(DEFCONCEPT UserPageVisits (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (supersededBy UserPageVisits InteractionCounter)
  :AXIOMS (/XMLNS/RDFS/label UserPageVisits "UserPageVisits"))

(DEFRELATION businessFunction (?X ?Y)
  :DOCUMENTATION "The business function (e.g. sell, lease, repair, dispose) of the offer or component of a bundle (TypeAndQuantityNode). The default is http://purl.org/goodrelations/v1#Sell."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes businessFunction TypeAndQuantityNode)
  :AXIOMS (/XMLNS/DC/source businessFunction |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes businessFunction Demand)
  :AXIOMS (domainIncludes businessFunction Offer)
  :AXIOMS (/XMLNS/RDFS/label businessFunction "businessFunction")
  :AXIOMS (rangeIncludes businessFunction BusinessFunction))

(DEFRELATION discusses (?X ?Y)
  :DOCUMENTATION "Specifies the CreativeWork associated with the UserComment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes discusses UserComments)
  :AXIOMS (/XMLNS/RDFS/label discusses "discusses")
  :AXIOMS (rangeIncludes discusses CreativeWork))

(DEFCONCEPT PublicSwimmingPool (?X)
  :DOCUMENTATION "A public swimming pool."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label PublicSwimmingPool "PublicSwimmingPool"))

(DEFCONCEPT IceCreamShop (?X)
  :DOCUMENTATION "An ice cream shop."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label IceCreamShop "IceCreamShop"))

(DEFCONCEPT HousePainter (?X)
  :DOCUMENTATION "A house painting service."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label HousePainter "HousePainter"))

(DEFRELATION releasedEvent (?X ?Y)
  :DOCUMENTATION "The place and time the release was issued, expressed as a PublicationEvent."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes releasedEvent CreativeWork)
  :AXIOMS (rangeIncludes releasedEvent PublicationEvent)
  :AXIOMS (/XMLNS/RDFS/label releasedEvent "releasedEvent"))

(DEFRELATION ticketedSeat (?X ?Y)
  :DOCUMENTATION "The seat associated with the ticket."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes ticketedSeat Ticket)
  :AXIOMS (/XMLNS/RDFS/label ticketedSeat "ticketedSeat")
  :AXIOMS (rangeIncludes ticketedSeat Seat))

(DEFRELATION courseCode (?X ?Y)
  :DOCUMENTATION "The identifier for the <a class=\"localLink\" href=\"http://schema.org/Course\">Course</a> used by the course <a class=\"localLink\" href=\"http://schema.org/provider\">provider</a> (e.g. CS101 or 6.001)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes courseCode Text)
  :AXIOMS (/XMLNS/RDFS/label courseCode "courseCode")
  :AXIOMS (domainIncludes courseCode Course))

(DEFRELATION replacee (?X ?Y)
  :DOCUMENTATION "A sub property of object. The object that is being replaced."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (/XMLNS/RDFS/label replacee "replacee")
  :AXIOMS (domainIncludes replacee ReplaceAction)
  :AXIOMS (rangeIncludes replacee Thing))

(ASSERT
 (AND (RsvpResponseType RsvpResponseNo) (DOCUMENTATION RsvpResponseNo "The invitee will not attend.") (/XMLNS/RDFS/label RsvpResponseNo "RsvpResponseNo")))

(ASSERT
 (AND (GameServerStatus OfflinePermanently) (/XMLNS/RDFS/label OfflinePermanently "OfflinePermanently")
  (DOCUMENTATION OfflinePermanently "Game server status: OfflinePermanently. Server is offline and not available.")))

(DEFRELATION priceRange (?X ?Y)
  :DOCUMENTATION "The price range of the business, for example <code>$$$</code>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes priceRange Text)
  :AXIOMS (domainIncludes priceRange LocalBusiness)
  :AXIOMS (/XMLNS/RDFS/label priceRange "priceRange"))

(DEFRELATION gameItem (?X ?Y)
  :DOCUMENTATION "An item is an object within the game world that can be collected by a player or, occasionally, a non-player character."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes gameItem Game)
  :AXIOMS (/XMLNS/RDFS/label gameItem "gameItem")
  :AXIOMS (rangeIncludes gameItem Thing)
  :AXIOMS (domainIncludes gameItem VideoGameSeries))

(DEFRELATION cheatCode (?X ?Y)
  :DOCUMENTATION "Cheat codes to the game."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes cheatCode CreativeWork)
  :AXIOMS (domainIncludes cheatCode VideoGame)
  :AXIOMS (/XMLNS/RDFS/label cheatCode "cheatCode")
  :AXIOMS (domainIncludes cheatCode VideoGameSeries))

(DEFRELATION awards (?X ?Y)
  :DOCUMENTATION "Awards won by or for this item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes awards Organization)
  :AXIOMS (domainIncludes awards Person)
  :AXIOMS (supersededBy awards award)
  :AXIOMS (/XMLNS/RDFS/label awards "awards")
  :AXIOMS (rangeIncludes awards Text)
  :AXIOMS (domainIncludes awards CreativeWork)
  :AXIOMS (domainIncludes awards Product))

(DEFRELATION award (?X ?Y)
  :DOCUMENTATION "An award won by or for this item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes award Product)
  :AXIOMS (/XMLNS/RDFS/label award "award")
  :AXIOMS (domainIncludes award Service)
  :AXIOMS (domainIncludes award CreativeWork)
  :AXIOMS (rangeIncludes award Text)
  :AXIOMS (domainIncludes award Organization)
  :AXIOMS (domainIncludes award Person))

(DEFRELATION availableAtOrFrom (?X ?Y)
  :DOCUMENTATION "The place(s) from which the offer can be obtained (e.g. store locations)."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF areaServed
  :AXIOMS (/XMLNS/DC/source availableAtOrFrom |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes availableAtOrFrom Demand)
  :AXIOMS (domainIncludes availableAtOrFrom Offer)
  :AXIOMS (/XMLNS/RDFS/label availableAtOrFrom "availableAtOrFrom")
  :AXIOMS (rangeIncludes availableAtOrFrom Place))

(DEFRELATION areaServed (?X ?Y)
  :DOCUMENTATION "The geographic area where a service or offered item is provided."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes areaServed ContactPoint)
  :AXIOMS (/XMLNS/RDFS/label areaServed "areaServed")
  :AXIOMS (domainIncludes areaServed Offer)
  :AXIOMS (domainIncludes areaServed Organization)
  :AXIOMS (domainIncludes areaServed DeliveryChargeSpecification)
  :AXIOMS (rangeIncludes areaServed Text)
  :AXIOMS (rangeIncludes areaServed Place)
  :AXIOMS (rangeIncludes areaServed AdministrativeArea)
  :AXIOMS (rangeIncludes areaServed GeoShape)
  :AXIOMS (domainIncludes areaServed Service)
  :AXIOMS (domainIncludes areaServed Demand))

(DEFRELATION courseMode (?X ?Y)
  :DOCUMENTATION "The medium or means of delivery of the course instance or the mode of study, either as a text label (e.g. \"online\", \"onsite\" or \"blended\"; \"synchronous\" or \"asynchronous\"; \"full-time\" or \"part-time\") or as a URL reference to a term from a controlled vocabulary (e.g. https://ceds.ed.gov/element/001311#Asynchronous )."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label courseMode "courseMode")
  :AXIOMS (domainIncludes courseMode CourseInstance)
  :AXIOMS (rangeIncludes courseMode Text)
  :AXIOMS (rangeIncludes courseMode URL))

(DEFRELATION geo (?X ?Y)
  :DOCUMENTATION "The geo coordinates of the place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes geo Place)
  :AXIOMS (rangeIncludes geo GeoShape)
  :AXIOMS (rangeIncludes geo GeoCoordinates)
  :AXIOMS (/XMLNS/RDFS/label geo "geo"))

(DEFRELATION follows (?X ?Y)
  :DOCUMENTATION "The most generic uni-directional social relation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes follows Person)
  :AXIOMS (domainIncludes follows Person)
  :AXIOMS (/XMLNS/RDFS/label follows "follows"))

(DEFCONCEPT WantAction (?X)
  :DOCUMENTATION "The act of expressing a desire about the object. An agent wants an object."
  :SUBSET-OF ReactAction
  :AXIOMS (/XMLNS/RDFS/label WantAction "WantAction"))

(DEFRELATION exifData (?X ?Y)
  :DOCUMENTATION "exif data for this object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes exifData PropertyValue)
  :AXIOMS (/XMLNS/RDFS/label exifData "exifData")
  :AXIOMS (domainIncludes exifData ImageObject)
  :AXIOMS (rangeIncludes exifData Text))

(DEFRELATION vehicleSpecialUsage (?X ?Y)
  :DOCUMENTATION "Indicates whether the vehicle has been used for special purposes, like commercial rental, driving school, or as a taxi. The legislation in many countries requires this information to be revealed when offering a car for sale."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes vehicleSpecialUsage Vehicle)
  :AXIOMS (rangeIncludes vehicleSpecialUsage Text)
  :AXIOMS (/XMLNS/RDFS/label vehicleSpecialUsage "vehicleSpecialUsage"))

(ASSERT
 (AND (ActionStatusType FailedActionStatus) (/XMLNS/RDFS/label FailedActionStatus "FailedActionStatus")
  (DOCUMENTATION FailedActionStatus
   "An action that failed to complete. The action's error property and the HTTP return code contain more information about the failure.")))

(DEFRELATION dateReceived (?X ?Y)
  :DOCUMENTATION "The date/time the message was received if a single recipient exists."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateReceived Message)
  :AXIOMS (/XMLNS/RDFS/label dateReceived "dateReceived")
  :AXIOMS (rangeIncludes dateReceived DateTime))

(DEFRELATION musicReleaseFormat (?X ?Y)
  :DOCUMENTATION "Format of this release (the type of recording media used, ie. compact disc, digital media, LP, etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source musicReleaseFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (domainIncludes musicReleaseFormat MusicRelease)
  :AXIOMS (/XMLNS/RDFS/label musicReleaseFormat "musicReleaseFormat")
  :AXIOMS (rangeIncludes musicReleaseFormat MusicReleaseFormatType))

(DEFRELATION aggregateRating (?X ?Y)
  :DOCUMENTATION "The overall rating, based on a collection of reviews or ratings, of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes aggregateRating Event)
  :AXIOMS (domainIncludes aggregateRating CreativeWork)
  :AXIOMS (domainIncludes aggregateRating Brand)
  :AXIOMS (domainIncludes aggregateRating Place)
  :AXIOMS (domainIncludes aggregateRating Product)
  :AXIOMS (rangeIncludes aggregateRating AggregateRating)
  :AXIOMS (/XMLNS/RDFS/label aggregateRating "aggregateRating")
  :AXIOMS (domainIncludes aggregateRating Offer)
  :AXIOMS (domainIncludes aggregateRating Organization)
  :AXIOMS (domainIncludes aggregateRating Service))

(DEFRELATION lender (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The person that lends the object being borrowed."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (domainIncludes lender BorrowAction)
  :AXIOMS (rangeIncludes lender Person)
  :AXIOMS (/XMLNS/RDFS/label lender "lender"))

(DEFRELATION inAlbum (?X ?Y)
  :DOCUMENTATION "The album to which this recording belongs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes inAlbum MusicAlbum)
  :AXIOMS (domainIncludes inAlbum MusicRecording)
  :AXIOMS (/XMLNS/RDFS/label inAlbum "inAlbum"))

(DEFCONCEPT ActivateAction (?X)
  :DOCUMENTATION "The act of starting or activating a device or application (e.g. starting a timer or turning on a flashlight)."
  :SUBSET-OF ControlAction
  :AXIOMS (/XMLNS/RDFS/label ActivateAction "ActivateAction"))

(DEFRELATION performerIn (?X ?Y)
  :DOCUMENTATION "Event that this person is a performer or participant in."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes performerIn Event)
  :AXIOMS (/XMLNS/RDFS/label performerIn "performerIn")
  :AXIOMS (domainIncludes performerIn Person))

(DEFRELATION dateModified (?X ?Y)
  :DOCUMENTATION "The date on which the CreativeWork was most recently modified or when the item's entry was modified within a DataFeed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateModified CreativeWork)
  :AXIOMS (rangeIncludes dateModified Date)
  :AXIOMS (rangeIncludes dateModified DateTime)
  :AXIOMS (/XMLNS/RDFS/label dateModified "dateModified")
  :AXIOMS (domainIncludes dateModified DataFeedItem))

(DEFRELATION collection (?X ?Y)
  :DOCUMENTATION "A sub property of object. The collection target of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (supersededBy collection targetCollection)
  :AXIOMS (/XMLNS/RDFS/label collection "collection")
  :AXIOMS (rangeIncludes collection Thing)
  :AXIOMS (domainIncludes collection UpdateAction))

(DEFRELATION targetCollection (?X ?Y)
  :DOCUMENTATION "A sub property of object. The collection target of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (domainIncludes targetCollection UpdateAction)
  :AXIOMS (/XMLNS/RDFS/label targetCollection "targetCollection")
  :AXIOMS (rangeIncludes targetCollection Thing))

(DEFRELATION httpMethod (?X ?Y)
  :DOCUMENTATION "An HTTP method that specifies the appropriate HTTP method for a request to an HTTP EntryPoint. Values are capitalized strings as used in HTTP."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label httpMethod "httpMethod")
  :AXIOMS (rangeIncludes httpMethod Text)
  :AXIOMS (domainIncludes httpMethod EntryPoint))

(DEFRELATION screenshot (?X ?Y)
  :DOCUMENTATION "A link to a screenshot image of the app."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes screenshot SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label screenshot "screenshot")
  :AXIOMS (rangeIncludes screenshot ImageObject)
  :AXIOMS (rangeIncludes screenshot URL))

(DEFCONCEPT ComedyEvent (?X)
  :DOCUMENTATION "Event type: Comedy event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label ComedyEvent "ComedyEvent"))

(DEFRELATION valueReference (?X ?Y)
  :DOCUMENTATION "A pointer to a secondary value that provides additional information on the original value, e.g. a reference temperature."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label valueReference "valueReference")
  :AXIOMS (rangeIncludes valueReference QualitativeValue)
  :AXIOMS (rangeIncludes valueReference StructuredValue)
  :AXIOMS (rangeIncludes valueReference Enumeration)
  :AXIOMS (domainIncludes valueReference QualitativeValue)
  :AXIOMS (domainIncludes valueReference QuantitativeValue)
  :AXIOMS (rangeIncludes valueReference QuantitativeValue)
  :AXIOMS (rangeIncludes valueReference PropertyValue)
  :AXIOMS (domainIncludes valueReference PropertyValue)
  :AXIOMS (/XMLNS/DC/source valueReference |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION valueAddedTaxIncluded (?X ?Y)
  :DOCUMENTATION "Specifies whether the applicable value-added tax (VAT) is included in the price specification or not."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes valueAddedTaxIncluded Boolean)
  :AXIOMS (/XMLNS/DC/source valueAddedTaxIncluded |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label valueAddedTaxIncluded "valueAddedTaxIncluded")
  :AXIOMS (domainIncludes valueAddedTaxIncluded PriceSpecification))

(DEFRELATION lowPrice (?X ?Y)
  :DOCUMENTATION "The lowest price of all offers available."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes lowPrice Text)
  :AXIOMS (domainIncludes lowPrice AggregateOffer)
  :AXIOMS (/XMLNS/RDFS/label lowPrice "lowPrice")
  :AXIOMS (rangeIncludes lowPrice Number))

(DEFRELATION bookFormat (?X ?Y)
  :DOCUMENTATION "The format of the book."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label bookFormat "bookFormat")
  :AXIOMS (domainIncludes bookFormat Book)
  :AXIOMS (rangeIncludes bookFormat BookFormatType))

(DEFRELATION includedDataCatalog (?X ?Y)
  :DOCUMENTATION "A data catalog which contains this dataset (this property was previously 'catalog', preferred name is now 'includedInDataCatalog')."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy includedDataCatalog includedInDataCatalog)
  :AXIOMS (rangeIncludes includedDataCatalog DataCatalog)
  :AXIOMS (/XMLNS/RDFS/label includedDataCatalog "includedDataCatalog")
  :AXIOMS (domainIncludes includedDataCatalog Dataset))

(DEFRELATION includedInDataCatalog (?X ?Y)
  :DOCUMENTATION "A data catalog which contains this dataset."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label includedInDataCatalog "includedInDataCatalog")
  :AXIOMS (domainIncludes includedInDataCatalog Dataset)
  :AXIOMS (inverseOf includedInDataCatalog dataset)
  :AXIOMS (rangeIncludes includedInDataCatalog DataCatalog))

(DEFCONCEPT School (?X)
  :DOCUMENTATION "A school."
  :SUBSET-OF EducationalOrganization
  :AXIOMS (/XMLNS/RDFS/label School "School"))

(DEFRELATION audienceType (?X ?Y)
  :DOCUMENTATION "The target group associated with a given audience (e.g. veterans, car owners, musicians, etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label audienceType "audienceType")
  :AXIOMS (rangeIncludes audienceType Text)
  :AXIOMS (domainIncludes audienceType Audience))

(DEFRELATION startDate (?X ?Y)
  :DOCUMENTATION "The start date and time of the item (in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 date format</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes startDate CreativeWorkSeries)
  :AXIOMS (/XMLNS/RDFS/label startDate "startDate")
  :AXIOMS (domainIncludes startDate CreativeWorkSeason)
  :AXIOMS (domainIncludes startDate DatedMoneySpecification)
  :AXIOMS (rangeIncludes startDate DateTime)
  :AXIOMS (domainIncludes startDate Role)
  :AXIOMS (domainIncludes startDate Event)
  :AXIOMS (rangeIncludes startDate Date))

(DEFRELATION department (?X ?Y)
  :DOCUMENTATION "A relationship between an organization and a department of that organization, also described as an organization (allowing different urls, logos, opening hours). For example: a store with a pharmacy, or a bakery with a cafe."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes department Organization)
  :AXIOMS (domainIncludes department Organization)
  :AXIOMS (/XMLNS/RDFS/label department "department"))

(DEFCONCEPT HinduTemple (?X)
  :DOCUMENTATION "A Hindu temple."
  :SUBSET-OF PlaceOfWorship
  :AXIOMS (/XMLNS/RDFS/label HinduTemple "HinduTemple"))

(ASSERT
 (AND (RestrictedDiet HalalDiet) (DOCUMENTATION HalalDiet "A diet conforming to Islamic dietary practices.") (/XMLNS/RDFS/label HalalDiet "HalalDiet")))

(DEFRELATION countriesNotSupported (?X ?Y)
  :DOCUMENTATION "Countries for which the application is not supported. You can also provide the two-letter ISO 3166-1 alpha-2 country code."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes countriesNotSupported SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label countriesNotSupported "countriesNotSupported")
  :AXIOMS (rangeIncludes countriesNotSupported Text))

(DEFCONCEPT NoteDigitalDocument (?X)
  :DOCUMENTATION "A file containing a note, primarily for the author."
  :SUBSET-OF DigitalDocument
  :AXIOMS (/XMLNS/RDFS/label NoteDigitalDocument "NoteDigitalDocument"))

(DEFRELATION originAddress (?X ?Y)
  :DOCUMENTATION "Shipper's address."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes originAddress ParcelDelivery)
  :AXIOMS (rangeIncludes originAddress PostalAddress)
  :AXIOMS (/XMLNS/RDFS/label originAddress "originAddress"))

(DEFRELATION carbohydrateContent (?X ?Y)
  :DOCUMENTATION "The number of grams of carbohydrates."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes carbohydrateContent NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label carbohydrateContent "carbohydrateContent")
  :AXIOMS (rangeIncludes carbohydrateContent Mass))

(DEFCONCEPT MovieRentalStore (?X)
  :DOCUMENTATION "A movie rental store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label MovieRentalStore "MovieRentalStore"))

(DEFRELATION workHours (?X ?Y)
  :DOCUMENTATION "The typical working hours for this job (e.g. 1st shift, night shift, 8am-5pm)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label workHours "workHours")
  :AXIOMS (domainIncludes workHours JobPosting)
  :AXIOMS (rangeIncludes workHours Text))

(DEFRELATION alternativeHeadline (?X ?Y)
  :DOCUMENTATION "A secondary title of the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label alternativeHeadline "alternativeHeadline")
  :AXIOMS (domainIncludes alternativeHeadline CreativeWork)
  :AXIOMS (rangeIncludes alternativeHeadline Text))

(DEFRELATION typicalAgeRange (?X ?Y)
  :DOCUMENTATION "The typical expected age range, e.g. '7-9', '11-'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes typicalAgeRange CreativeWork)
  :AXIOMS (rangeIncludes typicalAgeRange Text)
  :AXIOMS (/XMLNS/RDFS/label typicalAgeRange "typicalAgeRange")
  :AXIOMS (domainIncludes typicalAgeRange Event))

(DEFRELATION childMaxAge (?X ?Y)
  :DOCUMENTATION "Maximal age of the child."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label childMaxAge "childMaxAge")
  :AXIOMS (domainIncludes childMaxAge ParentAudience)
  :AXIOMS (rangeIncludes childMaxAge Number))

(DEFRELATION totalPrice (?X ?Y)
  :DOCUMENTATION "The total price for the reservation or ticket, including applicable taxes, shipping, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes totalPrice Number)
  :AXIOMS (/XMLNS/RDFS/label totalPrice "totalPrice")
  :AXIOMS (rangeIncludes totalPrice Text)
  :AXIOMS (rangeIncludes totalPrice PriceSpecification)
  :AXIOMS (domainIncludes totalPrice Reservation)
  :AXIOMS (domainIncludes totalPrice Ticket))

(DEFRELATION suitableForDiet (?X ?Y)
  :DOCUMENTATION "Indicates a dietary restriction or guideline for which this recipe is suitable, e.g. diabetic, halal etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes suitableForDiet RestrictedDiet)
  :AXIOMS (/XMLNS/RDFS/label suitableForDiet "suitableForDiet")
  :AXIOMS (domainIncludes suitableForDiet Recipe))

(DEFRELATION permissions (?X ?Y)
  :DOCUMENTATION "Permission(s) required to run the app (for example, a mobile app may require full internet access or may run only on wifi)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label permissions "permissions")
  :AXIOMS (rangeIncludes permissions Text)
  :AXIOMS (domainIncludes permissions SoftwareApplication))

(DEFRELATION interestRate (?X ?Y)
  :DOCUMENTATION "The interest rate, charged or paid, applicable to the financial product. Note: This is different from the calculated annualPercentageRate."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes interestRate Number)
  :AXIOMS (rangeIncludes interestRate QuantitativeValue)
  :AXIOMS (domainIncludes interestRate FinancialProduct)
  :AXIOMS (/XMLNS/DC/source interestRate |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label interestRate "interestRate"))

(DEFRELATION requiredMinAge (?X ?Y)
  :DOCUMENTATION "Audiences defined by a person's minimum age."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes requiredMinAge Integer)
  :AXIOMS (/XMLNS/RDFS/label requiredMinAge "requiredMinAge")
  :AXIOMS (domainIncludes requiredMinAge PeopleAudience))

(DEFRELATION minPrice (?X ?Y)
  :DOCUMENTATION "The lowest price if the price is a range."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label minPrice "minPrice")
  :AXIOMS (/XMLNS/DC/source minPrice |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes minPrice Number)
  :AXIOMS (domainIncludes minPrice PriceSpecification))

(DEFRELATION unitCode (?X ?Y)
  :DOCUMENTATION "The unit of measurement given using the UN/CEFACT Common Code (3 characters) or a URL. Other codes than the UN/CEFACT Common Code may be used with a prefix followed by a colon."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label unitCode "unitCode")
  :AXIOMS (rangeIncludes unitCode Text)
  :AXIOMS (domainIncludes unitCode QuantitativeValue)
  :AXIOMS (domainIncludes unitCode PropertyValue)
  :AXIOMS (rangeIncludes unitCode URL)
  :AXIOMS (/XMLNS/DC/source unitCode |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes unitCode UnitPriceSpecification)
  :AXIOMS (domainIncludes unitCode TypeAndQuantityNode))

(ASSERT
 (AND (MusicAlbumReleaseType BroadcastRelease) (/XMLNS/DC/source BroadcastRelease |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (DOCUMENTATION BroadcastRelease "BroadcastRelease.") (/XMLNS/RDFS/label BroadcastRelease "BroadcastRelease")))

(DEFRELATION printEdition (?X ?Y)
  :DOCUMENTATION "The edition of the print product in which the NewsArticle appears."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes printEdition NewsArticle)
  :AXIOMS (/XMLNS/RDFS/label printEdition "printEdition")
  :AXIOMS (rangeIncludes printEdition Text))

(DEFRELATION departureStation (?X ?Y)
  :DOCUMENTATION "The station from which the train departs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label departureStation "departureStation")
  :AXIOMS (domainIncludes departureStation TrainTrip)
  :AXIOMS (rangeIncludes departureStation TrainStation))

(DEFRELATION openingHoursSpecification (?X ?Y)
  :DOCUMENTATION "The opening hours of a certain place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes openingHoursSpecification Place)
  :AXIOMS (/XMLNS/RDFS/label openingHoursSpecification "openingHoursSpecification")
  :AXIOMS (rangeIncludes openingHoursSpecification OpeningHoursSpecification)
  :AXIOMS (/XMLNS/DC/source openingHoursSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION contentUrl (?X ?Y)
  :DOCUMENTATION "Actual bytes of the media object, for example the image file or video file."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contentUrl "contentUrl")
  :AXIOMS (domainIncludes contentUrl MediaObject)
  :AXIOMS (rangeIncludes contentUrl URL))

(DEFRELATION referenceQuantity (?X ?Y)
  :DOCUMENTATION "The reference quantity for which a certain price applies, e.g. 1 EUR per 4 kWh of electricity. This property is a replacement for unitOfMeasurement for the advanced cases where the price does not relate to a standard unit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source referenceQuantity |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes referenceQuantity QuantitativeValue)
  :AXIOMS (domainIncludes referenceQuantity UnitPriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label referenceQuantity "referenceQuantity")
  :AXIOMS (/XMLNS/DC/source referenceQuantity |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsProperties|))

(DEFCONCEPT Courthouse (?X)
  :DOCUMENTATION "A courthouse."
  :SUBSET-OF GovernmentBuilding
  :AXIOMS (/XMLNS/RDFS/label Courthouse "Courthouse"))

(DEFRELATION trainName (?X ?Y)
  :DOCUMENTATION "The name of the train (e.g. The Orient Express)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes trainName TrainTrip)
  :AXIOMS (/XMLNS/RDFS/label trainName "trainName")
  :AXIOMS (rangeIncludes trainName Text))

(DEFRELATION requiredCollateral (?X ?Y)
  :DOCUMENTATION "Assets required to secure loan or credit repayments. It may take form of third party pledge, goods, financial instruments (cash, securities, etc.)"
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes requiredCollateral Text)
  :AXIOMS (rangeIncludes requiredCollateral Thing)
  :AXIOMS (/XMLNS/DC/source requiredCollateral |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (domainIncludes requiredCollateral LoanOrCredit)
  :AXIOMS (/XMLNS/RDFS/label requiredCollateral "requiredCollateral"))

(DEFRELATION videoFormat (?X ?Y)
  :DOCUMENTATION "The type of screening or video broadcast used (e.g. IMAX, 3D, SD, HD, etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes videoFormat BroadcastService)
  :AXIOMS (/XMLNS/RDFS/label videoFormat "videoFormat")
  :AXIOMS (domainIncludes videoFormat ScreeningEvent)
  :AXIOMS (rangeIncludes videoFormat Text)
  :AXIOMS (domainIncludes videoFormat BroadcastEvent))

(DEFRELATION reservationId (?X ?Y)
  :DOCUMENTATION "A unique identifier for the reservation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes reservationId Text)
  :AXIOMS (domainIncludes reservationId Reservation)
  :AXIOMS (/XMLNS/RDFS/label reservationId "reservationId"))

(DEFRELATION potentialAction (?X ?Y)
  :DOCUMENTATION "Indicates a potential Action, which describes an idealized action in which this thing would play an 'object' role."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label potentialAction "potentialAction")
  :AXIOMS (domainIncludes potentialAction Thing)
  :AXIOMS (rangeIncludes potentialAction Action))

(DEFRELATION musicGroupMember (?X ?Y)
  :DOCUMENTATION "A member of a music group&#x2014;for example, John, Paul, George, or Ringo."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label musicGroupMember "musicGroupMember")
  :AXIOMS (domainIncludes musicGroupMember MusicGroup)
  :AXIOMS (supersededBy musicGroupMember member)
  :AXIOMS (rangeIncludes musicGroupMember Person))

(DEFRELATION discountCurrency (?X ?Y)
  :DOCUMENTATION "The currency (in 3-letter ISO 4217 format) of the discount."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes discountCurrency Order)
  :AXIOMS (/XMLNS/RDFS/label discountCurrency "discountCurrency")
  :AXIOMS (rangeIncludes discountCurrency Text))

(DEFRELATION checkinTime (?X ?Y)
  :DOCUMENTATION "The earliest someone may check into a lodging establishment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes checkinTime LodgingReservation)
  :AXIOMS (/XMLNS/RDFS/label checkinTime "checkinTime")
  :AXIOMS (domainIncludes checkinTime LodgingBusiness)
  :AXIOMS (rangeIncludes checkinTime DateTime))

(DEFRELATION actors (?X ?Y)
  :DOCUMENTATION "An actor, e.g. in tv, radio, movie, video games etc. Actors can be associated with individual items or with a series, episode, clip."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes actors Episode)
  :AXIOMS (domainIncludes actors Clip)
  :AXIOMS (rangeIncludes actors Person)
  :AXIOMS (domainIncludes actors MovieSeries)
  :AXIOMS (domainIncludes actors VideoObject)
  :AXIOMS (/XMLNS/RDFS/label actors "actors")
  :AXIOMS (domainIncludes actors VideoGameSeries)
  :AXIOMS (domainIncludes actors RadioSeries)
  :AXIOMS (domainIncludes actors Movie)
  :AXIOMS (supersededBy actors actor)
  :AXIOMS (domainIncludes actors VideoGame)
  :AXIOMS (domainIncludes actors TVSeries))

(DEFRELATION actor (?X ?Y)
  :DOCUMENTATION "An actor, e.g. in tv, radio, movie, video games etc., or in an event. Actors can be associated with individual items or with a series, episode, clip."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes actor Episode)
  :AXIOMS (domainIncludes actor RadioSeries)
  :AXIOMS (domainIncludes actor Movie)
  :AXIOMS (domainIncludes actor TVSeries)
  :AXIOMS (domainIncludes actor VideoGame)
  :AXIOMS (/XMLNS/RDFS/label actor "actor")
  :AXIOMS (domainIncludes actor CreativeWorkSeason)
  :AXIOMS (domainIncludes actor Event)
  :AXIOMS (domainIncludes actor Clip)
  :AXIOMS (domainIncludes actor VideoObject)
  :AXIOMS (domainIncludes actor MovieSeries)
  :AXIOMS (domainIncludes actor VideoGameSeries)
  :AXIOMS (rangeIncludes actor Person))

(DEFRELATION currency (?X ?Y)
  :DOCUMENTATION "The currency in which the monetary amount is expressed (in 3-letter <a href=\"http://en.wikipedia.org/wiki/ISO_4217\">ISO 4217</a> format)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label currency "currency")
  :AXIOMS (domainIncludes currency DatedMoneySpecification)
  :AXIOMS (domainIncludes currency MonetaryAmount)
  :AXIOMS (rangeIncludes currency Text))

(DEFRELATION itemOffered (?X ?Y)
  :DOCUMENTATION "The item being offered."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes itemOffered Product)
  :AXIOMS (domainIncludes itemOffered Offer)
  :AXIOMS (rangeIncludes itemOffered Service)
  :AXIOMS (domainIncludes itemOffered Demand)
  :AXIOMS (/XMLNS/RDFS/label itemOffered "itemOffered"))

(DEFCONCEPT Dentist (?X)
  :DOCUMENTATION "A dentist."
  :SUBSET-OF LocalBusiness
  :SUBSET-OF MedicalOrganization
  :AXIOMS (/XMLNS/RDFS/label Dentist "Dentist"))

(ASSERT (AND (GenderType Female) (DOCUMENTATION Female "The female gender.") (/XMLNS/RDFS/label Female "Female")))

(DEFRELATION paymentMethod (?X ?Y)
  :DOCUMENTATION "The name of the credit card or other method of payment for the order."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes paymentMethod Order)
  :AXIOMS (/XMLNS/RDFS/label paymentMethod "paymentMethod")
  :AXIOMS (domainIncludes paymentMethod Invoice)
  :AXIOMS (rangeIncludes paymentMethod PaymentMethod))

(DEFRELATION paymentDue (?X ?Y)
  :DOCUMENTATION "The date that payment is due."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes paymentDue Order)
  :AXIOMS (/XMLNS/RDFS/label paymentDue "paymentDue")
  :AXIOMS (domainIncludes paymentDue Invoice)
  :AXIOMS (supersededBy paymentDue paymentDueDate)
  :AXIOMS (rangeIncludes paymentDue DateTime))

(DEFRELATION paymentDueDate (?X ?Y)
  :DOCUMENTATION "The date that payment is due."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes paymentDueDate Order)
  :AXIOMS (domainIncludes paymentDueDate Invoice)
  :AXIOMS (rangeIncludes paymentDueDate DateTime)
  :AXIOMS (/XMLNS/RDFS/label paymentDueDate "paymentDueDate"))

(DEFRELATION flightNumber (?X ?Y)
  :DOCUMENTATION "The unique identifier for a flight including the airline IATA code. For example, if describing United flight 110, where the IATA code for United is 'UA', the flightNumber is 'UA110'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes flightNumber Text)
  :AXIOMS (domainIncludes flightNumber Flight)
  :AXIOMS (/XMLNS/RDFS/label flightNumber "flightNumber"))

(DEFRELATION realEstateAgent (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The real estate agent involved in the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (rangeIncludes realEstateAgent RealEstateAgent)
  :AXIOMS (/XMLNS/RDFS/label realEstateAgent "realEstateAgent")
  :AXIOMS (domainIncludes realEstateAgent RentAction))

(DEFRELATION previousItem (?X ?Y)
  :DOCUMENTATION "A link to the ListItem that preceeds the current one."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes previousItem ListItem)
  :AXIOMS (/XMLNS/RDFS/label previousItem "previousItem")
  :AXIOMS (rangeIncludes previousItem ListItem))

(DEFRELATION isrcCode (?X ?Y)
  :DOCUMENTATION "The International Standard Recording Code for the recording."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes isrcCode MusicRecording)
  :AXIOMS (rangeIncludes isrcCode Text)
  :AXIOMS (/XMLNS/DC/source isrcCode |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label isrcCode "isrcCode"))

(DEFRELATION deliveryStatus (?X ?Y)
  :DOCUMENTATION "New entry added as the package passes through each leg of its journey (from shipment to final delivery)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes deliveryStatus ParcelDelivery)
  :AXIOMS (rangeIncludes deliveryStatus DeliveryEvent)
  :AXIOMS (/XMLNS/RDFS/label deliveryStatus "deliveryStatus"))

(DEFRELATION recipeCategory (?X ?Y)
  :DOCUMENTATION "The category of the recipe—for example, appetizer, entree, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes recipeCategory Text)
  :AXIOMS (/XMLNS/RDFS/label recipeCategory "recipeCategory")
  :AXIOMS (domainIncludes recipeCategory Recipe))

(ASSERT
 (AND (RestrictedDiet LowFatDiet) (DOCUMENTATION LowFatDiet "A diet focused on reduced fat and cholesterol intake.")
  (/XMLNS/RDFS/label LowFatDiet "LowFatDiet")))

(DEFCONCEPT RadioClip (?X)
  :DOCUMENTATION "A short radio program or a segment/part of a radio program."
  :SUBSET-OF Clip
  :AXIOMS (/XMLNS/RDFS/label RadioClip "RadioClip"))

(DEFRELATION duns (?X ?Y)
  :DOCUMENTATION "The Dun &amp; Bradstreet DUNS number for identifying an organization or business person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label duns "duns")
  :AXIOMS (domainIncludes duns Organization)
  :AXIOMS (domainIncludes duns Person)
  :AXIOMS (/XMLNS/DC/source duns |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes duns Text))

(DEFRELATION assemblyVersion (?X ?Y)
  :DOCUMENTATION "Associated product/technology version. e.g., .NET Framework 4.5."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label assemblyVersion "assemblyVersion")
  :AXIOMS (rangeIncludes assemblyVersion Text)
  :AXIOMS (domainIncludes assemblyVersion APIReference))

(DEFRELATION nationality (?X ?Y)
  :DOCUMENTATION "Nationality of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label nationality "nationality")
  :AXIOMS (domainIncludes nationality Person)
  :AXIOMS (rangeIncludes nationality Country))

(DEFRELATION lastReviewed (?X ?Y)
  :DOCUMENTATION "Date on which the content on this web page was last reviewed for accuracy and/or completeness."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes lastReviewed Date)
  :AXIOMS (/XMLNS/RDFS/label lastReviewed "lastReviewed")
  :AXIOMS (domainIncludes lastReviewed WebPage))

(DEFCONCEPT ContactPage (?X)
  :DOCUMENTATION "Web page type: Contact page."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label ContactPage "ContactPage"))

(DEFRELATION offers (?X ?Y)
  :DOCUMENTATION "An offer to provide this item&#x2014;for example, an offer to sell a product, rent the DVD of a movie, perform a service, or give away tickets to an event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes offers Product)
  :AXIOMS (domainIncludes offers CreativeWork)
  :AXIOMS (domainIncludes offers AggregateOffer)
  :AXIOMS (domainIncludes offers Event)
  :AXIOMS (rangeIncludes offers Offer)
  :AXIOMS (/XMLNS/RDFS/label offers "offers")
  :AXIOMS (domainIncludes offers Service))

(DEFRELATION distance (?X ?Y)
  :DOCUMENTATION "The distance travelled, e.g. exercising or travelling."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes distance ExerciseAction)
  :AXIOMS (/XMLNS/RDFS/label distance "distance")
  :AXIOMS (domainIncludes distance TravelAction)
  :AXIOMS (rangeIncludes distance Distance))

(ASSERT
 (AND (OrderStatus OrderCancelled) (/XMLNS/RDFS/label OrderCancelled "OrderCancelled")
  (DOCUMENTATION OrderCancelled "OrderStatus representing cancellation of an order.")))

(DEFRELATION taxID (?X ?Y)
  :DOCUMENTATION "The Tax / Fiscal ID of the organization or person, e.g. the TIN in the US or the CIF/NIF in Spain."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes taxID Text)
  :AXIOMS (domainIncludes taxID Organization)
  :AXIOMS (domainIncludes taxID Person)
  :AXIOMS (/XMLNS/DC/source taxID |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label taxID "taxID"))

(ASSERT
 (AND (MusicAlbumProductionType DJMixAlbum) (/XMLNS/DC/source DJMixAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (DOCUMENTATION DJMixAlbum "DJMixAlbum.") (/XMLNS/RDFS/label DJMixAlbum "DJMixAlbum")))

(DEFRELATION directors (?X ?Y)
  :DOCUMENTATION "A director of e.g. tv, radio, movie, video games etc. content. Directors can be associated with individual items or with a series, episode, clip."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label directors "directors")
  :AXIOMS (domainIncludes directors Movie)
  :AXIOMS (domainIncludes directors RadioSeries)
  :AXIOMS (domainIncludes directors VideoGame)
  :AXIOMS (rangeIncludes directors Person)
  :AXIOMS (domainIncludes directors VideoGameSeries)
  :AXIOMS (domainIncludes directors Episode)
  :AXIOMS (domainIncludes directors MovieSeries)
  :AXIOMS (domainIncludes directors VideoObject)
  :AXIOMS (domainIncludes directors TVSeries)
  :AXIOMS (domainIncludes directors Clip)
  :AXIOMS (supersededBy directors director))

(DEFRELATION director (?X ?Y)
  :DOCUMENTATION "A director of e.g. tv, radio, movie, video gaming etc. content, or of an event. Directors can be associated with individual items or with a series, episode, clip."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes director Person)
  :AXIOMS (domainIncludes director VideoObject)
  :AXIOMS (domainIncludes director MovieSeries)
  :AXIOMS (/XMLNS/RDFS/label director "director")
  :AXIOMS (domainIncludes director CreativeWorkSeason)
  :AXIOMS (domainIncludes director Event)
  :AXIOMS (domainIncludes director Episode)
  :AXIOMS (domainIncludes director Movie)
  :AXIOMS (domainIncludes director RadioSeries)
  :AXIOMS (domainIncludes director TVSeries)
  :AXIOMS (domainIncludes director Clip)
  :AXIOMS (domainIncludes director VideoGame)
  :AXIOMS (domainIncludes director VideoGameSeries))

(DEFRELATION icaoCode (?X ?Y)
  :DOCUMENTATION "ICAO identifier for an airport."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes icaoCode Airport)
  :AXIOMS (/XMLNS/RDFS/label icaoCode "icaoCode")
  :AXIOMS (rangeIncludes icaoCode Text))

(DEFRELATION warrantyPromise (?X ?Y)
  :DOCUMENTATION "The warranty promise(s) included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes warrantyPromise SellAction)
  :AXIOMS (/XMLNS/RDFS/label warrantyPromise "warrantyPromise")
  :AXIOMS (domainIncludes warrantyPromise BuyAction)
  :AXIOMS (supersededBy warrantyPromise warranty)
  :AXIOMS (rangeIncludes warrantyPromise WarrantyPromise))

(DEFRELATION warranty (?X ?Y)
  :DOCUMENTATION "The warranty promise(s) included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source warranty |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes warranty WarrantyPromise)
  :AXIOMS (/XMLNS/RDFS/label warranty "warranty")
  :AXIOMS (domainIncludes warranty Demand)
  :AXIOMS (domainIncludes warranty Offer))

(DEFRELATION elevation (?X ?Y)
  :DOCUMENTATION "The elevation of a location (<a href=\"https://en.wikipedia.org/wiki/World_Geodetic_System\">WGS 84</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes elevation Number)
  :AXIOMS (domainIncludes elevation GeoShape)
  :AXIOMS (rangeIncludes elevation Text)
  :AXIOMS (domainIncludes elevation GeoCoordinates)
  :AXIOMS (/XMLNS/RDFS/label elevation "elevation"))

(DEFRELATION availability (?X ?Y)
  :DOCUMENTATION "The availability of this item&#x2014;for example In stock, Out of stock, Pre-order, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availability Demand)
  :AXIOMS (domainIncludes availability Offer)
  :AXIOMS (rangeIncludes availability ItemAvailability)
  :AXIOMS (/XMLNS/RDFS/label availability "availability"))

(DEFRELATION expires (?X ?Y)
  :DOCUMENTATION "Date the content expires and is no longer useful or available. Useful for videos."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes expires MediaObject)
  :AXIOMS (rangeIncludes expires Date)
  :AXIOMS (/XMLNS/RDFS/label expires "expires"))

(DEFRELATION error (?X ?Y)
  :DOCUMENTATION "For failed actions, more information on the cause of the failure."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes error Thing)
  :AXIOMS (domainIncludes error Action)
  :AXIOMS (/XMLNS/RDFS/label error "error"))

(DEFRELATION translator (?X ?Y)
  :DOCUMENTATION "Organization or person who adapts a creative work to different languages, regional differences and technical requirements of a target market, or that translates during some event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes translator Organization)
  :AXIOMS (/XMLNS/RDFS/label translator "translator")
  :AXIOMS (rangeIncludes translator Person)
  :AXIOMS (domainIncludes translator Event)
  :AXIOMS (domainIncludes translator CreativeWork))

(DEFRELATION minimumPaymentDue (?X ?Y)
  :DOCUMENTATION "The minimum payment required at this time."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes minimumPaymentDue MonetaryAmount)
  :AXIOMS (rangeIncludes minimumPaymentDue PriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label minimumPaymentDue "minimumPaymentDue")
  :AXIOMS (domainIncludes minimumPaymentDue Invoice))

(DEFRELATION sampleType (?X ?Y)
  :DOCUMENTATION "What type of code sample: full (compile ready) solution, code snippet, inline code, scripts, template."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes sampleType SoftwareSourceCode)
  :AXIOMS (supersededBy sampleType codeSampleType)
  :AXIOMS (rangeIncludes sampleType Text)
  :AXIOMS (/XMLNS/RDFS/label sampleType "sampleType"))

(DEFRELATION codeSampleType (?X ?Y)
  :DOCUMENTATION "What type of code sample: full (compile ready) solution, code snippet, inline code, scripts, template."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes codeSampleType Text)
  :AXIOMS (domainIncludes codeSampleType SoftwareSourceCode)
  :AXIOMS (/XMLNS/RDFS/label codeSampleType "codeSampleType"))

(DEFCONCEPT AcceptAction (?X)
  :DOCUMENTATION "<p>The act of committing to/adopting an object.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/RejectAction\">RejectAction</a>: The antonym of AcceptAction.</li>
</ul>
"
  :SUBSET-OF AllocateAction
  :AXIOMS (/XMLNS/RDFS/label AcceptAction "AcceptAction"))

(DEFRELATION fileSize (?X ?Y)
  :DOCUMENTATION "Size of the application / package (e.g. 18MB). In the absence of a unit (MB, KB etc.), KB will be assumed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes fileSize Text)
  :AXIOMS (/XMLNS/RDFS/label fileSize "fileSize")
  :AXIOMS (domainIncludes fileSize SoftwareApplication))

(DEFRELATION nonEqual (?X ?Y)
  :DOCUMENTATION "This ordering relation for qualitative values indicates that the subject is not equal to the object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes nonEqual QualitativeValue)
  :AXIOMS (/XMLNS/RDFS/label nonEqual "nonEqual")
  :AXIOMS (/XMLNS/DC/source nonEqual |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes nonEqual QualitativeValue))

(DEFCONCEPT Motel (?X)
  :DOCUMENTATION "A motel.
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF LodgingBusiness
  :AXIOMS (/XMLNS/RDFS/label Motel "Motel"))

(DEFCONCEPT ExerciseGym (?X)
  :DOCUMENTATION "A gym."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label ExerciseGym "ExerciseGym"))

(DEFRELATION winner (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The winner of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (rangeIncludes winner Person)
  :AXIOMS (domainIncludes winner LoseAction)
  :AXIOMS (/XMLNS/RDFS/label winner "winner"))

(DEFRELATION recipeInstructions (?X ?Y)
  :DOCUMENTATION "A step or instruction involved in making the recipe."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes recipeInstructions Recipe)
  :AXIOMS (rangeIncludes recipeInstructions Text)
  :AXIOMS (rangeIncludes recipeInstructions ItemList)
  :AXIOMS (/XMLNS/RDFS/label recipeInstructions "recipeInstructions"))

(DEFRELATION eligibleDuration (?X ?Y)
  :DOCUMENTATION "The duration for which the given offer is valid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes eligibleDuration Offer)
  :AXIOMS (domainIncludes eligibleDuration Demand)
  :AXIOMS (/XMLNS/RDFS/label eligibleDuration "eligibleDuration")
  :AXIOMS (/XMLNS/DC/source eligibleDuration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes eligibleDuration QuantitativeValue))

(DEFRELATION programmingLanguage (?X ?Y)
  :DOCUMENTATION "The computer programming language."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes programmingLanguage ComputerLanguage)
  :AXIOMS (/XMLNS/RDFS/label programmingLanguage "programmingLanguage")
  :AXIOMS (rangeIncludes programmingLanguage Text)
  :AXIOMS (domainIncludes programmingLanguage SoftwareSourceCode))

(DEFRELATION address (?X ?Y)
  :DOCUMENTATION "Physical address of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes address Person)
  :AXIOMS (domainIncludes address GeoShape)
  :AXIOMS (/XMLNS/RDFS/label address "address")
  :AXIOMS (domainIncludes address Place)
  :AXIOMS (domainIncludes address GeoCoordinates)
  :AXIOMS (domainIncludes address Organization)
  :AXIOMS (rangeIncludes address Text)
  :AXIOMS (rangeIncludes address PostalAddress))

(DEFRELATION doorTime (?X ?Y)
  :DOCUMENTATION "The time admission will commence."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label doorTime "doorTime")
  :AXIOMS (domainIncludes doorTime Event)
  :AXIOMS (rangeIncludes doorTime DateTime))

(DEFRELATION educationalUse (?X ?Y)
  :DOCUMENTATION "The purpose of a work in the context of education; for example, 'assignment', 'group work'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes educationalUse CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label educationalUse "educationalUse")
  :AXIOMS (rangeIncludes educationalUse Text))

(DEFCONCEPT CreditCard (?X)
  :DOCUMENTATION "<p>A card payment method of a particular brand or name.  Used to mark up a particular payment method and/or the financial product/service that supplies the card account.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#AmericanExpress</li>
<li>http://purl.org/goodrelations/v1#DinersClub</li>
<li>http://purl.org/goodrelations/v1#Discover</li>
<li>http://purl.org/goodrelations/v1#JCB</li>
<li>http://purl.org/goodrelations/v1#MasterCard</li>
<li>http://purl.org/goodrelations/v1#VISA</li>
</ul>
"
  :SUBSET-OF LoanOrCredit
  :SUBSET-OF PaymentCard
  :AXIOMS (/XMLNS/RDFS/label CreditCard "CreditCard")
  :AXIOMS (/XMLNS/DC/source CreditCard |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/DC/source CreditCard |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFRELATION model (?X ?Y)
  :DOCUMENTATION "The model of the product. Use with the URL of a ProductModel or a textual representation of the model identifier. The URL of the ProductModel can be from an external source. It is recommended to additionally provide strong product identifiers via the gtin8/gtin13/gtin14 and mpn properties."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes model Text)
  :AXIOMS (domainIncludes model Product)
  :AXIOMS (rangeIncludes model ProductModel)
  :AXIOMS (/XMLNS/RDFS/label model "model"))

(DEFCONCEPT Library (?X)
  :DOCUMENTATION "A library."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label Library "Library"))

(DEFRELATION uploadDate (?X ?Y)
  :DOCUMENTATION "Date when this media object was uploaded to this site."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes uploadDate MediaObject)
  :AXIOMS (rangeIncludes uploadDate Date)
  :AXIOMS (/XMLNS/RDFS/label uploadDate "uploadDate"))

(DEFRELATION serviceUrl (?X ?Y)
  :DOCUMENTATION "The website to access the service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serviceUrl URL)
  :AXIOMS (/XMLNS/RDFS/label serviceUrl "serviceUrl")
  :AXIOMS (domainIncludes serviceUrl ServiceChannel))

(DEFRELATION alignmentType (?X ?Y)
  :DOCUMENTATION "A category of alignment between the learning resource and the framework node. Recommended values include: 'assesses', 'teaches', 'requires', 'textComplexity', 'readingLevel', 'educationalSubject', and 'educationalLevel'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label alignmentType "alignmentType")
  :AXIOMS (rangeIncludes alignmentType Text)
  :AXIOMS (domainIncludes alignmentType AlignmentObject))

(DEFRELATION artMedium (?X ?Y)
  :DOCUMENTATION "The material used. (e.g. Oil, Watercolour, Acrylic, Linoprint, Marble, Cyanotype, Digital, Lithograph, DryPoint, Intaglio, Pastel, Woodcut, Pencil, Mixed Media, etc.)"
  ;:BINARY-RELATION TRUE
  :SUBSET-OF material
  :AXIOMS (rangeIncludes artMedium URL)
  :AXIOMS (/XMLNS/RDFS/label artMedium "artMedium")
  :AXIOMS (rangeIncludes artMedium Text)
  :AXIOMS (domainIncludes artMedium VisualArtwork))

(DEFRELATION material (?X ?Y)
  :DOCUMENTATION "A material that something is made from, e.g. leather, wool, cotton, paper."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes material CreativeWork)
  :AXIOMS (rangeIncludes material Text)
  :AXIOMS (rangeIncludes material Product)
  :AXIOMS (rangeIncludes material URL)
  :AXIOMS (/XMLNS/RDFS/label material "material")
  :AXIOMS (domainIncludes material Product))

(DEFRELATION articleBody (?X ?Y)
  :DOCUMENTATION "The actual body of the article."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label articleBody "articleBody")
  :AXIOMS (domainIncludes articleBody Article)
  :AXIOMS (rangeIncludes articleBody Text))

(DEFRELATION vehicleIdentificationNumber (?X ?Y)
  :DOCUMENTATION "The Vehicle Identification Number (VIN) is a unique serial number used by the automotive industry to identify individual motor vehicles."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF serialNumber
  :AXIOMS (/XMLNS/RDFS/label vehicleIdentificationNumber "vehicleIdentificationNumber")
  :AXIOMS (domainIncludes vehicleIdentificationNumber Vehicle)
  :AXIOMS (rangeIncludes vehicleIdentificationNumber Text)
  :AXIOMS (/XMLNS/DC/source vehicleIdentificationNumber |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFRELATION serialNumber (?X ?Y)
  :DOCUMENTATION "The serial number or any alphanumeric identifier of a particular product. When attached to an offer, it is a shortcut for the serial number of the product included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serialNumber Text)
  :AXIOMS (domainIncludes serialNumber IndividualProduct)
  :AXIOMS (domainIncludes serialNumber Offer)
  :AXIOMS (/XMLNS/RDFS/label serialNumber "serialNumber")
  :AXIOMS (/XMLNS/DC/source serialNumber |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes serialNumber Demand))

(DEFRELATION dateline (?X ?Y)
  :DOCUMENTATION "The location where the NewsArticle was produced."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateline NewsArticle)
  :AXIOMS (/XMLNS/RDFS/label dateline "dateline")
  :AXIOMS (rangeIncludes dateline Text))

(DEFCONCEPT TrainReservation (?X)
  :DOCUMENTATION "A reservation for train travel.</p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use <a class=\"localLink\" href=\"http://schema.org/Offer\">Offer</a>."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label TrainReservation "TrainReservation"))

(DEFRELATION orderItemStatus (?X ?Y)
  :DOCUMENTATION "The current status of the order item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes orderItemStatus OrderStatus)
  :AXIOMS (/XMLNS/RDFS/label orderItemStatus "orderItemStatus")
  :AXIOMS (domainIncludes orderItemStatus OrderItem))

(DEFRELATION confirmationNumber (?X ?Y)
  :DOCUMENTATION "A number that confirms the given order or payment has been received."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes confirmationNumber Invoice)
  :AXIOMS (/XMLNS/RDFS/label confirmationNumber "confirmationNumber")
  :AXIOMS (rangeIncludes confirmationNumber Text)
  :AXIOMS (domainIncludes confirmationNumber Order))

(DEFRELATION programmingModel (?X ?Y)
  :DOCUMENTATION "Indicates whether API is managed or unmanaged."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes programmingModel APIReference)
  :AXIOMS (/XMLNS/RDFS/label programmingModel "programmingModel")
  :AXIOMS (rangeIncludes programmingModel Text))

(DEFRELATION mealService (?X ?Y)
  :DOCUMENTATION "Description of the meals that will be provided or available for purchase."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label mealService "mealService")
  :AXIOMS (rangeIncludes mealService Text)
  :AXIOMS (domainIncludes mealService Flight))

(DEFRELATION recordLabel (?X ?Y)
  :DOCUMENTATION "The label that issued the release."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes recordLabel Organization)
  :AXIOMS (/XMLNS/RDFS/label recordLabel "recordLabel")
  :AXIOMS (/XMLNS/OWL/equivalentProperty recordLabel |http://purl.org/ontology/mo/label|)
  :AXIOMS (domainIncludes recordLabel MusicRelease)
  :AXIOMS (/XMLNS/DC/source recordLabel |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|))

(DEFRELATION query (?X ?Y)
  :DOCUMENTATION "A sub property of instrument. The query used on this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF instrument
  :AXIOMS (domainIncludes query SearchAction)
  :AXIOMS (/XMLNS/RDFS/label query "query")
  :AXIOMS (rangeIncludes query Text))

(DEFRELATION specialty (?X ?Y)
  :DOCUMENTATION "One of the domain specialities to which this web page's content applies."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes specialty WebPage)
  :AXIOMS (/XMLNS/RDFS/label specialty "specialty")
  :AXIOMS (rangeIncludes specialty Specialty))

(DEFRELATION naics (?X ?Y)
  :DOCUMENTATION "The North American Industry Classification System (NAICS) code for a particular organization or business person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source naics |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes naics Text)
  :AXIOMS (domainIncludes naics Organization)
  :AXIOMS (domainIncludes naics Person)
  :AXIOMS (/XMLNS/RDFS/label naics "naics"))

(ASSERT
 (AND (EventStatusType EventPostponed) (/XMLNS/RDFS/label EventPostponed "EventPostponed")
  (DOCUMENTATION EventPostponed "The event has been postponed and no new date has been set. The event's previousStartDate should be set.")))

(DEFRELATION language (?X ?Y)
  :DOCUMENTATION "A sub property of instrument. The language used on this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF instrument
  :AXIOMS (supersededBy language inLanguage)
  :AXIOMS (domainIncludes language WriteAction)
  :AXIOMS (rangeIncludes language Language)
  :AXIOMS (domainIncludes language CommunicateAction)
  :AXIOMS (/XMLNS/RDFS/label language "language"))

(DEFRELATION inLanguage (?X ?Y)
  :DOCUMENTATION "The language of the content or performance or used in an action. Please use one of the language codes from the <a href=\"http://tools.ietf.org/html/bcp47\">IETF BCP 47 standard</a>. See also <a class=\"localLink\" href=\"http://schema.org/availableLanguage\">availableLanguage</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes inLanguage Event)
  :AXIOMS (domainIncludes inLanguage CommunicateAction)
  :AXIOMS (rangeIncludes inLanguage Language)
  :AXIOMS (domainIncludes inLanguage WriteAction)
  :AXIOMS (rangeIncludes inLanguage Text)
  :AXIOMS (domainIncludes inLanguage CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label inLanguage "inLanguage"))

(DEFRELATION orderNumber (?X ?Y)
  :DOCUMENTATION "The identifier of the transaction."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes orderNumber Text)
  :AXIOMS (/XMLNS/RDFS/label orderNumber "orderNumber")
  :AXIOMS (domainIncludes orderNumber Order))

(DEFRELATION serviceLocation (?X ?Y)
  :DOCUMENTATION "The location (e.g. civic structure, local business, etc.) where a person can go to access the service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes serviceLocation ServiceChannel)
  :AXIOMS (/XMLNS/RDFS/label serviceLocation "serviceLocation")
  :AXIOMS (rangeIncludes serviceLocation Place))

(DEFCONCEPT NailSalon (?X)
  :DOCUMENTATION "A nail salon."
  :SUBSET-OF HealthAndBeautyBusiness
  :AXIOMS (/XMLNS/RDFS/label NailSalon "NailSalon"))

(DEFRELATION acceptedAnswer (?X ?Y)
  :DOCUMENTATION "The answer that has been accepted as best, typically on a Question/Answer site. Sites vary in their selection mechanisms, e.g. drawing on community opinion and/or the view of the Question author."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF suggestedAnswer
  :AXIOMS (domainIncludes acceptedAnswer Question)
  :AXIOMS (rangeIncludes acceptedAnswer Answer)
  :AXIOMS (/XMLNS/RDFS/label acceptedAnswer "acceptedAnswer"))

(DEFRELATION suggestedAnswer (?X ?Y)
  :DOCUMENTATION "An answer (possibly one of several, possibly incorrect) to a Question, e.g. on a Question/Answer site."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes suggestedAnswer Answer)
  :AXIOMS (/XMLNS/RDFS/label suggestedAnswer "suggestedAnswer")
  :AXIOMS (domainIncludes suggestedAnswer Question))

(DEFRELATION interactivityType (?X ?Y)
  :DOCUMENTATION "The predominant mode of learning supported by the learning resource. Acceptable values are 'active', 'expositive', or 'mixed'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes interactivityType CreativeWork)
  :AXIOMS (rangeIncludes interactivityType Text)
  :AXIOMS (/XMLNS/RDFS/label interactivityType "interactivityType"))

(DEFCONCEPT MeetingRoom (?X)
  :DOCUMENTATION "A meeting room, conference room, or conference hall is a room provided for singular events such as business conferences and meetings (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/Conference_hall\">http://en.wikipedia.org/wiki/Conference_hall</a>).
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF Room
  :AXIOMS (/XMLNS/DC/source MeetingRoom |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label MeetingRoom "MeetingRoom"))

(DEFRELATION datasetTimeInterval (?X ?Y)
  :DOCUMENTATION "The range of temporal applicability of a dataset, e.g. for a 2011 census dataset, the year 2011 (in ISO 8601 time interval format)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy datasetTimeInterval temporalCoverage)
  :AXIOMS (domainIncludes datasetTimeInterval Dataset)
  :AXIOMS (/XMLNS/RDFS/label datasetTimeInterval "datasetTimeInterval")
  :AXIOMS (rangeIncludes datasetTimeInterval DateTime))

(DEFCONCEPT Winery (?X)
  :DOCUMENTATION "A winery."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label Winery "Winery"))

(DEFCONCEPT Taxi (?X)
  :DOCUMENTATION "A taxi."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label Taxi "Taxi")
  :AXIOMS (supersededBy Taxi TaxiService))

(DEFRELATION instructor (?X ?Y)
  :DOCUMENTATION "A person assigned to instruct or provide instructional assistance for the <a class=\"localLink\" href=\"http://schema.org/CourseInstance\">CourseInstance</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label instructor "instructor")
  :AXIOMS (domainIncludes instructor CourseInstance)
  :AXIOMS (rangeIncludes instructor Person))

(DEFRELATION knows (?X ?Y)
  :DOCUMENTATION "The most generic bi-directional social/work relation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes knows Person)
  :AXIOMS (rangeIncludes knows Person)
  :AXIOMS (/XMLNS/RDFS/label knows "knows"))

(DEFRELATION catalog (?X ?Y)
  :DOCUMENTATION "A data catalog which contains this dataset."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes catalog DataCatalog)
  :AXIOMS (/XMLNS/RDFS/label catalog "catalog")
  :AXIOMS (supersededBy catalog includedInDataCatalog)
  :AXIOMS (domainIncludes catalog Dataset))

(DEFRELATION copyrightYear (?X ?Y)
  :DOCUMENTATION "The year during which the claimed copyright for the CreativeWork was first asserted."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes copyrightYear CreativeWork)
  :AXIOMS (rangeIncludes copyrightYear Number)
  :AXIOMS (/XMLNS/RDFS/label copyrightYear "copyrightYear"))

(DEFRELATION geographicArea (?X ?Y)
  :DOCUMENTATION "The geographic area associated with the audience."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes geographicArea AdministrativeArea)
  :AXIOMS (/XMLNS/RDFS/label geographicArea "geographicArea")
  :AXIOMS (domainIncludes geographicArea Audience))

(DEFRELATION subReservation (?X ?Y)
  :DOCUMENTATION "The individual reservations included in the package. Typically a repeated property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label subReservation "subReservation")
  :AXIOMS (domainIncludes subReservation ReservationPackage)
  :AXIOMS (rangeIncludes subReservation Reservation))

(DEFRELATION releaseDate (?X ?Y)
  :DOCUMENTATION "The release date of a product or product model. This can be used to distinguish the exact variant of a product."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label releaseDate "releaseDate")
  :AXIOMS (/XMLNS/DC/source releaseDate |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes releaseDate Product)
  :AXIOMS (rangeIncludes releaseDate Date))

(DEFRELATION scheduledTime (?X ?Y)
  :DOCUMENTATION "The time the object is scheduled to."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes scheduledTime DateTime)
  :AXIOMS (/XMLNS/RDFS/label scheduledTime "scheduledTime")
  :AXIOMS (domainIncludes scheduledTime PlanAction))

(DEFCONCEPT RadioStation (?X)
  :DOCUMENTATION "A radio station."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label RadioStation "RadioStation"))

(DEFRELATION screenCount (?X ?Y)
  :DOCUMENTATION "The number of screens in the movie theater."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes screenCount Number)
  :AXIOMS (/XMLNS/RDFS/label screenCount "screenCount")
  :AXIOMS (domainIncludes screenCount MovieTheater))

(DEFRELATION homeLocation (?X ?Y)
  :DOCUMENTATION "A contact location for a person's residence."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (rangeIncludes homeLocation Place)
  :AXIOMS (/XMLNS/RDFS/label homeLocation "homeLocation")
  :AXIOMS (domainIncludes homeLocation Person)
  :AXIOMS (rangeIncludes homeLocation ContactPoint))

(DEFRELATION location (?X ?Y)
  :DOCUMENTATION "The location of for example where the event is happening, an organization is located, or where an action takes place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes location Place)
  :AXIOMS (domainIncludes location Organization)
  :AXIOMS (rangeIncludes location PostalAddress)
  :AXIOMS (/XMLNS/RDFS/label location "location")
  :AXIOMS (rangeIncludes location Text)
  :AXIOMS (domainIncludes location Event)
  :AXIOMS (domainIncludes location Action))

(DEFRELATION producer (?X ?Y)
  :DOCUMENTATION "The person or organization who produced the work (e.g. music album, movie, tv/radio series etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes producer CreativeWork)
  :AXIOMS (rangeIncludes producer Organization)
  :AXIOMS (rangeIncludes producer Person)
  :AXIOMS (/XMLNS/RDFS/label producer "producer"))

(DEFRELATION seatNumber (?X ?Y)
  :DOCUMENTATION "The location of the reserved seat (e.g., 27)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes seatNumber Text)
  :AXIOMS (domainIncludes seatNumber Seat)
  :AXIOMS (/XMLNS/RDFS/label seatNumber "seatNumber"))

(DEFRELATION device (?X ?Y)
  :DOCUMENTATION "Device required to run the application. Used in cases where a specific make/model is required to run the application."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes device SoftwareApplication)
  :AXIOMS (supersededBy device availableOnDevice)
  :AXIOMS (rangeIncludes device Text)
  :AXIOMS (/XMLNS/RDFS/label device "device"))

(DEFRELATION availableOnDevice (?X ?Y)
  :DOCUMENTATION "Device required to run the application. Used in cases where a specific make/model is required to run the application."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availableOnDevice SoftwareApplication)
  :AXIOMS (rangeIncludes availableOnDevice Text)
  :AXIOMS (/XMLNS/RDFS/label availableOnDevice "availableOnDevice"))

(DEFRELATION issuedThrough (?X ?Y)
  :DOCUMENTATION "The service through with the permit was granted."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes issuedThrough Service)
  :AXIOMS (/XMLNS/RDFS/label issuedThrough "issuedThrough")
  :AXIOMS (domainIncludes issuedThrough Permit))

(DEFRELATION downvoteCount (?X ?Y)
  :DOCUMENTATION "The number of downvotes this question, answer or comment has received from the community."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label downvoteCount "downvoteCount")
  :AXIOMS (rangeIncludes downvoteCount Integer)
  :AXIOMS (domainIncludes downvoteCount Question)
  :AXIOMS (domainIncludes downvoteCount Comment))

(DEFRELATION workLocation (?X ?Y)
  :DOCUMENTATION "A contact location for a person's place of work."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (/XMLNS/RDFS/label workLocation "workLocation")
  :AXIOMS (rangeIncludes workLocation Place)
  :AXIOMS (domainIncludes workLocation Person)
  :AXIOMS (rangeIncludes workLocation ContactPoint))

(ASSERT
 (AND (MusicAlbumProductionType DemoAlbum) (/XMLNS/RDFS/label DemoAlbum "DemoAlbum")
  (/XMLNS/DC/source DemoAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|) (DOCUMENTATION DemoAlbum "DemoAlbum.")))

(DEFRELATION musicCompositionForm (?X ?Y)
  :DOCUMENTATION "The type of composition (e.g. overture, sonata, symphony, etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes musicCompositionForm Text)
  :AXIOMS (/XMLNS/RDFS/label musicCompositionForm "musicCompositionForm")
  :AXIOMS (domainIncludes musicCompositionForm MusicComposition)
  :AXIOMS (/XMLNS/DC/source musicCompositionForm |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|))

(DEFCONCEPT MarryAction (?X)
  :DOCUMENTATION "The act of marrying a person."
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label MarryAction "MarryAction"))

(DEFCONCEPT ResumeAction (?X)
  :DOCUMENTATION "The act of resuming a device or application which was formerly paused (e.g. resume music playback or resume a timer)."
  :SUBSET-OF ControlAction
  :AXIOMS (/XMLNS/RDFS/label ResumeAction "ResumeAction"))

(DEFRELATION map (?X ?Y)
  :DOCUMENTATION "A URL to a map of the place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label map "map")
  :AXIOMS (supersededBy map hasMap)
  :AXIOMS (domainIncludes map Place)
  :AXIOMS (rangeIncludes map URL))

(DEFRELATION hasMap (?X ?Y)
  :DOCUMENTATION "A URL to a map of the place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label hasMap "hasMap")
  :AXIOMS (rangeIncludes hasMap URL)
  :AXIOMS (rangeIncludes hasMap Map)
  :AXIOMS (domainIncludes hasMap Place))

(DEFCONCEPT MobilePhoneStore (?X)
  :DOCUMENTATION "A store that sells mobile phones and related accessories."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label MobilePhoneStore "MobilePhoneStore"))

(DEFRELATION carrier (?X ?Y)
  :DOCUMENTATION "'carrier' is an out-dated term indicating the 'provider' for parcel delivery and flights."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes carrier ParcelDelivery)
  :AXIOMS (rangeIncludes carrier Organization)
  :AXIOMS (domainIncludes carrier Flight)
  :AXIOMS (/XMLNS/RDFS/label carrier "carrier")
  :AXIOMS (supersededBy carrier provider))

(DEFRELATION provider (?X ?Y)
  :DOCUMENTATION "The service provider, service operator, or service performer; the goods producer. Another party (a seller) may offer those services or goods on behalf of the provider. A provider may also serve as the seller."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes provider Flight)
  :AXIOMS (/XMLNS/RDFS/label provider "provider")
  :AXIOMS (domainIncludes provider TrainTrip)
  :AXIOMS (domainIncludes provider Service)
  :AXIOMS (domainIncludes provider ParcelDelivery)
  :AXIOMS (domainIncludes provider Invoice)
  :AXIOMS (domainIncludes provider Reservation)
  :AXIOMS (domainIncludes provider BusTrip)
  :AXIOMS (domainIncludes provider CreativeWork)
  :AXIOMS (rangeIncludes provider Organization)
  :AXIOMS (rangeIncludes provider Person))

(DEFRELATION flightDistance (?X ?Y)
  :DOCUMENTATION "The distance of the flight."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label flightDistance "flightDistance")
  :AXIOMS (rangeIncludes flightDistance Text)
  :AXIOMS (domainIncludes flightDistance Flight)
  :AXIOMS (rangeIncludes flightDistance Distance))

(DEFRELATION creditedTo (?X ?Y)
  :DOCUMENTATION "The group the release is credited to if different than the byArtist. For example, Red and Blue is credited to \"Stefani Germanotta Band\", but by Lady Gaga."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source creditedTo |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (rangeIncludes creditedTo Organization)
  :AXIOMS (rangeIncludes creditedTo Person)
  :AXIOMS (domainIncludes creditedTo MusicRelease)
  :AXIOMS (/XMLNS/RDFS/label creditedTo "creditedTo"))

(DEFRELATION vehicleInteriorColor (?X ?Y)
  :DOCUMENTATION "The color or color combination of the interior of the vehicle."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label vehicleInteriorColor "vehicleInteriorColor")
  :AXIOMS (domainIncludes vehicleInteriorColor Vehicle)
  :AXIOMS (rangeIncludes vehicleInteriorColor Text)
  :AXIOMS (/XMLNS/DC/source vehicleInteriorColor |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFCONCEPT Museum (?X)
  :DOCUMENTATION "A museum."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Museum "Museum"))

(DEFRELATION membershipNumber (?X ?Y)
  :DOCUMENTATION "A unique identifier for the membership."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes membershipNumber ProgramMembership)
  :AXIOMS (/XMLNS/RDFS/label membershipNumber "membershipNumber")
  :AXIOMS (rangeIncludes membershipNumber Text))

(DEFRELATION vehicleModelDate (?X ?Y)
  :DOCUMENTATION "The release date of a vehicle model (often used to differentiate versions of the same make and model)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source vehicleModelDate |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (/XMLNS/RDFS/label vehicleModelDate "vehicleModelDate")
  :AXIOMS (rangeIncludes vehicleModelDate Date)
  :AXIOMS (domainIncludes vehicleModelDate Vehicle))

(DEFRELATION fileFormat (?X ?Y)
  :DOCUMENTATION "Media type, typically MIME format (see <a href=\"http://www.iana.org/assignments/media-types/media-types.xhtml\">IANA site</a>) of the content e.g. application/zip of a SoftwareApplication binary. In cases where a CreativeWork has several media type representations, 'encoding' can be used to indicate each MediaObject alongside particular fileFormat information. Unregistered or niche file formats can be indicated instead via the most appropriate URL, e.g. defining Web page or a Wikipedia entry."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes fileFormat URL)
  :AXIOMS (domainIncludes fileFormat CreativeWork)
  :AXIOMS (rangeIncludes fileFormat Text)
  :AXIOMS (/XMLNS/RDFS/label fileFormat "fileFormat"))

(DEFCONCEPT SportingGoodsStore (?X)
  :DOCUMENTATION "A sporting goods store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label SportingGoodsStore "SportingGoodsStore"))

(DEFRELATION cholesterolContent (?X ?Y)
  :DOCUMENTATION "The number of milligrams of cholesterol."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label cholesterolContent "cholesterolContent")
  :AXIOMS (rangeIncludes cholesterolContent Mass)
  :AXIOMS (domainIncludes cholesterolContent NutritionInformation))

(DEFCONCEPT DanceEvent (?X)
  :DOCUMENTATION "Event type: A social dance."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label DanceEvent "DanceEvent"))

(DEFCONCEPT ArriveAction (?X)
  :DOCUMENTATION "The act of arriving at a place. An agent arrives at a destination from a fromLocation, optionally with participants."
  :SUBSET-OF MoveAction
  :AXIOMS (/XMLNS/RDFS/label ArriveAction "ArriveAction"))

(DEFCONCEPT Pharmacy (?X)
  :DOCUMENTATION "A pharmacy or drugstore."
  :SUBSET-OF MedicalOrganization
  :AXIOMS (/XMLNS/RDFS/label Pharmacy "Pharmacy"))

(DEFCONCEPT PhotographAction (?X)
  :DOCUMENTATION "The act of capturing still images of objects using a camera."
  :SUBSET-OF CreateAction
  :AXIOMS (/XMLNS/RDFS/label PhotographAction "PhotographAction"))

(DEFRELATION serviceType (?X ?Y)
  :DOCUMENTATION "The type of service being offered, e.g. veterans' benefits, emergency relief, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serviceType Text)
  :AXIOMS (/XMLNS/RDFS/label serviceType "serviceType")
  :AXIOMS (domainIncludes serviceType Service))

(DEFCONCEPT AutoBodyShop (?X)
  :DOCUMENTATION "Auto body shop."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label AutoBodyShop "AutoBodyShop"))

(ASSERT
 (AND (OfferItemCondition UsedCondition) (DOCUMENTATION UsedCondition "Indicates that the item is used.")
  (/XMLNS/RDFS/label UsedCondition "UsedCondition")))

(DEFCONCEPT Festival (?X)
  :DOCUMENTATION "Event type: Festival."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label Festival "Festival"))

(DEFRELATION gtin14 (?X ?Y)
  :DOCUMENTATION "The <a href=\"http://apps.gs1.org/GDD/glossary/Pages/GTIN-14.aspx\">GTIN-14</a> code of the product, or the product to which the offer refers. See <a href=\"http://www.gs1.org/barcodes/technical/idkeys/gtin\">GS1 GTIN Summary</a> for more details."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label gtin14 "gtin14")
  :AXIOMS (domainIncludes gtin14 Product)
  :AXIOMS (domainIncludes gtin14 Offer)
  :AXIOMS (domainIncludes gtin14 Demand)
  :AXIOMS (/XMLNS/DC/source gtin14 |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes gtin14 Text))

(DEFCONCEPT OceanBodyOfWater (?X)
  :DOCUMENTATION "An ocean (for example, the Pacific)."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label OceanBodyOfWater "OceanBodyOfWater"))

(DEFRELATION line (?X ?Y)
  :DOCUMENTATION "A line is a point-to-point path consisting of two or more points. A line is expressed as a series of two or more point objects separated by space."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes line Text)
  :AXIOMS (domainIncludes line GeoShape)
  :AXIOMS (/XMLNS/RDFS/label line "line"))

(DEFRELATION lesserOrEqual (?X ?Y)
  :DOCUMENTATION "This ordering relation for qualitative values indicates that the subject is lesser than or equal to the object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source lesserOrEqual |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes lesserOrEqual QualitativeValue)
  :AXIOMS (rangeIncludes lesserOrEqual QualitativeValue)
  :AXIOMS (/XMLNS/RDFS/label lesserOrEqual "lesserOrEqual"))

(DEFCONCEPT TireShop (?X)
  :DOCUMENTATION "A tire shop."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label TireShop "TireShop"))

(DEFRELATION hoursAvailable (?X ?Y)
  :DOCUMENTATION "The hours during which this service or contact is available."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes hoursAvailable OpeningHoursSpecification)
  :AXIOMS (domainIncludes hoursAvailable ContactPoint)
  :AXIOMS (domainIncludes hoursAvailable LocationFeatureSpecification)
  :AXIOMS (domainIncludes hoursAvailable Service)
  :AXIOMS (/XMLNS/RDFS/label hoursAvailable "hoursAvailable"))

(DEFCONCEPT DepartAction (?X)
  :DOCUMENTATION "The act of  departing from a place. An agent departs from an fromLocation for a destination, optionally with participants."
  :SUBSET-OF MoveAction
  :AXIOMS (/XMLNS/RDFS/label DepartAction "DepartAction"))

(DEFCONCEPT UserCheckins (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (/XMLNS/RDFS/label UserCheckins "UserCheckins")
  :AXIOMS (supersededBy UserCheckins InteractionCounter))

(DEFRELATION readonlyValue (?X ?Y)
  :DOCUMENTATION "Whether or not a property is mutable.  Default is false. Specifying this for a property that also has a value makes it act similar to a \"hidden\" input in an HTML form."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label readonlyValue "readonlyValue")
  :AXIOMS (rangeIncludes readonlyValue Boolean)
  :AXIOMS (domainIncludes readonlyValue PropertyValueSpecification))

(DEFRELATION liveBlogUpdate (?X ?Y)
  :DOCUMENTATION "An update to the LiveBlog."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label liveBlogUpdate "liveBlogUpdate")
  :AXIOMS (domainIncludes liveBlogUpdate LiveBlogPosting)
  :AXIOMS (rangeIncludes liveBlogUpdate BlogPosting))

(DEFRELATION currenciesAccepted (?X ?Y)
  :DOCUMENTATION "The currency accepted (in <a href=\"http://en.wikipedia.org/wiki/ISO_4217\">ISO 4217 currency format</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label currenciesAccepted "currenciesAccepted")
  :AXIOMS (domainIncludes currenciesAccepted LocalBusiness)
  :AXIOMS (rangeIncludes currenciesAccepted Text))

(DEFRELATION partOfEpisode (?X ?Y)
  :DOCUMENTATION "The episode to which this clip belongs."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF isPartOf
  :AXIOMS (/XMLNS/RDFS/label partOfEpisode "partOfEpisode")
  :AXIOMS (rangeIncludes partOfEpisode Episode)
  :AXIOMS (domainIncludes partOfEpisode Clip))

(DEFCONCEPT TennisComplex (?X)
  :DOCUMENTATION "A tennis complex."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label TennisComplex "TennisComplex"))

(DEFRELATION validIn (?X ?Y)
  :DOCUMENTATION "The geographic area where the permit is valid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label validIn "validIn")
  :AXIOMS (rangeIncludes validIn AdministrativeArea)
  :AXIOMS (domainIncludes validIn Permit))

(DEFRELATION dissolutionDate (?X ?Y)
  :DOCUMENTATION "The date that this organization was dissolved."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes dissolutionDate Date)
  :AXIOMS (/XMLNS/RDFS/label dissolutionDate "dissolutionDate")
  :AXIOMS (domainIncludes dissolutionDate Organization))

(DEFRELATION accessibilityControl (?X ?Y)
  :DOCUMENTATION "Identifies input methods that are sufficient to fully control the described resource (<a href=\"http://www.w3.org/wiki/WebSchemas/Accessibility\">WebSchemas wiki lists possible values</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes accessibilityControl CreativeWork)
  :AXIOMS (rangeIncludes accessibilityControl Text)
  :AXIOMS (/XMLNS/RDFS/label accessibilityControl "accessibilityControl"))

(DEFRELATION targetUrl (?X ?Y)
  :DOCUMENTATION "The URL of a node in an established educational framework."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes targetUrl URL)
  :AXIOMS (/XMLNS/RDFS/label targetUrl "targetUrl")
  :AXIOMS (domainIncludes targetUrl AlignmentObject))

(DEFRELATION expectedArrivalFrom (?X ?Y)
  :DOCUMENTATION "The earliest date the package may arrive."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes expectedArrivalFrom ParcelDelivery)
  :AXIOMS (rangeIncludes expectedArrivalFrom DateTime)
  :AXIOMS (/XMLNS/RDFS/label expectedArrivalFrom "expectedArrivalFrom"))

(DEFRELATION suggestedMaxAge (?X ?Y)
  :DOCUMENTATION "Maximal age recommended for viewing content."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes suggestedMaxAge PeopleAudience)
  :AXIOMS (/XMLNS/RDFS/label suggestedMaxAge "suggestedMaxAge")
  :AXIOMS (rangeIncludes suggestedMaxAge Number))

(DEFCONCEPT FoodService (?X)
  :DOCUMENTATION "A food service, like breakfast, lunch, or dinner."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label FoodService "FoodService")
  :AXIOMS (/XMLNS/DC/source FoodService |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFCONCEPT HardwareStore (?X)
  :DOCUMENTATION "A hardware store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label HardwareStore "HardwareStore"))

(DEFRELATION isLiveBroadcast (?X ?Y)
  :DOCUMENTATION "True is the broadcast is of a live event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes isLiveBroadcast BroadcastEvent)
  :AXIOMS (/XMLNS/RDFS/label isLiveBroadcast "isLiveBroadcast")
  :AXIOMS (rangeIncludes isLiveBroadcast Boolean))

(DEFCONCEPT Restaurant (?X)
  :DOCUMENTATION "A restaurant."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label Restaurant "Restaurant"))

(DEFRELATION primaryImageOfPage (?X ?Y)
  :DOCUMENTATION "Indicates the main image on the page."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes primaryImageOfPage ImageObject)
  :AXIOMS (domainIncludes primaryImageOfPage WebPage)
  :AXIOMS (/XMLNS/RDFS/label primaryImageOfPage "primaryImageOfPage"))

(DEFRELATION namedPosition (?X ?Y)
  :DOCUMENTATION "A position played, performed or filled by a person or organization, as part of an organization. For example, an athlete in a SportsTeam might play in the position named 'Quarterback'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes namedPosition Text)
  :AXIOMS (/XMLNS/RDFS/label namedPosition "namedPosition")
  :AXIOMS (domainIncludes namedPosition Role)
  :AXIOMS (supersededBy namedPosition roleName)
  :AXIOMS (rangeIncludes namedPosition URL))

(DEFRELATION roleName (?X ?Y)
  :DOCUMENTATION "A role played, performed or filled by a person or organization. For example, the team of creators for a comic book might fill the roles named 'inker', 'penciller', and 'letterer'; or an athlete in a SportsTeam might play in the position named 'Quarterback'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label roleName "roleName")
  :AXIOMS (rangeIncludes roleName Text)
  :AXIOMS (domainIncludes roleName Role)
  :AXIOMS (rangeIncludes roleName URL))

(DEFRELATION targetPlatform (?X ?Y)
  :DOCUMENTATION "Type of app development: phone, Metro style, desktop, XBox, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label targetPlatform "targetPlatform")
  :AXIOMS (rangeIncludes targetPlatform Text)
  :AXIOMS (domainIncludes targetPlatform APIReference))

(DEFRELATION parentService (?X ?Y)
  :DOCUMENTATION "A broadcast service to which the broadcast service may belong to such as regional variations of a national channel."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes parentService BroadcastService)
  :AXIOMS (domainIncludes parentService BroadcastService)
  :AXIOMS (/XMLNS/RDFS/label parentService "parentService"))

(DEFCONCEPT TravelAgency (?X)
  :DOCUMENTATION "A travel agency."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label TravelAgency "TravelAgency"))

(DEFCONCEPT Crematorium (?X)
  :DOCUMENTATION "A crematorium."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Crematorium "Crematorium"))

(DEFRELATION merchant (?X ?Y)
  :DOCUMENTATION "'merchant' is an out-dated term for 'seller'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy merchant seller)
  :AXIOMS (rangeIncludes merchant Organization)
  :AXIOMS (rangeIncludes merchant Person)
  :AXIOMS (domainIncludes merchant Order)
  :AXIOMS (/XMLNS/RDFS/label merchant "merchant"))

(DEFRELATION suggestedGender (?X ?Y)
  :DOCUMENTATION "The gender of the person or audience."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes suggestedGender Text)
  :AXIOMS (/XMLNS/RDFS/label suggestedGender "suggestedGender")
  :AXIOMS (domainIncludes suggestedGender PeopleAudience))

(DEFRELATION creator (?X ?Y)
  :DOCUMENTATION "The creator/author of this CreativeWork. This is the same as the Author property for CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes creator UserComments)
  :AXIOMS (/XMLNS/RDFS/label creator "creator")
  :AXIOMS (domainIncludes creator CreativeWork)
  :AXIOMS (rangeIncludes creator Organization)
  :AXIOMS (rangeIncludes creator Person))

(DEFCONCEPT FireStation (?X)
  :DOCUMENTATION "A fire station. With firemen."
  :SUBSET-OF CivicStructure
  :SUBSET-OF EmergencyService
  :AXIOMS (/XMLNS/RDFS/label FireStation "FireStation"))

(DEFRELATION associatedArticle (?X ?Y)
  :DOCUMENTATION "A NewsArticle associated with the Media Object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes associatedArticle MediaObject)
  :AXIOMS (/XMLNS/RDFS/label associatedArticle "associatedArticle")
  :AXIOMS (rangeIncludes associatedArticle NewsArticle))

(DEFRELATION arrivalAirport (?X ?Y)
  :DOCUMENTATION "The airport where the flight terminates."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label arrivalAirport "arrivalAirport")
  :AXIOMS (rangeIncludes arrivalAirport Airport)
  :AXIOMS (domainIncludes arrivalAirport Flight))

(DEFRELATION valueRequired (?X ?Y)
  :DOCUMENTATION "Whether the property must be filled in to complete the action.  Default is false."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label valueRequired "valueRequired")
  :AXIOMS (rangeIncludes valueRequired Boolean)
  :AXIOMS (domainIncludes valueRequired PropertyValueSpecification))

(DEFRELATION requirements (?X ?Y)
  :DOCUMENTATION "Component dependency requirements for application. This includes runtime environments and shared libraries that are not included in the application distribution package, but required to run the application (Examples: DirectX, Java or .NET runtime)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label requirements "requirements")
  :AXIOMS (rangeIncludes requirements URL)
  :AXIOMS (supersededBy requirements softwareRequirements)
  :AXIOMS (domainIncludes requirements SoftwareApplication)
  :AXIOMS (rangeIncludes requirements Text))

(DEFRELATION softwareRequirements (?X ?Y)
  :DOCUMENTATION "Component dependency requirements for application. This includes runtime environments and shared libraries that are not included in the application distribution package, but required to run the application (Examples: DirectX, Java or .NET runtime)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label softwareRequirements "softwareRequirements")
  :AXIOMS (domainIncludes softwareRequirements SoftwareApplication)
  :AXIOMS (rangeIncludes softwareRequirements URL)
  :AXIOMS (rangeIncludes softwareRequirements Text))

(DEFRELATION featureList (?X ?Y)
  :DOCUMENTATION "Features or modules provided by this application (and possibly required by other applications)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes featureList URL)
  :AXIOMS (rangeIncludes featureList Text)
  :AXIOMS (domainIncludes featureList SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label featureList "featureList"))

(DEFCONCEPT LockerDelivery (?X)
  :DOCUMENTATION "A DeliveryMethod in which an item is made available via locker."
  :SUBSET-OF DeliveryMethod
  :AXIOMS (/XMLNS/RDFS/label LockerDelivery "LockerDelivery"))

(DEFRELATION employmentType (?X ?Y)
  :DOCUMENTATION "Type of employment (e.g. full-time, part-time, contract, temporary, seasonal, internship)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes employmentType Text)
  :AXIOMS (/XMLNS/RDFS/label employmentType "employmentType")
  :AXIOMS (domainIncludes employmentType JobPosting))

(DEFRELATION proteinContent (?X ?Y)
  :DOCUMENTATION "The number of grams of protein."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes proteinContent Mass)
  :AXIOMS (/XMLNS/RDFS/label proteinContent "proteinContent")
  :AXIOMS (domainIncludes proteinContent NutritionInformation))

(ASSERT
 (AND (MusicAlbumReleaseType EPRelease) (DOCUMENTATION EPRelease "EPRelease.")
  (/XMLNS/DC/source EPRelease |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|) (/XMLNS/RDFS/label EPRelease "EPRelease")))

(DEFCONCEPT ToyStore (?X)
  :DOCUMENTATION "A toy store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label ToyStore "ToyStore"))

(DEFRELATION referencesOrder (?X ?Y)
  :DOCUMENTATION "The Order(s) related to this Invoice. One or more Orders may be combined into a single Invoice."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label referencesOrder "referencesOrder")
  :AXIOMS (rangeIncludes referencesOrder Order)
  :AXIOMS (domainIncludes referencesOrder Invoice))

(ASSERT
 (AND (ContactPointOption TollFree) (/XMLNS/RDFS/label TollFree "TollFree") (DOCUMENTATION TollFree "The associated telephone number is toll free.")))

(ASSERT (AND (MapCategoryType TransitMap) (DOCUMENTATION TransitMap "A transit map.") (/XMLNS/RDFS/label TransitMap "TransitMap")))

(DEFCONCEPT AnimalShelter (?X)
  :DOCUMENTATION "Animal shelter."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label AnimalShelter "AnimalShelter"))

(DEFCONCEPT MovieClip (?X)
  :DOCUMENTATION "A short segment/part of a movie."
  :SUBSET-OF Clip
  :AXIOMS (/XMLNS/RDFS/label MovieClip "MovieClip"))

(DEFRELATION userInteractionCount (?X ?Y)
  :DOCUMENTATION "The number of interactions for the CreativeWork using the WebSite or SoftwareApplication."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label userInteractionCount "userInteractionCount")
  :AXIOMS (rangeIncludes userInteractionCount Integer)
  :AXIOMS (domainIncludes userInteractionCount InteractionCounter))

(DEFRELATION pickupLocation (?X ?Y)
  :DOCUMENTATION "Where a taxi will pick up a passenger or a rental car can be picked up."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes pickupLocation TaxiReservation)
  :AXIOMS (/XMLNS/RDFS/label pickupLocation "pickupLocation")
  :AXIOMS (domainIncludes pickupLocation RentalCarReservation)
  :AXIOMS (rangeIncludes pickupLocation Place))

(DEFRELATION interactionCount (?X ?Y)
  :DOCUMENTATION "This property is deprecated, alongside the UserInteraction types on which it depended."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label interactionCount "interactionCount")
  :AXIOMS (supersededBy interactionCount interactionStatistic))

(DEFRELATION interactionStatistic (?X ?Y)
  :DOCUMENTATION "The number of interactions for the CreativeWork using the WebSite or SoftwareApplication. The most specific child type of InteractionCounter should be used."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label interactionStatistic "interactionStatistic")
  :AXIOMS (domainIncludes interactionStatistic CreativeWork)
  :AXIOMS (rangeIncludes interactionStatistic InteractionCounter))

(DEFRELATION subEvents (?X ?Y)
  :DOCUMENTATION "Events that are a part of this event. For example, a conference event includes many presentations, each subEvents of the conference."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label subEvents "subEvents")
  :AXIOMS (rangeIncludes subEvents Event)
  :AXIOMS (domainIncludes subEvents Event)
  :AXIOMS (supersededBy subEvents subEvent))

(DEFRELATION subEvent (?X ?Y)
  :DOCUMENTATION "An Event that is part of this event. For example, a conference event includes many presentations, each of which is a subEvent of the conference."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes subEvent Event)
  :AXIOMS (domainIncludes subEvent Event)
  :AXIOMS (inverseOf subEvent superEvent)
  :AXIOMS (/XMLNS/RDFS/label subEvent "subEvent"))

(DEFRELATION broadcastDisplayName (?X ?Y)
  :DOCUMENTATION "The name displayed in the channel guide. For many US affiliates, it is the network name."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes broadcastDisplayName Text)
  :AXIOMS (/XMLNS/RDFS/label broadcastDisplayName "broadcastDisplayName")
  :AXIOMS (domainIncludes broadcastDisplayName BroadcastService))

(DEFRELATION recipeYield (?X ?Y)
  :DOCUMENTATION "The quantity produced by the recipe (for example, number of people served, number of servings, etc)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes recipeYield Text)
  :AXIOMS (domainIncludes recipeYield Recipe)
  :AXIOMS (/XMLNS/RDFS/label recipeYield "recipeYield"))

(DEFRELATION text (?X ?Y)
  :DOCUMENTATION "The textual content of this CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes text CreativeWork)
  :AXIOMS (rangeIncludes text Text)
  :AXIOMS (/XMLNS/RDFS/label text "text"))

(DEFRELATION openingHours (?X ?Y)
  :DOCUMENTATION "<p>The general opening hours for a business. Opening hours can be specified as a weekly time range, starting with days, then times per day. Multiple days can be listed with commas ',' separating each day. Day or time ranges are specified using a hyphen '-'.</p>

<ul>
<li>Days are specified using the following two-letter combinations: <code>Mo</code>, <code>Tu</code>, <code>We</code>, <code>Th</code>, <code>Fr</code>, <code>Sa</code>, <code>Su</code>.</li>
<li>Times are specified using 24:00 time. For example, 3pm is specified as <code>15:00</code>. </li>
<li>Here is an example: <code>&lt;time itemprop=\"openingHours\" datetime=&quot;Tu,Th 16:00-20:00&quot;&gt;Tuesdays and Thursdays 4-8pm&lt;/time&gt;</code>.</li>
<li>If a business is open 7 days a week, then it can be specified as <code>&lt;time itemprop=&quot;openingHours&quot; datetime=&quot;Mo-Su&quot;&gt;Monday through Sunday, all day&lt;/time&gt;</code>.</li>
</ul>
"
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes openingHours Text)
  :AXIOMS (domainIncludes openingHours CivicStructure)
  :AXIOMS (/XMLNS/RDFS/label openingHours "openingHours")
  :AXIOMS (domainIncludes openingHours LocalBusiness))

(DEFRELATION addressLocality (?X ?Y)
  :DOCUMENTATION "The locality. For example, Mountain View."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes addressLocality PostalAddress)
  :AXIOMS (rangeIncludes addressLocality Text)
  :AXIOMS (/XMLNS/RDFS/label addressLocality "addressLocality"))

(DEFRELATION isRelatedTo (?X ?Y)
  :DOCUMENTATION "A pointer to another, somehow related product (or multiple products)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source isRelatedTo |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label isRelatedTo "isRelatedTo")
  :AXIOMS (domainIncludes isRelatedTo Service)
  :AXIOMS (rangeIncludes isRelatedTo Service)
  :AXIOMS (domainIncludes isRelatedTo Product)
  :AXIOMS (rangeIncludes isRelatedTo Product))

(DEFRELATION qualifications (?X ?Y)
  :DOCUMENTATION "Specific qualifications required for this role."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes qualifications JobPosting)
  :AXIOMS (rangeIncludes qualifications Text)
  :AXIOMS (/XMLNS/RDFS/label qualifications "qualifications"))

(DEFRELATION publisher (?X ?Y)
  :DOCUMENTATION "The publisher of the creative work."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes publisher Organization)
  :AXIOMS (/XMLNS/RDFS/label publisher "publisher")
  :AXIOMS (rangeIncludes publisher Person)
  :AXIOMS (domainIncludes publisher CreativeWork))

(DEFRELATION fiberContent (?X ?Y)
  :DOCUMENTATION "The number of grams of fiber."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes fiberContent Mass)
  :AXIOMS (domainIncludes fiberContent NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label fiberContent "fiberContent"))

(DEFRELATION numChildren (?X ?Y)
  :DOCUMENTATION "The number of children staying in the unit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numChildren Integer)
  :AXIOMS (domainIncludes numChildren LodgingReservation)
  :AXIOMS (/XMLNS/RDFS/label numChildren "numChildren")
  :AXIOMS (rangeIncludes numChildren QuantitativeValue))

(DEFRELATION processingTime (?X ?Y)
  :DOCUMENTATION "Estimated processing time for the service using this channel."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes processingTime ServiceChannel)
  :AXIOMS (/XMLNS/RDFS/label processingTime "processingTime")
  :AXIOMS (rangeIncludes processingTime Duration))

(DEFRELATION value (?X ?Y)
  :DOCUMENTATION "<p>The value of the quantitative value or property value node.</p>

<ul>
<li>For <a class=\"localLink\" href=\"http://schema.org/QuantitativeValue\">QuantitativeValue</a> and <a class=\"localLink\" href=\"http://schema.org/MonetaryAmount\">MonetaryAmount</a>, the recommended type for values is 'Number'.</li>
<li>For <a class=\"localLink\" href=\"http://schema.org/PropertyValue\">PropertyValue</a>, it can be 'Text;', 'Number', 'Boolean', or 'StructuredValue'.</li>
</ul>
"
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes value PropertyValue)
  :AXIOMS (rangeIncludes value Text)
  :AXIOMS (rangeIncludes value Boolean)
  :AXIOMS (/XMLNS/DC/source value |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes value QuantitativeValue)
  :AXIOMS (rangeIncludes value Number)
  :AXIOMS (/XMLNS/RDFS/label value "value")
  :AXIOMS (rangeIncludes value StructuredValue)
  :AXIOMS (domainIncludes value MonetaryAmount))

(DEFCONCEPT GasStation (?X)
  :DOCUMENTATION "A gas station."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label GasStation "GasStation"))

(DEFCONCEPT Reservoir (?X)
  :DOCUMENTATION "A reservoir of water, typically an artificially created lake, like the Lake Kariba reservoir."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label Reservoir "Reservoir"))

(DEFRELATION weight (?X ?Y)
  :DOCUMENTATION "The weight of the product or person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source weight |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes weight Product)
  :AXIOMS (rangeIncludes weight QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label weight "weight")
  :AXIOMS (domainIncludes weight Person))

(DEFRELATION sameAs (?X ?Y)
  :DOCUMENTATION "URL of a reference Web page that unambiguously indicates the item's identity. E.g. the URL of the item's Wikipedia page, Wikidata entry, or official website."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes sameAs URL)
  :AXIOMS (/XMLNS/RDFS/label sameAs "sameAs")
  :AXIOMS (domainIncludes sameAs Thing))

(DEFRELATION iataCode (?X ?Y)
  :DOCUMENTATION "IATA identifier for an airline or airport."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes iataCode Airline)
  :AXIOMS (domainIncludes iataCode Airport)
  :AXIOMS (/XMLNS/RDFS/label iataCode "iataCode")
  :AXIOMS (rangeIncludes iataCode Text))

(DEFRELATION logo (?X ?Y)
  :DOCUMENTATION "An associated logo."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF image
  :AXIOMS (rangeIncludes logo URL)
  :AXIOMS (domainIncludes logo Service)
  :AXIOMS (domainIncludes logo Place)
  :AXIOMS (/XMLNS/RDFS/label logo "logo")
  :AXIOMS (/XMLNS/DC/source logo |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes logo Product)
  :AXIOMS (rangeIncludes logo ImageObject)
  :AXIOMS (domainIncludes logo Brand)
  :AXIOMS (domainIncludes logo Organization))

(DEFRELATION image (?X ?Y)
  :DOCUMENTATION "An image of the item. This can be a <a class=\"localLink\" href=\"http://schema.org/URL\">URL</a> or a fully described <a class=\"localLink\" href=\"http://schema.org/ImageObject\">ImageObject</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes image ImageObject)
  :AXIOMS (/XMLNS/RDFS/label image "image")
  :AXIOMS (domainIncludes image Thing)
  :AXIOMS (rangeIncludes image URL))

(DEFRELATION addressCountry (?X ?Y)
  :DOCUMENTATION "The country. For example, USA. You can also provide the two-letter <a href=\"http://en.wikipedia.org/wiki/ISO_3166-1\">ISO 3166-1 alpha-2 country code</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label addressCountry "addressCountry")
  :AXIOMS (rangeIncludes addressCountry Text)
  :AXIOMS (rangeIncludes addressCountry Country)
  :AXIOMS (domainIncludes addressCountry GeoShape)
  :AXIOMS (domainIncludes addressCountry PostalAddress)
  :AXIOMS (domainIncludes addressCountry GeoCoordinates))

(DEFRELATION gamePlatform (?X ?Y)
  :DOCUMENTATION "The electronic systems used to play <a href=\"http://en.wikipedia.org/wiki/Category:Video_game_platforms\">video games</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes gamePlatform VideoGameSeries)
  :AXIOMS (rangeIncludes gamePlatform Thing)
  :AXIOMS (rangeIncludes gamePlatform Text)
  :AXIOMS (rangeIncludes gamePlatform URL)
  :AXIOMS (domainIncludes gamePlatform VideoGame)
  :AXIOMS (/XMLNS/RDFS/label gamePlatform "gamePlatform"))

(DEFRELATION availableChannel (?X ?Y)
  :DOCUMENTATION "A means of accessing the service (e.g. a phone bank, a web site, a location, etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availableChannel Service)
  :AXIOMS (/XMLNS/RDFS/label availableChannel "availableChannel")
  :AXIOMS (rangeIncludes availableChannel ServiceChannel))

(DEFCONCEPT Pond (?X)
  :DOCUMENTATION "A pond."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label Pond "Pond"))

(DEFCONCEPT Attorney (?X)
  :DOCUMENTATION "Professional service: Attorney. </p>

<p>This type is deprecated - <a class=\"localLink\" href=\"http://schema.org/LegalService\">LegalService</a> is more inclusive and less ambiguous."
  :SUBSET-OF LegalService
  :AXIOMS (/XMLNS/RDFS/label Attorney "Attorney"))

(DEFCONCEPT BikeStore (?X)
  :DOCUMENTATION "A bike store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label BikeStore "BikeStore"))

(DEFRELATION isBasedOnUrl (?X ?Y)
  :DOCUMENTATION "A resource that was used in the creation of this resource. This term can be repeated for multiple sources. For example, http://example.com/great-multiplication-intro.html."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes isBasedOnUrl CreativeWork)
  :AXIOMS (domainIncludes isBasedOnUrl CreativeWork)
  :AXIOMS (rangeIncludes isBasedOnUrl Product)
  :AXIOMS (supersededBy isBasedOnUrl isBasedOn)
  :AXIOMS (rangeIncludes isBasedOnUrl URL)
  :AXIOMS (/XMLNS/RDFS/label isBasedOnUrl "isBasedOnUrl"))

(DEFRELATION isBasedOn (?X ?Y)
  :DOCUMENTATION "A resource that was used in the creation of this resource. This term can be repeated for multiple sources. For example, http://example.com/great-multiplication-intro.html."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes isBasedOn Product)
  :AXIOMS (/XMLNS/RDFS/label isBasedOn "isBasedOn")
  :AXIOMS (domainIncludes isBasedOn CreativeWork)
  :AXIOMS (rangeIncludes isBasedOn URL)
  :AXIOMS (rangeIncludes isBasedOn CreativeWork))

(DEFRELATION skills (?X ?Y)
  :DOCUMENTATION "Skills required to fulfill this role."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes skills JobPosting)
  :AXIOMS (rangeIncludes skills Text)
  :AXIOMS (/XMLNS/RDFS/label skills "skills"))

(DEFCONCEPT TaxiStand (?X)
  :DOCUMENTATION "A taxi stand."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label TaxiStand "TaxiStand"))

(ASSERT
 (AND (MusicAlbumProductionType MixtapeAlbum) (DOCUMENTATION MixtapeAlbum "MixtapeAlbum.")
  (/XMLNS/DC/source MixtapeAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|) (/XMLNS/RDFS/label MixtapeAlbum "MixtapeAlbum")))

(DEFRELATION orderedItem (?X ?Y)
  :DOCUMENTATION "The item ordered."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes orderedItem Order)
  :AXIOMS (/XMLNS/RDFS/label orderedItem "orderedItem")
  :AXIOMS (rangeIncludes orderedItem OrderItem)
  :AXIOMS (rangeIncludes orderedItem Product)
  :AXIOMS (domainIncludes orderedItem OrderItem))

(DEFRELATION discountCode (?X ?Y)
  :DOCUMENTATION "Code used to redeem a discount."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes discountCode Order)
  :AXIOMS (/XMLNS/RDFS/label discountCode "discountCode")
  :AXIOMS (rangeIncludes discountCode Text))

(DEFRELATION permissionType (?X ?Y)
  :DOCUMENTATION "The type of permission granted the person, organization, or audience."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes permissionType DigitalDocumentPermission)
  :AXIOMS (/XMLNS/RDFS/label permissionType "permissionType")
  :AXIOMS (rangeIncludes permissionType DigitalDocumentPermissionType))

(DEFRELATION publishingPrinciples (?X ?Y)
  :DOCUMENTATION "Link to page describing the editorial principles of the organization primarily responsible for the creation of the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes publishingPrinciples URL)
  :AXIOMS (domainIncludes publishingPrinciples CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label publishingPrinciples "publishingPrinciples"))

(DEFRELATION maximumAttendeeCapacity (?X ?Y)
  :DOCUMENTATION "The total number of individuals that may attend an event or venue."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes maximumAttendeeCapacity Place)
  :AXIOMS (rangeIncludes maximumAttendeeCapacity Integer)
  :AXIOMS (/XMLNS/RDFS/label maximumAttendeeCapacity "maximumAttendeeCapacity")
  :AXIOMS (domainIncludes maximumAttendeeCapacity Event))

(ASSERT
 (AND (ReservationStatusType ReservationCancelled)
  (DOCUMENTATION ReservationCancelled "The status for a previously confirmed reservation that is now cancelled.")
  (/XMLNS/RDFS/label ReservationCancelled "ReservationCancelled")))

(ASSERT
 (AND (GamePlayMode SinglePlayer) (DOCUMENTATION SinglePlayer "Play mode: SinglePlayer. Which is played by a lone player.")
  (/XMLNS/RDFS/label SinglePlayer "SinglePlayer")))

(DEFRELATION siblings (?X ?Y)
  :DOCUMENTATION "A sibling of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label siblings "siblings")
  :AXIOMS (rangeIncludes siblings Person)
  :AXIOMS (domainIncludes siblings Person)
  :AXIOMS (supersededBy siblings sibling))

(DEFRELATION sibling (?X ?Y)
  :DOCUMENTATION "A sibling of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label sibling "sibling")
  :AXIOMS (rangeIncludes sibling Person)
  :AXIOMS (domainIncludes sibling Person))

(DEFRELATION interactionType (?X ?Y)
  :DOCUMENTATION "The Action representing the type of interaction. For up votes, +1s, etc. use <a class=\"localLink\" href=\"http://schema.org/LikeAction\">LikeAction</a>. For down votes use <a class=\"localLink\" href=\"http://schema.org/DislikeAction\">DislikeAction</a>. Otherwise, use the most specific Action."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes interactionType Action)
  :AXIOMS (/XMLNS/RDFS/label interactionType "interactionType")
  :AXIOMS (domainIncludes interactionType InteractionCounter))

(DEFRELATION partOfOrder (?X ?Y)
  :DOCUMENTATION "The overall order the items in this delivery were included in."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes partOfOrder ParcelDelivery)
  :AXIOMS (/XMLNS/RDFS/label partOfOrder "partOfOrder")
  :AXIOMS (rangeIncludes partOfOrder Order))

(DEFCONCEPT Florist (?X)
  :DOCUMENTATION "A florist."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label Florist "Florist"))

(DEFCONCEPT RVPark (?X)
  :DOCUMENTATION "A place offering space for \"Recreational Vehicles\", Caravans, mobile homes and the like."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label RVPark "RVPark"))

(ASSERT
 (AND (MusicReleaseFormatType DigitalAudioTapeFormat) (/XMLNS/RDFS/label DigitalAudioTapeFormat "DigitalAudioTapeFormat")
  (/XMLNS/DC/source DigitalAudioTapeFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (DOCUMENTATION DigitalAudioTapeFormat "DigitalAudioTapeFormat.")))

(ASSERT
 (AND (EventStatusType EventCancelled)
  (DOCUMENTATION EventCancelled
   "The event has been cancelled. If the event has multiple startDate values, all are assumed to be cancelled. Either startDate or previousStartDate may be used to specify the event's cancelled date(s).")
  (/XMLNS/RDFS/label EventCancelled "EventCancelled")))

(DEFCONCEPT HealthClub (?X)
  :DOCUMENTATION "A health club."
  :SUBSET-OF SportsActivityLocation
  :SUBSET-OF HealthAndBeautyBusiness
  :AXIOMS (/XMLNS/RDFS/label HealthClub "HealthClub"))

(DEFRELATION partOfInvoice (?X ?Y)
  :DOCUMENTATION "The order is being paid as part of the referenced Invoice."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label partOfInvoice "partOfInvoice")
  :AXIOMS (domainIncludes partOfInvoice Order)
  :AXIOMS (rangeIncludes partOfInvoice Invoice))

(DEFRELATION permittedUsage (?X ?Y)
  :DOCUMENTATION "Indications regarding the permitted usage of the accommodation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes permittedUsage Accommodation)
  :AXIOMS (/XMLNS/DC/source permittedUsage |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label permittedUsage "permittedUsage")
  :AXIOMS (rangeIncludes permittedUsage Text))

(DEFCONCEPT Beach (?X)
  :DOCUMENTATION "Beach."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Beach "Beach"))

(DEFRELATION sodiumContent (?X ?Y)
  :DOCUMENTATION "The number of milligrams of sodium."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes sodiumContent NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label sodiumContent "sodiumContent")
  :AXIOMS (rangeIncludes sodiumContent Mass))

(ASSERT
 (AND (PaymentStatusType PaymentAutomaticallyApplied) (/XMLNS/RDFS/label PaymentAutomaticallyApplied "PaymentAutomaticallyApplied")
  (DOCUMENTATION PaymentAutomaticallyApplied "An automatic payment system is in place and will be used.")))

(DEFRELATION arrivalGate (?X ?Y)
  :DOCUMENTATION "Identifier of the flight's arrival gate."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes arrivalGate Flight)
  :AXIOMS (/XMLNS/RDFS/label arrivalGate "arrivalGate")
  :AXIOMS (rangeIncludes arrivalGate Text))

(DEFRELATION fromLocation (?X ?Y)
  :DOCUMENTATION "A sub property of location. The original location of the object or the agent before the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (domainIncludes fromLocation TransferAction)
  :AXIOMS (domainIncludes fromLocation ExerciseAction)
  :AXIOMS (rangeIncludes fromLocation Place)
  :AXIOMS (domainIncludes fromLocation MoveAction)
  :AXIOMS (/XMLNS/RDFS/label fromLocation "fromLocation"))

(DEFRELATION workPerformed (?X ?Y)
  :DOCUMENTATION "A work performed in some event, for example a play performed in a TheaterEvent."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF workFeatured
  :AXIOMS (rangeIncludes workPerformed CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label workPerformed "workPerformed")
  :AXIOMS (domainIncludes workPerformed Event))

(DEFRELATION workFeatured (?X ?Y)
  :DOCUMENTATION "A work featured in some event, e.g. exhibited in an ExhibitionEvent.
       Specific subproperties are available for workPerformed (e.g. a play), or a workPresented (a Movie at a ScreeningEvent)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes workFeatured CreativeWork)
  :AXIOMS (domainIncludes workFeatured Event)
  :AXIOMS (/XMLNS/RDFS/label workFeatured "workFeatured"))

(DEFCONCEPT QAPage (?X)
  :DOCUMENTATION "A QAPage is a WebPage focussed on a specific Question and its Answer(s), e.g. in a question answering site or documenting Frequently Asked Questions (FAQs)."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label QAPage "QAPage"))

(DEFRELATION departureTerminal (?X ?Y)
  :DOCUMENTATION "Identifier of the flight's departure terminal."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes departureTerminal Text)
  :AXIOMS (/XMLNS/RDFS/label departureTerminal "departureTerminal")
  :AXIOMS (domainIncludes departureTerminal Flight))

(DEFRELATION ticketNumber (?X ?Y)
  :DOCUMENTATION "The unique identifier for the ticket."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label ticketNumber "ticketNumber")
  :AXIOMS (domainIncludes ticketNumber Ticket)
  :AXIOMS (rangeIncludes ticketNumber Text))

(DEFRELATION bookEdition (?X ?Y)
  :DOCUMENTATION "The edition of the book."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes bookEdition Book)
  :AXIOMS (rangeIncludes bookEdition Text)
  :AXIOMS (/XMLNS/RDFS/label bookEdition "bookEdition"))

(DEFRELATION validThrough (?X ?Y)
  :DOCUMENTATION "The date after when the item is not valid. For example the end of an offer, salary period, or a period of opening hours."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes validThrough Demand)
  :AXIOMS (/XMLNS/RDFS/label validThrough "validThrough")
  :AXIOMS (domainIncludes validThrough PriceSpecification)
  :AXIOMS (domainIncludes validThrough JobPosting)
  :AXIOMS (domainIncludes validThrough OpeningHoursSpecification)
  :AXIOMS (/XMLNS/DC/source validThrough |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes validThrough LocationFeatureSpecification)
  :AXIOMS (rangeIncludes validThrough DateTime)
  :AXIOMS (domainIncludes validThrough MonetaryAmount)
  :AXIOMS (domainIncludes validThrough Offer))

(DEFCONCEPT AssignAction (?X)
  :DOCUMENTATION "The act of allocating an action/event/task to some destination (someone or something)."
  :SUBSET-OF AllocateAction
  :AXIOMS (/XMLNS/RDFS/label AssignAction "AssignAction"))

(DEFRELATION latitude (?X ?Y)
  :DOCUMENTATION "The latitude of a location. For example <code>37.42242</code> (<a href=\"https://en.wikipedia.org/wiki/World_Geodetic_System\">WGS 84</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes latitude Text)
  :AXIOMS (domainIncludes latitude GeoCoordinates)
  :AXIOMS (/XMLNS/RDFS/label latitude "latitude")
  :AXIOMS (rangeIncludes latitude Number))

(DEFRELATION category (?X ?Y)
  :DOCUMENTATION "A category for the item. Greater signs or slashes can be used to informally indicate a category hierarchy."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label category "category")
  :AXIOMS (rangeIncludes category Thing)
  :AXIOMS (domainIncludes category Invoice)
  :AXIOMS (domainIncludes category Product)
  :AXIOMS (domainIncludes category Offer)
  :AXIOMS (domainIncludes category Service)
  :AXIOMS (rangeIncludes category Text))

(DEFRELATION regionsAllowed (?X ?Y)
  :DOCUMENTATION "The regions where the media is allowed. If not specified, then it's assumed to be allowed everywhere. Specify the countries in <a href=\"http://en.wikipedia.org/wiki/ISO_3166\">ISO 3166 format</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label regionsAllowed "regionsAllowed")
  :AXIOMS (rangeIncludes regionsAllowed Place)
  :AXIOMS (domainIncludes regionsAllowed MediaObject))

(DEFRELATION hasOfferCatalog (?X ?Y)
  :DOCUMENTATION "Indicates an OfferCatalog listing for this Organization, Person, or Service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes hasOfferCatalog Service)
  :AXIOMS (domainIncludes hasOfferCatalog Organization)
  :AXIOMS (/XMLNS/RDFS/label hasOfferCatalog "hasOfferCatalog")
  :AXIOMS (domainIncludes hasOfferCatalog Person)
  :AXIOMS (rangeIncludes hasOfferCatalog OfferCatalog))

(DEFRELATION contributor (?X ?Y)
  :DOCUMENTATION "A secondary contributor to the CreativeWork or Event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes contributor Organization)
  :AXIOMS (domainIncludes contributor CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label contributor "contributor")
  :AXIOMS (domainIncludes contributor Event)
  :AXIOMS (rangeIncludes contributor Person))

(DEFRELATION schemaVersion (?X ?Y)
  :DOCUMENTATION "Indicates (by URL or string) a particular version of a schema used in some CreativeWork. For example, a document could declare a schemaVersion using an URL such as http://schema.org/version/2.0/ if precise indication of schema version was required by some application."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes schemaVersion CreativeWork)
  :AXIOMS (rangeIncludes schemaVersion URL)
  :AXIOMS (rangeIncludes schemaVersion Text)
  :AXIOMS (/XMLNS/RDFS/label schemaVersion "schemaVersion"))

(ASSERT
 (AND (DriveWheelConfigurationValue RearWheelDriveConfiguration) (/XMLNS/RDFS/label RearWheelDriveConfiguration "RearWheelDriveConfiguration")
  (/XMLNS/DC/source RearWheelDriveConfiguration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (DOCUMENTATION RearWheelDriveConfiguration "Real-wheel drive is a transmission layout where the engine drives the rear wheels.")))

(DEFRELATION organizer (?X ?Y)
  :DOCUMENTATION "An organizer of an Event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes organizer Organization)
  :AXIOMS (/XMLNS/RDFS/label organizer "organizer")
  :AXIOMS (rangeIncludes organizer Person)
  :AXIOMS (domainIncludes organizer Event))

(DEFRELATION manufacturer (?X ?Y)
  :DOCUMENTATION "The manufacturer of the product."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes manufacturer Product)
  :AXIOMS (/XMLNS/RDFS/label manufacturer "manufacturer")
  :AXIOMS (rangeIncludes manufacturer Organization))

(DEFRELATION episodes (?X ?Y)
  :DOCUMENTATION "An episode of a TV/radio series or season."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy episodes episode)
  :AXIOMS (domainIncludes episodes VideoGameSeries)
  :AXIOMS (rangeIncludes episodes Episode)
  :AXIOMS (domainIncludes episodes RadioSeries)
  :AXIOMS (domainIncludes episodes TVSeries)
  :AXIOMS (/XMLNS/RDFS/label episodes "episodes")
  :AXIOMS (domainIncludes episodes CreativeWorkSeason))

(DEFRELATION episode (?X ?Y)
  :DOCUMENTATION "An episode of a tv, radio or game media within a series or season."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF hasPart
  :AXIOMS (domainIncludes episode CreativeWorkSeason)
  :AXIOMS (/XMLNS/RDFS/label episode "episode")
  :AXIOMS (domainIncludes episode VideoGameSeries)
  :AXIOMS (domainIncludes episode TVSeries)
  :AXIOMS (rangeIncludes episode Episode)
  :AXIOMS (domainIncludes episode RadioSeries))

(DEFRELATION printSection (?X ?Y)
  :DOCUMENTATION "If this NewsArticle appears in print, this field indicates the print section in which the article appeared."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label printSection "printSection")
  :AXIOMS (rangeIncludes printSection Text)
  :AXIOMS (domainIncludes printSection NewsArticle))

(DEFRELATION headline (?X ?Y)
  :DOCUMENTATION "Headline of the article."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes headline Text)
  :AXIOMS (/XMLNS/RDFS/label headline "headline")
  :AXIOMS (domainIncludes headline CreativeWork))

(DEFCONCEPT EmploymentAgency (?X)
  :DOCUMENTATION "An employment agency."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label EmploymentAgency "EmploymentAgency"))

(DEFRELATION maxValue (?X ?Y)
  :DOCUMENTATION "The upper value of some characteristic or property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes maxValue MonetaryAmount)
  :AXIOMS (/XMLNS/DC/source maxValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes maxValue PropertyValue)
  :AXIOMS (domainIncludes maxValue PropertyValueSpecification)
  :AXIOMS (domainIncludes maxValue QuantitativeValue)
  :AXIOMS (rangeIncludes maxValue Number)
  :AXIOMS (/XMLNS/RDFS/label maxValue "maxValue"))

(DEFCONCEPT PostOffice (?X)
  :DOCUMENTATION "A post office."
  :SUBSET-OF GovernmentOffice
  :AXIOMS (/XMLNS/RDFS/label PostOffice "PostOffice"))

(DEFCONCEPT TelevisionChannel (?X)
  :DOCUMENTATION "A unique instance of a television BroadcastService on a CableOrSatelliteService lineup."
  :SUBSET-OF BroadcastChannel
  :AXIOMS (/XMLNS/RDFS/label TelevisionChannel "TelevisionChannel"))

(DEFCONCEPT Mosque (?X)
  :DOCUMENTATION "A mosque."
  :SUBSET-OF PlaceOfWorship
  :AXIOMS (/XMLNS/RDFS/label Mosque "Mosque"))

(DEFRELATION height (?X ?Y)
  :DOCUMENTATION "The height of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes height Distance)
  :AXIOMS (rangeIncludes height QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label height "height")
  :AXIOMS (domainIncludes height Person)
  :AXIOMS (domainIncludes height Product)
  :AXIOMS (domainIncludes height VisualArtwork)
  :AXIOMS (domainIncludes height MediaObject))

(ASSERT
 (AND (EventStatusType EventScheduled)
  (DOCUMENTATION EventScheduled
   "The event is taking place or has taken place on the startDate as scheduled. Use of this value is optional, as it is assumed by default.")
  (/XMLNS/RDFS/label EventScheduled "EventScheduled")))

(DEFCONCEPT Park (?X)
  :DOCUMENTATION "A park."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Park "Park"))

(DEFRELATION grantee (?X ?Y)
  :DOCUMENTATION "The person, organization, contact point, or audience that has been granted this permission."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes grantee ContactPoint)
  :AXIOMS (/XMLNS/RDFS/label grantee "grantee")
  :AXIOMS (domainIncludes grantee DigitalDocumentPermission)
  :AXIOMS (rangeIncludes grantee Person)
  :AXIOMS (rangeIncludes grantee Organization)
  :AXIOMS (rangeIncludes grantee Audience))

(DEFCONCEPT CityHall (?X)
  :DOCUMENTATION "A city hall."
  :SUBSET-OF GovernmentBuilding
  :AXIOMS (/XMLNS/RDFS/label CityHall "CityHall"))

(DEFRELATION eligibleTransactionVolume (?X ?Y)
  :DOCUMENTATION "The transaction volume, in a monetary unit, for which the offer or price specification is valid, e.g. for indicating a minimal purchasing volume, to express free shipping above a certain order volume, or to limit the acceptance of credit cards to purchases to a certain minimal amount."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes eligibleTransactionVolume PriceSpecification)
  :AXIOMS (domainIncludes eligibleTransactionVolume Offer)
  :AXIOMS (rangeIncludes eligibleTransactionVolume PriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label eligibleTransactionVolume "eligibleTransactionVolume")
  :AXIOMS (domainIncludes eligibleTransactionVolume Demand)
  :AXIOMS (/XMLNS/DC/source eligibleTransactionVolume |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION providerMobility (?X ?Y)
  :DOCUMENTATION "Indicates the mobility of a provided service (e.g. 'static', 'dynamic')."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes providerMobility Service)
  :AXIOMS (/XMLNS/RDFS/label providerMobility "providerMobility")
  :AXIOMS (rangeIncludes providerMobility Text))

(DEFCONCEPT AppendAction (?X)
  :DOCUMENTATION "The act of inserting at the end if an ordered collection."
  :SUBSET-OF InsertAction
  :AXIOMS (/XMLNS/RDFS/label AppendAction "AppendAction"))

(DEFRELATION softwareHelp (?X ?Y)
  :DOCUMENTATION "Software application help."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes softwareHelp SoftwareApplication)
  :AXIOMS (rangeIncludes softwareHelp CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label softwareHelp "softwareHelp"))

(DEFRELATION albums (?X ?Y)
  :DOCUMENTATION "A collection of music albums."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy albums album)
  :AXIOMS (/XMLNS/RDFS/label albums "albums")
  :AXIOMS (rangeIncludes albums MusicAlbum)
  :AXIOMS (domainIncludes albums MusicGroup))

(DEFRELATION album (?X ?Y)
  :DOCUMENTATION "A music album."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label album "album")
  :AXIOMS (domainIncludes album MusicGroup)
  :AXIOMS (rangeIncludes album MusicAlbum))

(DEFCONCEPT SubscribeAction (?X)
  :DOCUMENTATION "<p>The act of forming a personal connection with someone/something (object) unidirectionally/asymmetrically to get updates pushed to.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/FollowAction\">FollowAction</a>: Unlike FollowAction, SubscribeAction implies that the subscriber acts as a passive agent being constantly/actively pushed for updates.</li>
<li><a class=\"localLink\" href=\"http://schema.org/RegisterAction\">RegisterAction</a>: Unlike RegisterAction, SubscribeAction implies that the agent is interested in continuing receiving updates from the object.</li>
<li><a class=\"localLink\" href=\"http://schema.org/JoinAction\">JoinAction</a>: Unlike JoinAction, SubscribeAction implies that the agent is interested in continuing receiving updates from the object.</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label SubscribeAction "SubscribeAction"))

(DEFRELATION dateDeleted (?X ?Y)
  :DOCUMENTATION "The datetime the item was removed from the DataFeed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateDeleted DataFeedItem)
  :AXIOMS (/XMLNS/RDFS/label dateDeleted "dateDeleted")
  :AXIOMS (rangeIncludes dateDeleted DateTime))

(DEFCONCEPT ShoppingCenter (?X)
  :DOCUMENTATION "A shopping center or mall."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label ShoppingCenter "ShoppingCenter"))

(DEFRELATION title (?X ?Y)
  :DOCUMENTATION "The title of the job."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes title Text)
  :AXIOMS (domainIncludes title JobPosting)
  :AXIOMS (/XMLNS/RDFS/label title "title"))

(DEFRELATION acceptsReservations (?X ?Y)
  :DOCUMENTATION "Indicates whether a FoodEstablishment accepts reservations. Values can be Boolean, an URL at which reservations can be made or (for backwards compatibility) the strings <code>Yes</code> or <code>No</code>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes acceptsReservations Text)
  :AXIOMS (/XMLNS/RDFS/label acceptsReservations "acceptsReservations")
  :AXIOMS (rangeIncludes acceptsReservations Boolean)
  :AXIOMS (domainIncludes acceptsReservations FoodEstablishment)
  :AXIOMS (rangeIncludes acceptsReservations URL))

(DEFRELATION servesCuisine (?X ?Y)
  :DOCUMENTATION "The cuisine of the restaurant."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label servesCuisine "servesCuisine")
  :AXIOMS (domainIncludes servesCuisine FoodEstablishment)
  :AXIOMS (rangeIncludes servesCuisine Text))

(DEFRELATION attendees (?X ?Y)
  :DOCUMENTATION "A person attending the event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy attendees attendee)
  :AXIOMS (domainIncludes attendees Event)
  :AXIOMS (rangeIncludes attendees Person)
  :AXIOMS (rangeIncludes attendees Organization)
  :AXIOMS (/XMLNS/RDFS/label attendees "attendees"))

(DEFRELATION attendee (?X ?Y)
  :DOCUMENTATION "A person or organization attending the event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes attendee Person)
  :AXIOMS (domainIncludes attendee Event)
  :AXIOMS (/XMLNS/RDFS/label attendee "attendee")
  :AXIOMS (rangeIncludes attendee Organization))

(DEFRELATION boardingPolicy (?X ?Y)
  :DOCUMENTATION "The type of boarding policy used by the airline (e.g. zone-based or group-based)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes boardingPolicy Flight)
  :AXIOMS (/XMLNS/RDFS/label boardingPolicy "boardingPolicy")
  :AXIOMS (rangeIncludes boardingPolicy BoardingPolicyType)
  :AXIOMS (domainIncludes boardingPolicy Airline))

(DEFRELATION inventoryLevel (?X ?Y)
  :DOCUMENTATION "The current approximate inventory level for the item or items."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source inventoryLevel |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes inventoryLevel Demand)
  :AXIOMS (/XMLNS/RDFS/label inventoryLevel "inventoryLevel")
  :AXIOMS (domainIncludes inventoryLevel SomeProducts)
  :AXIOMS (domainIncludes inventoryLevel Offer)
  :AXIOMS (rangeIncludes inventoryLevel QuantitativeValue))

(DEFRELATION operatingSystem (?X ?Y)
  :DOCUMENTATION "Operating systems supported (Windows 7, OSX 10.6, Android 1.6)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes operatingSystem Text)
  :AXIOMS (domainIncludes operatingSystem SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label operatingSystem "operatingSystem"))

(DEFCONCEPT ScholarlyArticle (?X)
  :DOCUMENTATION "A scholarly article."
  :SUBSET-OF Article
  :AXIOMS (/XMLNS/RDFS/label ScholarlyArticle "ScholarlyArticle"))

(ASSERT
 (AND (RsvpResponseType RsvpResponseMaybe) (DOCUMENTATION RsvpResponseMaybe "The invitee may or may not attend.")
  (/XMLNS/RDFS/label RsvpResponseMaybe "RsvpResponseMaybe")))

(DEFRELATION gtin13 (?X ?Y)
  :DOCUMENTATION "The <a href=\"http://apps.gs1.org/GDD/glossary/Pages/GTIN-13.aspx\">GTIN-13</a> code of the product, or the product to which the offer refers. This is equivalent to 13-digit ISBN codes and EAN UCC-13. Former 12-digit UPC codes can be converted into a GTIN-13 code by simply adding a preceeding zero. See <a href=\"http://www.gs1.org/barcodes/technical/idkeys/gtin\">GS1 GTIN Summary</a> for more details."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes gtin13 Text)
  :AXIOMS (/XMLNS/DC/source gtin13 |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes gtin13 Product)
  :AXIOMS (domainIncludes gtin13 Offer)
  :AXIOMS (domainIncludes gtin13 Demand)
  :AXIOMS (/XMLNS/RDFS/label gtin13 "gtin13"))

(DEFRELATION boardingGroup (?X ?Y)
  :DOCUMENTATION "The airline-specific indicator of boarding order / preference."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label boardingGroup "boardingGroup")
  :AXIOMS (domainIncludes boardingGroup FlightReservation)
  :AXIOMS (rangeIncludes boardingGroup Text))

(DEFCONCEPT BedAndBreakfast (?X)
  :DOCUMENTATION "Bed and breakfast.
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF LodgingBusiness
  :AXIOMS (/XMLNS/RDFS/label BedAndBreakfast "BedAndBreakfast"))

(DEFRELATION gtin8 (?X ?Y)
  :DOCUMENTATION "The <a href=\"http://apps.gs1.org/GDD/glossary/Pages/GTIN-8.aspx\">GTIN-8</a> code of the product, or the product to which the offer refers. This code is also known as EAN/UCC-8 or 8-digit EAN. See <a href=\"http://www.gs1.org/barcodes/technical/idkeys/gtin\">GS1 GTIN Summary</a> for more details."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes gtin8 Offer)
  :AXIOMS (/XMLNS/DC/source gtin8 |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes gtin8 Product)
  :AXIOMS (domainIncludes gtin8 Demand)
  :AXIOMS (/XMLNS/RDFS/label gtin8 "gtin8")
  :AXIOMS (rangeIncludes gtin8 Text))

(DEFRELATION dateRead (?X ?Y)
  :DOCUMENTATION "The date/time at which the message has been read by the recipient if a single recipient exists."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes dateRead DateTime)
  :AXIOMS (/XMLNS/RDFS/label dateRead "dateRead")
  :AXIOMS (domainIncludes dateRead Message))

(DEFRELATION bookingTime (?X ?Y)
  :DOCUMENTATION "The date and time the reservation was booked."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes bookingTime Reservation)
  :AXIOMS (rangeIncludes bookingTime DateTime)
  :AXIOMS (/XMLNS/RDFS/label bookingTime "bookingTime"))

(DEFCONCEPT ListenAction (?X)
  :DOCUMENTATION "The act of consuming audio content."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label ListenAction "ListenAction"))

(DEFCONCEPT MusicStore (?X)
  :DOCUMENTATION "A music store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label MusicStore "MusicStore"))

(ASSERT
 (AND (DayOfWeek Wednesday) (DOCUMENTATION Wednesday "The day of the week between Tuesday and Thursday.") (/XMLNS/RDFS/label Wednesday "Wednesday")
  (sameAs Wednesday |http://www.wikidata.org/entity/Q128|)))

(DEFCONCEPT VisualArtsEvent (?X)
  :DOCUMENTATION "Event type: Visual arts event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label VisualArtsEvent "VisualArtsEvent"))

(DEFRELATION priceComponent (?X ?Y)
  :DOCUMENTATION "This property links to all <a class=\"localLink\" href=\"http://schema.org/UnitPriceSpecification\">UnitPriceSpecification</a> nodes that apply in parallel for the <a class=\"localLink\" href=\"http://schema.org/CompoundPriceSpecification\">CompoundPriceSpecification</a> node."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes priceComponent UnitPriceSpecification)
  :AXIOMS (/XMLNS/DC/source priceComponent |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label priceComponent "priceComponent")
  :AXIOMS (domainIncludes priceComponent CompoundPriceSpecification))

(DEFRELATION cookingMethod (?X ?Y)
  :DOCUMENTATION "The method of cooking, such as Frying, Steaming, ..."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label cookingMethod "cookingMethod")
  :AXIOMS (rangeIncludes cookingMethod Text)
  :AXIOMS (domainIncludes cookingMethod Recipe))

(DEFRELATION downloadUrl (?X ?Y)
  :DOCUMENTATION "If the file can be downloaded, URL to download the binary."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes downloadUrl URL)
  :AXIOMS (domainIncludes downloadUrl SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label downloadUrl "downloadUrl"))

(DEFCONCEPT CampingPitch (?X)
  :DOCUMENTATION "A camping pitch is an individual place for overnight stay in the outdoors, typically being part of a larger camping site.
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF Accommodation
  :AXIOMS (/XMLNS/RDFS/label CampingPitch "CampingPitch")
  :AXIOMS (/XMLNS/DC/source CampingPitch |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFRELATION itemShipped (?X ?Y)
  :DOCUMENTATION "Item(s) being shipped."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes itemShipped Product)
  :AXIOMS (domainIncludes itemShipped ParcelDelivery)
  :AXIOMS (/XMLNS/RDFS/label itemShipped "itemShipped"))

(DEFRELATION storageRequirements (?X ?Y)
  :DOCUMENTATION "Storage requirements (free space required)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes storageRequirements Text)
  :AXIOMS (/XMLNS/RDFS/label storageRequirements "storageRequirements")
  :AXIOMS (domainIncludes storageRequirements SoftwareApplication)
  :AXIOMS (rangeIncludes storageRequirements URL))

(DEFRELATION lodgingUnitDescription (?X ?Y)
  :DOCUMENTATION "A full description of the lodging unit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes lodgingUnitDescription Text)
  :AXIOMS (/XMLNS/RDFS/label lodgingUnitDescription "lodgingUnitDescription")
  :AXIOMS (domainIncludes lodgingUnitDescription LodgingReservation))

(DEFRELATION reviews (?X ?Y)
  :DOCUMENTATION "Review of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label reviews "reviews")
  :AXIOMS (domainIncludes reviews Product)
  :AXIOMS (domainIncludes reviews CreativeWork)
  :AXIOMS (domainIncludes reviews Place)
  :AXIOMS (supersededBy reviews review)
  :AXIOMS (rangeIncludes reviews Review)
  :AXIOMS (domainIncludes reviews Offer)
  :AXIOMS (domainIncludes reviews Organization))

(DEFRELATION review (?X ?Y)
  :DOCUMENTATION "A review of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes review Offer)
  :AXIOMS (rangeIncludes review Review)
  :AXIOMS (domainIncludes review Event)
  :AXIOMS (domainIncludes review Service)
  :AXIOMS (domainIncludes review Place)
  :AXIOMS (domainIncludes review CreativeWork)
  :AXIOMS (domainIncludes review Product)
  :AXIOMS (/XMLNS/RDFS/label review "review")
  :AXIOMS (domainIncludes review Brand)
  :AXIOMS (domainIncludes review Organization))

(DEFRELATION mainContentOfPage (?X ?Y)
  :DOCUMENTATION "Indicates if this web page element is the main subject of the page."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label mainContentOfPage "mainContentOfPage")
  :AXIOMS (domainIncludes mainContentOfPage WebPage)
  :AXIOMS (rangeIncludes mainContentOfPage WebPageElement))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_WikiDoc|)
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_WikiDoc| "WikiDoc")
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_WikiDoc|
   "This class contains information contributed by <a href=\"http://wikidoc.org>WikiDoc\">http://wikidoc.org>WikiDoc</a>.")))

(DEFCONCEPT TextDigitalDocument (?X)
  :DOCUMENTATION "A file composed primarily of text."
  :SUBSET-OF DigitalDocument
  :AXIOMS (/XMLNS/RDFS/label TextDigitalDocument "TextDigitalDocument"))

(DEFRELATION expectsAcceptanceOf (?X ?Y)
  :DOCUMENTATION "An Offer which must be accepted before the user can perform the Action. For example, the user may need to buy a movie before being able to watch it."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes expectsAcceptanceOf ConsumeAction)
  :AXIOMS (rangeIncludes expectsAcceptanceOf Offer)
  :AXIOMS (/XMLNS/RDFS/label expectsAcceptanceOf "expectsAcceptanceOf"))

(DEFRELATION globalLocationNumber (?X ?Y)
  :DOCUMENTATION "The <a href=\"http://www.gs1.org/gln\">Global Location Number</a> (GLN, sometimes also referred to as International Location Number or ILN) of the respective organization, person, or place. The GLN is a 13-digit number used to identify parties and physical locations."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes globalLocationNumber Person)
  :AXIOMS (domainIncludes globalLocationNumber Place)
  :AXIOMS (domainIncludes globalLocationNumber Organization)
  :AXIOMS (rangeIncludes globalLocationNumber Text)
  :AXIOMS (/XMLNS/RDFS/label globalLocationNumber "globalLocationNumber")
  :AXIOMS (/XMLNS/DC/source globalLocationNumber |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFCONCEPT TelevisionStation (?X)
  :DOCUMENTATION "A television station."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label TelevisionStation "TelevisionStation"))

(DEFRELATION issn (?X ?Y)
  :DOCUMENTATION "The International Standard Serial Number (ISSN) that identifies this periodical. You can repeat this property to (for example) identify different formats of this periodical."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/OWL/equivalentProperty issn |http://purl.org/ontology/bibo/issn|)
  :AXIOMS (/XMLNS/RDFS/label issn "issn")
  :AXIOMS (domainIncludes issn Periodical)
  :AXIOMS (rangeIncludes issn Text)
  :AXIOMS (/XMLNS/DC/source issn |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|))

(DEFCONCEPT UserPlays (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (/XMLNS/RDFS/label UserPlays "UserPlays")
  :AXIOMS (supersededBy UserPlays InteractionCounter))

(ASSERT
 (AND (ItemAvailability OnlineOnly) (DOCUMENTATION OnlineOnly "Indicates that the item is available only online.")
  (/XMLNS/RDFS/label OnlineOnly "OnlineOnly")))

(DEFRELATION deliveryLeadTime (?X ?Y)
  :DOCUMENTATION "The typical delay between the receipt of the order and the goods either leaving the warehouse or being prepared for pickup, in case the delivery method is on site pickup."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label deliveryLeadTime "deliveryLeadTime")
  :AXIOMS (/XMLNS/DC/source deliveryLeadTime |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes deliveryLeadTime Demand)
  :AXIOMS (rangeIncludes deliveryLeadTime QuantitativeValue)
  :AXIOMS (domainIncludes deliveryLeadTime Offer))

(ASSERT
 (AND (MusicAlbumProductionType LiveAlbum) (DOCUMENTATION LiveAlbum "LiveAlbum.") (/XMLNS/RDFS/label LiveAlbum "LiveAlbum")
  (/XMLNS/DC/source LiveAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION vehicleInteriorType (?X ?Y)
  :DOCUMENTATION "The type or material of the interior of the vehicle (e.g. synthetic fabric, leather, wood, etc.). While most interior types are characterized by the material used, an interior type can also be based on vehicle usage or target audience."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes vehicleInteriorType Text)
  :AXIOMS (domainIncludes vehicleInteriorType Vehicle)
  :AXIOMS (/XMLNS/DC/source vehicleInteriorType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (/XMLNS/RDFS/label vehicleInteriorType "vehicleInteriorType"))

(DEFRELATION birthPlace (?X ?Y)
  :DOCUMENTATION "The place where the person was born."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label birthPlace "birthPlace")
  :AXIOMS (domainIncludes birthPlace Person)
  :AXIOMS (rangeIncludes birthPlace Place))

(DEFRELATION accessibilityHazard (?X ?Y)
  :DOCUMENTATION "A characteristic of the described resource that is physiologically dangerous to some users. Related to WCAG 2.0 guideline 2.3 (<a href=\"http://www.w3.org/wiki/WebSchemas/Accessibility\">WebSchemas wiki lists possible values</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes accessibilityHazard CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label accessibilityHazard "accessibilityHazard")
  :AXIOMS (rangeIncludes accessibilityHazard Text))

(DEFCONCEPT NightClub (?X)
  :DOCUMENTATION "A nightclub or discotheque."
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label NightClub "NightClub"))

(DEFRELATION iswcCode (?X ?Y)
  :DOCUMENTATION "The International Standard Musical Work Code for the composition."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source iswcCode |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (domainIncludes iswcCode MusicComposition)
  :AXIOMS (/XMLNS/RDFS/label iswcCode "iswcCode")
  :AXIOMS (rangeIncludes iswcCode Text))

(DEFRELATION appliesToPaymentMethod (?X ?Y)
  :DOCUMENTATION "The payment method(s) to which the payment charge specification applies."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source appliesToPaymentMethod |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes appliesToPaymentMethod PaymentChargeSpecification)
  :AXIOMS (rangeIncludes appliesToPaymentMethod PaymentMethod)
  :AXIOMS (/XMLNS/RDFS/label appliesToPaymentMethod "appliesToPaymentMethod"))

(DEFCONCEPT MensClothingStore (?X)
  :DOCUMENTATION "A men's clothing store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label MensClothingStore "MensClothingStore"))

(DEFCONCEPT ParcelService (?X)
  :DOCUMENTATION "<p>A private parcel service as the delivery mode available for a certain offer.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#DHL</li>
<li>http://purl.org/goodrelations/v1#FederalExpress</li>
<li>http://purl.org/goodrelations/v1#UPS</li>
</ul>
"
  :SUBSET-OF DeliveryMethod
  :AXIOMS (/XMLNS/RDFS/label ParcelService "ParcelService")
  :AXIOMS (/XMLNS/DC/source ParcelService |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT SelfStorage (?X)
  :DOCUMENTATION "A self-storage facility."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label SelfStorage "SelfStorage"))

(DEFRELATION codeRepository (?X ?Y)
  :DOCUMENTATION "Link to the repository where the un-compiled, human readable code and related code is located (SVN, github, CodePlex)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes codeRepository URL)
  :AXIOMS (domainIncludes codeRepository SoftwareSourceCode)
  :AXIOMS (/XMLNS/RDFS/label codeRepository "codeRepository"))

(DEFCONCEPT SpreadsheetDigitalDocument (?X)
  :DOCUMENTATION "A spreadsheet file."
  :SUBSET-OF DigitalDocument
  :AXIOMS (/XMLNS/RDFS/label SpreadsheetDigitalDocument "SpreadsheetDigitalDocument"))

(DEFRELATION printPage (?X ?Y)
  :DOCUMENTATION "If this NewsArticle appears in print, this field indicates the name of the page on which the article is found. Please note that this field is intended for the exact page name (e.g. A5, B18)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes printPage NewsArticle)
  :AXIOMS (/XMLNS/RDFS/label printPage "printPage")
  :AXIOMS (rangeIncludes printPage Text))

(DEFRELATION brand (?X ?Y)
  :DOCUMENTATION "The brand(s) associated with a product or service, or the brand(s) maintained by an organization or business person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes brand Organization)
  :AXIOMS (domainIncludes brand Service)
  :AXIOMS (domainIncludes brand Person)
  :AXIOMS (rangeIncludes brand Organization)
  :AXIOMS (/XMLNS/RDFS/label brand "brand")
  :AXIOMS (domainIncludes brand Product)
  :AXIOMS (rangeIncludes brand Brand))

(DEFRELATION illustrator (?X ?Y)
  :DOCUMENTATION "The illustrator of the book."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label illustrator "illustrator")
  :AXIOMS (domainIncludes illustrator Book)
  :AXIOMS (rangeIncludes illustrator Person))

(DEFRELATION colleagues (?X ?Y)
  :DOCUMENTATION "A colleague of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy colleagues colleague)
  :AXIOMS (rangeIncludes colleagues Person)
  :AXIOMS (/XMLNS/RDFS/label colleagues "colleagues")
  :AXIOMS (domainIncludes colleagues Person))

(DEFRELATION colleague (?X ?Y)
  :DOCUMENTATION "A colleague of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes colleague Person)
  :AXIOMS (/XMLNS/RDFS/label colleague "colleague")
  :AXIOMS (domainIncludes colleague Person)
  :AXIOMS (rangeIncludes colleague URL))

(DEFRELATION mpn (?X ?Y)
  :DOCUMENTATION "The Manufacturer Part Number (MPN) of the product, or the product to which the offer refers."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes mpn Product)
  :AXIOMS (domainIncludes mpn Offer)
  :AXIOMS (/XMLNS/RDFS/label mpn "mpn")
  :AXIOMS (domainIncludes mpn Demand)
  :AXIOMS (rangeIncludes mpn Text)
  :AXIOMS (/XMLNS/DC/source mpn |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION fuelEfficiency (?X ?Y)
  :DOCUMENTATION "<p>The distance traveled per unit of fuel used; most commonly miles per gallon (mpg) or kilometers per liter (km/L).</p>

<ul>
<li>Note 1: There are unfortunately no standard unit codes for miles per gallon or kilometers per liter. Use <a class=\"localLink\" href=\"http://schema.org/unitText\">unitText</a> to indicate the unit of measurement, e.g. mpg or km/L.</li>
<li>Note 2: There are two ways of indicating the fuel consumption, <a class=\"localLink\" href=\"http://schema.org/fuelConsumption\">fuelConsumption</a> (e.g. 8 liters per 100 km) and <a class=\"localLink\" href=\"http://schema.org/fuelEfficiency\">fuelEfficiency</a> (e.g. 30 miles per gallon). They are reciprocal.</li>
<li>Note 3: Often, the absolute value is useful only when related to driving speed (\"at 80 km/h\") or usage pattern (\"city traffic\"). You can use <a class=\"localLink\" href=\"http://schema.org/valueReference\">valueReference</a> to link the value for the fuel economy to another value.</li>
</ul>
"
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label fuelEfficiency "fuelEfficiency")
  :AXIOMS (/XMLNS/DC/source fuelEfficiency |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes fuelEfficiency QuantitativeValue)
  :AXIOMS (domainIncludes fuelEfficiency Vehicle))

(DEFCONCEPT DanceGroup (?X)
  :DOCUMENTATION "A dance group&#x2014;for example, the Alvin Ailey Dance Theater or Riverdance."
  :SUBSET-OF PerformingGroup
  :AXIOMS (/XMLNS/RDFS/label DanceGroup "DanceGroup"))

(DEFRELATION foodEstablishment (?X ?Y)
  :DOCUMENTATION "A sub property of location. The specific food establishment where the action occurred."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (/XMLNS/RDFS/label foodEstablishment "foodEstablishment")
  :AXIOMS (domainIncludes foodEstablishment CookAction)
  :AXIOMS (rangeIncludes foodEstablishment Place)
  :AXIOMS (rangeIncludes foodEstablishment FoodEstablishment))

(DEFRELATION volumeNumber (?X ?Y)
  :DOCUMENTATION "Identifies the volume of publication or multi-part work; for example, \"iii\" or \"2\"."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF position
  :AXIOMS (/XMLNS/OWL/equivalentProperty volumeNumber |http://purl.org/ontology/bibo/volume|)
  :AXIOMS (/XMLNS/DC/source volumeNumber |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (rangeIncludes volumeNumber Text)
  :AXIOMS (rangeIncludes volumeNumber Integer)
  :AXIOMS (domainIncludes volumeNumber PublicationVolume)
  :AXIOMS (/XMLNS/RDFS/label volumeNumber "volumeNumber"))

(DEFRELATION isbn (?X ?Y)
  :DOCUMENTATION "The ISBN of the book."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label isbn "isbn")
  :AXIOMS (domainIncludes isbn Book)
  :AXIOMS (/XMLNS/OWL/equivalentProperty isbn |http://purl.org/ontology/bibo/isbn|)
  :AXIOMS (rangeIncludes isbn Text))

(DEFCONCEPT SportsClub (?X)
  :DOCUMENTATION "A sports club."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label SportsClub "SportsClub"))

(DEFCONCEPT HighSchool (?X)
  :DOCUMENTATION "A high school."
  :SUBSET-OF EducationalOrganization
  :AXIOMS (/XMLNS/RDFS/label HighSchool "HighSchool"))

(DEFRELATION publication (?X ?Y)
  :DOCUMENTATION "A publication event associated with the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes publication CreativeWork)
  :AXIOMS (rangeIncludes publication PublicationEvent)
  :AXIOMS (/XMLNS/RDFS/label publication "publication"))

(ASSERT
 (AND (PaymentStatusType PaymentPastDue) (DOCUMENTATION PaymentPastDue "The payment is due and considered late.")
  (/XMLNS/RDFS/label PaymentPastDue "PaymentPastDue")))

(DEFCONCEPT RadioEpisode (?X)
  :DOCUMENTATION "A radio episode which can be part of a series or season."
  :SUBSET-OF Episode
  :AXIOMS (/XMLNS/RDFS/label RadioEpisode "RadioEpisode"))

(DEFRELATION author (?X ?Y)
  :DOCUMENTATION "The author of this content or rating. Please note that author is special in that HTML 5 provides a special mechanism for indicating authorship via the rel tag. That is equivalent to this and may be used interchangeably."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes author Rating)
  :AXIOMS (domainIncludes author CreativeWork)
  :AXIOMS (rangeIncludes author Organization)
  :AXIOMS (/XMLNS/RDFS/label author "author")
  :AXIOMS (rangeIncludes author Person))

(DEFRELATION broadcastAffiliateOf (?X ?Y)
  :DOCUMENTATION "The media network(s) whose content is broadcast on this station."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes broadcastAffiliateOf BroadcastService)
  :AXIOMS (rangeIncludes broadcastAffiliateOf Organization)
  :AXIOMS (/XMLNS/RDFS/label broadcastAffiliateOf "broadcastAffiliateOf"))

(DEFRELATION accessCode (?X ?Y)
  :DOCUMENTATION "Password, PIN, or access code needed for delivery (e.g. from a locker)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes accessCode DeliveryEvent)
  :AXIOMS (/XMLNS/RDFS/label accessCode "accessCode")
  :AXIOMS (rangeIncludes accessCode Text))

(DEFRELATION amount (?X ?Y)
  :DOCUMENTATION "The amount of money."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes amount LoanOrCredit)
  :AXIOMS (domainIncludes amount InvestmentOrDeposit)
  :AXIOMS (rangeIncludes amount MonetaryAmount)
  :AXIOMS (rangeIncludes amount Number)
  :AXIOMS (/XMLNS/RDFS/label amount "amount")
  :AXIOMS (domainIncludes amount DatedMoneySpecification))

(DEFRELATION greaterOrEqual (?X ?Y)
  :DOCUMENTATION "This ordering relation for qualitative values indicates that the subject is greater than or equal to the object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source greaterOrEqual |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label greaterOrEqual "greaterOrEqual")
  :AXIOMS (rangeIncludes greaterOrEqual QualitativeValue)
  :AXIOMS (domainIncludes greaterOrEqual QualitativeValue))

(DEFCONCEPT AmusementPark (?X)
  :DOCUMENTATION "An amusement park."
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label AmusementPark "AmusementPark"))

(DEFRELATION postOfficeBoxNumber (?X ?Y)
  :DOCUMENTATION "The post office box number for PO box addresses."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes postOfficeBoxNumber Text)
  :AXIOMS (/XMLNS/RDFS/label postOfficeBoxNumber "postOfficeBoxNumber")
  :AXIOMS (domainIncludes postOfficeBoxNumber PostalAddress))

(DEFRELATION recipeCuisine (?X ?Y)
  :DOCUMENTATION "The cuisine of the recipe (for example, French or Ethiopian)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes recipeCuisine Text)
  :AXIOMS (domainIncludes recipeCuisine Recipe)
  :AXIOMS (/XMLNS/RDFS/label recipeCuisine "recipeCuisine"))

(DEFRELATION sport (?X ?Y)
  :DOCUMENTATION "A type of sport (e.g. Baseball)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label sport "sport")
  :AXIOMS (rangeIncludes sport URL)
  :AXIOMS (rangeIncludes sport Text)
  :AXIOMS (domainIncludes sport SportsOrganization))

(DEFRELATION equal (?X ?Y)
  :DOCUMENTATION "This ordering relation for qualitative values indicates that the subject is equal to the object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes equal QualitativeValue)
  :AXIOMS (/XMLNS/DC/source equal |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label equal "equal")
  :AXIOMS (domainIncludes equal QualitativeValue))

(DEFCONCEPT GolfCourse (?X)
  :DOCUMENTATION "A golf course."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label GolfCourse "GolfCourse"))

(ASSERT
 (AND (MusicReleaseFormatType CDFormat) (/XMLNS/RDFS/label CDFormat "CDFormat") (DOCUMENTATION CDFormat "CDFormat.")
  (/XMLNS/DC/source CDFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION arrivalTerminal (?X ?Y)
  :DOCUMENTATION "Identifier of the flight's arrival terminal."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label arrivalTerminal "arrivalTerminal")
  :AXIOMS (rangeIncludes arrivalTerminal Text)
  :AXIOMS (domainIncludes arrivalTerminal Flight))

(DEFCONCEPT SearchResultsPage (?X)
  :DOCUMENTATION "Web page type: Search results page."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label SearchResultsPage "SearchResultsPage"))

(DEFRELATION hostingOrganization (?X ?Y)
  :DOCUMENTATION "The organization (airline, travelers' club, etc.) the membership is made with."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes hostingOrganization ProgramMembership)
  :AXIOMS (/XMLNS/RDFS/label hostingOrganization "hostingOrganization")
  :AXIOMS (rangeIncludes hostingOrganization Organization))

(DEFCONCEPT Church (?X)
  :DOCUMENTATION "A church."
  :SUBSET-OF PlaceOfWorship
  :AXIOMS (/XMLNS/RDFS/label Church "Church"))

(DEFRELATION relatedTo (?X ?Y)
  :DOCUMENTATION "The most generic familial relation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes relatedTo Person)
  :AXIOMS (domainIncludes relatedTo Person)
  :AXIOMS (/XMLNS/RDFS/label relatedTo "relatedTo"))

(DEFRELATION ratingValue (?X ?Y)
  :DOCUMENTATION "The rating for the content."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes ratingValue Number)
  :AXIOMS (rangeIncludes ratingValue Text)
  :AXIOMS (domainIncludes ratingValue Rating)
  :AXIOMS (/XMLNS/RDFS/label ratingValue "ratingValue"))

(DEFCONCEPT MotorcycleRepair (?X)
  :DOCUMENTATION "A motorcycle repair shop."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label MotorcycleRepair "MotorcycleRepair"))

(DEFRELATION byArtist (?X ?Y)
  :DOCUMENTATION "The artist that performed this album or recording."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes byArtist MusicGroup)
  :AXIOMS (domainIncludes byArtist MusicRecording)
  :AXIOMS (/XMLNS/RDFS/label byArtist "byArtist")
  :AXIOMS (domainIncludes byArtist MusicAlbum))

(ASSERT
 (AND (DayOfWeek Saturday) (DOCUMENTATION Saturday "The day of the week between Friday and Sunday.") (/XMLNS/RDFS/label Saturday "Saturday")
  (sameAs Saturday |http://www.wikidata.org/entity/Q131|)))

(DEFCONCEPT BarOrPub (?X)
  :DOCUMENTATION "A bar or pub."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label BarOrPub "BarOrPub"))

(ASSERT
 (AND (ReservationStatusType ReservationConfirmed) (/XMLNS/RDFS/label ReservationConfirmed "ReservationConfirmed")
  (DOCUMENTATION ReservationConfirmed "The status of a confirmed reservation.")))

(DEFRELATION item (?X ?Y)
  :DOCUMENTATION "An entity represented by an entry in a list or data feed (e.g. an 'artist' in a list of 'artists')’."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes item DataFeedItem)
  :AXIOMS (rangeIncludes item Thing)
  :AXIOMS (/XMLNS/RDFS/label item "item")
  :AXIOMS (domainIncludes item ListItem))

(DEFRELATION priceSpecification (?X ?Y)
  :DOCUMENTATION "One or more detailed price specifications, indicating the unit price and delivery or payment charges."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label priceSpecification "priceSpecification")
  :AXIOMS (/XMLNS/DC/source priceSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes priceSpecification PriceSpecification)
  :AXIOMS (domainIncludes priceSpecification Demand)
  :AXIOMS (domainIncludes priceSpecification Offer)
  :AXIOMS (domainIncludes priceSpecification TradeAction))

(DEFCONCEPT WholesaleStore (?X)
  :DOCUMENTATION "A wholesale store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label WholesaleStore "WholesaleStore"))

(DEFCONCEPT ReadAction (?X)
  :DOCUMENTATION "The act of consuming written content."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label ReadAction "ReadAction"))

(ASSERT
 (AND (SteeringPositionValue LeftHandDriving)
  (/XMLNS/DC/source LeftHandDriving |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (/XMLNS/RDFS/label LeftHandDriving "LeftHandDriving")
  (DOCUMENTATION LeftHandDriving "The steering position is on the left side of the vehicle (viewed from the main direction of driving).")))

(DEFCONCEPT Hostel (?X)
  :DOCUMENTATION "A hostel - cheap accommodation, often in shared dormitories.
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF LodgingBusiness
  :AXIOMS (/XMLNS/RDFS/label Hostel "Hostel"))

(DEFRELATION locationCreated (?X ?Y)
  :DOCUMENTATION "The location where the CreativeWork was created, which may not be the same as the location depicted in the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes locationCreated Place)
  :AXIOMS (/XMLNS/RDFS/label locationCreated "locationCreated")
  :AXIOMS (domainIncludes locationCreated CreativeWork))

(DEFCONCEPT CollegeOrUniversity (?X)
  :DOCUMENTATION "A college, university, or other third-level educational institution."
  :SUBSET-OF EducationalOrganization
  :AXIOMS (/XMLNS/RDFS/label CollegeOrUniversity "CollegeOrUniversity"))

(DEFRELATION carrierRequirements (?X ?Y)
  :DOCUMENTATION "Specifies specific carrier(s) requirements for the application (e.g. an application may only work on a specific carrier network)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label carrierRequirements "carrierRequirements")
  :AXIOMS (rangeIncludes carrierRequirements Text)
  :AXIOMS (domainIncludes carrierRequirements MobileApplication))

(DEFRELATION passengerSequenceNumber (?X ?Y)
  :DOCUMENTATION "The passenger's sequence number as assigned by the airline."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label passengerSequenceNumber "passengerSequenceNumber")
  :AXIOMS (rangeIncludes passengerSequenceNumber Text)
  :AXIOMS (domainIncludes passengerSequenceNumber FlightReservation))

(DEFRELATION clipNumber (?X ?Y)
  :DOCUMENTATION "Position of the clip within an ordered group of clips."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF position
  :AXIOMS (rangeIncludes clipNumber Text)
  :AXIOMS (/XMLNS/RDFS/label clipNumber "clipNumber")
  :AXIOMS (domainIncludes clipNumber Clip)
  :AXIOMS (rangeIncludes clipNumber Integer))

(DEFRELATION reviewCount (?X ?Y)
  :DOCUMENTATION "The count of total number of reviews."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label reviewCount "reviewCount")
  :AXIOMS (rangeIncludes reviewCount Integer)
  :AXIOMS (domainIncludes reviewCount AggregateRating))

(DEFRELATION workPresented (?X ?Y)
  :DOCUMENTATION "The movie presented during this event."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF workFeatured
  :AXIOMS (domainIncludes workPresented ScreeningEvent)
  :AXIOMS (/XMLNS/RDFS/label workPresented "workPresented")
  :AXIOMS (rangeIncludes workPresented Movie))

(DEFCONCEPT DislikeAction (?X)
  :DOCUMENTATION "The act of expressing a negative sentiment about the object. An agent dislikes an object (a proposition, topic or theme) with participants."
  :SUBSET-OF ReactAction
  :AXIOMS (/XMLNS/RDFS/label DislikeAction "DislikeAction"))

(DEFRELATION sportsEvent (?X ?Y)
  :DOCUMENTATION "A sub property of location. The sports event where this action occurred."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (rangeIncludes sportsEvent SportsEvent)
  :AXIOMS (/XMLNS/RDFS/label sportsEvent "sportsEvent")
  :AXIOMS (domainIncludes sportsEvent ExerciseAction))

(ASSERT
 (AND (MusicAlbumProductionType RemixAlbum) (/XMLNS/RDFS/label RemixAlbum "RemixAlbum") (DOCUMENTATION RemixAlbum "RemixAlbum.")
  (/XMLNS/DC/source RemixAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFCONCEPT HobbyShop (?X)
  :DOCUMENTATION "A store that sells materials useful or necessary for various hobbies."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label HobbyShop "HobbyShop"))

(DEFRELATION accountablePerson (?X ?Y)
  :DOCUMENTATION "Specifies the Person that is legally accountable for the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes accountablePerson CreativeWork)
  :AXIOMS (rangeIncludes accountablePerson Person)
  :AXIOMS (/XMLNS/RDFS/label accountablePerson "accountablePerson"))

(DEFCONCEPT BusinessEvent (?X)
  :DOCUMENTATION "Event type: Business event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label BusinessEvent "BusinessEvent"))

(DEFCONCEPT TattooParlor (?X)
  :DOCUMENTATION "A tattoo parlor."
  :SUBSET-OF HealthAndBeautyBusiness
  :AXIOMS (/XMLNS/RDFS/label TattooParlor "TattooParlor"))

(DEFRELATION ticketToken (?X ?Y)
  :DOCUMENTATION "Reference to an asset (e.g., Barcode, QR code image or PDF) usable for entrance."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label ticketToken "ticketToken")
  :AXIOMS (rangeIncludes ticketToken URL)
  :AXIOMS (rangeIncludes ticketToken Text)
  :AXIOMS (domainIncludes ticketToken Ticket))

(DEFRELATION processorRequirements (?X ?Y)
  :DOCUMENTATION "Processor architecture required to run the application (e.g. IA64)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes processorRequirements Text)
  :AXIOMS (domainIncludes processorRequirements SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label processorRequirements "processorRequirements"))

(DEFCONCEPT WPFooter (?X)
  :DOCUMENTATION "The footer section of the page."
  :SUBSET-OF WebPageElement
  :AXIOMS (/XMLNS/RDFS/label WPFooter "WPFooter"))

(DEFRELATION arrivalPlatform (?X ?Y)
  :DOCUMENTATION "The platform where the train arrives."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes arrivalPlatform Text)
  :AXIOMS (domainIncludes arrivalPlatform TrainTrip)
  :AXIOMS (/XMLNS/RDFS/label arrivalPlatform "arrivalPlatform"))

(DEFCONCEPT OnDemandEvent (?X)
  :DOCUMENTATION "A publication event e.g. catch-up TV or radio podcast, during which a program is available on-demand."
  :SUBSET-OF PublicationEvent
  :AXIOMS (/XMLNS/RDFS/label OnDemandEvent "OnDemandEvent"))

(DEFRELATION streetAddress (?X ?Y)
  :DOCUMENTATION "The street address. For example, 1600 Amphitheatre Pkwy."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes streetAddress Text)
  :AXIOMS (domainIncludes streetAddress PostalAddress)
  :AXIOMS (/XMLNS/RDFS/label streetAddress "streetAddress"))

(DEFCONCEPT FilmAction (?X)
  :DOCUMENTATION "The act of capturing sound and moving images on film, video, or digitally."
  :SUBSET-OF CreateAction
  :AXIOMS (/XMLNS/RDFS/label FilmAction "FilmAction"))

(DEFRELATION pickupTime (?X ?Y)
  :DOCUMENTATION "When a taxi will pickup a passenger or a rental car can be picked up."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label pickupTime "pickupTime")
  :AXIOMS (rangeIncludes pickupTime DateTime)
  :AXIOMS (domainIncludes pickupTime TaxiReservation)
  :AXIOMS (domainIncludes pickupTime RentalCarReservation))

(DEFRELATION funder (?X ?Y)
  :DOCUMENTATION "A person or organization that supports (sponsors) something through some kind of financial contribution."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF sponsor
  :AXIOMS (domainIncludes funder CreativeWork)
  :AXIOMS (rangeIncludes funder Person)
  :AXIOMS (rangeIncludes funder Organization)
  :AXIOMS (domainIncludes funder Person)
  :AXIOMS (domainIncludes funder Organization)
  :AXIOMS (domainIncludes funder Event)
  :AXIOMS (/XMLNS/RDFS/label funder "funder"))

(DEFRELATION sponsor (?X ?Y)
  :DOCUMENTATION "A person or organization that supports a thing through a pledge, promise, or financial contribution. e.g. a sponsor of a Medical Study or a corporate sponsor of an event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes sponsor Organization)
  :AXIOMS (rangeIncludes sponsor Person)
  :AXIOMS (domainIncludes sponsor Organization)
  :AXIOMS (domainIncludes sponsor Person)
  :AXIOMS (domainIncludes sponsor Event)
  :AXIOMS (domainIncludes sponsor CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label sponsor "sponsor"))

(DEFRELATION paymentAccepted (?X ?Y)
  :DOCUMENTATION "Cash, credit card, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes paymentAccepted Text)
  :AXIOMS (domainIncludes paymentAccepted LocalBusiness)
  :AXIOMS (/XMLNS/RDFS/label paymentAccepted "paymentAccepted"))

(DEFRELATION numTracks (?X ?Y)
  :DOCUMENTATION "The number of tracks in this album or playlist."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numTracks "numTracks")
  :AXIOMS (domainIncludes numTracks MusicPlaylist)
  :AXIOMS (rangeIncludes numTracks Integer))

(DEFRELATION composer (?X ?Y)
  :DOCUMENTATION "The person or organization who wrote a composition, or who is the composer of a work performed at some event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes composer MusicComposition)
  :AXIOMS (/XMLNS/DC/source composer |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (rangeIncludes composer Person)
  :AXIOMS (rangeIncludes composer Organization)
  :AXIOMS (domainIncludes composer Event)
  :AXIOMS (/XMLNS/RDFS/label composer "composer"))

(DEFRELATION longitude (?X ?Y)
  :DOCUMENTATION "The longitude of a location. For example <code>-122.08585</code> (<a href=\"https://en.wikipedia.org/wiki/World_Geodetic_System\">WGS 84</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes longitude GeoCoordinates)
  :AXIOMS (/XMLNS/RDFS/label longitude "longitude")
  :AXIOMS (rangeIncludes longitude Text)
  :AXIOMS (rangeIncludes longitude Number))

(DEFCONCEPT Casino (?X)
  :DOCUMENTATION "A casino."
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label Casino "Casino"))

(DEFRELATION calories (?X ?Y)
  :DOCUMENTATION "The number of calories."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes calories NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label calories "calories")
  :AXIOMS (rangeIncludes calories Energy))

(DEFRELATION billingAddress (?X ?Y)
  :DOCUMENTATION "The billing address for the order."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label billingAddress "billingAddress")
  :AXIOMS (rangeIncludes billingAddress PostalAddress)
  :AXIOMS (domainIncludes billingAddress Order))

(DEFRELATION serverStatus (?X ?Y)
  :DOCUMENTATION "Status of a game server."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes serverStatus GameServer)
  :AXIOMS (rangeIncludes serverStatus GameServerStatus)
  :AXIOMS (/XMLNS/RDFS/label serverStatus "serverStatus"))

(DEFRELATION free (?X ?Y)
  :DOCUMENTATION "A flag to signal that the publication or event is accessible for free."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes free Boolean)
  :AXIOMS (/XMLNS/RDFS/label free "free")
  :AXIOMS (supersededBy free isAccessibleForFree)
  :AXIOMS (domainIncludes free PublicationEvent))

(DEFRELATION isAccessibleForFree (?X ?Y)
  :DOCUMENTATION "A flag to signal that the publication is accessible for free."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label isAccessibleForFree "isAccessibleForFree")
  :AXIOMS (domainIncludes isAccessibleForFree PublicationEvent)
  :AXIOMS (rangeIncludes isAccessibleForFree Boolean)
  :AXIOMS (domainIncludes isAccessibleForFree CreativeWork)
  :AXIOMS (domainIncludes isAccessibleForFree Event))

(ASSERT
 (AND (MusicAlbumProductionType SoundtrackAlbum) (/XMLNS/DC/source SoundtrackAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (/XMLNS/RDFS/label SoundtrackAlbum "SoundtrackAlbum") (DOCUMENTATION SoundtrackAlbum "SoundtrackAlbum.")))

(DEFRELATION publishedOn (?X ?Y)
  :DOCUMENTATION "A broadcast service associated with the publication event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label publishedOn "publishedOn")
  :AXIOMS (rangeIncludes publishedOn BroadcastService)
  :AXIOMS (domainIncludes publishedOn PublicationEvent))

(DEFRELATION eligibleQuantity (?X ?Y)
  :DOCUMENTATION "The interval and unit of measurement of ordering quantities for which the offer or price specification is valid. This allows e.g. specifying that a certain freight charge is valid only for a certain quantity."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes eligibleQuantity Offer)
  :AXIOMS (rangeIncludes eligibleQuantity QuantitativeValue)
  :AXIOMS (domainIncludes eligibleQuantity PriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label eligibleQuantity "eligibleQuantity")
  :AXIOMS (/XMLNS/DC/source eligibleQuantity |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes eligibleQuantity Demand))

(DEFRELATION providesService (?X ?Y)
  :DOCUMENTATION "The service provided by this channel."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label providesService "providesService")
  :AXIOMS (rangeIncludes providesService Service)
  :AXIOMS (domainIncludes providesService ServiceChannel))

(DEFRELATION smokingAllowed (?X ?Y)
  :DOCUMENTATION "Indicates whether it is allowed to smoke in the place, e.g. in the restaurant, hotel or hotel room."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes smokingAllowed Boolean)
  :AXIOMS (domainIncludes smokingAllowed Place)
  :AXIOMS (/XMLNS/DC/source smokingAllowed |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label smokingAllowed "smokingAllowed"))

(DEFRELATION advanceBookingRequirement (?X ?Y)
  :DOCUMENTATION "The amount of time that is required between accepting the offer and the actual usage of the resource or service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes advanceBookingRequirement Offer)
  :AXIOMS (/XMLNS/RDFS/label advanceBookingRequirement "advanceBookingRequirement")
  :AXIOMS (/XMLNS/DC/source advanceBookingRequirement |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes advanceBookingRequirement QuantitativeValue)
  :AXIOMS (domainIncludes advanceBookingRequirement Demand))

(ASSERT
 (AND (RestrictedDiet VegetarianDiet) (/XMLNS/RDFS/label VegetarianDiet "VegetarianDiet")
  (DOCUMENTATION VegetarianDiet "A diet exclusive of animal meat.")))

(DEFRELATION familyName (?X ?Y)
  :DOCUMENTATION "Family name. In the U.S., the last name of an Person. This can be used along with givenName instead of the name property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes familyName Text)
  :AXIOMS (/XMLNS/RDFS/label familyName "familyName")
  :AXIOMS (domainIncludes familyName Person))

(DEFRELATION priceCurrency (?X ?Y)
  :DOCUMENTATION "The currency (in 3-letter ISO 4217 format) of the price or a price component, when attached to <a class=\"localLink\" href=\"http://schema.org/PriceSpecification\">PriceSpecification</a> and its subtypes."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes priceCurrency Offer)
  :AXIOMS (/XMLNS/RDFS/label priceCurrency "priceCurrency")
  :AXIOMS (domainIncludes priceCurrency Ticket)
  :AXIOMS (rangeIncludes priceCurrency Text)
  :AXIOMS (domainIncludes priceCurrency PriceSpecification)
  :AXIOMS (domainIncludes priceCurrency Reservation))

(DEFCONCEPT Barcode (?X)
  :DOCUMENTATION "An image of a visual machine-readable code such as a barcode or QR code."
  :SUBSET-OF ImageObject
  :AXIOMS (/XMLNS/RDFS/label Barcode "Barcode"))

(DEFRELATION orderStatus (?X ?Y)
  :DOCUMENTATION "The current status of the order."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label orderStatus "orderStatus")
  :AXIOMS (rangeIncludes orderStatus OrderStatus)
  :AXIOMS (domainIncludes orderStatus Order))

(DEFRELATION toLocation (?X ?Y)
  :DOCUMENTATION "A sub property of location. The final location of the object or the agent after the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (domainIncludes toLocation MoveAction)
  :AXIOMS (domainIncludes toLocation TransferAction)
  :AXIOMS (domainIncludes toLocation InsertAction)
  :AXIOMS (rangeIncludes toLocation Place)
  :AXIOMS (/XMLNS/RDFS/label toLocation "toLocation")
  :AXIOMS (domainIncludes toLocation ExerciseAction))

(DEFCONCEPT TheaterEvent (?X)
  :DOCUMENTATION "Event type: Theater performance."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label TheaterEvent "TheaterEvent"))

(DEFRELATION significantLinks (?X ?Y)
  :DOCUMENTATION "The most significant URLs on the page. Typically, these are the non-navigation links that are clicked on the most."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy significantLinks significantLink)
  :AXIOMS (/XMLNS/RDFS/label significantLinks "significantLinks")
  :AXIOMS (domainIncludes significantLinks WebPage)
  :AXIOMS (rangeIncludes significantLinks URL))

(DEFRELATION significantLink (?X ?Y)
  :DOCUMENTATION "One of the more significant URLs on the page. Typically, these are the non-navigation links that are clicked on the most."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label significantLink "significantLink")
  :AXIOMS (domainIncludes significantLink WebPage)
  :AXIOMS (rangeIncludes significantLink URL))

(DEFRELATION starRating (?X ?Y)
  :DOCUMENTATION "An official rating for a lodging business or food establishment, e.g. from national associations or standards bodies. Use the author property to indicate the rating organization, e.g. as an Organization with name such as (e.g. HOTREC, DEHOGA, WHR, or Hotelstars)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes starRating FoodEstablishment)
  :AXIOMS (/XMLNS/DC/source starRating |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (domainIncludes starRating LodgingBusiness)
  :AXIOMS (rangeIncludes starRating Rating)
  :AXIOMS (/XMLNS/RDFS/label starRating "starRating"))

(DEFRELATION commentText (?X ?Y)
  :DOCUMENTATION "The text of the UserComment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes commentText UserComments)
  :AXIOMS (/XMLNS/RDFS/label commentText "commentText")
  :AXIOMS (rangeIncludes commentText Text))

(DEFRELATION coursePrerequisites (?X ?Y)
  :DOCUMENTATION "Requirements for taking the Course. May be completion of another <a class=\"localLink\" href=\"http://schema.org/Course\">Course</a> or a textual description like \"permission of instructor\". Requirements may be a pre-requisite competency, referenced using <a class=\"localLink\" href=\"http://schema.org/AlignmentObject\">AlignmentObject</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes coursePrerequisites Text)
  :AXIOMS (rangeIncludes coursePrerequisites Course)
  :AXIOMS (rangeIncludes coursePrerequisites AlignmentObject)
  :AXIOMS (/XMLNS/RDFS/label coursePrerequisites "coursePrerequisites")
  :AXIOMS (domainIncludes coursePrerequisites Course))

(DEFRELATION contentType (?X ?Y)
  :DOCUMENTATION "The supported content type(s) for an EntryPoint response."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes contentType EntryPoint)
  :AXIOMS (/XMLNS/RDFS/label contentType "contentType")
  :AXIOMS (rangeIncludes contentType Text))

(DEFCONCEPT SkiResort (?X)
  :DOCUMENTATION "A ski resort."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label SkiResort "SkiResort"))

(DEFRELATION datePosted (?X ?Y)
  :DOCUMENTATION "Publication date for the job posting."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label datePosted "datePosted")
  :AXIOMS (rangeIncludes datePosted Date)
  :AXIOMS (domainIncludes datePosted JobPosting))

(DEFRELATION disambiguatingDescription (?X ?Y)
  :DOCUMENTATION "A sub property of description. A short description of the item used to disambiguate from other, similar items. Information from other properties (in particular, name) may be necessary for the description to be useful for disambiguation."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF description
  :AXIOMS (/XMLNS/RDFS/label disambiguatingDescription "disambiguatingDescription")
  :AXIOMS (rangeIncludes disambiguatingDescription Text)
  :AXIOMS (domainIncludes disambiguatingDescription Thing))

(DEFRELATION description (?X ?Y)
  :DOCUMENTATION "A description of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes description Text)
  :AXIOMS (domainIncludes description Thing)
  :AXIOMS (/XMLNS/OWL/equivalentProperty description /XMLNS/DC/description)
  :AXIOMS (/XMLNS/RDFS/label description "description"))

(DEFRELATION encodings (?X ?Y)
  :DOCUMENTATION "A media object that encodes this CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (supersededBy encodings encoding)
  :AXIOMS (rangeIncludes encodings MediaObject)
  :AXIOMS (/XMLNS/RDFS/label encodings "encodings")
  :AXIOMS (domainIncludes encodings CreativeWork))

(DEFRELATION encoding (?X ?Y)
  :DOCUMENTATION "A media object that encodes this CreativeWork. This property is a synonym for associatedMedia."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label encoding "encoding")
  :AXIOMS (rangeIncludes encoding MediaObject)
  :AXIOMS (domainIncludes encoding CreativeWork))

(DEFCONCEPT ProfessionalService (?X)
  :DOCUMENTATION "Original definition: \"provider of professional services.\"</p>

<p>The general <a class=\"localLink\" href=\"http://schema.org/ProfessionalService\">ProfessionalService</a> type for local businesses was deprecated due to confusion with <a class=\"localLink\" href=\"http://schema.org/Service\">Service</a>. For reference, the types that it included were: <a class=\"localLink\" href=\"http://schema.org/Dentist\">Dentist</a>,
        <a class=\"localLink\" href=\"http://schema.org/AccountingService\">AccountingService</a>, <a class=\"localLink\" href=\"http://schema.org/Attorney\">Attorney</a>, <a class=\"localLink\" href=\"http://schema.org/Notary\">Notary</a>, as well as types for several kinds of <a class=\"localLink\" href=\"http://schema.org/HomeAndConstructionBusiness\">HomeAndConstructionBusiness</a>: <a class=\"localLink\" href=\"http://schema.org/Electrician\">Electrician</a>, <a class=\"localLink\" href=\"http://schema.org/GeneralContractor\">GeneralContractor</a>,
        <a class=\"localLink\" href=\"http://schema.org/HousePainter\">HousePainter</a>, <a class=\"localLink\" href=\"http://schema.org/Locksmith\">Locksmith</a>, <a class=\"localLink\" href=\"http://schema.org/Plumber\">Plumber</a>, <a class=\"localLink\" href=\"http://schema.org/RoofingContractor\">RoofingContractor</a>. <a class=\"localLink\" href=\"http://schema.org/LegalService\">LegalService</a> was introduced as a more inclusive supertype of <a class=\"localLink\" href=\"http://schema.org/Attorney\">Attorney</a>."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label ProfessionalService "ProfessionalService"))

(DEFRELATION vatID (?X ?Y)
  :DOCUMENTATION "The Value-added Tax ID of the organization or person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label vatID "vatID")
  :AXIOMS (/XMLNS/DC/source vatID |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes vatID Person)
  :AXIOMS (domainIncludes vatID Organization)
  :AXIOMS (rangeIncludes vatID Text))

(DEFRELATION albumProductionType (?X ?Y)
  :DOCUMENTATION "Classification of the album by it's type of content: soundtrack, live album, studio album, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes albumProductionType MusicAlbumProductionType)
  :AXIOMS (/XMLNS/DC/source albumProductionType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (domainIncludes albumProductionType MusicAlbum)
  :AXIOMS (/XMLNS/RDFS/label albumProductionType "albumProductionType"))

(DEFRELATION postalCode (?X ?Y)
  :DOCUMENTATION "The postal code. For example, 94043."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes postalCode Text)
  :AXIOMS (domainIncludes postalCode PostalAddress)
  :AXIOMS (domainIncludes postalCode GeoShape)
  :AXIOMS (domainIncludes postalCode GeoCoordinates)
  :AXIOMS (/XMLNS/RDFS/label postalCode "postalCode"))

(DEFRELATION jobLocation (?X ?Y)
  :DOCUMENTATION "A (typically single) geographic location associated with the job position."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes jobLocation JobPosting)
  :AXIOMS (rangeIncludes jobLocation Place)
  :AXIOMS (/XMLNS/RDFS/label jobLocation "jobLocation"))

(DEFRELATION issueNumber (?X ?Y)
  :DOCUMENTATION "Identifies the issue of publication; for example, \"iii\" or \"2\"."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF position
  :AXIOMS (rangeIncludes issueNumber Integer)
  :AXIOMS (/XMLNS/DC/source issueNumber |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/OWL/equivalentProperty issueNumber |http://purl.org/ontology/bibo/issue|)
  :AXIOMS (/XMLNS/RDFS/label issueNumber "issueNumber")
  :AXIOMS (domainIncludes issueNumber PublicationIssue)
  :AXIOMS (rangeIncludes issueNumber Text))

(DEFCONCEPT Bakery (?X)
  :DOCUMENTATION "A bakery."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label Bakery "Bakery"))

(DEFRELATION valueMaxLength (?X ?Y)
  :DOCUMENTATION "Specifies the allowed range for number of characters in a literal value."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes valueMaxLength Number)
  :AXIOMS (/XMLNS/RDFS/label valueMaxLength "valueMaxLength")
  :AXIOMS (domainIncludes valueMaxLength PropertyValueSpecification))

(DEFCONCEPT Embassy (?X)
  :DOCUMENTATION "An embassy."
  :SUBSET-OF GovernmentBuilding
  :AXIOMS (/XMLNS/RDFS/label Embassy "Embassy"))

(DEFRELATION worstRating (?X ?Y)
  :DOCUMENTATION "The lowest value allowed in this rating system. If worstRating is omitted, 1 is assumed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes worstRating Rating)
  :AXIOMS (/XMLNS/RDFS/label worstRating "worstRating")
  :AXIOMS (rangeIncludes worstRating Number)
  :AXIOMS (rangeIncludes worstRating Text))

(DEFCONCEPT EmailMessage (?X)
  :DOCUMENTATION "An email message."
  :SUBSET-OF Message
  :AXIOMS (/XMLNS/RDFS/label EmailMessage "EmailMessage"))

(DEFCONCEPT PoliceStation (?X)
  :DOCUMENTATION "A police station."
  :SUBSET-OF CivicStructure
  :SUBSET-OF EmergencyService
  :AXIOMS (/XMLNS/RDFS/label PoliceStation "PoliceStation"))

(DEFRELATION yearlyRevenue (?X ?Y)
  :DOCUMENTATION "The size of the business in annual revenue."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes yearlyRevenue BusinessAudience)
  :AXIOMS (rangeIncludes yearlyRevenue QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label yearlyRevenue "yearlyRevenue"))

(DEFCONCEPT Preschool (?X)
  :DOCUMENTATION "A preschool."
  :SUBSET-OF EducationalOrganization
  :AXIOMS (/XMLNS/RDFS/label Preschool "Preschool"))

(DEFCONCEPT DepartmentStore (?X)
  :DOCUMENTATION "A department store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label DepartmentStore "DepartmentStore"))

(DEFCONCEPT Brewery (?X)
  :DOCUMENTATION "Brewery."
  :SUBSET-OF FoodEstablishment
  :AXIOMS (/XMLNS/RDFS/label Brewery "Brewery"))

(DEFRELATION surface (?X ?Y)
  :DOCUMENTATION "A material used as a surface in some artwork, e.g. Canvas, Paper, Wood, Board, etc."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF material
  :AXIOMS (rangeIncludes surface Text)
  :AXIOMS (rangeIncludes surface URL)
  :AXIOMS (/XMLNS/RDFS/label surface "surface")
  :AXIOMS (supersededBy surface artworkSurface)
  :AXIOMS (domainIncludes surface VisualArtwork))

(DEFRELATION artworkSurface (?X ?Y)
  :DOCUMENTATION "The supporting materials for the artwork, e.g. Canvas, Paper, Wood, Board, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes artworkSurface Text)
  :AXIOMS (domainIncludes artworkSurface VisualArtwork)
  :AXIOMS (/XMLNS/RDFS/label artworkSurface "artworkSurface")
  :AXIOMS (rangeIncludes artworkSurface URL))

(DEFRELATION worksFor (?X ?Y)
  :DOCUMENTATION "Organizations that the person works for."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label worksFor "worksFor")
  :AXIOMS (domainIncludes worksFor Person)
  :AXIOMS (rangeIncludes worksFor Organization))

(DEFRELATION numberOfPages (?X ?Y)
  :DOCUMENTATION "The number of pages in the book."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numberOfPages "numberOfPages")
  :AXIOMS (rangeIncludes numberOfPages Integer)
  :AXIOMS (domainIncludes numberOfPages Book))

(DEFRELATION subtitleLanguage (?X ?Y)
  :DOCUMENTATION "Languages in which subtitles/captions are available, in <a href=\"http://tools.ietf.org/html/bcp47\">IETF BCP 47 standard format</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes subtitleLanguage TVEpisode)
  :AXIOMS (rangeIncludes subtitleLanguage Text)
  :AXIOMS (/XMLNS/RDFS/label subtitleLanguage "subtitleLanguage")
  :AXIOMS (domainIncludes subtitleLanguage Movie)
  :AXIOMS (domainIncludes subtitleLanguage ScreeningEvent)
  :AXIOMS (rangeIncludes subtitleLanguage Language))

(DEFRELATION responsibilities (?X ?Y)
  :DOCUMENTATION "Responsibilities associated with this role."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes responsibilities JobPosting)
  :AXIOMS (rangeIncludes responsibilities Text)
  :AXIOMS (/XMLNS/RDFS/label responsibilities "responsibilities"))

(DEFRELATION ineligibleRegion (?X ?Y)
  :DOCUMENTATION "The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is not valid, e.g. a region where the transaction is not allowed.</p>

<p>See also <a class=\"localLink\" href=\"http://schema.org/eligibleRegion\">eligibleRegion</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes ineligibleRegion Place)
  :AXIOMS (rangeIncludes ineligibleRegion GeoShape)
  :AXIOMS (domainIncludes ineligibleRegion Demand)
  :AXIOMS (domainIncludes ineligibleRegion DeliveryChargeSpecification)
  :AXIOMS (/XMLNS/RDFS/label ineligibleRegion "ineligibleRegion")
  :AXIOMS (domainIncludes ineligibleRegion Offer)
  :AXIOMS (rangeIncludes ineligibleRegion Text))

(DEFCONCEPT UserDownloads (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (/XMLNS/RDFS/label UserDownloads "UserDownloads")
  :AXIOMS (supersededBy UserDownloads InteractionCounter))

(ASSERT
 (AND (RestrictedDiet LowCalorieDiet) (/XMLNS/RDFS/label LowCalorieDiet "LowCalorieDiet")
  (DOCUMENTATION LowCalorieDiet "A diet focused on reduced calorie intake.")))

(DEFCONCEPT LegislativeBuilding (?X)
  :DOCUMENTATION "A legislative building&#x2014;for example, the state capitol."
  :SUBSET-OF GovernmentBuilding
  :AXIOMS (/XMLNS/RDFS/label LegislativeBuilding "LegislativeBuilding"))

(DEFCONCEPT EventVenue (?X)
  :DOCUMENTATION "An event venue."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label EventVenue "EventVenue"))

(ASSERT
 (AND (DayOfWeek Sunday) (DOCUMENTATION Sunday "The day of the week between Saturday and Monday.") (/XMLNS/RDFS/label Sunday "Sunday")
  (sameAs Sunday |http://www.wikidata.org/entity/Q132|)))

(DEFRELATION isVariantOf (?X ?Y)
  :DOCUMENTATION "A pointer to a base product from which this product is a variant. It is safe to infer that the variant inherits all product features from the base model, unless defined locally. This is not transitive."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes isVariantOf ProductModel)
  :AXIOMS (/XMLNS/DC/source isVariantOf |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes isVariantOf ProductModel)
  :AXIOMS (/XMLNS/RDFS/label isVariantOf "isVariantOf"))

(DEFRELATION contactType (?X ?Y)
  :DOCUMENTATION "A person or organization can have different contact points, for different purposes. For example, a sales contact point, a PR contact point and so on. This property is used to specify the kind of contact point."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contactType "contactType")
  :AXIOMS (rangeIncludes contactType Text)
  :AXIOMS (domainIncludes contactType ContactPoint))

(DEFRELATION maps (?X ?Y)
  :DOCUMENTATION "A URL to a map of the place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes maps Place)
  :AXIOMS (/XMLNS/RDFS/label maps "maps")
  :AXIOMS (rangeIncludes maps URL)
  :AXIOMS (supersededBy maps hasMap))

(DEFRELATION suggestedMinAge (?X ?Y)
  :DOCUMENTATION "Minimal age recommended for viewing content."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label suggestedMinAge "suggestedMinAge")
  :AXIOMS (rangeIncludes suggestedMinAge Number)
  :AXIOMS (domainIncludes suggestedMinAge PeopleAudience))

(ASSERT
 (AND (ItemListOrderType ItemListOrderDescending) (DOCUMENTATION ItemListOrderDescending "An ItemList ordered with higher values listed first.")
  (/XMLNS/RDFS/label ItemListOrderDescending "ItemListOrderDescending")))

(DEFRELATION branchCode (?X ?Y)
  :DOCUMENTATION "A short textual code (also called \"store code\") that uniquely identifies a place of business. The code is typically assigned by the parentOrganization and used in structured URLs.</p>

<p>For example, in the URL http://www.starbucks.co.uk/store-locator/etc/detail/3047 the code \"3047\" is a branchCode for a particular branch."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label branchCode "branchCode")
  :AXIOMS (domainIncludes branchCode Place)
  :AXIOMS (rangeIncludes branchCode Text))

(DEFCONCEPT ExhibitionEvent (?X)
  :DOCUMENTATION "Event type: Exhibition event, e.g. at a museum, library, archive, tradeshow, ..."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label ExhibitionEvent "ExhibitionEvent"))

(DEFRELATION employees (?X ?Y)
  :DOCUMENTATION "People working for this organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label employees "employees")
  :AXIOMS (rangeIncludes employees Person)
  :AXIOMS (supersededBy employees employee)
  :AXIOMS (domainIncludes employees Organization))

(DEFRELATION employee (?X ?Y)
  :DOCUMENTATION "Someone working for this organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label employee "employee")
  :AXIOMS (rangeIncludes employee Person)
  :AXIOMS (domainIncludes employee Organization))

(DEFRELATION customer (?X ?Y)
  :DOCUMENTATION "Party placing the order or paying the invoice."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes customer Organization)
  :AXIOMS (rangeIncludes customer Person)
  :AXIOMS (domainIncludes customer Order)
  :AXIOMS (domainIncludes customer Invoice)
  :AXIOMS (/XMLNS/RDFS/label customer "customer"))

(DEFRELATION countriesSupported (?X ?Y)
  :DOCUMENTATION "Countries for which the application is supported. You can also provide the two-letter ISO 3166-1 alpha-2 country code."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label countriesSupported "countriesSupported")
  :AXIOMS (domainIncludes countriesSupported SoftwareApplication)
  :AXIOMS (rangeIncludes countriesSupported Text))

(DEFCONCEPT DeactivateAction (?X)
  :DOCUMENTATION "The act of stopping or deactivating a device or application (e.g. stopping a timer or turning off a flashlight)."
  :SUBSET-OF ControlAction
  :AXIOMS (/XMLNS/RDFS/label DeactivateAction "DeactivateAction"))

(DEFRELATION vehicleEngine (?X ?Y)
  :DOCUMENTATION "Information about the engine or engines of the vehicle."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes vehicleEngine Vehicle)
  :AXIOMS (/XMLNS/DC/source vehicleEngine |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes vehicleEngine EngineSpecification)
  :AXIOMS (/XMLNS/RDFS/label vehicleEngine "vehicleEngine"))

(DEFRELATION runtime (?X ?Y)
  :DOCUMENTATION "Runtime platform or script interpreter dependencies (Example - Java v1, Python2.3, .Net Framework 3.0)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label runtime "runtime")
  :AXIOMS (domainIncludes runtime SoftwareSourceCode)
  :AXIOMS (rangeIncludes runtime Text)
  :AXIOMS (supersededBy runtime runtimePlatform))

(DEFRELATION runtimePlatform (?X ?Y)
  :DOCUMENTATION "Runtime platform or script interpreter dependencies (Example - Java v1, Python2.3, .Net Framework 3.0)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label runtimePlatform "runtimePlatform")
  :AXIOMS (domainIncludes runtimePlatform SoftwareSourceCode)
  :AXIOMS (rangeIncludes runtimePlatform Text))

(DEFRELATION fatContent (?X ?Y)
  :DOCUMENTATION "The number of grams of fat."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label fatContent "fatContent")
  :AXIOMS (domainIncludes fatContent NutritionInformation)
  :AXIOMS (rangeIncludes fatContent Mass))

(DEFCONCEPT TouristInformationCenter (?X)
  :DOCUMENTATION "A tourist information center."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label TouristInformationCenter "TouristInformationCenter"))

(DEFCONCEPT Table (?X)
  :DOCUMENTATION "A table on a Web page."
  :SUBSET-OF WebPageElement
  :AXIOMS (/XMLNS/RDFS/label Table "Table"))

(DEFRELATION arrivalBusStop (?X ?Y)
  :DOCUMENTATION "The stop or station from which the bus arrives."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label arrivalBusStop "arrivalBusStop")
  :AXIOMS (domainIncludes arrivalBusStop BusTrip)
  :AXIOMS (rangeIncludes arrivalBusStop BusStation)
  :AXIOMS (rangeIncludes arrivalBusStop BusStop))

(ASSERT
 (AND (MusicReleaseFormatType DVDFormat) (DOCUMENTATION DVDFormat "DVDFormat.") (/XMLNS/RDFS/label DVDFormat "DVDFormat")
  (/XMLNS/DC/source DVDFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION additionalType (?X ?Y)
  :DOCUMENTATION "An additional type for the item, typically used for adding more specific types from external vocabularies in microdata syntax. This is a relationship between something and a class that the thing is in. In RDFa syntax, it is better to use the native RDFa syntax - the 'typeof' attribute - for multiple types. Schema.org tools may have only weaker understanding of extra types, in particular those defined externally."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF INSTANCE-OF
  :AXIOMS (domainIncludes additionalType Thing)
  :AXIOMS (rangeIncludes additionalType URL)
  :AXIOMS (/XMLNS/RDFS/label additionalType "additionalType"))

(DEFRELATION numberOfForwardGears (?X ?Y)
  :DOCUMENTATION "The total number of forward gears available for the transmission system of the vehicle.</p>

<p>Typical unit code(s): C62"
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numberOfForwardGears "numberOfForwardGears")
  :AXIOMS (rangeIncludes numberOfForwardGears Number)
  :AXIOMS (domainIncludes numberOfForwardGears Vehicle)
  :AXIOMS (/XMLNS/DC/source numberOfForwardGears |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes numberOfForwardGears QuantitativeValue))

(DEFRELATION itemCondition (?X ?Y)
  :DOCUMENTATION "A predefined value from OfferItemCondition or a textual description of the condition of the product or service, or the products or services included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label itemCondition "itemCondition")
  :AXIOMS (domainIncludes itemCondition Offer)
  :AXIOMS (domainIncludes itemCondition Demand)
  :AXIOMS (rangeIncludes itemCondition OfferItemCondition)
  :AXIOMS (domainIncludes itemCondition Product))

(DEFRELATION browserRequirements (?X ?Y)
  :DOCUMENTATION "Specifies browser requirements in human-readable text. For example, 'requires HTML5 support'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes browserRequirements Text)
  :AXIOMS (domainIncludes browserRequirements WebApplication)
  :AXIOMS (/XMLNS/RDFS/label browserRequirements "browserRequirements"))

(DEFRELATION hasDeliveryMethod (?X ?Y)
  :DOCUMENTATION "Method used for delivery or shipping."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes hasDeliveryMethod ParcelDelivery)
  :AXIOMS (domainIncludes hasDeliveryMethod DeliveryEvent)
  :AXIOMS (rangeIncludes hasDeliveryMethod DeliveryMethod)
  :AXIOMS (/XMLNS/RDFS/label hasDeliveryMethod "hasDeliveryMethod"))

(ASSERT
 (AND (GamePlayMode CoOp) (DOCUMENTATION CoOp "Play mode: CoOp. Co-operative games, where you play on the same team with friends.")
  (/XMLNS/RDFS/label CoOp "CoOp")))

(DEFRELATION sender (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The participant who is at the sending end of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (rangeIncludes sender Audience)
  :AXIOMS (rangeIncludes sender Person)
  :AXIOMS (rangeIncludes sender Organization)
  :AXIOMS (domainIncludes sender ReceiveAction)
  :AXIOMS (/XMLNS/RDFS/label sender "sender")
  :AXIOMS (domainIncludes sender Message))

(DEFCONCEPT Conversation (?X)
  :DOCUMENTATION "One or more messages between organizations or people on a particular topic. Individual messages can be linked to the conversation with isPartOf or hasPart properties."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Conversation "Conversation"))

(DEFRELATION dateCreated (?X ?Y)
  :DOCUMENTATION "The date on which the CreativeWork was created or the item was added to a DataFeed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateCreated DataFeedItem)
  :AXIOMS (rangeIncludes dateCreated DateTime)
  :AXIOMS (/XMLNS/RDFS/label dateCreated "dateCreated")
  :AXIOMS (domainIncludes dateCreated CreativeWork)
  :AXIOMS (rangeIncludes dateCreated Date))

(DEFRELATION availableDeliveryMethod (?X ?Y)
  :DOCUMENTATION "The delivery method(s) available for this offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availableDeliveryMethod Offer)
  :AXIOMS (/XMLNS/DC/source availableDeliveryMethod |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes availableDeliveryMethod Demand)
  :AXIOMS (/XMLNS/RDFS/label availableDeliveryMethod "availableDeliveryMethod")
  :AXIOMS (rangeIncludes availableDeliveryMethod DeliveryMethod))

(DEFRELATION sportsActivityLocation (?X ?Y)
  :DOCUMENTATION "A sub property of location. The sports activity location where this action occurred."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (/XMLNS/RDFS/label sportsActivityLocation "sportsActivityLocation")
  :AXIOMS (rangeIncludes sportsActivityLocation SportsActivityLocation)
  :AXIOMS (domainIncludes sportsActivityLocation ExerciseAction))

(DEFRELATION targetProduct (?X ?Y)
  :DOCUMENTATION "Target Operating System / Product to which the code applies.  If applies to several versions, just the product name can be used."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label targetProduct "targetProduct")
  :AXIOMS (domainIncludes targetProduct SoftwareSourceCode)
  :AXIOMS (rangeIncludes targetProduct SoftwareApplication))

(DEFCONCEPT BookStore (?X)
  :DOCUMENTATION "A bookstore."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label BookStore "BookStore"))

(DEFCONCEPT GeneralContractor (?X)
  :DOCUMENTATION "A general contractor."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label GeneralContractor "GeneralContractor"))

(DEFRELATION telephone (?X ?Y)
  :DOCUMENTATION "The telephone number."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label telephone "telephone")
  :AXIOMS (domainIncludes telephone Person)
  :AXIOMS (domainIncludes telephone Organization)
  :AXIOMS (rangeIncludes telephone Text)
  :AXIOMS (domainIncludes telephone ContactPoint)
  :AXIOMS (domainIncludes telephone Place))

(DEFRELATION educationalRole (?X ?Y)
  :DOCUMENTATION "An educationalRole of an EducationalAudience."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes educationalRole EducationalAudience)
  :AXIOMS (/XMLNS/RDFS/label educationalRole "educationalRole")
  :AXIOMS (rangeIncludes educationalRole Text))

(DEFRELATION petsAllowed (?X ?Y)
  :DOCUMENTATION "Indicates whether pets are allowed to enter the accommodation or lodging business. More detailed information can be put in a text value."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes petsAllowed LodgingBusiness)
  :AXIOMS (domainIncludes petsAllowed Accommodation)
  :AXIOMS (/XMLNS/DC/source petsAllowed |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label petsAllowed "petsAllowed")
  :AXIOMS (rangeIncludes petsAllowed Text)
  :AXIOMS (rangeIncludes petsAllowed Boolean))

(DEFRELATION eventStatus (?X ?Y)
  :DOCUMENTATION "An eventStatus of an event represents its status; particularly useful when an event is cancelled or rescheduled."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes eventStatus EventStatusType)
  :AXIOMS (/XMLNS/RDFS/label eventStatus "eventStatus")
  :AXIOMS (domainIncludes eventStatus Event))

(DEFRELATION contentRating (?X ?Y)
  :DOCUMENTATION "Official rating of a piece of content&#x2014;for example,'MPAA PG-13'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contentRating "contentRating")
  :AXIOMS (rangeIncludes contentRating Text)
  :AXIOMS (domainIncludes contentRating CreativeWork))

(DEFCONCEPT ApartmentComplex (?X)
  :DOCUMENTATION "Residence type: Apartment complex."
  :SUBSET-OF Residence
  :AXIOMS (/XMLNS/RDFS/label ApartmentComplex "ApartmentComplex"))

(DEFRELATION proficiencyLevel (?X ?Y)
  :DOCUMENTATION "Proficiency needed for this content; expected values: 'Beginner', 'Expert'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes proficiencyLevel TechArticle)
  :AXIOMS (/XMLNS/RDFS/label proficiencyLevel "proficiencyLevel")
  :AXIOMS (rangeIncludes proficiencyLevel Text))

(DEFRELATION orderDate (?X ?Y)
  :DOCUMENTATION "Date order was placed."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes orderDate Order)
  :AXIOMS (/XMLNS/RDFS/label orderDate "orderDate")
  :AXIOMS (rangeIncludes orderDate DateTime))

(DEFRELATION departureAirport (?X ?Y)
  :DOCUMENTATION "The airport where the flight originates."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label departureAirport "departureAirport")
  :AXIOMS (domainIncludes departureAirport Flight)
  :AXIOMS (rangeIncludes departureAirport Airport))

(ASSERT
 (AND (SteeringPositionValue RightHandDriving)
  (/XMLNS/DC/source RightHandDriving |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (DOCUMENTATION RightHandDriving "The steering position is on the right side of the vehicle (viewed from the main direction of driving).")
  (/XMLNS/RDFS/label RightHandDriving "RightHandDriving")))

(DEFCONCEPT CheckAction (?X)
  :DOCUMENTATION "An agent inspects/determines/investigates/inquire or examine an object's accuracy/quality/condition or state."
  :SUBSET-OF FindAction
  :AXIOMS (/XMLNS/RDFS/label CheckAction "CheckAction"))

(DEFCONCEPT AdultEntertainment (?X)
  :DOCUMENTATION "An adult entertainment establishment."
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label AdultEntertainment "AdultEntertainment"))

(DEFRELATION additionalName (?X ?Y)
  :DOCUMENTATION "An additional name for a Person, can be used for a middle name."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes additionalName Text)
  :AXIOMS (/XMLNS/RDFS/label additionalName "additionalName")
  :AXIOMS (domainIncludes additionalName Person))

(DEFRELATION valuePattern (?X ?Y)
  :DOCUMENTATION "Specifies a regular expression for testing literal values according to the HTML spec."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes valuePattern PropertyValueSpecification)
  :AXIOMS (/XMLNS/RDFS/label valuePattern "valuePattern")
  :AXIOMS (rangeIncludes valuePattern Text))

(DEFRELATION target (?X ?Y)
  :DOCUMENTATION "Indicates a target EntryPoint for an Action."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label target "target")
  :AXIOMS (rangeIncludes target EntryPoint)
  :AXIOMS (domainIncludes target Action))

(DEFRELATION dataFeedElement (?X ?Y)
  :DOCUMENTATION "An item within in a data feed. Data feeds may have many elements."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes dataFeedElement DataFeedItem)
  :AXIOMS (/XMLNS/RDFS/label dataFeedElement "dataFeedElement")
  :AXIOMS (rangeIncludes dataFeedElement Text)
  :AXIOMS (rangeIncludes dataFeedElement Thing)
  :AXIOMS (domainIncludes dataFeedElement DataFeed))

(DEFRELATION audio (?X ?Y)
  :DOCUMENTATION "An embedded audio object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label audio "audio")
  :AXIOMS (rangeIncludes audio AudioObject)
  :AXIOMS (domainIncludes audio CreativeWork))

(DEFRELATION actionPlatform (?X ?Y)
  :DOCUMENTATION "The high level platform(s) where the Action can be performed for the given URL. To specify a specific application or operating system instance, use actionApplication."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes actionPlatform EntryPoint)
  :AXIOMS (/XMLNS/RDFS/label actionPlatform "actionPlatform")
  :AXIOMS (rangeIncludes actionPlatform URL)
  :AXIOMS (rangeIncludes actionPlatform Text))

(ASSERT
 (AND (OfferItemCondition NewCondition) (/XMLNS/RDFS/label NewCondition "NewCondition") (DOCUMENTATION NewCondition "Indicates that the item is new.")))

(DEFRELATION loanTerm (?X ?Y)
  :DOCUMENTATION "The duration of the loan or credit agreement."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF duration
  :AXIOMS (domainIncludes loanTerm LoanOrCredit)
  :AXIOMS (rangeIncludes loanTerm QuantitativeValue)
  :AXIOMS (/XMLNS/DC/source loanTerm |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label loanTerm "loanTerm"))

(DEFRELATION duration (?X ?Y)
  :DOCUMENTATION "The duration of the item (movie, audio recording, event, etc.) in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 date format</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes duration Movie)
  :AXIOMS (/XMLNS/RDFS/label duration "duration")
  :AXIOMS (domainIncludes duration Event)
  :AXIOMS (domainIncludes duration MusicRelease)
  :AXIOMS (domainIncludes duration MusicRecording)
  :AXIOMS (domainIncludes duration MediaObject)
  :AXIOMS (rangeIncludes duration Duration))

(DEFRELATION arrivalTime (?X ?Y)
  :DOCUMENTATION "The expected arrival time."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes arrivalTime Flight)
  :AXIOMS (domainIncludes arrivalTime TrainTrip)
  :AXIOMS (domainIncludes arrivalTime BusTrip)
  :AXIOMS (rangeIncludes arrivalTime DateTime)
  :AXIOMS (/XMLNS/RDFS/label arrivalTime "arrivalTime"))

(DEFRELATION hasCourseInstance (?X ?Y)
  :DOCUMENTATION "An offering of the course at a specific time and place or through specific media or mode of study or to a specific section of students."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes hasCourseInstance Course)
  :AXIOMS (rangeIncludes hasCourseInstance CourseInstance)
  :AXIOMS (/XMLNS/RDFS/label hasCourseInstance "hasCourseInstance"))

(DEFCONCEPT BefriendAction (?X)
  :DOCUMENTATION "<p>The act of forming a personal connection with someone (object) mutually/bidirectionally/symmetrically.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/FollowAction\">FollowAction</a>: Unlike FollowAction, BefriendAction implies that the connection is reciprocal.</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label BefriendAction "BefriendAction"))

(DEFRELATION discount (?X ?Y)
  :DOCUMENTATION "Any discount applied (to an Order)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label discount "discount")
  :AXIOMS (rangeIncludes discount Number)
  :AXIOMS (rangeIncludes discount Text)
  :AXIOMS (domainIncludes discount Order))

(ASSERT
 (AND (ItemAvailability PreSale) (/XMLNS/RDFS/label PreSale "PreSale")
  (DOCUMENTATION PreSale "Indicates that the item is available for ordering and delivery before general availability.")))

(DEFRELATION foundingDate (?X ?Y)
  :DOCUMENTATION "The date that this organization was founded."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes foundingDate Organization)
  :AXIOMS (rangeIncludes foundingDate Date)
  :AXIOMS (/XMLNS/RDFS/label foundingDate "foundingDate"))

(DEFRELATION associatedMedia (?X ?Y)
  :DOCUMENTATION "A media object that encodes this CreativeWork. This property is a synonym for encoding."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes associatedMedia CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label associatedMedia "associatedMedia")
  :AXIOMS (rangeIncludes associatedMedia MediaObject))

(DEFCONCEPT BookmarkAction (?X)
  :DOCUMENTATION "An agent bookmarks/flags/labels/tags/marks an object."
  :SUBSET-OF OrganizeAction
  :AXIOMS (/XMLNS/RDFS/label BookmarkAction "BookmarkAction"))

(DEFRELATION videoQuality (?X ?Y)
  :DOCUMENTATION "The quality of the video."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes videoQuality Text)
  :AXIOMS (/XMLNS/RDFS/label videoQuality "videoQuality")
  :AXIOMS (domainIncludes videoQuality VideoObject))

(DEFRELATION accessibilityAPI (?X ?Y)
  :DOCUMENTATION "Indicates that the resource is compatible with the referenced accessibility API (<a href=\"http://www.w3.org/wiki/WebSchemas/Accessibility\">WebSchemas wiki lists possible values</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes accessibilityAPI CreativeWork)
  :AXIOMS (rangeIncludes accessibilityAPI Text)
  :AXIOMS (/XMLNS/RDFS/label accessibilityAPI "accessibilityAPI"))

(DEFRELATION arrivalStation (?X ?Y)
  :DOCUMENTATION "The station where the train trip ends."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes arrivalStation TrainStation)
  :AXIOMS (/XMLNS/RDFS/label arrivalStation "arrivalStation")
  :AXIOMS (domainIncludes arrivalStation TrainTrip))

(DEFRELATION legalName (?X ?Y)
  :DOCUMENTATION "The official name of the organization, e.g. the registered company name."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes legalName Organization)
  :AXIOMS (rangeIncludes legalName Text)
  :AXIOMS (/XMLNS/RDFS/label legalName "legalName")
  :AXIOMS (/XMLNS/DC/source legalName |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION offerCount (?X ?Y)
  :DOCUMENTATION "The number of offers for the product."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes offerCount Integer)
  :AXIOMS (/XMLNS/RDFS/label offerCount "offerCount")
  :AXIOMS (domainIncludes offerCount AggregateOffer))

(DEFRELATION ownedFrom (?X ?Y)
  :DOCUMENTATION "The date and time of obtaining the product."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source ownedFrom |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes ownedFrom DateTime)
  :AXIOMS (domainIncludes ownedFrom OwnershipInfo)
  :AXIOMS (/XMLNS/RDFS/label ownedFrom "ownedFrom"))

(ASSERT
 (AND (PaymentStatusType PaymentComplete) (/XMLNS/RDFS/label PaymentComplete "PaymentComplete")
  (DOCUMENTATION PaymentComplete "The payment has been received and processed.")))

(DEFRELATION availableFrom (?X ?Y)
  :DOCUMENTATION "When the item is available for pickup from the store, locker, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availableFrom DeliveryEvent)
  :AXIOMS (/XMLNS/RDFS/label availableFrom "availableFrom")
  :AXIOMS (rangeIncludes availableFrom DateTime))

(ASSERT
 (AND (DriveWheelConfigurationValue FourWheelDriveConfiguration)
  (/XMLNS/DC/source FourWheelDriveConfiguration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (/XMLNS/RDFS/label FourWheelDriveConfiguration "FourWheelDriveConfiguration")
  (DOCUMENTATION FourWheelDriveConfiguration
   "Four-wheel drive is a transmission layout where the engine primarily drives two wheels with a part-time four-wheel drive capability.")))

(DEFCONCEPT Bridge (?X)
  :DOCUMENTATION "A bridge."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Bridge "Bridge"))

(DEFRELATION dateVehicleFirstRegistered (?X ?Y)
  :DOCUMENTATION "The date of the first registration of the vehicle with the respective public authorities."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dateVehicleFirstRegistered Vehicle)
  :AXIOMS (/XMLNS/RDFS/label dateVehicleFirstRegistered "dateVehicleFirstRegistered")
  :AXIOMS (/XMLNS/DC/source dateVehicleFirstRegistered |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes dateVehicleFirstRegistered Date))

(DEFRELATION educationalAlignment (?X ?Y)
  :DOCUMENTATION "An alignment to an established educational framework."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label educationalAlignment "educationalAlignment")
  :AXIOMS (rangeIncludes educationalAlignment AlignmentObject)
  :AXIOMS (domainIncludes educationalAlignment CreativeWork))

(DEFRELATION commentTime (?X ?Y)
  :DOCUMENTATION "The time at which the UserComment was made."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes commentTime Date)
  :AXIOMS (/XMLNS/RDFS/label commentTime "commentTime")
  :AXIOMS (domainIncludes commentTime UserComments)
  :AXIOMS (rangeIncludes commentTime DateTime))

(DEFRELATION releaseNotes (?X ?Y)
  :DOCUMENTATION "Description of what changed in this version."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes releaseNotes SoftwareApplication)
  :AXIOMS (rangeIncludes releaseNotes Text)
  :AXIOMS (/XMLNS/RDFS/label releaseNotes "releaseNotes")
  :AXIOMS (rangeIncludes releaseNotes URL))

(DEFRELATION urlTemplate (?X ?Y)
  :DOCUMENTATION "An url template (RFC6570) that will be used to construct the target of the execution of the action."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label urlTemplate "urlTemplate")
  :AXIOMS (rangeIncludes urlTemplate Text)
  :AXIOMS (domainIncludes urlTemplate EntryPoint))

(ASSERT (AND (MapCategoryType ParkingMap) (/XMLNS/RDFS/label ParkingMap "ParkingMap") (DOCUMENTATION ParkingMap "A parking map.")))

(DEFRELATION vehicleSeatingCapacity (?X ?Y)
  :DOCUMENTATION "The number of passengers that can be seated in the vehicle, both in terms of the physical space available, and in terms of limitations set by law.</p>

<p>Typical unit code(s): C62 for persons."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes vehicleSeatingCapacity Number)
  :AXIOMS (domainIncludes vehicleSeatingCapacity Vehicle)
  :AXIOMS (/XMLNS/DC/source vehicleSeatingCapacity |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes vehicleSeatingCapacity QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label vehicleSeatingCapacity "vehicleSeatingCapacity"))

(DEFRELATION validFrom (?X ?Y)
  :DOCUMENTATION "The date when the item becomes valid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes validFrom Demand)
  :AXIOMS (rangeIncludes validFrom DateTime)
  :AXIOMS (/XMLNS/RDFS/label validFrom "validFrom")
  :AXIOMS (domainIncludes validFrom Permit)
  :AXIOMS (domainIncludes validFrom Offer)
  :AXIOMS (domainIncludes validFrom MonetaryAmount)
  :AXIOMS (/XMLNS/DC/source validFrom |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes validFrom PriceSpecification)
  :AXIOMS (domainIncludes validFrom OpeningHoursSpecification)
  :AXIOMS (domainIncludes validFrom LocationFeatureSpecification))

(ASSERT
 (AND (OrderStatus OrderPaymentDue) (DOCUMENTATION OrderPaymentDue "OrderStatus representing that payment is due on an order.")
  (/XMLNS/RDFS/label OrderPaymentDue "OrderPaymentDue")))

(DEFRELATION purchaseDate (?X ?Y)
  :DOCUMENTATION "The date the item e.g. vehicle was purchased by the current owner."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes purchaseDate Product)
  :AXIOMS (/XMLNS/RDFS/label purchaseDate "purchaseDate")
  :AXIOMS (/XMLNS/DC/source purchaseDate |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes purchaseDate Date)
  :AXIOMS (domainIncludes purchaseDate Vehicle))

(DEFCONCEPT CheckInAction (?X)
  :DOCUMENTATION "<p>The act of an agent communicating (service provider, social media, etc) their arrival by registering/confirming for a previously reserved service (e.g. flight check in) or at a place (e.g. hotel), possibly resulting in a result (boarding pass, etc).</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/CheckOutAction\">CheckOutAction</a>: The antonym of CheckInAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/ArriveAction\">ArriveAction</a>: Unlike ArriveAction, CheckInAction implies that the agent is informing/confirming the start of a previously reserved service.</li>
<li><a class=\"localLink\" href=\"http://schema.org/ConfirmAction\">ConfirmAction</a>: Unlike ConfirmAction, CheckInAction implies that the agent is informing/confirming the <em>start</em> of a previously reserved service rather than its validity/existence.</li>
</ul>
"
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label CheckInAction "CheckInAction"))

(DEFRELATION maxPrice (?X ?Y)
  :DOCUMENTATION "The highest price if the price is a range."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source maxPrice |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label maxPrice "maxPrice")
  :AXIOMS (domainIncludes maxPrice PriceSpecification)
  :AXIOMS (rangeIncludes maxPrice Number))

(DEFCONCEPT UserTweets (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (supersededBy UserTweets InteractionCounter)
  :AXIOMS (/XMLNS/RDFS/label UserTweets "UserTweets"))

(DEFCONCEPT SocialEvent (?X)
  :DOCUMENTATION "Event type: Social event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label SocialEvent "SocialEvent"))

(DEFCONCEPT CancelAction (?X)
  :DOCUMENTATION "<p>The act of asserting that a future event/action is no longer going to happen.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/ConfirmAction\">ConfirmAction</a>: The antonym of CancelAction.</li>
</ul>
"
  :SUBSET-OF PlanAction
  :AXIOMS (/XMLNS/RDFS/label CancelAction "CancelAction"))

(DEFRELATION cookTime (?X ?Y)
  :DOCUMENTATION "The time it takes to actually cook the dish, in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 duration format</a>."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label cookTime "cookTime")
  :AXIOMS (domainIncludes cookTime Recipe)
  :AXIOMS (rangeIncludes cookTime Duration))

(DEFRELATION breadcrumb (?X ?Y)
  :DOCUMENTATION "A set of links that can help a user understand and navigate a website hierarchy."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes breadcrumb BreadcrumbList)
  :AXIOMS (/XMLNS/RDFS/label breadcrumb "breadcrumb")
  :AXIOMS (rangeIncludes breadcrumb Text)
  :AXIOMS (domainIncludes breadcrumb WebPage))

(DEFCONCEPT Playground (?X)
  :DOCUMENTATION "A playground."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Playground "Playground"))

(ASSERT
 (AND (MusicAlbumProductionType StudioAlbum) (DOCUMENTATION StudioAlbum "StudioAlbum.") (/XMLNS/RDFS/label StudioAlbum "StudioAlbum")
  (/XMLNS/DC/source StudioAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION datePublished (?X ?Y)
  :DOCUMENTATION "Date of first broadcast/publication."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label datePublished "datePublished")
  :AXIOMS (rangeIncludes datePublished Date)
  :AXIOMS (domainIncludes datePublished CreativeWork))

(DEFRELATION busNumber (?X ?Y)
  :DOCUMENTATION "The unique identifier for the bus."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes busNumber BusTrip)
  :AXIOMS (/XMLNS/RDFS/label busNumber "busNumber")
  :AXIOMS (rangeIncludes busNumber Text))

(DEFRELATION priceType (?X ?Y)
  :DOCUMENTATION "A short text or acronym indicating multiple price specifications for the same offer, e.g. SRP for the suggested retail price or INVOICE for the invoice price, mostly used in the car industry."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes priceType Text)
  :AXIOMS (/XMLNS/DC/source priceType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes priceType UnitPriceSpecification)
  :AXIOMS (/XMLNS/RDFS/label priceType "priceType"))

(DEFRELATION occupationalCategory (?X ?Y)
  :DOCUMENTATION "Category or categories describing the job. Use BLS O*NET-SOC taxonomy: http://www.onetcenter.org/taxonomy.html. Ideally includes textual label and formal code, with the property repeated for each applicable value."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes occupationalCategory JobPosting)
  :AXIOMS (rangeIncludes occupationalCategory Text)
  :AXIOMS (/XMLNS/RDFS/label occupationalCategory "occupationalCategory"))

(DEFRELATION targetName (?X ?Y)
  :DOCUMENTATION "The name of a node in an established educational framework."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label targetName "targetName")
  :AXIOMS (domainIncludes targetName AlignmentObject)
  :AXIOMS (rangeIncludes targetName Text))

(DEFCONCEPT Aquarium (?X)
  :DOCUMENTATION "Aquarium."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Aquarium "Aquarium"))

(DEFRELATION representativeOfPage (?X ?Y)
  :DOCUMENTATION "Indicates whether this image is representative of the content of the page."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes representativeOfPage Boolean)
  :AXIOMS (/XMLNS/RDFS/label representativeOfPage "representativeOfPage")
  :AXIOMS (domainIncludes representativeOfPage ImageObject))

(DEFRELATION spatial (?X ?Y)
  :DOCUMENTATION "The range of spatial applicability of a dataset, e.g. for a dataset of New York weather, the state of New York."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes spatial Dataset)
  :AXIOMS (rangeIncludes spatial Place)
  :AXIOMS (/XMLNS/RDFS/label spatial "spatial")
  :AXIOMS (supersededBy spatial spatialCoverage)
  :AXIOMS (/XMLNS/OWL/equivalentProperty spatial /XMLNS/DC/spatial))

(DEFRELATION spatialCoverage (?X ?Y)
  :DOCUMENTATION "The spatialCoverage of a CreativeWork indicates the place(s) which are the focus of the content. It is a subproperty of
      contentLocation intended primarily for more technical and detailed materials. For example with a Dataset, it indicates
      areas that the dataset describes: a dataset of New York weather would have spatialCoverage which was the place: the state of New York."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF contentLocation
  :AXIOMS (/XMLNS/OWL/equivalentProperty spatialCoverage /XMLNS/DC/spatial)
  :AXIOMS (domainIncludes spatialCoverage CreativeWork)
  :AXIOMS (rangeIncludes spatialCoverage Place)
  :AXIOMS (/XMLNS/RDFS/label spatialCoverage "spatialCoverage"))

(DEFRELATION nutrition (?X ?Y)
  :DOCUMENTATION "Nutrition information about the recipe."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes nutrition Recipe)
  :AXIOMS (/XMLNS/RDFS/label nutrition "nutrition")
  :AXIOMS (rangeIncludes nutrition NutritionInformation))

(DEFRELATION experienceRequirements (?X ?Y)
  :DOCUMENTATION "Description of skills and experience needed for the position."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes experienceRequirements JobPosting)
  :AXIOMS (rangeIncludes experienceRequirements Text)
  :AXIOMS (/XMLNS/RDFS/label experienceRequirements "experienceRequirements"))

(DEFRELATION polygon (?X ?Y)
  :DOCUMENTATION "A polygon is the area enclosed by a point-to-point path for which the starting and ending points are the same. A polygon is expressed as a series of four or more space delimited points where the first and final points are identical."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes polygon GeoShape)
  :AXIOMS (/XMLNS/RDFS/label polygon "polygon")
  :AXIOMS (rangeIncludes polygon Text))

(DEFCONCEPT DiscoverAction (?X)
  :DOCUMENTATION "The act of discovering/finding an object."
  :SUBSET-OF FindAction
  :AXIOMS (/XMLNS/RDFS/label DiscoverAction "DiscoverAction"))

(DEFRELATION lodgingUnitType (?X ?Y)
  :DOCUMENTATION "Textual description of the unit type (including suite vs. room, size of bed, etc.)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label lodgingUnitType "lodgingUnitType")
  :AXIOMS (rangeIncludes lodgingUnitType Text)
  :AXIOMS (rangeIncludes lodgingUnitType QualitativeValue)
  :AXIOMS (domainIncludes lodgingUnitType LodgingReservation))

(DEFRELATION validFor (?X ?Y)
  :DOCUMENTATION "The time validity of the permit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes validFor Permit)
  :AXIOMS (rangeIncludes validFor Duration)
  :AXIOMS (/XMLNS/RDFS/label validFor "validFor"))

(DEFRELATION countryOfOrigin (?X ?Y)
  :DOCUMENTATION "The country of the principal offices of the production company or individual responsible for the movie or program."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes countryOfOrigin Country)
  :AXIOMS (domainIncludes countryOfOrigin TVSeason)
  :AXIOMS (domainIncludes countryOfOrigin TVSeries)
  :AXIOMS (/XMLNS/RDFS/label countryOfOrigin "countryOfOrigin")
  :AXIOMS (domainIncludes countryOfOrigin TVEpisode)
  :AXIOMS (domainIncludes countryOfOrigin Movie))

(DEFCONCEPT UnRegisterAction (?X)
  :DOCUMENTATION "<p>The act of un-registering from a service.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/RegisterAction\">RegisterAction</a>: antonym of UnRegisterAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/Leave\">Leave</a>: Unlike LeaveAction, UnRegisterAction implies that you are unregistering from a service you werer previously registered, rather than leaving a team/group of people.</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label UnRegisterAction "UnRegisterAction"))

(ASSERT (AND (RestrictedDiet VeganDiet) (DOCUMENTATION VeganDiet "A diet exclusive of all animal products.") (/XMLNS/RDFS/label VeganDiet "VeganDiet")))

(ASSERT
 (AND (MusicReleaseFormatType LaserDiscFormat) (DOCUMENTATION LaserDiscFormat "LaserDiscFormat.") (/XMLNS/RDFS/label LaserDiscFormat "LaserDiscFormat")
  (/XMLNS/DC/source LaserDiscFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION thumbnail (?X ?Y)
  :DOCUMENTATION "Thumbnail image for an image or video."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes thumbnail ImageObject)
  :AXIOMS (/XMLNS/RDFS/label thumbnail "thumbnail")
  :AXIOMS (rangeIncludes thumbnail ImageObject)
  :AXIOMS (domainIncludes thumbnail VideoObject))

(DEFRELATION educationalFramework (?X ?Y)
  :DOCUMENTATION "The framework to which the resource being described is aligned."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label educationalFramework "educationalFramework")
  :AXIOMS (domainIncludes educationalFramework AlignmentObject)
  :AXIOMS (rangeIncludes educationalFramework Text))

(DEFRELATION deathPlace (?X ?Y)
  :DOCUMENTATION "The place where the person died."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes deathPlace Person)
  :AXIOMS (rangeIncludes deathPlace Place)
  :AXIOMS (/XMLNS/RDFS/label deathPlace "deathPlace"))

(DEFRELATION permitAudience (?X ?Y)
  :DOCUMENTATION "The target audience for this permit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes permitAudience Permit)
  :AXIOMS (/XMLNS/RDFS/label permitAudience "permitAudience")
  :AXIOMS (rangeIncludes permitAudience Audience))

(DEFCONCEPT ShoeStore (?X)
  :DOCUMENTATION "A shoe store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label ShoeStore "ShoeStore"))

(DEFRELATION blogPosts (?X ?Y)
  :DOCUMENTATION "The postings that are part of this blog."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes blogPosts BlogPosting)
  :AXIOMS (domainIncludes blogPosts Blog)
  :AXIOMS (/XMLNS/RDFS/label blogPosts "blogPosts")
  :AXIOMS (supersededBy blogPosts blogPost))

(DEFRELATION blogPost (?X ?Y)
  :DOCUMENTATION "A posting that is part of this blog."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes blogPost Blog)
  :AXIOMS (rangeIncludes blogPost BlogPosting)
  :AXIOMS (/XMLNS/RDFS/label blogPost "blogPost"))

(ASSERT
 (AND (ActionStatusType CompletedActionStatus) (DOCUMENTATION CompletedActionStatus "An action that has already taken place.")
  (/XMLNS/RDFS/label CompletedActionStatus "CompletedActionStatus")))

(DEFRELATION availableLanguage (?X ?Y)
  :DOCUMENTATION "A language someone may use with the item. Please use one of the language codes from the <a href=\"http://tools.ietf.org/html/bcp47\">IETF BCP 47 standard</a>. See also <a class=\"localLink\" href=\"http://schema.org/inLanguage\">inLanguage</a>"
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availableLanguage ServiceChannel)
  :AXIOMS (rangeIncludes availableLanguage Text)
  :AXIOMS (domainIncludes availableLanguage ContactPoint)
  :AXIOMS (/XMLNS/RDFS/label availableLanguage "availableLanguage")
  :AXIOMS (rangeIncludes availableLanguage Language)
  :AXIOMS (domainIncludes availableLanguage LodgingBusiness))

(DEFRELATION orderItemNumber (?X ?Y)
  :DOCUMENTATION "The identifier of the order item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes orderItemNumber OrderItem)
  :AXIOMS (/XMLNS/RDFS/label orderItemNumber "orderItemNumber")
  :AXIOMS (rangeIncludes orderItemNumber Text))

(DEFCONCEPT RadioSeason (?X)
  :DOCUMENTATION "Season dedicated to radio broadcast and associated online delivery."
  :SUBSET-OF CreativeWorkSeason
  :AXIOMS (/XMLNS/RDFS/label RadioSeason "RadioSeason"))

(DEFCONCEPT ItemPage (?X)
  :DOCUMENTATION "A page devoted to a single item, such as a particular product or hotel."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label ItemPage "ItemPage"))

(DEFRELATION availabilityStarts (?X ?Y)
  :DOCUMENTATION "The beginning of the availability of the product or service included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label availabilityStarts "availabilityStarts")
  :AXIOMS (rangeIncludes availabilityStarts DateTime)
  :AXIOMS (/XMLNS/DC/source availabilityStarts |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes availabilityStarts Demand)
  :AXIOMS (domainIncludes availabilityStarts Offer))

(DEFRELATION replacer (?X ?Y)
  :DOCUMENTATION "A sub property of object. The object that replaces."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (/XMLNS/RDFS/label replacer "replacer")
  :AXIOMS (domainIncludes replacer ReplaceAction)
  :AXIOMS (rangeIncludes replacer Thing))

(DEFRELATION opens (?X ?Y)
  :DOCUMENTATION "The opening hour of the place or service on the given day(s) of the week."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label opens "opens")
  :AXIOMS (/XMLNS/DC/source opens |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes opens Time)
  :AXIOMS (domainIncludes opens OpeningHoursSpecification))

(DEFCONCEPT DrawAction (?X)
  :DOCUMENTATION "The act of producing a visual/graphical representation of an object, typically with a pen/pencil and paper as instruments."
  :SUBSET-OF CreateAction
  :AXIOMS (/XMLNS/RDFS/label DrawAction "DrawAction"))

(DEFRELATION articleSection (?X ?Y)
  :DOCUMENTATION "Articles may belong to one or more 'sections' in a magazine or newspaper, such as Sports, Lifestyle, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes articleSection Article)
  :AXIOMS (/XMLNS/RDFS/label articleSection "articleSection")
  :AXIOMS (rangeIncludes articleSection Text))

(DEFCONCEPT AgreeAction (?X)
  :DOCUMENTATION "The act of expressing a consistency of opinion with the object. An agent agrees to/about an object (a proposition, topic or theme) with participants."
  :SUBSET-OF ReactAction
  :AXIOMS (/XMLNS/RDFS/label AgreeAction "AgreeAction"))

(DEFRELATION question (?X ?Y)
  :DOCUMENTATION "A sub property of object. A question."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (/XMLNS/RDFS/label question "question")
  :AXIOMS (domainIncludes question AskAction)
  :AXIOMS (rangeIncludes question Question))

(DEFCONCEPT VideoGallery (?X)
  :DOCUMENTATION "Web page type: Video gallery page."
  :SUBSET-OF CollectionPage
  :AXIOMS (/XMLNS/RDFS/label VideoGallery "VideoGallery"))

(DEFRELATION entertainmentBusiness (?X ?Y)
  :DOCUMENTATION "A sub property of location. The entertainment business where the action occurred."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (/XMLNS/RDFS/label entertainmentBusiness "entertainmentBusiness")
  :AXIOMS (rangeIncludes entertainmentBusiness EntertainmentBusiness)
  :AXIOMS (domainIncludes entertainmentBusiness PerformAction))

(DEFCONCEPT PaintAction (?X)
  :DOCUMENTATION "The act of producing a painting, typically with paint and canvas as instruments."
  :SUBSET-OF CreateAction
  :AXIOMS (/XMLNS/RDFS/label PaintAction "PaintAction"))

(ASSERT (AND (BookFormatType Paperback) (DOCUMENTATION Paperback "Book format: Paperback.") (/XMLNS/RDFS/label Paperback "Paperback")))

(DEFRELATION reservedTicket (?X ?Y)
  :DOCUMENTATION "A ticket associated with the reservation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label reservedTicket "reservedTicket")
  :AXIOMS (domainIncludes reservedTicket Reservation)
  :AXIOMS (rangeIncludes reservedTicket Ticket))

(DEFRELATION lesser (?X ?Y)
  :DOCUMENTATION "This ordering relation for qualitative values indicates that the subject is lesser than the object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes lesser QualitativeValue)
  :AXIOMS (/XMLNS/DC/source lesser |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label lesser "lesser")
  :AXIOMS (domainIncludes lesser QualitativeValue))

(DEFRELATION validUntil (?X ?Y)
  :DOCUMENTATION "The date when the item is no longer valid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label validUntil "validUntil")
  :AXIOMS (domainIncludes validUntil Permit)
  :AXIOMS (rangeIncludes validUntil Date))

(DEFRELATION resultReview (?X ?Y)
  :DOCUMENTATION "A sub property of result. The review that resulted in the performing of the action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF result
  :AXIOMS (rangeIncludes resultReview Review)
  :AXIOMS (domainIncludes resultReview ReviewAction)
  :AXIOMS (/XMLNS/RDFS/label resultReview "resultReview"))

(DEFRELATION rsvpResponse (?X ?Y)
  :DOCUMENTATION "The response (yes, no, maybe) to the RSVP."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes rsvpResponse RsvpResponseType)
  :AXIOMS (domainIncludes rsvpResponse RsvpAction)
  :AXIOMS (/XMLNS/RDFS/label rsvpResponse "rsvpResponse"))

(DEFCONCEPT LikeAction (?X)
  :DOCUMENTATION "The act of expressing a positive sentiment about the object. An agent likes an object (a proposition, topic or theme) with participants."
  :SUBSET-OF ReactAction
  :AXIOMS (/XMLNS/RDFS/label LikeAction "LikeAction"))

(DEFRELATION broadcastTimezone (?X ?Y)
  :DOCUMENTATION "The timezone in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 format</a> for which the service bases its broadcasts"
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes broadcastTimezone Text)
  :AXIOMS (domainIncludes broadcastTimezone BroadcastService)
  :AXIOMS (/XMLNS/RDFS/label broadcastTimezone "broadcastTimezone"))

(DEFRELATION trackingNumber (?X ?Y)
  :DOCUMENTATION "Shipper tracking number."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes trackingNumber Text)
  :AXIOMS (domainIncludes trackingNumber ParcelDelivery)
  :AXIOMS (/XMLNS/RDFS/label trackingNumber "trackingNumber"))

(DEFCONCEPT WearAction (?X)
  :DOCUMENTATION "The act of dressing oneself in clothing."
  :SUBSET-OF UseAction
  :AXIOMS (/XMLNS/RDFS/label WearAction "WearAction"))

(DEFRELATION answerCount (?X ?Y)
  :DOCUMENTATION "The number of answers this question has received."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes answerCount Integer)
  :AXIOMS (/XMLNS/RDFS/label answerCount "answerCount")
  :AXIOMS (domainIncludes answerCount Question))

(DEFRELATION previousStartDate (?X ?Y)
  :DOCUMENTATION "Used in conjunction with eventStatus for rescheduled or cancelled events. This property contains the previously scheduled start date. For rescheduled events, the startDate property should be used for the newly scheduled start date. In the (rare) case of an event that has been postponed and rescheduled multiple times, this field may be repeated."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label previousStartDate "previousStartDate")
  :AXIOMS (rangeIncludes previousStartDate Date)
  :AXIOMS (domainIncludes previousStartDate Event))

(DEFRELATION servingSize (?X ?Y)
  :DOCUMENTATION "The serving size, in terms of the number of volume or mass."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label servingSize "servingSize")
  :AXIOMS (domainIncludes servingSize NutritionInformation)
  :AXIOMS (rangeIncludes servingSize Text))

(DEFCONCEPT Continent (?X)
  :DOCUMENTATION "One of the continents (for example, Europe or Africa)."
  :SUBSET-OF Landform
  :AXIOMS (/XMLNS/RDFS/label Continent "Continent"))

(DEFRELATION landlord (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The owner of the real estate property."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (/XMLNS/RDFS/label landlord "landlord")
  :AXIOMS (domainIncludes landlord RentAction)
  :AXIOMS (rangeIncludes landlord Organization)
  :AXIOMS (rangeIncludes landlord Person))

(DEFRELATION estimatedFlightDuration (?X ?Y)
  :DOCUMENTATION "The estimated time the flight will take."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label estimatedFlightDuration "estimatedFlightDuration")
  :AXIOMS (domainIncludes estimatedFlightDuration Flight)
  :AXIOMS (rangeIncludes estimatedFlightDuration Duration)
  :AXIOMS (rangeIncludes estimatedFlightDuration Text))

(DEFRELATION application (?X ?Y)
  :DOCUMENTATION "An application that can complete the request."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes application EntryPoint)
  :AXIOMS (/XMLNS/RDFS/label application "application")
  :AXIOMS (rangeIncludes application SoftwareApplication)
  :AXIOMS (supersededBy application actionApplication))

(DEFRELATION actionApplication (?X ?Y)
  :DOCUMENTATION "An application that can complete the request."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label actionApplication "actionApplication")
  :AXIOMS (rangeIncludes actionApplication SoftwareApplication)
  :AXIOMS (domainIncludes actionApplication EntryPoint))

(DEFRELATION departureGate (?X ?Y)
  :DOCUMENTATION "Identifier of the flight's departure gate."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label departureGate "departureGate")
  :AXIOMS (domainIncludes departureGate Flight)
  :AXIOMS (rangeIncludes departureGate Text))

(DEFRELATION musicalKey (?X ?Y)
  :DOCUMENTATION "The key, mode, or scale this composition uses."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes musicalKey Text)
  :AXIOMS (domainIncludes musicalKey MusicComposition)
  :AXIOMS (/XMLNS/RDFS/label musicalKey "musicalKey")
  :AXIOMS (/XMLNS/DC/source musicalKey |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|))

(DEFRELATION reservationStatus (?X ?Y)
  :DOCUMENTATION "The current status of the reservation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes reservationStatus ReservationStatusType)
  :AXIOMS (/XMLNS/RDFS/label reservationStatus "reservationStatus")
  :AXIOMS (domainIncludes reservationStatus Reservation))

(ASSERT
 (AND (ItemAvailability OutOfStock) (DOCUMENTATION OutOfStock "Indicates that the item is out of stock.") (/XMLNS/RDFS/label OutOfStock "OutOfStock")))

(ASSERT
 (AND (OrderStatus OrderDelivered) (/XMLNS/RDFS/label OrderDelivered "OrderDelivered")
  (DOCUMENTATION OrderDelivered "OrderStatus representing successful delivery of an order.")))

(DEFRELATION printColumn (?X ?Y)
  :DOCUMENTATION "The number of the column in which the NewsArticle appears in the print edition."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label printColumn "printColumn")
  :AXIOMS (rangeIncludes printColumn Text)
  :AXIOMS (domainIncludes printColumn NewsArticle))

(DEFCONCEPT Campground (?X)
  :DOCUMENTATION "A camping site, campsite, or campground is a place used for overnight stay in the outdoors. In British English a campsite is an area, usually divided into a number of pitches, where people can camp overnight using tents or camper vans or caravans; this British English use of the word is synonymous with the American English expression campground. In American English the term campsite generally means an area where an individual, family, group, or military unit can pitch a tent or parks a camper; a campground may contain many campsites (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/Campsite\">http://en.wikipedia.org/wiki/Campsite</a>).
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF LodgingBusiness
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Campground "Campground")
  :AXIOMS (/XMLNS/DC/source Campground |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFRELATION acceptedOffer (?X ?Y)
  :DOCUMENTATION "The offer(s) -- e.g., product, quantity and price combinations -- included in the order."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes acceptedOffer Order)
  :AXIOMS (rangeIncludes acceptedOffer Offer)
  :AXIOMS (/XMLNS/RDFS/label acceptedOffer "acceptedOffer"))

(DEFCONCEPT GardenStore (?X)
  :DOCUMENTATION "A garden store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label GardenStore "GardenStore"))

(DEFCONCEPT Car (?X)
  :DOCUMENTATION "A car is a wheeled, self-powered motor vehicle used for transportation."
  :SUBSET-OF Vehicle
  :AXIOMS (/XMLNS/DC/source Car |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (/XMLNS/RDFS/label Car "Car"))

(DEFRELATION tracks (?X ?Y)
  :DOCUMENTATION "A music recording (track)&#x2014;usually a single song."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes tracks MusicGroup)
  :AXIOMS (/XMLNS/RDFS/label tracks "tracks")
  :AXIOMS (rangeIncludes tracks MusicRecording)
  :AXIOMS (domainIncludes tracks MusicPlaylist)
  :AXIOMS (supersededBy tracks track))

(DEFRELATION track (?X ?Y)
  :DOCUMENTATION "A music recording (track)&#x2014;usually a single song. If an ItemList is given, the list should contain items of type MusicRecording."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label track "track")
  :AXIOMS (rangeIncludes track ItemList)
  :AXIOMS (/XMLNS/DC/source track |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (rangeIncludes track MusicRecording)
  :AXIOMS (domainIncludes track MusicPlaylist)
  :AXIOMS (domainIncludes track MusicGroup))

(DEFRELATION childMinAge (?X ?Y)
  :DOCUMENTATION "Minimal age of the child."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes childMinAge ParentAudience)
  :AXIOMS (rangeIncludes childMinAge Number)
  :AXIOMS (/XMLNS/RDFS/label childMinAge "childMinAge"))

(DEFRELATION applicationSubCategory (?X ?Y)
  :DOCUMENTATION "Subcategory of the application, e.g. 'Arcade Game'."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes applicationSubCategory Text)
  :AXIOMS (domainIncludes applicationSubCategory SoftwareApplication)
  :AXIOMS (/XMLNS/RDFS/label applicationSubCategory "applicationSubCategory")
  :AXIOMS (rangeIncludes applicationSubCategory URL))

(DEFCONCEPT SubwayStation (?X)
  :DOCUMENTATION "A subway station."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label SubwayStation "SubwayStation"))

(DEFCONCEPT ConvenienceStore (?X)
  :DOCUMENTATION "A convenience store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label ConvenienceStore "ConvenienceStore"))

(ASSERT
 (AND (ItemAvailability PreOrder)
  (DOCUMENTATION PreOrder "Indicates that the item is available for pre-order, but will be delivered when generally available.")
  (/XMLNS/RDFS/label PreOrder "PreOrder")))

(ASSERT
 (AND (ItemAvailability Discontinued) (/XMLNS/RDFS/label Discontinued "Discontinued")
  (DOCUMENTATION Discontinued "Indicates that the item has been discontinued.")))

(DEFRELATION webCheckinTime (?X ?Y)
  :DOCUMENTATION "The time when a passenger can check into the flight online."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes webCheckinTime Flight)
  :AXIOMS (rangeIncludes webCheckinTime DateTime)
  :AXIOMS (/XMLNS/RDFS/label webCheckinTime "webCheckinTime"))

(DEFRELATION typeOfBed (?X ?Y)
  :DOCUMENTATION "The type of bed to which the BedDetail refers, i.e. the type of bed available in the quantity indicated by quantity."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source typeOfBed |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label typeOfBed "typeOfBed")
  :AXIOMS (domainIncludes typeOfBed BedDetails)
  :AXIOMS (rangeIncludes typeOfBed Text))

(DEFRELATION predecessorOf (?X ?Y)
  :DOCUMENTATION "A pointer from a previous, often discontinued variant of the product to its newer variant."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label predecessorOf "predecessorOf")
  :AXIOMS (rangeIncludes predecessorOf ProductModel)
  :AXIOMS (domainIncludes predecessorOf ProductModel)
  :AXIOMS (/XMLNS/DC/source predecessorOf |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION playersOnline (?X ?Y)
  :DOCUMENTATION "Number of players on the server."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes playersOnline GameServer)
  :AXIOMS (rangeIncludes playersOnline Integer)
  :AXIOMS (/XMLNS/RDFS/label playersOnline "playersOnline"))

(DEFCONCEPT Code (?X)
  :DOCUMENTATION "Computer programming source code. Example: Full (compile ready) solutions, code snippet samples, scripts, templates."
  :SUBSET-OF CreativeWork
  :AXIOMS (supersededBy Code SoftwareSourceCode)
  :AXIOMS (/XMLNS/RDFS/label Code "Code"))

(DEFRELATION industry (?X ?Y)
  :DOCUMENTATION "The industry associated with the job position."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes industry JobPosting)
  :AXIOMS (/XMLNS/RDFS/label industry "industry")
  :AXIOMS (rangeIncludes industry Text))

(DEFRELATION productionDate (?X ?Y)
  :DOCUMENTATION "The date of production of the item, e.g. vehicle."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source productionDate |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (/XMLNS/RDFS/label productionDate "productionDate")
  :AXIOMS (domainIncludes productionDate Product)
  :AXIOMS (rangeIncludes productionDate Date)
  :AXIOMS (domainIncludes productionDate Vehicle))

(ASSERT (AND (Boolean False) (DOCUMENTATION False "The boolean value false.") (/XMLNS/RDFS/label False "False")))

(DEFRELATION addressRegion (?X ?Y)
  :DOCUMENTATION "The region. For example, CA."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes addressRegion PostalAddress)
  :AXIOMS (/XMLNS/RDFS/label addressRegion "addressRegion")
  :AXIOMS (rangeIncludes addressRegion Text))

(ASSERT
 (AND (MusicAlbumProductionType CompilationAlbum) (DOCUMENTATION CompilationAlbum "CompilationAlbum.")
  (/XMLNS/RDFS/label CompilationAlbum "CompilationAlbum") (/XMLNS/DC/source CompilationAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION videoFrameSize (?X ?Y)
  :DOCUMENTATION "The frame size of the video."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes videoFrameSize VideoObject)
  :AXIOMS (/XMLNS/RDFS/label videoFrameSize "videoFrameSize")
  :AXIOMS (rangeIncludes videoFrameSize Text))

(DEFRELATION nextItem (?X ?Y)
  :DOCUMENTATION "A link to the ListItem that follows the current one."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label nextItem "nextItem")
  :AXIOMS (domainIncludes nextItem ListItem)
  :AXIOMS (rangeIncludes nextItem ListItem))

(DEFRELATION departureTime (?X ?Y)
  :DOCUMENTATION "The expected departure time."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes departureTime DateTime)
  :AXIOMS (domainIncludes departureTime Flight)
  :AXIOMS (/XMLNS/RDFS/label departureTime "departureTime")
  :AXIOMS (domainIncludes departureTime BusTrip)
  :AXIOMS (domainIncludes departureTime TrainTrip))

(DEFRELATION playMode (?X ?Y)
  :DOCUMENTATION "Indicates whether this game is multi-player, co-op or single-player.  The game can be marked as multi-player, co-op and single-player at the same time."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label playMode "playMode")
  :AXIOMS (rangeIncludes playMode GamePlayMode)
  :AXIOMS (domainIncludes playMode VideoGameSeries)
  :AXIOMS (domainIncludes playMode VideoGame))

(DEFRELATION name (?X ?Y)
  :DOCUMENTATION "The name of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes name Thing)
  :AXIOMS (/XMLNS/RDFS/label name "name")
  :AXIOMS (rangeIncludes name Text))

(DEFCONCEPT VideoGameClip (?X)
  :DOCUMENTATION "A short segment/part of a video game."
  :SUBSET-OF Clip
  :AXIOMS (/XMLNS/RDFS/label VideoGameClip "VideoGameClip"))

(ASSERT
 (AND (DayOfWeek Thursday) (DOCUMENTATION Thursday "The day of the week between Wednesday and Friday.") (/XMLNS/RDFS/label Thursday "Thursday")
  (sameAs Thursday |http://www.wikidata.org/entity/Q129|)))

(ASSERT (AND (GenderType Male) (DOCUMENTATION Male "The male gender.") (/XMLNS/RDFS/label Male "Male")))

(ASSERT
 (AND (ItemAvailability LimitedAvailability) (/XMLNS/RDFS/label LimitedAvailability "LimitedAvailability")
  (DOCUMENTATION LimitedAvailability "Indicates that the item has limited availability.")))

(DEFRELATION geoRadius (?X ?Y)
  :DOCUMENTATION "Indicates the approximate radius of a GeoCircle (metres unless indicated otherwise via Distance notation)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes geoRadius Text)
  :AXIOMS (/XMLNS/RDFS/label geoRadius "geoRadius")
  :AXIOMS (rangeIncludes geoRadius Number)
  :AXIOMS (rangeIncludes geoRadius Distance)
  :AXIOMS (domainIncludes geoRadius GeoCircle))

(DEFRELATION greater (?X ?Y)
  :DOCUMENTATION "This ordering relation for qualitative values indicates that the subject is greater than the object."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes greater QualitativeValue)
  :AXIOMS (/XMLNS/DC/source greater |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes greater QualitativeValue)
  :AXIOMS (/XMLNS/RDFS/label greater "greater"))

(DEFRELATION interactionService (?X ?Y)
  :DOCUMENTATION "The WebSite or SoftwareApplication where the interactions took place."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes interactionService SoftwareApplication)
  :AXIOMS (rangeIncludes interactionService WebSite)
  :AXIOMS (/XMLNS/RDFS/label interactionService "interactionService")
  :AXIOMS (domainIncludes interactionService InteractionCounter))

(DEFRELATION scheduledPaymentDate (?X ?Y)
  :DOCUMENTATION "The date the invoice is scheduled to be paid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes scheduledPaymentDate Invoice)
  :AXIOMS (rangeIncludes scheduledPaymentDate Date)
  :AXIOMS (/XMLNS/RDFS/label scheduledPaymentDate "scheduledPaymentDate"))

(DEFRELATION keywords (?X ?Y)
  :DOCUMENTATION "Keywords or tags used to describe this content. Multiple entries in a keywords list are typically delimited by commas."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label keywords "keywords")
  :AXIOMS (domainIncludes keywords CreativeWork)
  :AXIOMS (rangeIncludes keywords Text))

(DEFCONCEPT SeaBodyOfWater (?X)
  :DOCUMENTATION "A sea (for example, the Caspian sea)."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label SeaBodyOfWater "SeaBodyOfWater"))

(DEFCONCEPT DaySpa (?X)
  :DOCUMENTATION "A day spa."
  :SUBSET-OF HealthAndBeautyBusiness
  :AXIOMS (/XMLNS/RDFS/label DaySpa "DaySpa"))

(DEFCONCEPT TouristAttraction (?X)
  :DOCUMENTATION "A tourist attraction."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label TouristAttraction "TouristAttraction"))

(DEFCONCEPT BowlingAlley (?X)
  :DOCUMENTATION "A bowling alley."
  :SUBSET-OF SportsActivityLocation
  :AXIOMS (/XMLNS/RDFS/label BowlingAlley "BowlingAlley"))

(DEFRELATION hasPOS (?X ?Y)
  :DOCUMENTATION "Points-of-Sales operated by the organization or person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source hasPOS |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label hasPOS "hasPOS")
  :AXIOMS (domainIncludes hasPOS Person)
  :AXIOMS (domainIncludes hasPOS Organization)
  :AXIOMS (rangeIncludes hasPOS Place))

(DEFRELATION numberOfItems (?X ?Y)
  :DOCUMENTATION "The number of items in an ItemList. Note that some descriptions might not fully describe all items in a list (e.g., multi-page pagination); in such cases, the numberOfItems would be for the entire list."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numberOfItems Integer)
  :AXIOMS (/XMLNS/RDFS/label numberOfItems "numberOfItems")
  :AXIOMS (domainIncludes numberOfItems ItemList))

(DEFCONCEPT MusicEvent (?X)
  :DOCUMENTATION "Event type: Music event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label MusicEvent "MusicEvent"))

(DEFRELATION jobTitle (?X ?Y)
  :DOCUMENTATION "The job title of the person (for example, Financial Manager)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes jobTitle Person)
  :AXIOMS (/XMLNS/RDFS/label jobTitle "jobTitle")
  :AXIOMS (rangeIncludes jobTitle Text))

(DEFRELATION containedIn (?X ?Y)
  :DOCUMENTATION "The basic containment relation between a place and one that contains it."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label containedIn "containedIn")
  :AXIOMS (rangeIncludes containedIn Place)
  :AXIOMS (supersededBy containedIn containedInPlace)
  :AXIOMS (domainIncludes containedIn Place))

(DEFRELATION containedInPlace (?X ?Y)
  :DOCUMENTATION "The basic containment relation between a place and one that contains it."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes containedInPlace Place)
  :AXIOMS (rangeIncludes containedInPlace Place)
  :AXIOMS (inverseOf containedInPlace containsPlace)
  :AXIOMS (/XMLNS/RDFS/label containedInPlace "containedInPlace"))

(DEFRELATION candidate (?X ?Y)
  :DOCUMENTATION "A sub property of object. The candidate subject of this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF object
  :AXIOMS (rangeIncludes candidate Person)
  :AXIOMS (/XMLNS/RDFS/label candidate "candidate")
  :AXIOMS (domainIncludes candidate VoteAction))

(DEFRELATION isSimilarTo (?X ?Y)
  :DOCUMENTATION "A pointer to another, functionally similar product (or multiple products)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label isSimilarTo "isSimilarTo")
  :AXIOMS (rangeIncludes isSimilarTo Service)
  :AXIOMS (/XMLNS/DC/source isSimilarTo |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes isSimilarTo Product)
  :AXIOMS (domainIncludes isSimilarTo Service)
  :AXIOMS (rangeIncludes isSimilarTo Product))

(DEFCONCEPT MovingCompany (?X)
  :DOCUMENTATION "A moving company."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label MovingCompany "MovingCompany"))

(DEFCONCEPT Float (?X)
  :DOCUMENTATION "Data type: Floating number."
  :SUBSET-OF Number
  :AXIOMS (/XMLNS/RDFS/label Float "Float"))

(DEFRELATION startTime (?X ?Y)
  :DOCUMENTATION "The startTime of something. For a reserved event or service (e.g. FoodEstablishmentReservation), the time that it is expected to start. For actions that span a period of time, when the action was performed. e.g. John wrote a book from <em>January</em> to December.</p>

<p>Note that Event uses startDate/endDate instead of startTime/endTime, even when describing dates with times. This situation may be clarified in future revisions."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label startTime "startTime")
  :AXIOMS (domainIncludes startTime FoodEstablishmentReservation)
  :AXIOMS (domainIncludes startTime Action)
  :AXIOMS (rangeIncludes startTime DateTime))

(DEFCONCEPT GatedResidenceCommunity (?X)
  :DOCUMENTATION "Residence type: Gated community."
  :SUBSET-OF Residence
  :AXIOMS (/XMLNS/RDFS/label GatedResidenceCommunity "GatedResidenceCommunity"))

(DEFCONCEPT TheaterGroup (?X)
  :DOCUMENTATION "A theater group or company, for example, the Royal Shakespeare Company or Druid Theatre."
  :SUBSET-OF PerformingGroup
  :AXIOMS (/XMLNS/RDFS/label TheaterGroup "TheaterGroup"))

(DEFRELATION area (?X ?Y)
  :DOCUMENTATION "The area within which users can expect to reach the broadcast service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label area "area")
  :AXIOMS (rangeIncludes area Place)
  :AXIOMS (domainIncludes area BroadcastService)
  :AXIOMS (supersededBy area serviceArea))

(DEFRELATION serviceArea (?X ?Y)
  :DOCUMENTATION "The geographic area where the service is provided."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serviceArea AdministrativeArea)
  :AXIOMS (domainIncludes serviceArea Service)
  :AXIOMS (domainIncludes serviceArea Organization)
  :AXIOMS (rangeIncludes serviceArea Place)
  :AXIOMS (domainIncludes serviceArea ContactPoint)
  :AXIOMS (supersededBy serviceArea areaServed)
  :AXIOMS (rangeIncludes serviceArea GeoShape)
  :AXIOMS (/XMLNS/RDFS/label serviceArea "serviceArea"))

(DEFRELATION paymentMethodId (?X ?Y)
  :DOCUMENTATION "An identifier for the method of payment used (e.g. the last 4 digits of the credit card)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes paymentMethodId Text)
  :AXIOMS (domainIncludes paymentMethodId Order)
  :AXIOMS (/XMLNS/RDFS/label paymentMethodId "paymentMethodId")
  :AXIOMS (domainIncludes paymentMethodId Invoice))

(DEFRELATION specialCommitments (?X ?Y)
  :DOCUMENTATION "Any special commitments associated with this job posting. Valid entries include VeteranCommit, MilitarySpouseCommit, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes specialCommitments JobPosting)
  :AXIOMS (rangeIncludes specialCommitments Text)
  :AXIOMS (/XMLNS/RDFS/label specialCommitments "specialCommitments"))

(ASSERT
 (AND (DayOfWeek Tuesday) (/XMLNS/RDFS/label Tuesday "Tuesday") (DOCUMENTATION Tuesday "The day of the week between Monday and Wednesday.")
  (sameAs Tuesday |http://www.wikidata.org/entity/Q127|)))

(DEFRELATION durationOfWarranty (?X ?Y)
  :DOCUMENTATION "The duration of the warranty promise. Common unitCode values are ANN for year, MON for months, or DAY for days."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes durationOfWarranty QuantitativeValue)
  :AXIOMS (/XMLNS/DC/source durationOfWarranty |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label durationOfWarranty "durationOfWarranty")
  :AXIOMS (domainIncludes durationOfWarranty WarrantyPromise))

(DEFCONCEPT RiverBodyOfWater (?X)
  :DOCUMENTATION "A river (for example, the broad majestic Shannon)."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label RiverBodyOfWater "RiverBodyOfWater"))

(ASSERT (AND (Boolean True) (DOCUMENTATION True "The boolean value true.") (/XMLNS/RDFS/label True "True")))

(DEFCONCEPT DefenceEstablishment (?X)
  :DOCUMENTATION "A defence establishment, such as an army or navy base."
  :SUBSET-OF GovernmentBuilding
  :AXIOMS (/XMLNS/RDFS/label DefenceEstablishment "DefenceEstablishment"))

(DEFRELATION amountOfThisGood (?X ?Y)
  :DOCUMENTATION "The quantity of the goods included in the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes amountOfThisGood Number)
  :AXIOMS (domainIncludes amountOfThisGood TypeAndQuantityNode)
  :AXIOMS (/XMLNS/RDFS/label amountOfThisGood "amountOfThisGood")
  :AXIOMS (/XMLNS/DC/source amountOfThisGood |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION width (?X ?Y)
  :DOCUMENTATION "The width of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes width QuantitativeValue)
  :AXIOMS (/XMLNS/RDFS/label width "width")
  :AXIOMS (domainIncludes width VisualArtwork)
  :AXIOMS (domainIncludes width Product)
  :AXIOMS (rangeIncludes width Distance)
  :AXIOMS (domainIncludes width MediaObject))

(DEFRELATION benefits (?X ?Y)
  :DOCUMENTATION "Description of benefits associated with the job."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes benefits Text)
  :AXIOMS (domainIncludes benefits JobPosting)
  :AXIOMS (/XMLNS/RDFS/label benefits "benefits")
  :AXIOMS (supersededBy benefits jobBenefits))

(DEFRELATION jobBenefits (?X ?Y)
  :DOCUMENTATION "Description of benefits associated with the job."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes jobBenefits JobPosting)
  :AXIOMS (/XMLNS/RDFS/label jobBenefits "jobBenefits")
  :AXIOMS (rangeIncludes jobBenefits Text))

(DEFCONCEPT RecyclingCenter (?X)
  :DOCUMENTATION "A recycling center."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label RecyclingCenter "RecyclingCenter"))

(DEFRELATION character (?X ?Y)
  :DOCUMENTATION "Fictional person connected with a creative work."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes character CreativeWork)
  :AXIOMS (rangeIncludes character Person)
  :AXIOMS (/XMLNS/RDFS/label character "character"))

(DEFRELATION depth (?X ?Y)
  :DOCUMENTATION "The depth of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes depth Distance)
  :AXIOMS (domainIncludes depth Product)
  :AXIOMS (/XMLNS/RDFS/label depth "depth")
  :AXIOMS (domainIncludes depth VisualArtwork)
  :AXIOMS (rangeIncludes depth QuantitativeValue)
  :AXIOMS (/XMLNS/DC/source depth |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFRELATION departureBusStop (?X ?Y)
  :DOCUMENTATION "The stop or station from which the bus departs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes departureBusStop BusStop)
  :AXIOMS (rangeIncludes departureBusStop BusStation)
  :AXIOMS (domainIncludes departureBusStop BusTrip)
  :AXIOMS (/XMLNS/RDFS/label departureBusStop "departureBusStop"))

(DEFCONCEPT DepositAccount (?X)
  :DOCUMENTATION "A type of Bank Account with a main purpose of depositing funds to gain interest or other benefits."
  :SUBSET-OF InvestmentOrDeposit
  :SUBSET-OF BankAccount
  :AXIOMS (/XMLNS/DC/source DepositAccount |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label DepositAccount "DepositAccount"))

(DEFRELATION serviceAudience (?X ?Y)
  :DOCUMENTATION "The audience eligible for this service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes serviceAudience Service)
  :AXIOMS (/XMLNS/RDFS/label serviceAudience "serviceAudience")
  :AXIOMS (rangeIncludes serviceAudience Audience)
  :AXIOMS (supersededBy serviceAudience audience))

(DEFRELATION audience (?X ?Y)
  :DOCUMENTATION "An intended audience, i.e. a group for whom something was created."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes audience Product)
  :AXIOMS (domainIncludes audience PlayAction)
  :AXIOMS (domainIncludes audience CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label audience "audience")
  :AXIOMS (rangeIncludes audience Audience)
  :AXIOMS (domainIncludes audience Service)
  :AXIOMS (domainIncludes audience LodgingBusiness))

(DEFRELATION opponent (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The opponent on this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (/XMLNS/RDFS/label opponent "opponent")
  :AXIOMS (domainIncludes opponent ExerciseAction)
  :AXIOMS (rangeIncludes opponent Person))

(DEFCONCEPT ViewAction (?X)
  :DOCUMENTATION "The act of consuming static visual content."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label ViewAction "ViewAction"))

(DEFCONCEPT PrependAction (?X)
  :DOCUMENTATION "The act of inserting at the beginning if an ordered collection."
  :SUBSET-OF InsertAction
  :AXIOMS (/XMLNS/RDFS/label PrependAction "PrependAction"))

(DEFRELATION accessibilityFeature (?X ?Y)
  :DOCUMENTATION "Content features of the resource, such as accessible media, alternatives and supported enhancements for accessibility (<a href=\"http://www.w3.org/wiki/WebSchemas/Accessibility\">WebSchemas wiki lists possible values</a>)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label accessibilityFeature "accessibilityFeature")
  :AXIOMS (domainIncludes accessibilityFeature CreativeWork)
  :AXIOMS (rangeIncludes accessibilityFeature Text))

(DEFRELATION children (?X ?Y)
  :DOCUMENTATION "A child of the person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes children Person)
  :AXIOMS (/XMLNS/RDFS/label children "children")
  :AXIOMS (domainIncludes children Person))

(DEFCONCEPT AutomatedTeller (?X)
  :DOCUMENTATION "ATM/cash machine."
  :SUBSET-OF FinancialService
  :AXIOMS (/XMLNS/RDFS/label AutomatedTeller "AutomatedTeller"))

(DEFRELATION parentItem (?X ?Y)
  :DOCUMENTATION "The parent of a question, answer or item in general."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes parentItem Comment)
  :AXIOMS (/XMLNS/RDFS/label parentItem "parentItem")
  :AXIOMS (rangeIncludes parentItem Question))

(DEFRELATION foodEvent (?X ?Y)
  :DOCUMENTATION "A sub property of location. The specific food event where the action occurred."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (rangeIncludes foodEvent FoodEvent)
  :AXIOMS (domainIncludes foodEvent CookAction)
  :AXIOMS (/XMLNS/RDFS/label foodEvent "foodEvent"))

(DEFRELATION trackingUrl (?X ?Y)
  :DOCUMENTATION "Tracking url for the parcel delivery."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes trackingUrl ParcelDelivery)
  :AXIOMS (/XMLNS/RDFS/label trackingUrl "trackingUrl")
  :AXIOMS (rangeIncludes trackingUrl URL))

(DEFCONCEPT WPHeader (?X)
  :DOCUMENTATION "The header section of the page."
  :SUBSET-OF WebPageElement
  :AXIOMS (/XMLNS/RDFS/label WPHeader "WPHeader"))

(DEFRELATION numberOfEmployees (?X ?Y)
  :DOCUMENTATION "The number of employees in an organization e.g. business."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes numberOfEmployees QuantitativeValue)
  :AXIOMS (domainIncludes numberOfEmployees Organization)
  :AXIOMS (domainIncludes numberOfEmployees BusinessAudience)
  :AXIOMS (/XMLNS/RDFS/label numberOfEmployees "numberOfEmployees"))

(ASSERT
 (AND (OrderStatus OrderProblem) (/XMLNS/RDFS/label OrderProblem "OrderProblem")
  (DOCUMENTATION OrderProblem "OrderStatus representing that there is a problem with the order.")))

(DEFRELATION copyrightHolder (?X ?Y)
  :DOCUMENTATION "The party holding the legal copyright to the CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes copyrightHolder CreativeWork)
  :AXIOMS (rangeIncludes copyrightHolder Person)
  :AXIOMS (rangeIncludes copyrightHolder Organization)
  :AXIOMS (/XMLNS/RDFS/label copyrightHolder "copyrightHolder"))

(DEFRELATION foundingLocation (?X ?Y)
  :DOCUMENTATION "The place where the Organization was founded."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label foundingLocation "foundingLocation")
  :AXIOMS (rangeIncludes foundingLocation Place)
  :AXIOMS (domainIncludes foundingLocation Organization))

(DEFRELATION availableThrough (?X ?Y)
  :DOCUMENTATION "After this date, the item will no longer be available for pickup."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes availableThrough DeliveryEvent)
  :AXIOMS (/XMLNS/RDFS/label availableThrough "availableThrough")
  :AXIOMS (rangeIncludes availableThrough DateTime))

(DEFRELATION itemListOrder (?X ?Y)
  :DOCUMENTATION "Type of ordering (e.g. Ascending, Descending, Unordered)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label itemListOrder "itemListOrder")
  :AXIOMS (rangeIncludes itemListOrder Text)
  :AXIOMS (rangeIncludes itemListOrder ItemListOrderType)
  :AXIOMS (domainIncludes itemListOrder ItemList))

(DEFRELATION deliveryAddress (?X ?Y)
  :DOCUMENTATION "Destination address."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes deliveryAddress ParcelDelivery)
  :AXIOMS (rangeIncludes deliveryAddress PostalAddress)
  :AXIOMS (/XMLNS/RDFS/label deliveryAddress "deliveryAddress"))

(DEFRELATION includedComposition (?X ?Y)
  :DOCUMENTATION "Smaller compositions included in this work (e.g. a movement in a symphony)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes includedComposition MusicComposition)
  :AXIOMS (/XMLNS/RDFS/label includedComposition "includedComposition")
  :AXIOMS (/XMLNS/DC/source includedComposition |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (rangeIncludes includedComposition MusicComposition))

(DEFRELATION numberOfDoors (?X ?Y)
  :DOCUMENTATION "The number of doors.</p>

<p>Typical unit code(s): C62"
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numberOfDoors "numberOfDoors")
  :AXIOMS (rangeIncludes numberOfDoors QuantitativeValue)
  :AXIOMS (rangeIncludes numberOfDoors Number)
  :AXIOMS (domainIncludes numberOfDoors Vehicle)
  :AXIOMS (/XMLNS/DC/source numberOfDoors |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFRELATION awayTeam (?X ?Y)
  :DOCUMENTATION "The away team in a sports event."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF competitor
  :AXIOMS (rangeIncludes awayTeam Person)
  :AXIOMS (domainIncludes awayTeam SportsEvent)
  :AXIOMS (/XMLNS/RDFS/label awayTeam "awayTeam")
  :AXIOMS (rangeIncludes awayTeam SportsTeam))

(DEFRELATION servicePhone (?X ?Y)
  :DOCUMENTATION "The phone number to use to access the service."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes servicePhone ContactPoint)
  :AXIOMS (domainIncludes servicePhone ServiceChannel)
  :AXIOMS (/XMLNS/RDFS/label servicePhone "servicePhone"))

(ASSERT
 (AND (MusicReleaseFormatType DigitalFormat) (/XMLNS/DC/source DigitalFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (DOCUMENTATION DigitalFormat "DigitalFormat.") (/XMLNS/RDFS/label DigitalFormat "DigitalFormat")))

(DEFCONCEPT Mountain (?X)
  :DOCUMENTATION "A mountain, like Mount Whitney or Mount Everest."
  :SUBSET-OF Landform
  :AXIOMS (/XMLNS/RDFS/label Mountain "Mountain"))

(DEFRELATION fuelConsumption (?X ?Y)
  :DOCUMENTATION "<p>The amount of fuel consumed for traveling a particular distance or temporal duration with the given vehicle (e.g. liters per 100 km).</p>

<ul>
<li>Note 1: There are unfortunately no standard unit codes for liters per 100 km.  Use <a class=\"localLink\" href=\"http://schema.org/unitText\">unitText</a> to indicate the unit of measurement, e.g. L/100 km.</li>
<li>Note 2: There are two ways of indicating the fuel consumption, <a class=\"localLink\" href=\"http://schema.org/fuelConsumption\">fuelConsumption</a> (e.g. 8 liters per 100 km) and <a class=\"localLink\" href=\"http://schema.org/fuelEfficiency\">fuelEfficiency</a> (e.g. 30 miles per gallon). They are reciprocal.</li>
<li>Note 3: Often, the absolute value is useful only when related to driving speed (\"at 80 km/h\") or usage pattern (\"city traffic\"). You can use <a class=\"localLink\" href=\"http://schema.org/valueReference\">valueReference</a> to link the value for the fuel consumption to another value.</li>
</ul>
"
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes fuelConsumption Vehicle)
  :AXIOMS (/XMLNS/RDFS/label fuelConsumption "fuelConsumption")
  :AXIOMS (/XMLNS/DC/source fuelConsumption |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes fuelConsumption QuantitativeValue))

(DEFRELATION transFatContent (?X ?Y)
  :DOCUMENTATION "The number of grams of trans fat."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes transFatContent Mass)
  :AXIOMS (domainIncludes transFatContent NutritionInformation)
  :AXIOMS (/XMLNS/RDFS/label transFatContent "transFatContent"))

(DEFRELATION course (?X ?Y)
  :DOCUMENTATION "A sub property of location. The course where this action was taken."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (domainIncludes course ExerciseAction)
  :AXIOMS (rangeIncludes course Place)
  :AXIOMS (/XMLNS/RDFS/label course "course")
  :AXIOMS (supersededBy course exerciseCourse))

(DEFRELATION exerciseCourse (?X ?Y)
  :DOCUMENTATION "A sub property of location. The course where this action was taken."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF location
  :AXIOMS (domainIncludes exerciseCourse ExerciseAction)
  :AXIOMS (rangeIncludes exerciseCourse Place)
  :AXIOMS (/XMLNS/RDFS/label exerciseCourse "exerciseCourse"))

(DEFCONCEPT DiscussionForumPosting (?X)
  :DOCUMENTATION "A posting to a discussion forum."
  :SUBSET-OF SocialMediaPosting
  :AXIOMS (/XMLNS/RDFS/label DiscussionForumPosting "DiscussionForumPosting"))

(DEFCONCEPT ChildCare (?X)
  :DOCUMENTATION "A Childcare center."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label ChildCare "ChildCare"))

(DEFCONCEPT ScheduleAction (?X)
  :DOCUMENTATION "<p>Scheduling future actions, events, or tasks.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/ReserveAction\">ReserveAction</a>: Unlike ReserveAction, ScheduleAction allocates future actions (e.g. an event, a task, etc) towards a time slot / spatial allocation.</li>
</ul>
"
  :SUBSET-OF PlanAction
  :AXIOMS (/XMLNS/RDFS/label ScheduleAction "ScheduleAction"))

(DEFCONCEPT Plumber (?X)
  :DOCUMENTATION "A plumbing service."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label Plumber "Plumber"))

(DEFRELATION seatRow (?X ?Y)
  :DOCUMENTATION "The row location of the reserved seat (e.g., B)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label seatRow "seatRow")
  :AXIOMS (rangeIncludes seatRow Text)
  :AXIOMS (domainIncludes seatRow Seat))

(DEFRELATION multipleValues (?X ?Y)
  :DOCUMENTATION "Whether multiple values are allowed for the property.  Default is false."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes multipleValues PropertyValueSpecification)
  :AXIOMS (rangeIncludes multipleValues Boolean)
  :AXIOMS (/XMLNS/RDFS/label multipleValues "multipleValues"))

(DEFRELATION lyricist (?X ?Y)
  :DOCUMENTATION "The person who wrote the words."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes lyricist Person)
  :AXIOMS (/XMLNS/DC/source lyricist |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (domainIncludes lyricist MusicComposition)
  :AXIOMS (/XMLNS/RDFS/label lyricist "lyricist"))

(DEFCONCEPT InstallAction (?X)
  :DOCUMENTATION "The act of installing an application."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label InstallAction "InstallAction"))

(DEFCONCEPT AccountingService (?X)
  :DOCUMENTATION "Accountancy business.</p>

<p>As a <a class=\"localLink\" href=\"http://schema.org/LocalBusiness\">LocalBusiness</a> it can be described as a <a class=\"localLink\" href=\"http://schema.org/provider\">provider</a> of one or more <a class=\"localLink\" href=\"http://schema.org/Service\">Service</a>(s)."
  :SUBSET-OF FinancialService
  :AXIOMS (/XMLNS/RDFS/label AccountingService "AccountingService"))

(DEFRELATION inBroadcastLineup (?X ?Y)
  :DOCUMENTATION "The CableOrSatelliteService offering the channel."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes inBroadcastLineup BroadcastChannel)
  :AXIOMS (rangeIncludes inBroadcastLineup CableOrSatelliteService)
  :AXIOMS (/XMLNS/RDFS/label inBroadcastLineup "inBroadcastLineup"))

(DEFCONCEPT BankOrCreditUnion (?X)
  :DOCUMENTATION "Bank or credit union."
  :SUBSET-OF FinancialService
  :AXIOMS (/XMLNS/RDFS/label BankOrCreditUnion "BankOrCreditUnion"))

(DEFRELATION salaryCurrency (?X ?Y)
  :DOCUMENTATION "The currency (coded using <a href=\"http://en.wikipedia.org/wiki/ISO_4217\">ISO 4217</a> ) used for the main salary information in this job posting or for this employee."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label salaryCurrency "salaryCurrency")
  :AXIOMS (rangeIncludes salaryCurrency Text)
  :AXIOMS (domainIncludes salaryCurrency EmployeeRole)
  :AXIOMS (domainIncludes salaryCurrency JobPosting))

(ASSERT
 (AND (RestrictedDiet DiabeticDiet) (/XMLNS/RDFS/label DiabeticDiet "DiabeticDiet")
  (DOCUMENTATION DiabeticDiet "A diet appropriate for people with diabetes.")))

(ASSERT
 (AND (DayOfWeek Monday) (DOCUMENTATION Monday "The day of the week between Sunday and Tuesday.") (/XMLNS/RDFS/label Monday "Monday")
  (sameAs Monday |http://www.wikidata.org/entity/Q105|)))

(DEFRELATION thumbnailUrl (?X ?Y)
  :DOCUMENTATION "A thumbnail image relevant to the Thing."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes thumbnailUrl CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label thumbnailUrl "thumbnailUrl")
  :AXIOMS (rangeIncludes thumbnailUrl URL))

(DEFRELATION replyToUrl (?X ?Y)
  :DOCUMENTATION "The URL at which a reply may be posted to the specified UserComment."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes replyToUrl UserComments)
  :AXIOMS (rangeIncludes replyToUrl URL)
  :AXIOMS (/XMLNS/RDFS/label replyToUrl "replyToUrl"))

(DEFRELATION eligibleRegion (?X ?Y)
  :DOCUMENTATION "The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is valid.</p>

<p>See also <a class=\"localLink\" href=\"http://schema.org/ineligibleRegion\">ineligibleRegion</a>."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF areaServed
  :AXIOMS (domainIncludes eligibleRegion DeliveryChargeSpecification)
  :AXIOMS (/XMLNS/DC/source eligibleRegion |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (domainIncludes eligibleRegion Demand)
  :AXIOMS (/XMLNS/RDFS/label eligibleRegion "eligibleRegion")
  :AXIOMS (domainIncludes eligibleRegion Offer)
  :AXIOMS (rangeIncludes eligibleRegion Place)
  :AXIOMS (rangeIncludes eligibleRegion Text)
  :AXIOMS (rangeIncludes eligibleRegion GeoShape))

(DEFRELATION url (?X ?Y)
  :DOCUMENTATION "URL of the item."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes url Thing)
  :AXIOMS (rangeIncludes url URL)
  :AXIOMS (/XMLNS/RDFS/label url "url"))

(DEFCONCEPT WPAdBlock (?X)
  :DOCUMENTATION "An advertising section of the page."
  :SUBSET-OF WebPageElement
  :AXIOMS (/XMLNS/RDFS/label WPAdBlock "WPAdBlock"))

(DEFRELATION busName (?X ?Y)
  :DOCUMENTATION "The name of the bus (e.g. Bolt Express)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes busName BusTrip)
  :AXIOMS (/XMLNS/RDFS/label busName "busName")
  :AXIOMS (rangeIncludes busName Text))

(ASSERT
 (AND (OrderStatus OrderPickupAvailable) (DOCUMENTATION OrderPickupAvailable "OrderStatus representing availability of an order for pickup.")
  (/XMLNS/RDFS/label OrderPickupAvailable "OrderPickupAvailable")))

(DEFRELATION menu (?X ?Y)
  :DOCUMENTATION "Either the actual menu or a URL of the menu."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes menu Text)
  :AXIOMS (rangeIncludes menu URL)
  :AXIOMS (/XMLNS/RDFS/label menu "menu")
  :AXIOMS (domainIncludes menu FoodEstablishment))

(DEFCONCEPT AutoWash (?X)
  :DOCUMENTATION "A car wash business."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label AutoWash "AutoWash"))

(ASSERT
 (AND (MapCategoryType VenueMap) (/XMLNS/RDFS/label VenueMap "VenueMap")
  (DOCUMENTATION VenueMap "A venue map (e.g. for malls, auditoriums, museums, etc.).")))

(DEFRELATION isFamilyFriendly (?X ?Y)
  :DOCUMENTATION "Indicates whether this content is family friendly."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label isFamilyFriendly "isFamilyFriendly")
  :AXIOMS (domainIncludes isFamilyFriendly CreativeWork)
  :AXIOMS (rangeIncludes isFamilyFriendly Boolean))

(DEFRELATION servicePostalAddress (?X ?Y)
  :DOCUMENTATION "The address for accessing the service by mail."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes servicePostalAddress ServiceChannel)
  :AXIOMS (rangeIncludes servicePostalAddress PostalAddress)
  :AXIOMS (/XMLNS/RDFS/label servicePostalAddress "servicePostalAddress"))

(DEFRELATION driveWheelConfiguration (?X ?Y)
  :DOCUMENTATION "The drive wheel configuration, i.e. which roadwheels will receive torque from the vehicle's engine via the drivetrain."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source driveWheelConfiguration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes driveWheelConfiguration Text)
  :AXIOMS (/XMLNS/RDFS/label driveWheelConfiguration "driveWheelConfiguration")
  :AXIOMS (domainIncludes driveWheelConfiguration Vehicle)
  :AXIOMS (rangeIncludes driveWheelConfiguration DriveWheelConfigurationValue))

(DEFRELATION netWorth (?X ?Y)
  :DOCUMENTATION "The total financial value of the person as calculated by subtracting assets from liabilities."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label netWorth "netWorth")
  :AXIOMS (rangeIncludes netWorth PriceSpecification)
  :AXIOMS (rangeIncludes netWorth MonetaryAmount)
  :AXIOMS (domainIncludes netWorth Person))

(DEFCONCEPT LiteraryEvent (?X)
  :DOCUMENTATION "Event type: Literary event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label LiteraryEvent "LiteraryEvent"))

(DEFCONCEPT AutoDealer (?X)
  :DOCUMENTATION "An car dealership."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label AutoDealer "AutoDealer"))

(DEFCONCEPT RadioChannel (?X)
  :DOCUMENTATION "A unique instance of a radio BroadcastService on a CableOrSatelliteService lineup."
  :SUBSET-OF BroadcastChannel
  :AXIOMS (/XMLNS/RDFS/label RadioChannel "RadioChannel"))

(ASSERT
 (AND (BookFormatType AudiobookFormat) (/XMLNS/RDFS/label AudiobookFormat "AudiobookFormat")
  (DOCUMENTATION AudiobookFormat
   "Book format: Audiobook. This is an enumerated value for use with the bookFormat property. There is also a type 'Audiobook' in the bib extension which includes Audiobook specific properties.")))

(DEFCONCEPT EducationEvent (?X)
  :DOCUMENTATION "Event type: Education event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label EducationEvent "EducationEvent"))

(DEFCONCEPT Electrician (?X)
  :DOCUMENTATION "An electrician."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label Electrician "Electrician"))

(DEFRELATION embedUrl (?X ?Y)
  :DOCUMENTATION "A URL pointing to a player for a specific video. In general, this is the information in the <code>src</code> element of an <code>embed</code> tag and should not be the same as the content of the <code>loc</code> tag."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label embedUrl "embedUrl")
  :AXIOMS (rangeIncludes embedUrl URL)
  :AXIOMS (domainIncludes embedUrl MediaObject))

(DEFRELATION annualPercentageRate (?X ?Y)
  :DOCUMENTATION "The annual rate that is charged for borrowing (or made by investing), expressed as a single percentage number that represents the actual yearly cost of funds over the term of a loan. This includes any fees or additional costs associated with the transaction."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes annualPercentageRate QuantitativeValue)
  :AXIOMS (rangeIncludes annualPercentageRate Number)
  :AXIOMS (domainIncludes annualPercentageRate FinancialProduct)
  :AXIOMS (/XMLNS/DC/source annualPercentageRate |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label annualPercentageRate "annualPercentageRate"))

(DEFCONCEPT ShareAction (?X)
  :DOCUMENTATION "The act of distributing content to people for their amusement or edification."
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label ShareAction "ShareAction"))

(DEFCONCEPT ParkingFacility (?X)
  :DOCUMENTATION "A parking lot or other parking facility."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label ParkingFacility "ParkingFacility"))

(DEFRELATION affiliation (?X ?Y)
  :DOCUMENTATION "An organization that this person is affiliated with. For example, a school/university, a club, or a team."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF memberOf
  :AXIOMS (domainIncludes affiliation Person)
  :AXIOMS (/XMLNS/RDFS/label affiliation "affiliation")
  :AXIOMS (rangeIncludes affiliation Organization))

(DEFRELATION memberOf (?X ?Y)
  :DOCUMENTATION "An Organization (or ProgramMembership) to which this Person or Organization belongs."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes memberOf Organization)
  :AXIOMS (domainIncludes memberOf Person)
  :AXIOMS (inverseOf memberOf member)
  :AXIOMS (rangeIncludes memberOf Organization)
  :AXIOMS (rangeIncludes memberOf ProgramMembership)
  :AXIOMS (/XMLNS/RDFS/label memberOf "memberOf"))

(ASSERT
 (AND (ContactPointOption HearingImpairedSupported) (/XMLNS/RDFS/label HearingImpairedSupported "HearingImpairedSupported")
  (DOCUMENTATION HearingImpairedSupported "Uses devices to support users with hearing impairments.")))

(ASSERT
 (AND (OrderStatus OrderProcessing) (/XMLNS/RDFS/label OrderProcessing "OrderProcessing")
  (DOCUMENTATION OrderProcessing "OrderStatus representing that an order is being processed.")))

(DEFCONCEPT Locksmith (?X)
  :DOCUMENTATION "A locksmith."
  :SUBSET-OF HomeAndConstructionBusiness
  :AXIOMS (/XMLNS/RDFS/label Locksmith "Locksmith"))

(DEFCONCEPT GovernmentOrganization (?X)
  :DOCUMENTATION "A governmental organization or agency."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label GovernmentOrganization "GovernmentOrganization"))

(ASSERT
 (AND (MusicAlbumReleaseType AlbumRelease) (DOCUMENTATION AlbumRelease "AlbumRelease.") (/XMLNS/RDFS/label AlbumRelease "AlbumRelease")
  (/XMLNS/DC/source AlbumRelease |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)))

(DEFRELATION seasonNumber (?X ?Y)
  :DOCUMENTATION "Position of the season within an ordered group of seasons."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF position
  :AXIOMS (rangeIncludes seasonNumber Text)
  :AXIOMS (rangeIncludes seasonNumber Integer)
  :AXIOMS (/XMLNS/RDFS/label seasonNumber "seasonNumber")
  :AXIOMS (domainIncludes seasonNumber CreativeWorkSeason))

(DEFRELATION encodingType (?X ?Y)
  :DOCUMENTATION "The supported encoding type(s) for an EntryPoint request."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes encodingType Text)
  :AXIOMS (domainIncludes encodingType EntryPoint)
  :AXIOMS (/XMLNS/RDFS/label encodingType "encodingType"))

(DEFRELATION numberOfAirbags (?X ?Y)
  :DOCUMENTATION "The number or type of airbags in the vehicle."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numberOfAirbags "numberOfAirbags")
  :AXIOMS (domainIncludes numberOfAirbags Vehicle)
  :AXIOMS (/XMLNS/DC/source numberOfAirbags |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes numberOfAirbags Text)
  :AXIOMS (rangeIncludes numberOfAirbags Number))

(DEFRELATION geoMidpoint (?X ?Y)
  :DOCUMENTATION "Indicates the GeoCoordinates at the centre of a GeoShape e.g. GeoCircle."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes geoMidpoint GeoCoordinates)
  :AXIOMS (domainIncludes geoMidpoint GeoCircle)
  :AXIOMS (/XMLNS/RDFS/label geoMidpoint "geoMidpoint"))

(DEFRELATION valueMinLength (?X ?Y)
  :DOCUMENTATION "Specifies the minimum allowed range for number of characters in a literal value."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label valueMinLength "valueMinLength")
  :AXIOMS (rangeIncludes valueMinLength Number)
  :AXIOMS (domainIncludes valueMinLength PropertyValueSpecification))

(DEFRELATION sportsTeam (?X ?Y)
  :DOCUMENTATION "A sub property of participant. The sports team that participated on this action."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF participant
  :AXIOMS (domainIncludes sportsTeam ExerciseAction)
  :AXIOMS (rangeIncludes sportsTeam SportsTeam)
  :AXIOMS (/XMLNS/RDFS/label sportsTeam "sportsTeam"))

(ASSERT
 (AND (DigitalDocumentPermissionType ReadPermission) (DOCUMENTATION ReadPermission "Permission to read or view the document.")
  (/XMLNS/RDFS/label ReadPermission "ReadPermission")))

(DEFRELATION trainNumber (?X ?Y)
  :DOCUMENTATION "The unique identifier for the train."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label trainNumber "trainNumber")
  :AXIOMS (domainIncludes trainNumber TrainTrip)
  :AXIOMS (rangeIncludes trainNumber Text))

(DEFCONCEPT ApplyAction (?X)
  :DOCUMENTATION "<p>The act of registering to an organization/service without the guarantee to receive it.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/RegisterAction\">RegisterAction</a>: Unlike RegisterAction, ApplyAction has no guarantees that the application will be accepted.</li>
</ul>
"
  :SUBSET-OF OrganizeAction
  :AXIOMS (/XMLNS/RDFS/label ApplyAction "ApplyAction"))

(DEFRELATION box (?X ?Y)
  :DOCUMENTATION "A box is the area enclosed by the rectangle formed by two points. The first point is the lower corner, the second point is the upper corner. A box is expressed as two points separated by a space character."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label box "box")
  :AXIOMS (domainIncludes box GeoShape)
  :AXIOMS (rangeIncludes box Text))

(ASSERT
 (AND (ItemListOrderType ItemListOrderAscending) (DOCUMENTATION ItemListOrderAscending "An ItemList ordered with lower values listed first.")
  (/XMLNS/RDFS/label ItemListOrderAscending "ItemListOrderAscending")))

(ASSERT
 (AND (ReservationStatusType ReservationPending)
  (DOCUMENTATION ReservationPending "The status of a reservation when a request has been sent, but not confirmed.")
  (/XMLNS/RDFS/label ReservationPending "ReservationPending")))

(DEFCONCEPT Season (?X)
  :DOCUMENTATION "A media season e.g. tv, radio, video game etc."
  :SUBSET-OF CreativeWork
  :AXIOMS (supersededBy Season CreativeWorkSeason)
  :AXIOMS (/XMLNS/RDFS/label Season "Season"))

(DEFRELATION comment (?X ?Y)
  :DOCUMENTATION "Comments, typically from users."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes comment RsvpAction)
  :AXIOMS (rangeIncludes comment Comment)
  :AXIOMS (/XMLNS/RDFS/label comment "comment")
  :AXIOMS (domainIncludes comment CreativeWork))

(DEFRELATION requiresSubscription (?X ?Y)
  :DOCUMENTATION "Indicates if use of the media require a subscription  (either paid or free). Allowed values are <code>true</code> or <code>false</code> (note that an earlier version had 'yes', 'no')."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes requiresSubscription MediaObject)
  :AXIOMS (rangeIncludes requiresSubscription Boolean)
  :AXIOMS (/XMLNS/RDFS/label requiresSubscription "requiresSubscription"))

(DEFCONCEPT Series (?X)
  :DOCUMENTATION "A Series in schema.org is a group of related items, typically but not necessarily of the same kind."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Series "Series"))

(DEFRELATION itemListElement (?X ?Y)
  :DOCUMENTATION "For itemListElement values, you can use simple strings (e.g. \"Peter\", \"Paul\", \"Mary\"), existing entities, or use ListItem.</p>

<p>Text values are best if the elements in the list are plain strings. Existing entities are best for a simple, unordered list of existing things in your data. ListItem is used with ordered lists when you want to provide additional context about the element in that list or when the same item might be in different places in different lists.</p>

<p>Note: The order of elements in your mark-up is not sufficient for indicating the order or elements.  Use ListItem with a 'position' property in such cases."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes itemListElement ItemList)
  :AXIOMS (rangeIncludes itemListElement ListItem)
  :AXIOMS (/XMLNS/RDFS/label itemListElement "itemListElement")
  :AXIOMS (rangeIncludes itemListElement Thing)
  :AXIOMS (rangeIncludes itemListElement Text))

(DEFRELATION gtin12 (?X ?Y)
  :DOCUMENTATION "The <a href=\"http://apps.gs1.org/GDD/glossary/Pages/GTIN-12.aspx\">GTIN-12</a> code of the product, or the product to which the offer refers. The GTIN-12 is the 12-digit GS1 Identification Key composed of a U.P.C. Company Prefix, Item Reference, and Check Digit used to identify trade items. See <a href=\"http://www.gs1.org/barcodes/technical/idkeys/gtin\">GS1 GTIN Summary</a> for more details."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes gtin12 Offer)
  :AXIOMS (domainIncludes gtin12 Product)
  :AXIOMS (/XMLNS/RDFS/label gtin12 "gtin12")
  :AXIOMS (rangeIncludes gtin12 Text)
  :AXIOMS (domainIncludes gtin12 Demand))

(DEFCONCEPT ComputerStore (?X)
  :DOCUMENTATION "A computer store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label ComputerStore "ComputerStore"))

(ASSERT
 (AND (OfferItemCondition RefurbishedCondition) (/XMLNS/RDFS/label RefurbishedCondition "RefurbishedCondition")
  (DOCUMENTATION RefurbishedCondition "Indicates that the item is refurbished.")))

(ASSERT (AND (BookFormatType EBook) (DOCUMENTATION EBook "Book format: Ebook.") (/XMLNS/RDFS/label EBook "EBook")))

(ASSERT (AND (ItemAvailability InStock) (DOCUMENTATION InStock "Indicates that the item is in stock.") (/XMLNS/RDFS/label InStock "InStock")))

(ASSERT
 (AND (DigitalDocumentPermissionType CommentPermission) (DOCUMENTATION CommentPermission "Permission to add comments to the document.")
  (/XMLNS/RDFS/label CommentPermission "CommentPermission")))

(ASSERT
 (AND (DeliveryMethod OnSitePickup) (/XMLNS/RDFS/label OnSitePickup "OnSitePickup")
  (DOCUMENTATION OnSitePickup "A DeliveryMethod in which an item is collected on site, e.g. in a store or at a box office.")))

(DEFRELATION artEdition (?X ?Y)
  :DOCUMENTATION "The number of copies when multiple copies of a piece of artwork are produced - e.g. for a limited edition of 20 prints, 'artEdition' refers to the total number of copies (in this example \"20\")."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes artEdition Text)
  :AXIOMS (rangeIncludes artEdition Integer)
  :AXIOMS (/XMLNS/RDFS/label artEdition "artEdition")
  :AXIOMS (domainIncludes artEdition VisualArtwork))

(DEFRELATION messageAttachment (?X ?Y)
  :DOCUMENTATION "A CreativeWork attached to the message."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes messageAttachment Message)
  :AXIOMS (/XMLNS/RDFS/label messageAttachment "messageAttachment")
  :AXIOMS (rangeIncludes messageAttachment CreativeWork))

(DEFRELATION dayOfWeek (?X ?Y)
  :DOCUMENTATION "The day of the week for which these opening hours are valid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label dayOfWeek "dayOfWeek")
  :AXIOMS (domainIncludes dayOfWeek OpeningHoursSpecification)
  :AXIOMS (/XMLNS/DC/source dayOfWeek |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (rangeIncludes dayOfWeek DayOfWeek))

(DEFCONCEPT PawnShop (?X)
  :DOCUMENTATION "A shop that will buy, or lend money against the security of, personal possessions."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label PawnShop "PawnShop"))

(ASSERT
 (AND (ActionStatusType PotentialActionStatus) (DOCUMENTATION PotentialActionStatus "A description of an action that is supported.")
  (/XMLNS/RDFS/label PotentialActionStatus "PotentialActionStatus")))

(DEFCONCEPT SuspendAction (?X)
  :DOCUMENTATION "The act of momentarily pausing a device or application (e.g. pause music playback or pause a timer)."
  :SUBSET-OF ControlAction
  :AXIOMS (/XMLNS/RDFS/label SuspendAction "SuspendAction"))

(DEFCONCEPT ArtGallery (?X)
  :DOCUMENTATION "An art gallery."
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label ArtGallery "ArtGallery"))

(DEFCONCEPT CatholicChurch (?X)
  :DOCUMENTATION "A Catholic church."
  :SUBSET-OF PlaceOfWorship
  :AXIOMS (/XMLNS/RDFS/label CatholicChurch "CatholicChurch"))

(DEFCONCEPT ProfilePage (?X)
  :DOCUMENTATION "Web page type: Profile page."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label ProfilePage "ProfilePage"))

(DEFCONCEPT Waterfall (?X)
  :DOCUMENTATION "A waterfall, like Niagara."
  :SUBSET-OF BodyOfWater
  :AXIOMS (/XMLNS/RDFS/label Waterfall "Waterfall"))

(ASSERT
 (AND (BoardingPolicyType GroupBoardingPolicy) (/XMLNS/RDFS/label GroupBoardingPolicy "GroupBoardingPolicy")
  (DOCUMENTATION GroupBoardingPolicy "The airline boards by groups based on check-in time, priority, etc.")))

(DEFRELATION artform (?X ?Y)
  :DOCUMENTATION "e.g. Painting, Drawing, Sculpture, Print, Photograph, Assemblage, Collage, etc."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes artform VisualArtwork)
  :AXIOMS (/XMLNS/RDFS/label artform "artform")
  :AXIOMS (rangeIncludes artform URL)
  :AXIOMS (rangeIncludes artform Text))

(DEFRELATION contactOption (?X ?Y)
  :DOCUMENTATION "An option available on this contact point (e.g. a toll-free number or support for hearing-impaired callers)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contactOption "contactOption")
  :AXIOMS (rangeIncludes contactOption ContactPointOption)
  :AXIOMS (domainIncludes contactOption ContactPoint))

(DEFCONCEPT BusReservation (?X)
  :DOCUMENTATION "A reservation for bus travel. </p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use <a class=\"localLink\" href=\"http://schema.org/Offer\">Offer</a>."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label BusReservation "BusReservation"))

(DEFRELATION contactPoints (?X ?Y)
  :DOCUMENTATION "A contact point for a person or organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contactPoints "contactPoints")
  :AXIOMS (supersededBy contactPoints contactPoint)
  :AXIOMS (domainIncludes contactPoints Person)
  :AXIOMS (rangeIncludes contactPoints ContactPoint)
  :AXIOMS (domainIncludes contactPoints Organization))

(DEFRELATION contactPoint (?X ?Y)
  :DOCUMENTATION "A contact point for a person or organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contactPoint "contactPoint")
  :AXIOMS (rangeIncludes contactPoint ContactPoint)
  :AXIOMS (domainIncludes contactPoint Person)
  :AXIOMS (domainIncludes contactPoint Organization))

(ASSERT (AND (MapCategoryType SeatingMap) (DOCUMENTATION SeatingMap "A seating map.") (/XMLNS/RDFS/label SeatingMap "SeatingMap")))

(DEFRELATION eligibleCustomerType (?X ?Y)
  :DOCUMENTATION "The type(s) of customers for which the given offer is valid."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source eligibleCustomerType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label eligibleCustomerType "eligibleCustomerType")
  :AXIOMS (domainIncludes eligibleCustomerType Offer)
  :AXIOMS (domainIncludes eligibleCustomerType Demand)
  :AXIOMS (rangeIncludes eligibleCustomerType BusinessEntityType))

(DEFRELATION accountId (?X ?Y)
  :DOCUMENTATION "The identifier for the account the payment will be applied to."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes accountId Text)
  :AXIOMS (/XMLNS/RDFS/label accountId "accountId")
  :AXIOMS (domainIncludes accountId Invoice))

(ASSERT
 (AND (DriveWheelConfigurationValue AllWheelDriveConfiguration)
  (/XMLNS/DC/source AllWheelDriveConfiguration |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  (DOCUMENTATION AllWheelDriveConfiguration "All-wheel Drive is a transmission layout where the engine drives all four wheels.")
  (/XMLNS/RDFS/label AllWheelDriveConfiguration "AllWheelDriveConfiguration")))

(DEFRELATION seatingType (?X ?Y)
  :DOCUMENTATION "The type/class of the seat."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label seatingType "seatingType")
  :AXIOMS (rangeIncludes seatingType QualitativeValue)
  :AXIOMS (rangeIncludes seatingType Text)
  :AXIOMS (domainIncludes seatingType Seat))

(DEFRELATION musicArrangement (?X ?Y)
  :DOCUMENTATION "An arrangement derived from the composition."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label musicArrangement "musicArrangement")
  :AXIOMS (/XMLNS/DC/source musicArrangement |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (domainIncludes musicArrangement MusicComposition)
  :AXIOMS (rangeIncludes musicArrangement MusicComposition))

(DEFCONCEPT UserPlusOnes (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (supersededBy UserPlusOnes InteractionCounter)
  :AXIOMS (/XMLNS/RDFS/label UserPlusOnes "UserPlusOnes"))

(DEFCONCEPT InsuranceAgency (?X)
  :DOCUMENTATION "An Insurance agency."
  :SUBSET-OF FinancialService
  :AXIOMS (/XMLNS/RDFS/label InsuranceAgency "InsuranceAgency"))

(DEFCONCEPT DownloadAction (?X)
  :DOCUMENTATION "The act of downloading an object."
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label DownloadAction "DownloadAction"))

(DEFCONCEPT HairSalon (?X)
  :DOCUMENTATION "A hair salon."
  :SUBSET-OF HealthAndBeautyBusiness
  :AXIOMS (/XMLNS/RDFS/label HairSalon "HairSalon"))

(DEFCONCEPT ElectronicsStore (?X)
  :DOCUMENTATION "An electronics store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label ElectronicsStore "ElectronicsStore"))

(DEFCONCEPT DryCleaningOrLaundry (?X)
  :DOCUMENTATION "A dry-cleaning business."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label DryCleaningOrLaundry "DryCleaningOrLaundry"))

(DEFCONCEPT ComedyClub (?X)
  :DOCUMENTATION "A comedy club."
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label ComedyClub "ComedyClub"))

(ASSERT (AND (ItemAvailability SoldOut) (DOCUMENTATION SoldOut "Indicates that the item has sold out.") (/XMLNS/RDFS/label SoldOut "SoldOut")))

(DEFCONCEPT AutoPartsStore (?X)
  :DOCUMENTATION "An auto parts store."
  :SUBSET-OF Store
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label AutoPartsStore "AutoPartsStore"))

(DEFCONCEPT Physician (?X)
  :DOCUMENTATION "A doctor's office."
  :SUBSET-OF MedicalOrganization
  :AXIOMS (/XMLNS/RDFS/label Physician "Physician"))

(DEFRELATION endTime (?X ?Y)
  :DOCUMENTATION "The endTime of something. For a reserved event or service (e.g. FoodEstablishmentReservation), the time that it is expected to end. For actions that span a period of time, when the action was performed. e.g. John wrote a book from January to <em>December</em>.</p>

<p>Note that Event uses startDate/endDate instead of startTime/endTime, even when describing dates with times. This situation may be clarified in future revisions."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes endTime DateTime)
  :AXIOMS (domainIncludes endTime FoodEstablishmentReservation)
  :AXIOMS (/XMLNS/RDFS/label endTime "endTime")
  :AXIOMS (domainIncludes endTime Action))

(DEFCONCEPT MotorcycleDealer (?X)
  :DOCUMENTATION "A motorcycle dealer."
  :SUBSET-OF AutomotiveBusiness
  :AXIOMS (/XMLNS/RDFS/label MotorcycleDealer "MotorcycleDealer"))

(DEFCONCEPT JewelryStore (?X)
  :DOCUMENTATION "A jewelry store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label JewelryStore "JewelryStore"))

(DEFCONCEPT CheckoutPage (?X)
  :DOCUMENTATION "Web page type: Checkout page."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label CheckoutPage "CheckoutPage"))

(DEFRELATION broadcastServiceTier (?X ?Y)
  :DOCUMENTATION "The type of service required to have access to the channel (e.g. Standard or Premium)."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label broadcastServiceTier "broadcastServiceTier")
  :AXIOMS (domainIncludes broadcastServiceTier BroadcastChannel)
  :AXIOMS (rangeIncludes broadcastServiceTier Text))

(DEFRELATION programMembershipUsed (?X ?Y)
  :DOCUMENTATION "Any membership in a frequent flyer, hotel loyalty program, etc. being applied to the reservation."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes programMembershipUsed ProgramMembership)
  :AXIOMS (domainIncludes programMembershipUsed Reservation)
  :AXIOMS (/XMLNS/RDFS/label programMembershipUsed "programMembershipUsed"))

(DEFRELATION dateSent (?X ?Y)
  :DOCUMENTATION "The date/time at which the message was sent."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label dateSent "dateSent")
  :AXIOMS (domainIncludes dateSent Message)
  :AXIOMS (rangeIncludes dateSent DateTime))

(DEFRELATION numAdults (?X ?Y)
  :DOCUMENTATION "The number of adults staying in the unit."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numAdults "numAdults")
  :AXIOMS (domainIncludes numAdults LodgingReservation)
  :AXIOMS (rangeIncludes numAdults QuantitativeValue)
  :AXIOMS (rangeIncludes numAdults Integer))

(DEFRELATION broadcastOfEvent (?X ?Y)
  :DOCUMENTATION "The event being broadcast such as a sporting event or awards ceremony."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label broadcastOfEvent "broadcastOfEvent")
  :AXIOMS (rangeIncludes broadcastOfEvent Event)
  :AXIOMS (domainIncludes broadcastOfEvent BroadcastEvent))

(DEFRELATION reportNumber (?X ?Y)
  :DOCUMENTATION "The number or other unique designator assigned to a Report by the publishing organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label reportNumber "reportNumber")
  :AXIOMS (rangeIncludes reportNumber Text)
  :AXIOMS (domainIncludes reportNumber Report))

(ASSERT
 (AND (ActionStatusType ActiveActionStatus)
  (DOCUMENTATION ActiveActionStatus "An in-progress action (e.g, while watching the movie, or driving to a location).")
  (/XMLNS/RDFS/label ActiveActionStatus "ActiveActionStatus")))

(DEFRELATION modifiedTime (?X ?Y)
  :DOCUMENTATION "The date and time the reservation was modified."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes modifiedTime DateTime)
  :AXIOMS (domainIncludes modifiedTime Reservation)
  :AXIOMS (/XMLNS/RDFS/label modifiedTime "modifiedTime"))

(DEFCONCEPT QuoteAction (?X)
  :DOCUMENTATION "An agent quotes/estimates/appraises an object/product/service with a price at a location/store."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label QuoteAction "QuoteAction"))

(ASSERT
 (AND (GameServerStatus OfflineTemporarily)
  (DOCUMENTATION OfflineTemporarily "Game server status: OfflineTemporarily. Server is offline now but it can be online soon.")
  (/XMLNS/RDFS/label OfflineTemporarily "OfflineTemporarily")))

(DEFCONCEPT MusicVenue (?X)
  :DOCUMENTATION "A music venue."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label MusicVenue "MusicVenue"))

(ASSERT
 (AND (DayOfWeek Friday) (DOCUMENTATION Friday "The day of the week between Thursday and Saturday.") (/XMLNS/RDFS/label Friday "Friday")
  (sameAs Friday |http://www.wikidata.org/entity/Q130|)))

(ASSERT
 (AND (MusicAlbumProductionType SpokenWordAlbum) (/XMLNS/DC/source SpokenWordAlbum |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (/XMLNS/RDFS/label SpokenWordAlbum "SpokenWordAlbum") (DOCUMENTATION SpokenWordAlbum "SpokenWordAlbum.")))

(DEFCONCEPT BeautySalon (?X)
  :DOCUMENTATION "Beauty salon."
  :SUBSET-OF HealthAndBeautyBusiness
  :AXIOMS (/XMLNS/RDFS/label BeautySalon "BeautySalon"))

(DEFRELATION dropoffLocation (?X ?Y)
  :DOCUMENTATION "Where a rental car can be dropped off."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes dropoffLocation Place)
  :AXIOMS (/XMLNS/RDFS/label dropoffLocation "dropoffLocation")
  :AXIOMS (domainIncludes dropoffLocation RentalCarReservation))

(ASSERT
 (AND (GamePlayMode MultiPlayer) (/XMLNS/RDFS/label MultiPlayer "MultiPlayer")
  (DOCUMENTATION MultiPlayer "Play mode: MultiPlayer. Requiring or allowing multiple human players to play simultaneously.")))

(DEFCONCEPT Zoo (?X)
  :DOCUMENTATION "A zoo."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Zoo "Zoo"))

(ASSERT (AND (Audience Researcher) (DOCUMENTATION Researcher "Researchers.") (/XMLNS/RDFS/label Researcher "Researcher")))

(ASSERT
 (AND (OrderStatus OrderReturned) (/XMLNS/RDFS/label OrderReturned "OrderReturned")
  (DOCUMENTATION OrderReturned "OrderStatus representing that an order has been returned.")))

(DEFRELATION numberOfAxles (?X ?Y)
  :DOCUMENTATION "The number of axles.</p>

<p>Typical unit code(s): C62"
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label numberOfAxles "numberOfAxles")
  :AXIOMS (domainIncludes numberOfAxles Vehicle)
  :AXIOMS (/XMLNS/DC/source numberOfAxles |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (rangeIncludes numberOfAxles QuantitativeValue)
  :AXIOMS (rangeIncludes numberOfAxles Number))

(ASSERT
 (AND (MusicReleaseFormatType CassetteFormat) (/XMLNS/DC/source CassetteFormat |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  (/XMLNS/RDFS/label CassetteFormat "CassetteFormat") (DOCUMENTATION CassetteFormat "CassetteFormat.")))

(DEFRELATION ratingCount (?X ?Y)
  :DOCUMENTATION "The count of total number of ratings."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes ratingCount Integer)
  :AXIOMS (/XMLNS/RDFS/label ratingCount "ratingCount")
  :AXIOMS (domainIncludes ratingCount AggregateRating))

(ASSERT
 (AND (RestrictedDiet LowLactoseDiet) (DOCUMENTATION LowLactoseDiet "A diet appropriate for people with lactose intolerance.")
  (/XMLNS/RDFS/label LowLactoseDiet "LowLactoseDiet")))

(DEFCONCEPT IgnoreAction (?X)
  :DOCUMENTATION "The act of intentionally disregarding the object. An agent ignores an object."
  :SUBSET-OF AssessAction
  :AXIOMS (/XMLNS/RDFS/label IgnoreAction "IgnoreAction"))

(DEFCONCEPT BookSeries (?X)
  :DOCUMENTATION "A series of books. Included books can be indicated with the hasPart property."
  :SUBSET-OF CreativeWorkSeries
  :AXIOMS (/XMLNS/RDFS/label BookSeries "BookSeries"))

(DEFRELATION relatedLink (?X ?Y)
  :DOCUMENTATION "A link related to this web page, for example to other related web pages."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label relatedLink "relatedLink")
  :AXIOMS (domainIncludes relatedLink WebPage)
  :AXIOMS (rangeIncludes relatedLink URL))

(ASSERT
 (AND (BoardingPolicyType ZoneBoardingPolicy) (/XMLNS/RDFS/label ZoneBoardingPolicy "ZoneBoardingPolicy")
  (DOCUMENTATION ZoneBoardingPolicy "The airline boards by zones of the plane.")))

(DEFCONCEPT UserLikes (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (supersededBy UserLikes InteractionCounter)
  :AXIOMS (/XMLNS/RDFS/label UserLikes "UserLikes"))

(DEFRELATION isConsumableFor (?X ?Y)
  :DOCUMENTATION "A pointer to another product (or multiple products) for which this product is a consumable."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes isConsumableFor Product)
  :AXIOMS (rangeIncludes isConsumableFor Product)
  :AXIOMS (/XMLNS/DC/source isConsumableFor |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (/XMLNS/RDFS/label isConsumableFor "isConsumableFor"))

(DEFCONCEPT ReserveAction (?X)
  :DOCUMENTATION "<p>Reserving a concrete object.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/ScheduleAction\">ScheduleAction</a></a>: Unlike ScheduleAction, ReserveAction reserves concrete objects (e.g. a table, a hotel) towards a time slot / spatial allocation.</li>
</ul>
"
  :SUBSET-OF PlanAction
  :AXIOMS (/XMLNS/RDFS/label ReserveAction "ReserveAction"))

(DEFRELATION serviceSmsNumber (?X ?Y)
  :DOCUMENTATION "The number to access the service by text message."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes serviceSmsNumber ContactPoint)
  :AXIOMS (domainIncludes serviceSmsNumber ServiceChannel)
  :AXIOMS (/XMLNS/RDFS/label serviceSmsNumber "serviceSmsNumber"))

(DEFCONCEPT TakeAction (?X)
  :DOCUMENTATION "<p>The act of gaining ownership of an object from an origin. Reciprocal of GiveAction.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/GiveAction\">GiveAction</a>: The reciprocal of TakeAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/ReceiveAction\">ReceiveAction</a>: Unlike ReceiveAction, TakeAction implies that ownership has been transfered.</li>
</ul>
"
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label TakeAction "TakeAction"))

(ASSERT
 (AND (RestrictedDiet HinduDiet) (/XMLNS/RDFS/label HinduDiet "HinduDiet")
  (DOCUMENTATION HinduDiet "A diet conforming to Hindu dietary practices, in particular, beef-free.")))

(ASSERT
 (AND (RestrictedDiet GlutenFreeDiet) (/XMLNS/RDFS/label GlutenFreeDiet "GlutenFreeDiet") (DOCUMENTATION GlutenFreeDiet "A diet exclusive of gluten.")))

(ASSERT
 (AND (PaymentStatusType PaymentDue) (DOCUMENTATION PaymentDue "The payment is due, but still within an acceptable time to be received.")
  (/XMLNS/RDFS/label PaymentDue "PaymentDue")))

(ASSERT (AND (BookFormatType Hardcover) (/XMLNS/RDFS/label Hardcover "Hardcover") (DOCUMENTATION Hardcover "Book format: Hardcover.")))

(ASSERT
 (AND (RestrictedDiet LowSaltDiet) (DOCUMENTATION LowSaltDiet "A diet focused on reduced sodium intake.") (/XMLNS/RDFS/label LowSaltDiet "LowSaltDiet")))

(ASSERT
 (AND (RestrictedDiet KosherDiet) (DOCUMENTATION KosherDiet "A diet conforming to Jewish dietary practices.") (/XMLNS/RDFS/label KosherDiet "KosherDiet")))

(ASSERT
 (AND (EventStatusType EventRescheduled) (/XMLNS/RDFS/label EventRescheduled "EventRescheduled")
  (DOCUMENTATION EventRescheduled
   "The event has been rescheduled. The event's previousStartDate should be set to the old date and the startDate should be set to the event's new date. (If the event has been rescheduled multiple times, the previousStartDate property may be repeated).")))

(DEFCONCEPT HomeGoodsStore (?X)
  :DOCUMENTATION "A home goods store."
  :SUBSET-OF Store
  :AXIOMS (/XMLNS/RDFS/label HomeGoodsStore "HomeGoodsStore"))

(ASSERT (AND (GameServerStatus Online) (/XMLNS/RDFS/label Online "Online") (DOCUMENTATION Online "Game server status: Online. Server is available.")))

(DEFCONCEPT LandmarksOrHistoricalBuildings (?X)
  :DOCUMENTATION "An historical landmark or building."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label LandmarksOrHistoricalBuildings "LandmarksOrHistoricalBuildings"))

(DEFCONCEPT WarrantyScope (?X)
  :DOCUMENTATION "<p>A range of of services that will be provided to a customer free of charge in case of a defect or malfunction of a product.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#Labor-BringIn</li>
<li>http://purl.org/goodrelations/v1#PartsAndLabor-BringIn</li>
<li>http://purl.org/goodrelations/v1#PartsAndLabor-PickUp</li>
</ul>
"
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label WarrantyScope "WarrantyScope")
  :AXIOMS (/XMLNS/DC/source WarrantyScope |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFRELATION releaseOf (?X ?Y)
  :DOCUMENTATION "The album this is a release of."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes releaseOf MusicRelease)
  :AXIOMS (inverseOf releaseOf albumRelease)
  :AXIOMS (rangeIncludes releaseOf MusicAlbum)
  :AXIOMS (/XMLNS/DC/source releaseOf |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label releaseOf "releaseOf"))

(DEFRELATION albumRelease (?X ?Y)
  :DOCUMENTATION "A release of this album."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes albumRelease MusicRelease)
  :AXIOMS (inverseOf albumRelease releaseOf)
  :AXIOMS (domainIncludes albumRelease MusicAlbum)
  :AXIOMS (/XMLNS/RDFS/label albumRelease "albumRelease"))

(DEFRELATION alumni (?X ?Y)
  :DOCUMENTATION "Alumni of an organization."
  ;:BINARY-RELATION TRUE
  :AXIOMS (inverseOf alumni alumniOf)
  :AXIOMS (/XMLNS/RDFS/label alumni "alumni")
  :AXIOMS (rangeIncludes alumni Person)
  :AXIOMS (domainIncludes alumni EducationalOrganization)
  :AXIOMS (domainIncludes alumni Organization))

(DEFRELATION alumniOf (?X ?Y)
  :DOCUMENTATION "An organization that the person is an alumni of."
  ;:BINARY-RELATION TRUE
  :AXIOMS (inverseOf alumniOf alumni)
  :AXIOMS (domainIncludes alumniOf Person)
  :AXIOMS (rangeIncludes alumniOf Organization)
  :AXIOMS (rangeIncludes alumniOf EducationalOrganization)
  :AXIOMS (/XMLNS/RDFS/label alumniOf "alumniOf"))

(DEFCONCEPT PublicationIssue (?X)
  :DOCUMENTATION "A part of a successively published publication such as a periodical or publication volume, often numbered, usually containing a grouping of works such as articles.</p>

<p><a href=\"http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html\">blog post</a>."
  :EQUIVALENT-RELATION |http://purl.org/ontology/bibo/Issue|
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/DC/source PublicationIssue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label PublicationIssue "PublicationIssue"))

(DEFCONCEPT PerformAction (?X)
  :DOCUMENTATION "The act of participating in performance arts."
  :SUBSET-OF PlayAction
  :AXIOMS (/XMLNS/RDFS/label PerformAction "PerformAction"))

(DEFCONCEPT FlightReservation (?X)
  :DOCUMENTATION "A reservation for air travel.</p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use <a class=\"localLink\" href=\"http://schema.org/Offer\">Offer</a>."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label FlightReservation "FlightReservation"))

(DEFCONCEPT SoftwareSourceCode (?X)
  :DOCUMENTATION "Computer programming source code. Example: Full (compile ready) solutions, code snippet samples, scripts, templates."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label SoftwareSourceCode "SoftwareSourceCode"))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|)
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|
   "This class contains derivatives of IPTC rNews properties. rNews is a data model of publishing metadata with serializations currently available for RDFa as well as HTML5 Microdata. More information about the IPTC and rNews can be found at <a href=\"http://rnews.org\">rnews.org</a>.")
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews| "rNews")))

(DEFRELATION mainEntity (?X ?Y)
  :DOCUMENTATION "Indicates the primary entity described in some page or other CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label mainEntity "mainEntity")
  :AXIOMS (inverseOf mainEntity mainEntityOfPage)
  :AXIOMS (rangeIncludes mainEntity Thing)
  :AXIOMS (domainIncludes mainEntity CreativeWork))

(DEFRELATION mainEntityOfPage (?X ?Y)
  :DOCUMENTATION "Indicates a page (or other CreativeWork) for which this thing is the main entity being described. See <a href=\"/docs/datamodel.html#mainEntityBackground\">background notes</a> for details."
  ;:BINARY-RELATION TRUE
  :AXIOMS (inverseOf mainEntityOfPage mainEntity)
  :AXIOMS (rangeIncludes mainEntityOfPage CreativeWork)
  :AXIOMS (domainIncludes mainEntityOfPage Thing)
  :AXIOMS (rangeIncludes mainEntityOfPage URL)
  :AXIOMS (/XMLNS/RDFS/label mainEntityOfPage "mainEntityOfPage"))

(DEFCONCEPT Airline (?X)
  :DOCUMENTATION "An organization that provides flights for passengers."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label Airline "Airline"))

(DEFCONCEPT AchieveAction (?X)
  :DOCUMENTATION "The act of accomplishing something via previous efforts. It is an instantaneous action rather than an ongoing process."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label AchieveAction "AchieveAction"))

(DEFCONCEPT ReviewAction (?X)
  :DOCUMENTATION "The act of producing a balanced opinion about the object for an audience. An agent reviews an object with participants resulting in a review."
  :SUBSET-OF AssessAction
  :AXIOMS (/XMLNS/RDFS/label ReviewAction "ReviewAction"))

(DEFCONCEPT EventStatusType (?X)
  :DOCUMENTATION "EventStatusType is an enumeration type whose instances represent several states that an Event may be in."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label EventStatusType "EventStatusType"))

(DEFCONCEPT Seat (?X)
  :DOCUMENTATION "Used to describe a seat, such as a reserved seat in an event reservation."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Seat "Seat"))

(DEFCONCEPT Course (?X)
  :DOCUMENTATION "A description of an educational course which may be offered as distinct instances at which take place at different times or take place at different locations, or be offered through different media or modes of study. An educational course is a sequence of one or more educational events and/or creative works which aims to build knowledge, competence or ability of learners."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Course "Course"))

(DEFCONCEPT MovieTheater (?X)
  :DOCUMENTATION "A movie theater."
  :SUBSET-OF CivicStructure
  :SUBSET-OF EntertainmentBusiness
  :AXIOMS (/XMLNS/RDFS/label MovieTheater "MovieTheater"))

(DEFCONCEPT EducationalAudience (?X)
  :DOCUMENTATION "An EducationalAudience."
  :SUBSET-OF Audience
  :AXIOMS (/XMLNS/DC/source EducationalAudience |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_LRMIClass|)
  :AXIOMS (/XMLNS/RDFS/label EducationalAudience "EducationalAudience"))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_LRMIClass|)
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_LRMIClass|
   "This class is based on the work of the LRMI project, see lrmi.net for details.")
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_LRMIClass| "LRMIClass")))

(DEFCONCEPT UnitPriceSpecification (?X)
  :DOCUMENTATION "The price asked for a given offer by the respective organization or person."
  :SUBSET-OF PriceSpecification
  :AXIOMS (/XMLNS/DC/source UnitPriceSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label UnitPriceSpecification "UnitPriceSpecification"))

(DEFCONCEPT Person (?X)
  :DOCUMENTATION "A person (alive, dead, undead, or fictional)."
  :EQUIVALENT-RELATION |http://xmlns.com/foaf/0.1/Person|
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/DC/source Person |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|)
  :AXIOMS (/XMLNS/RDFS/label Person "Person"))

(DEFCONCEPT DigitalDocumentPermission (?X)
  :DOCUMENTATION "A permission for a particular person or group to access a particular file."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label DigitalDocumentPermission "DigitalDocumentPermission"))

(DEFCONCEPT PaymentCard (?X)
  :DOCUMENTATION "A payment method using a credit, debit, store or other card to associate the payment with an account."
  :SUBSET-OF PaymentMethod
  :SUBSET-OF FinancialProduct
  :AXIOMS (/XMLNS/DC/source PaymentCard |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label PaymentCard "PaymentCard"))

(DEFRELATION superEvent (?X ?Y)
  :DOCUMENTATION "An event that this event is a part of. For example, a collection of individual music performances might each have a music festival as their superEvent."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes superEvent Event)
  :AXIOMS (rangeIncludes superEvent Event)
  :AXIOMS (/XMLNS/RDFS/label superEvent "superEvent")
  :AXIOMS (inverseOf superEvent subEvent))

(DEFCONCEPT Question (?X)
  :DOCUMENTATION "A specific question - e.g. from a user seeking answers online, or collected in a Frequently Asked Questions (FAQ) document."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Question "Question")
  :AXIOMS (/XMLNS/DC/source Question |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_QAStackExchange|))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_QAStackExchange|)
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_QAStackExchange|
   "The Question/Answer types were <a href=\"https://www.w3.org/wiki/WebSchemas/QASchemaResearch\">based on</a> the Stack Overflow API.")
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_QAStackExchange| "Stack Exchange")))

(DEFCONCEPT UpdateAction (?X)
  :DOCUMENTATION "The act of managing by changing/editing the state of the object."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label UpdateAction "UpdateAction"))

(DEFCONCEPT EntryPoint (?X)
  :DOCUMENTATION "An entry point, within some Web-based protocol."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label EntryPoint "EntryPoint")
  :AXIOMS (/XMLNS/DC/source EntryPoint |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_ActionCollabClass|))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_ActionCollabClass|)
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_ActionCollabClass| "ActionCollabClass")
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_ActionCollabClass|
   "The schema.org Actions mechanism benefited from extensive discussions across the Web standards community around W3C, in particular from the <a href=\"http://purl.org/hydra/\">Hydra project</a>'s community group.")))

(DEFCONCEPT BusinessFunction (?X)
  :DOCUMENTATION "<p>The business function specifies the type of activity or access (i.e., the bundle of rights) offered by the organization or business person through the offer. Typical are sell, rental or lease, maintenance or repair, manufacture / produce, recycle / dispose, engineering / construction, or installation. Proprietary specifications of access rights are also instances of this class.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#ConstructionInstallation</li>
<li>http://purl.org/goodrelations/v1#Dispose</li>
<li>http://purl.org/goodrelations/v1#LeaseOut</li>
<li>http://purl.org/goodrelations/v1#Maintain</li>
<li>http://purl.org/goodrelations/v1#ProvideService</li>
<li>http://purl.org/goodrelations/v1#Repair</li>
<li>http://purl.org/goodrelations/v1#Sell</li>
<li>http://purl.org/goodrelations/v1#Buy</li>
</ul>
"
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/DC/source BusinessFunction |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label BusinessFunction "BusinessFunction"))

(DEFCONCEPT FindAction (?X)
  :DOCUMENTATION "<p>TThe act of finding an object.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/SearchAction\">SearchAction</a>: FindAction is generally lead by a SearchAction, but not necessarily.</li>
</ul>
"
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label FindAction "FindAction"))

(DEFCONCEPT ActionStatusType (?X)
  :DOCUMENTATION "The status of an Action."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label ActionStatusType "ActionStatusType"))

(DEFCONCEPT MonetaryAmount (?X)
  :DOCUMENTATION "A monetary value or range. This type can be used to describe an amount of money such as $50 USD, or a range as in describing a bank account being suitable for a balance between £1,000 and £1,000,000 GBP, or the value of a salary, etc. It is recommended to use <a class=\"localLink\" href=\"http://schema.org/PriceSpecification\">PriceSpecification</a> Types to describe the price of an Offer, Invoice, etc."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label MonetaryAmount "MonetaryAmount")
  :AXIOMS (/XMLNS/DC/source MonetaryAmount |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|))

(DEFCONCEPT Demand (?X)
  :DOCUMENTATION "A demand entity represents the public, not necessarily binding, not necessarily exclusive, announcement by an organization or person to seek a certain type of goods or services. For describing demand using this type, the very same properties used for Offer apply."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/DC/source Demand |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label Demand "Demand"))

(DEFRELATION workExample (?X ?Y)
  :DOCUMENTATION "Example/instance/realization/derivation of the concept of this creative work. eg. The paperback edition, first edition, or eBook."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes workExample CreativeWork)
  :AXIOMS (/XMLNS/DC/source workExample |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label workExample "workExample")
  :AXIOMS (inverseOf workExample exampleOfWork)
  :AXIOMS (domainIncludes workExample CreativeWork))

(DEFRELATION exampleOfWork (?X ?Y)
  :DOCUMENTATION "A creative work that this work is an example/instance/realization/derivation of."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/DC/source exampleOfWork |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label exampleOfWork "exampleOfWork")
  :AXIOMS (inverseOf exampleOfWork workExample)
  :AXIOMS (domainIncludes exampleOfWork CreativeWork)
  :AXIOMS (rangeIncludes exampleOfWork CreativeWork))

(DEFCONCEPT BlogPosting (?X)
  :DOCUMENTATION "A blog post."
  :SUBSET-OF SocialMediaPosting
  :AXIOMS (/XMLNS/RDFS/label BlogPosting "BlogPosting"))

(DEFCONCEPT Game (?X)
  :DOCUMENTATION "The Game type represents things which are games. These are typically rule-governed recreational activities, e.g. role-playing games in which players assume the role of characters in a fictional setting."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Game "Game"))

(DEFCONCEPT TVSeason (?X)
  :DOCUMENTATION "Season dedicated to TV broadcast and associated online delivery."
  :SUBSET-OF CreativeWorkSeason
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label TVSeason "TVSeason"))

(DEFCONCEPT CreativeWorkSeries (?X)
  :DOCUMENTATION "A CreativeWorkSeries in schema.org is a group of related items, typically but not necessarily of the same kind. CreativeWorkSeries are usually organized into some order, often chronological. Unlike <a class=\"localLink\" href=\"http://schema.org/ItemList\">ItemList</a> which is a general purpose data structure for lists of things, the emphasis with CreativeWorkSeries is on published materials (written e.g. books and periodicals, or media such as tv, radio and games).</p>

<p>Specific subtypes are available for describing <a class=\"localLink\" href=\"http://schema.org/TVSeries\">TVSeries</a>, <a class=\"localLink\" href=\"http://schema.org/RadioSeries\">RadioSeries</a>, <a class=\"localLink\" href=\"http://schema.org/MovieSeries\">MovieSeries</a>, <a class=\"localLink\" href=\"http://schema.org/BookSeries\">BookSeries</a>, <a class=\"localLink\" href=\"http://schema.org/Periodical\">Periodical</a> and <a class=\"localLink\" href=\"http://schema.org/VideoGameSeries\">VideoGameSeries</a>. In each case, the <a class=\"localLink\" href=\"http://schema.org/hasPart\">hasPart</a> / <a class=\"localLink\" href=\"http://schema.org/isPartOf\">isPartOf</a> properties can be used to relate the CreativeWorkSeries to its parts. The general CreativeWorkSeries type serves largely just to organize these more specific and practical subtypes.</p>

<p>It is common for properties applicable to an item from the series to be usefully applied to the containing group. Schema.org attempts to anticipate some of these cases, but publishers should be free to apply properties of the series parts to the series as a whole wherever they seem appropriate."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label CreativeWorkSeries "CreativeWorkSeries"))

(DEFCONCEPT BreadcrumbList (?X)
  :DOCUMENTATION "A BreadcrumbList is an ItemList consisting of a chain of linked Web pages, typically described using at least their URL and their name, and typically ending with the current page.</p>

<p>The <a class=\"localLink\" href=\"http://schema.org/position\">position</a> property is used to reconstruct the order of the items in a BreadcrumbList The convention is that a breadcrumb list has an <a class=\"localLink\" href=\"http://schema.org/itemListOrder\">itemListOrder</a> of <a class=\"localLink\" href=\"http://schema.org/ItemListOrderAscending\">ItemListOrderAscending</a> (lower values listed first), and that the first items in this list correspond to the \"top\" or beginning of the breadcrumb trail, e.g. with a site or section homepage. The specific values of 'position' are not assigned meaning for a BreadcrumbList, but they should be integers, e.g. beginning with '1' for the first item in the list."
  :SUBSET-OF ItemList
  :AXIOMS (/XMLNS/RDFS/label BreadcrumbList "BreadcrumbList"))

(DEFCONCEPT BroadcastChannel (?X)
  :DOCUMENTATION "A unique instance of a BroadcastService on a CableOrSatelliteService lineup."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label BroadcastChannel "BroadcastChannel"))

(DEFCONCEPT MusicReleaseFormatType (?X)
  :DOCUMENTATION "Format of this release (the type of recording media used, ie. compact disc, digital media, LP, etc.)."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/DC/source MusicReleaseFormatType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label MusicReleaseFormatType "MusicReleaseFormatType"))

(DEFCONCEPT ReturnAction (?X)
  :DOCUMENTATION "The act of returning to the origin that which was previously received (concrete objects) or taken (ownership)."
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label ReturnAction "ReturnAction"))

(DEFCONCEPT TrackAction (?X)
  :DOCUMENTATION "<p>An agent tracks an object for updates.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/FollowAction\">FollowAction</a>: Unlike FollowAction, TrackAction refers to the interest on the location of innanimates objects.</li>
<li><a class=\"localLink\" href=\"http://schema.org/SubscribeAction\">SubscribeAction</a>: Unlike SubscribeAction, TrackAction refers to  the interest on the location of innanimate objects.</li>
</ul>
"
  :SUBSET-OF FindAction
  :AXIOMS (/XMLNS/RDFS/label TrackAction "TrackAction"))

(DEFCONCEPT DayOfWeek (?X)
  :DOCUMENTATION "The day of the week, e.g. used to specify to which day the opening hours of an OpeningHoursSpecification refer.</p>

<p>Originally, URLs from <a href=\"http://purl.org/goodrelations/v1\">GoodRelations</a> were used (for <a class=\"localLink\" href=\"http://schema.org/Monday\">Monday</a>, <a class=\"localLink\" href=\"http://schema.org/Tuesday\">Tuesday</a>, <a class=\"localLink\" href=\"http://schema.org/Wednesday\">Wednesday</a>, <a class=\"localLink\" href=\"http://schema.org/Thursday\">Thursday</a>, <a class=\"localLink\" href=\"http://schema.org/Friday\">Friday</a>, <a class=\"localLink\" href=\"http://schema.org/Saturday\">Saturday</a>, <a class=\"localLink\" href=\"http://schema.org/Sunday\">Sunday</a> plus a special entry for <a class=\"localLink\" href=\"http://schema.org/PublicHolidays\">PublicHolidays</a>); these have now been integrated directly into schema.org."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/DC/source DayOfWeek |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label DayOfWeek "DayOfWeek"))

(DEFCONCEPT InteractAction (?X)
  :DOCUMENTATION "The act of interacting with another person or organization."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label InteractAction "InteractAction"))

(DEFRELATION contentLocation (?X ?Y)
  :DOCUMENTATION "The location depicted or described in the content. For example, the location in a photograph or painting."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label contentLocation "contentLocation")
  :AXIOMS (domainIncludes contentLocation CreativeWork)
  :AXIOMS (rangeIncludes contentLocation Place))

(DEFCONCEPT MusicAlbumProductionType (?X)
  :DOCUMENTATION "Classification of the album by it's type of content: soundtrack, live album, studio album, etc."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label MusicAlbumProductionType "MusicAlbumProductionType")
  :AXIOMS (/XMLNS/DC/source MusicAlbumProductionType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|))

(DEFCONCEPT ContactPointOption (?X)
  :DOCUMENTATION "Enumerated options related to a ContactPoint."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label ContactPointOption "ContactPointOption"))

(DEFCONCEPT Movie (?X)
  :DOCUMENTATION "A movie."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Movie "Movie"))

(DEFCONCEPT MusicComposition (?X)
  :DOCUMENTATION "A musical composition."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/DC/source MusicComposition |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label MusicComposition "MusicComposition"))

(DEFRELATION recordedAt (?X ?Y)
  :DOCUMENTATION "The Event where the CreativeWork was recorded. The CreativeWork may capture all or part of the event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (inverseOf recordedAt recordedIn)
  :AXIOMS (rangeIncludes recordedAt Event)
  :AXIOMS (domainIncludes recordedAt CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label recordedAt "recordedAt"))

(DEFRELATION recordedIn (?X ?Y)
  :DOCUMENTATION "The CreativeWork that captured all or part of this Event."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label recordedIn "recordedIn")
  :AXIOMS (domainIncludes recordedIn Event)
  :AXIOMS (rangeIncludes recordedIn CreativeWork)
  :AXIOMS (inverseOf recordedIn recordedAt))

(DEFCONCEPT InvestmentOrDeposit (?X)
  :DOCUMENTATION "A type of financial product that typically requires the client to transfer funds to a financial service in return for potential beneficial financial return."
  :SUBSET-OF FinancialProduct
  :AXIOMS (/XMLNS/DC/source InvestmentOrDeposit |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label InvestmentOrDeposit "InvestmentOrDeposit"))

(DEFCONCEPT Boolean (?X)
  :DOCUMENTATION "Boolean: True or False."
  :AXIOMS (/XMLNS/RDFS/label Boolean "Boolean")
  :AXIOMS (DataType Boolean))

(DEFCONCEPT DriveWheelConfigurationValue (?X)
  :DOCUMENTATION "A value indicating which roadwheels will receive torque."
  :SUBSET-OF QualitativeValue
  :AXIOMS (/XMLNS/DC/source DriveWheelConfigurationValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (/XMLNS/RDFS/label DriveWheelConfigurationValue "DriveWheelConfigurationValue"))

(DEFCONCEPT PublicationEvent (?X)
  :DOCUMENTATION "A PublicationEvent corresponds indifferently to the event of publication for a CreativeWork of any type e.g. a broadcast event, an on-demand event, a book/journal publication via a variety of delivery media."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label PublicationEvent "PublicationEvent"))

(DEFCONCEPT IndividualProduct (?X)
  :DOCUMENTATION "A single, identifiable product instance (e.g. a laptop with a particular serial number)."
  :SUBSET-OF Product
  :AXIOMS (/XMLNS/DC/source IndividualProduct |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label IndividualProduct "IndividualProduct"))

(DEFRELATION recordedAs (?X ?Y)
  :DOCUMENTATION "An audio recording of the work."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes recordedAs MusicRecording)
  :AXIOMS (/XMLNS/RDFS/label recordedAs "recordedAs")
  :AXIOMS (/XMLNS/DC/source recordedAs |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (inverseOf recordedAs recordingOf)
  :AXIOMS (domainIncludes recordedAs MusicComposition))

(DEFRELATION recordingOf (?X ?Y)
  :DOCUMENTATION "The composition this track is a recording of."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label recordingOf "recordingOf")
  :AXIOMS (rangeIncludes recordingOf MusicComposition)
  :AXIOMS (domainIncludes recordingOf MusicRecording)
  :AXIOMS (inverseOf recordingOf recordedAs)
  :AXIOMS (/XMLNS/DC/source recordingOf |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|))

(DEFCONCEPT OrganizeAction (?X)
  :DOCUMENTATION "The act of manipulating/administering/supervising/controlling one or more objects."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label OrganizeAction "OrganizeAction"))

(DEFCONCEPT AssessAction (?X)
  :DOCUMENTATION "The act of forming one's opinion, reaction or sentiment."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label AssessAction "AssessAction"))

(DEFRELATION dataset (?X ?Y)
  :DOCUMENTATION "A dataset contained in this catalog."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes dataset DataCatalog)
  :AXIOMS (rangeIncludes dataset Dataset)
  :AXIOMS (/XMLNS/RDFS/label dataset "dataset")
  :AXIOMS (inverseOf dataset includedInDataCatalog))

(DEFCONCEPT Product (?X)
  :DOCUMENTATION "Any offered product or service. For example: a pair of shoes; a concert ticket; the rental of a car; a haircut; or an episode of a TV show streamed online."
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/RDFS/label Product "Product")
  :AXIOMS (/XMLNS/DC/source Product |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFCONCEPT RsvpResponseType (?X)
  :DOCUMENTATION "RsvpResponseType is an enumeration type whose instances represent responding to an RSVP request."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label RsvpResponseType "RsvpResponseType"))

(DEFCONCEPT NutritionInformation (?X)
  :DOCUMENTATION "Nutritional information about the recipe."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label NutritionInformation "NutritionInformation"))

(DEFCONCEPT AudioObject (?X)
  :DOCUMENTATION "An audio file."
  :SUBSET-OF MediaObject
  :AXIOMS (/XMLNS/DC/source AudioObject |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|)
  :AXIOMS (/XMLNS/RDFS/label AudioObject "AudioObject"))

(DEFCONCEPT BusTrip (?X)
  :DOCUMENTATION "A trip on a commercial bus line."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label BusTrip "BusTrip"))

(DEFCONCEPT ReceiveAction (?X)
  :DOCUMENTATION "<p>The act of physically/electronically taking delivery of an object thathas been transferred from an origin to a destination. Reciprocal of SendAction.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/SendAction\">SendAction</a>: The reciprocal of ReceiveAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/TakeAction\">TakeAction</a>: Unlike TakeAction, ReceiveAction does not imply that the ownership has been transfered (e.g. I can receive a package, but it does not mean the package is now mine).</li>
</ul>
"
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label ReceiveAction "ReceiveAction"))

(DEFCONCEPT BankAccount (?X)
  :DOCUMENTATION "A product or service offered by a bank whereby one may deposit, withdraw or transfer money and in some cases be paid interest."
  :SUBSET-OF FinancialProduct
  :AXIOMS (/XMLNS/RDFS/label BankAccount "BankAccount")
  :AXIOMS (/XMLNS/DC/source BankAccount |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|))

(DEFCONCEPT Room (?X)
  :DOCUMENTATION "A room is a distinguishable space within a structure, usually separated from other spaces by interior walls. (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/Room\">http://en.wikipedia.org/wiki/Room</a>).
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF Accommodation
  :AXIOMS (/XMLNS/DC/source Room |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label Room "Room"))

(DEFCONCEPT ComputerLanguage (?X)
  :DOCUMENTATION "This type covers computer programming languages such as Scheme and Lisp, as well as other language-like computer representations. Natural languages are best represented with the <a class=\"localLink\" href=\"http://schema.org/Language\">Language</a> type."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label ComputerLanguage "ComputerLanguage"))

(DEFCONCEPT BroadcastService (?X)
  :DOCUMENTATION "A delivery service through which content is provided via broadcast over the air or online."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label BroadcastService "BroadcastService"))

(DEFCONCEPT MusicPlaylist (?X)
  :DOCUMENTATION "A collection of music tracks in playlist form."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label MusicPlaylist "MusicPlaylist"))

(DEFCONCEPT ServiceChannel (?X)
  :DOCUMENTATION "A means for accessing a service, e.g. a government office location, web site, or phone number."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label ServiceChannel "ServiceChannel"))

(DEFCONCEPT VideoObject (?X)
  :DOCUMENTATION "A video file."
  :SUBSET-OF MediaObject
  :AXIOMS (/XMLNS/DC/source VideoObject |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|)
  :AXIOMS (/XMLNS/RDFS/label VideoObject "VideoObject"))

(DEFCONCEPT AllocateAction (?X)
  :DOCUMENTATION "The act of organizing tasks/objects/events by associating resources to it."
  :SUBSET-OF OrganizeAction
  :AXIOMS (/XMLNS/RDFS/label AllocateAction "AllocateAction"))

(DEFCONCEPT AggregateRating (?X)
  :DOCUMENTATION "The average rating based on multiple ratings or reviews."
  :SUBSET-OF Rating
  :AXIOMS (/XMLNS/RDFS/label AggregateRating "AggregateRating"))

(DEFCONCEPT Article (?X)
  :DOCUMENTATION "An article, such as a news article or piece of investigative report. Newspapers and magazines have articles of many different types and this is intended to cover them all.</p>

<p>See also <a href=\"http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html\">blog post</a>."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Article "Article")
  :AXIOMS (/XMLNS/DC/source Article |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|))

(DEFRELATION game (?X ?Y)
  :DOCUMENTATION "Video game which is played on this server."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes game GameServer)
  :AXIOMS (rangeIncludes game VideoGame)
  :AXIOMS (/XMLNS/RDFS/label game "game")
  :AXIOMS (inverseOf game gameServer))

(DEFRELATION gameServer (?X ?Y)
  :DOCUMENTATION "The server on which  it is possible to play the game."
  ;:BINARY-RELATION TRUE
  :AXIOMS (rangeIncludes gameServer GameServer)
  :AXIOMS (/XMLNS/RDFS/label gameServer "gameServer")
  :AXIOMS (inverseOf gameServer game)
  :AXIOMS (domainIncludes gameServer VideoGame))

(DEFCONCEPT TipAction (?X)
  :DOCUMENTATION "The act of giving money voluntarily to a beneficiary in recognition of services rendered."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label TipAction "TipAction"))

(DEFCONCEPT PerformingGroup (?X)
  :DOCUMENTATION "A performance group, such as a band, an orchestra, or a circus."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label PerformingGroup "PerformingGroup"))

(DEFCONCEPT TaxiService (?X)
  :DOCUMENTATION "A service for a vehicle for hire with a driver for local travel. Fares are usually calculated based on distance traveled."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label TaxiService "TaxiService"))

(DEFCONCEPT Comment (?X)
  :DOCUMENTATION "A comment on an item - for example, a comment on a blog post. The comment's content is expressed via the <a class=\"localLink\" href=\"http://schema.org/text\">text</a> property, and its topic via <a class=\"localLink\" href=\"http://schema.org/about\">about</a>, properties shared with all CreativeWorks."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Comment "Comment"))

(DEFRELATION makesOffer (?X ?Y)
  :DOCUMENTATION "A pointer to products or services offered by the organization or person."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label makesOffer "makesOffer")
  :AXIOMS (rangeIncludes makesOffer Offer)
  :AXIOMS (/XMLNS/DC/source makesOffer |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|)
  :AXIOMS (inverseOf makesOffer offeredBy)
  :AXIOMS (domainIncludes makesOffer Organization)
  :AXIOMS (domainIncludes makesOffer Person))

(DEFRELATION offeredBy (?X ?Y)
  :DOCUMENTATION "A pointer to the organization or person making the offer."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes offeredBy Offer)
  :AXIOMS (rangeIncludes offeredBy Person)
  :AXIOMS (inverseOf offeredBy makesOffer)
  :AXIOMS (rangeIncludes offeredBy Organization)
  :AXIOMS (/XMLNS/RDFS/label offeredBy "offeredBy"))

(DEFCONCEPT CreativeWork (?X)
  :DOCUMENTATION "The most generic kind of creative work, including books, movies, photographs, software programs, etc."
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/RDFS/label CreativeWork "CreativeWork")
  :AXIOMS (/XMLNS/DC/source CreativeWork |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|))

(DEFCONCEPT ScreeningEvent (?X)
  :DOCUMENTATION "A screening of a movie or other video."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label ScreeningEvent "ScreeningEvent"))

(DEFCONCEPT MoveAction (?X)
  :DOCUMENTATION "<p>The act of an agent relocating to a place.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/TransferAction\">TransferAction</a>: Unlike TransferAction, the subject of the move is a living Person or Organization rather than an inanimate object.</li>
</ul>
"
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label MoveAction "MoveAction"))

(DEFCONCEPT SomeProducts (?X)
  :DOCUMENTATION "A placeholder for multiple similar products of the same kind."
  :SUBSET-OF Product
  :AXIOMS (/XMLNS/RDFS/label SomeProducts "SomeProducts")
  :AXIOMS (/XMLNS/DC/source SomeProducts |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT BookFormatType (?X)
  :DOCUMENTATION "The publication format of the book."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label BookFormatType "BookFormatType"))

(DEFCONCEPT WarrantyPromise (?X)
  :DOCUMENTATION "A structured value representing the duration and scope of services that will be provided to a customer free of charge in case of a defect or malfunction of a product."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label WarrantyPromise "WarrantyPromise")
  :AXIOMS (/XMLNS/DC/source WarrantyPromise |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT ParentAudience (?X)
  :DOCUMENTATION "A set of characteristics describing parents, who can be interested in viewing some content."
  :SUBSET-OF PeopleAudience
  :AXIOMS (/XMLNS/RDFS/label ParentAudience "ParentAudience"))

(DEFCONCEPT EndorseAction (?X)
  :DOCUMENTATION "An agent approves/certifies/likes/supports/sanction an object."
  :SUBSET-OF ReactAction
  :AXIOMS (/XMLNS/RDFS/label EndorseAction "EndorseAction"))

(DEFCONCEPT Dataset (?X)
  :DOCUMENTATION "A body of structured information describing some topic(s) of interest."
  :SUBSET-OF CreativeWork
  :EQUIVALENT-RELATION |http://rdfs.org/ns/void#Dataset|
  :EQUIVALENT-RELATION |http://www.w3.org/ns/dcat#Dataset|
  :EQUIVALENT-RELATION |http://purl.org/dc/dcmitype/Dataset|
  :AXIOMS (/XMLNS/DC/source Dataset |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_DatasetClass|)
  :AXIOMS (/XMLNS/RDFS/label Dataset "Dataset"))

(ASSERT
 (AND (Organization |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_DatasetClass|)
  (/XMLNS/RDFS/label |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_DatasetClass| "DatasetClass")
  (DOCUMENTATION |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_DatasetClass|
   "This class is based upon W3C DCAT work, and benefits from collaboration around the DCAT, ADMS and VoID vocabularies. See http://www.w3.org/wiki/WebSchemas/Datasets for full details and mappings.")))

(DEFCONCEPT DataCatalog (?X)
  :DOCUMENTATION "A collection of datasets."
  :SUBSET-OF CreativeWork
  :EQUIVALENT-RELATION |http://www.w3.org/ns/dcat#Catalog|
  :AXIOMS (/XMLNS/DC/source DataCatalog |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_DatasetClass|)
  :AXIOMS (/XMLNS/RDFS/label DataCatalog "DataCatalog"))

(DEFCONCEPT BroadcastEvent (?X)
  :DOCUMENTATION "An over the air or online broadcast event."
  :SUBSET-OF PublicationEvent
  :AXIOMS (/XMLNS/RDFS/label BroadcastEvent "BroadcastEvent"))

(DEFCONCEPT Review (?X)
  :DOCUMENTATION "A review of an item - for example, of a restaurant, movie, or store."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Review "Review"))

(DEFCONCEPT Corporation (?X)
  :DOCUMENTATION "Organization: A business corporation."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label Corporation "Corporation")
  :AXIOMS (/XMLNS/DC/source Corporation |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|))

(DEFCONCEPT QualitativeValue (?X)
  :DOCUMENTATION "A predefined value for a product characteristic, e.g. the power cord plug type 'US' or the garment sizes 'S', 'M', 'L', and 'XL'."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label QualitativeValue "QualitativeValue")
  :AXIOMS (/XMLNS/DC/source QualitativeValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT TrainStation (?X)
  :DOCUMENTATION "A train station."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label TrainStation "TrainStation"))

(DEFCONCEPT Time (?X)
  :DOCUMENTATION "A point in time recurring on multiple days in the form hh:mm:ss[Z|(+|-)hh:mm] (see <a href=\"http://www.w3.org/TR/xmlschema-2/#time\">XML schema for details</a>)."
  :AXIOMS (/XMLNS/RDFS/label Time "Time")
  :AXIOMS (DataType Time))

(DEFCONCEPT AdministrativeArea (?X)
  :DOCUMENTATION "A geographical region, typically under the jurisdiction of a particular government."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label AdministrativeArea "AdministrativeArea"))

(DEFCONCEPT JoinAction (?X)
  :DOCUMENTATION "<p>An agent joins an event/group with participants/friends at a location.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/RegisterAction\">RegisterAction</a>: Unlike RegisterAction, JoinAction refers to joining a group/team of people.</li>
<li><a class=\"localLink\" href=\"http://schema.org/SubscribeAction\">SubscribeAction</a>: Unlike SubscribeAction, JoinAction does not imply that you'll be receiving updates.</li>
<li><a class=\"localLink\" href=\"http://schema.org/FollowAction\">FollowAction</a>: Unlike FollowAction, JoinAction does not imply that you'll be polling for updates.</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label JoinAction "JoinAction"))

(DEFCONCEPT HotelRoom (?X)
  :DOCUMENTATION "A hotel room is a single room in a hotel.
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF Room
  :AXIOMS (/XMLNS/DC/source HotelRoom |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label HotelRoom "HotelRoom"))

(DEFCONCEPT ReactAction (?X)
  :DOCUMENTATION "The act of responding instinctively and emotionally to an object, expressing a sentiment."
  :SUBSET-OF AssessAction
  :AXIOMS (/XMLNS/RDFS/label ReactAction "ReactAction"))

(DEFCONCEPT Accommodation (?X)
  :DOCUMENTATION "An accommodation is a place that can accommodate human beings, e.g. a hotel room, a camping pitch, or a meeting room. Many accommodations are for overnight stays, but this is not a mandatory requirement.
For more specific types of accommodations not defined in schema.org, one can use additionalType with external vocabularies.
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label Accommodation "Accommodation")
  :AXIOMS (/XMLNS/DC/source Accommodation |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFCONCEPT Order (?X)
  :DOCUMENTATION "An order is a confirmation of a transaction (a receipt), which can contain multiple line items, each represented by an Offer that has been accepted by the customer."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Order "Order"))

(DEFCONCEPT MusicRecording (?X)
  :DOCUMENTATION "A music recording (track), usually a single song."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label MusicRecording "MusicRecording"))

(DEFCONCEPT GovernmentService (?X)
  :DOCUMENTATION "A service provided by a government organization, e.g. food stamps, veterans benefits, etc."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label GovernmentService "GovernmentService"))

(DEFCONCEPT BorrowAction (?X)
  :DOCUMENTATION "<p>The act of obtaining an object under an agreement to return it at a later date. Reciprocal of LendAction.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/LendAction\">LendAction</a>: Reciprocal of BorrowAction.</li>
</ul>
"
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label BorrowAction "BorrowAction"))

(DEFCONCEPT AddAction (?X)
  :DOCUMENTATION "The act of editing by adding an object to a collection."
  :SUBSET-OF UpdateAction
  :AXIOMS (/XMLNS/RDFS/label AddAction "AddAction"))

(DEFCONCEPT SportsEvent (?X)
  :DOCUMENTATION "Event type: Sports event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label SportsEvent "SportsEvent"))

(DEFCONCEPT AggregateOffer (?X)
  :DOCUMENTATION "When a single product is associated with multiple offers (for example, the same pair of shoes is offered by different merchants), then AggregateOffer can be used."
  :SUBSET-OF Offer
  :AXIOMS (/XMLNS/RDFS/label AggregateOffer "AggregateOffer"))

(DEFCONCEPT LodgingReservation (?X)
  :DOCUMENTATION "A reservation for lodging at a hotel, motel, inn, etc.</p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label LodgingReservation "LodgingReservation"))

(DEFCONCEPT NewsArticle (?X)
  :DOCUMENTATION "A news article."
  :SUBSET-OF Article
  :AXIOMS (/XMLNS/RDFS/label NewsArticle "NewsArticle")
  :AXIOMS (/XMLNS/DC/source NewsArticle |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|))

(DEFCONCEPT WriteAction (?X)
  :DOCUMENTATION "The act of authoring written creative content."
  :SUBSET-OF CreateAction
  :AXIOMS (/XMLNS/RDFS/label WriteAction "WriteAction"))

(DEFCONCEPT ControlAction (?X)
  :DOCUMENTATION "An agent controls a device or application."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label ControlAction "ControlAction"))

(DEFCONCEPT InsertAction (?X)
  :DOCUMENTATION "The act of adding at a specific location in an ordered collection."
  :SUBSET-OF AddAction
  :AXIOMS (/XMLNS/RDFS/label InsertAction "InsertAction"))

(DEFCONCEPT ConsumeAction (?X)
  :DOCUMENTATION "The act of ingesting information/resources/food."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label ConsumeAction "ConsumeAction"))

(DEFCONCEPT GeoCircle (?X)
  :DOCUMENTATION "A GeoCircle is a GeoShape representing a circular geographic area. As it is a GeoShape
          it provides the simple textual property 'circle', but also allows the combination of postalCode alongside geoRadius.
          The center of the circle can be indicated via the 'geoMidpoint' property, or more approximately using 'address', 'postalCode'."
  :SUBSET-OF GeoShape
  :AXIOMS (/XMLNS/RDFS/label GeoCircle "GeoCircle"))

(DEFCONCEPT FollowAction (?X)
  :DOCUMENTATION "<p>The act of forming a personal connection with someone/something (object) unidirectionally/asymmetrically to get updates polled from.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/BefriendAction\">BefriendAction</a>: Unlike BefriendAction, FollowAction implies that the connection is <em>not</em> necessarily reciprocal.</li>
<li><a class=\"localLink\" href=\"http://schema.org/SubscribeAction\">SubscribeAction</a>: Unlike SubscribeAction, FollowAction implies that the follower acts as an active agent constantly/actively polling for updates.</li>
<li><a class=\"localLink\" href=\"http://schema.org/RegisterAction\">RegisterAction</a>: Unlike RegisterAction, FollowAction implies that the agent is interested in continuing receiving updates from the object.</li>
<li><a class=\"localLink\" href=\"http://schema.org/JoinAction\">JoinAction</a>: Unlike JoinAction, FollowAction implies that the agent is interested in getting updates from the object.</li>
<li><a class=\"localLink\" href=\"http://schema.org/TrackAction\">TrackAction</a>: Unlike TrackAction, FollowAction refers to the polling of updates of all aspects of animate objects rather than the location of inanimate objects (e.g. you track a package, but you don't follow it).</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label FollowAction "FollowAction"))

(DEFCONCEPT SellAction (?X)
  :DOCUMENTATION "The act of taking money from a buyer in exchange for goods or services rendered. An agent sells an object, product, or service to a buyer for a price. Reciprocal of BuyAction."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label SellAction "SellAction"))

(DEFCONCEPT Recipe (?X)
  :DOCUMENTATION "A recipe. For dietary restrictions covered by the recipe, a few common restrictions are enumerated via <a class=\"localLink\" href=\"http://schema.org/suitableForDiet\">suitableForDiet</a>. The <a class=\"localLink\" href=\"http://schema.org/keywords\">keywords</a> property can also be used to add more detail."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Recipe "Recipe"))

(DEFCONCEPT LoseAction (?X)
  :DOCUMENTATION "The act of being defeated in a competitive activity."
  :SUBSET-OF AchieveAction
  :AXIOMS (/XMLNS/RDFS/label LoseAction "LoseAction"))

(DEFCONCEPT BusStop (?X)
  :DOCUMENTATION "A bus stop."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label BusStop "BusStop"))

(DEFCONCEPT RadioSeries (?X)
  :DOCUMENTATION "CreativeWorkSeries dedicated to radio broadcast and associated online delivery."
  :SUBSET-OF CreativeWorkSeries
  :AXIOMS (/XMLNS/RDFS/label RadioSeries "RadioSeries"))

(DEFCONCEPT SportsActivityLocation (?X)
  :DOCUMENTATION "A sports location, such as a playing field."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label SportsActivityLocation "SportsActivityLocation"))

(DEFCONCEPT ContactPoint (?X)
  :DOCUMENTATION "A contact point&#x2014;for example, a Customer Complaints department."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label ContactPoint "ContactPoint"))

(DEFCONCEPT MusicAlbumReleaseType (?X)
  :DOCUMENTATION "The kind of release which this album is: single, EP or album."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label MusicAlbumReleaseType "MusicAlbumReleaseType")
  :AXIOMS (/XMLNS/DC/source MusicAlbumReleaseType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|))

(DEFCONCEPT GovernmentBuilding (?X)
  :DOCUMENTATION "A government building."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label GovernmentBuilding "GovernmentBuilding"))

(DEFCONCEPT Periodical (?X)
  :DOCUMENTATION "A publication in any medium issued in successive parts bearing numerical or chronological designations and intended, such as a magazine, scholarly journal, or newspaper to continue indefinitely.</p>

<p>See also <a href=\"http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html\">blog post</a>."
  :EQUIVALENT-RELATION |http://purl.org/ontology/bibo/Periodical|
  :SUBSET-OF CreativeWorkSeries
  :AXIOMS (/XMLNS/DC/source Periodical |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label Periodical "Periodical"))

(DEFCONCEPT BusStation (?X)
  :DOCUMENTATION "A bus station."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label BusStation "BusStation"))

(DEFCONCEPT BodyOfWater (?X)
  :DOCUMENTATION "A body of water, such as a sea, ocean, or lake."
  :SUBSET-OF Landform
  :AXIOMS (/XMLNS/RDFS/label BodyOfWater "BodyOfWater"))

(DEFCONCEPT DigitalDocument (?X)
  :DOCUMENTATION "An electronic file or document."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label DigitalDocument "DigitalDocument"))

(DEFCONCEPT BusinessAudience (?X)
  :DOCUMENTATION "A set of characteristics belonging to businesses, e.g. who compose an item's target audience."
  :SUBSET-OF Audience
  :AXIOMS (/XMLNS/RDFS/label BusinessAudience "BusinessAudience"))

(DEFCONCEPT SearchAction (?X)
  :DOCUMENTATION "<p>The act of searching for an object.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/FindAction\">FindAction</a>: SearchAction generally leads to a FindAction, but not necessarily.</li>
</ul>
"
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label SearchAction "SearchAction"))

(DEFCONCEPT Map (?X)
  :DOCUMENTATION "A map."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Map "Map"))

(DEFCONCEPT PlayAction (?X)
  :DOCUMENTATION "<p>The act of playing/exercising/training/performing for enjoyment, leisure, recreation, Competition or exercise.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/ListenAction\">ListenAction</a>: Unlike ListenAction (which is under ConsumeAction), PlayAction refers to performing for an audience or at an event, rather than consuming music.</li>
<li><a class=\"localLink\" href=\"http://schema.org/WatchAction\">WatchAction</a>: Unlike WatchAction (which is under ConsumeAction), PlayAction refers to showing/displaying for an audience or at an event, rather than consuming visual content.</li>
</ul>
"
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label PlayAction "PlayAction"))

(DEFCONCEPT DonateAction (?X)
  :DOCUMENTATION "The act of providing goods, services, or money without compensation, often for philanthropic reasons."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label DonateAction "DonateAction"))

(DEFCONCEPT DataDownload (?X)
  :DOCUMENTATION "A dataset in downloadable form."
  :EQUIVALENT-RELATION |http://www.w3.org/ns/dcat#Distribution|
  :SUBSET-OF MediaObject
  :AXIOMS (/XMLNS/RDFS/label DataDownload "DataDownload")
  :AXIOMS (/XMLNS/DC/source DataDownload |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_DatasetClass|))

(DEFCONCEPT PeopleAudience (?X)
  :DOCUMENTATION "A set of characteristics belonging to people, e.g. who compose an item's target audience."
  :SUBSET-OF Audience
  :AXIOMS (/XMLNS/RDFS/label PeopleAudience "PeopleAudience"))

(DEFCONCEPT Country (?X)
  :DOCUMENTATION "A country."
  :SUBSET-OF AdministrativeArea
  :AXIOMS (/XMLNS/RDFS/label Country "Country"))

(DEFCONCEPT JobPosting (?X)
  :DOCUMENTATION "A listing that describes a job opening in a certain organization."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label JobPosting "JobPosting"))

(DEFCONCEPT TVSeries (?X)
  :DOCUMENTATION "CreativeWorkSeries dedicated to TV broadcast and associated online delivery."
  :SUBSET-OF CreativeWorkSeries
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label TVSeries "TVSeries"))

(DEFCONCEPT URL (?X)
  :DOCUMENTATION "Data type: URL."
  :SUBSET-OF Text
  :AXIOMS (/XMLNS/RDFS/label URL "URL"))

(DEFCONCEPT AuthorizeAction (?X)
  :DOCUMENTATION "The act of granting permission to an object."
  :SUBSET-OF AllocateAction
  :AXIOMS (/XMLNS/RDFS/label AuthorizeAction "AuthorizeAction"))

(DEFCONCEPT QuantitativeValue (?X)
  :DOCUMENTATION "A point value or interval for product characteristics and other purposes."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/DC/source QuantitativeValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label QuantitativeValue "QuantitativeValue"))

(DEFCONCEPT StructuredValue (?X)
  :DOCUMENTATION "Structured values are used when the value of a property has a more complex structure than simply being a textual value or a reference to another thing."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label StructuredValue "StructuredValue"))

(DEFCONCEPT ReplyAction (?X)
  :DOCUMENTATION "<p>The act of responding to a question/message asked/sent by the object. Related to <a class=\"localLink\" href=\"http://schema.org/AskAction\">AskAction</a></p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/AskAction\">AskAction</a>: Appears generally as an origin of a ReplyAction.</li>
</ul>
"
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label ReplyAction "ReplyAction"))

(DEFCONCEPT OfferItemCondition (?X)
  :DOCUMENTATION "A list of possible conditions for the item."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label OfferItemCondition "OfferItemCondition"))

(DEFCONCEPT WebPageElement (?X)
  :DOCUMENTATION "A web page element, like a table or an image."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label WebPageElement "WebPageElement"))

(DEFCONCEPT Message (?X)
  :DOCUMENTATION "A single message from a sender to one or more organizations or people."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Message "Message"))

(DEFCONCEPT TypeAndQuantityNode (?X)
  :DOCUMENTATION "A structured value indicating the quantity, unit of measurement, and business function of goods included in a bundle offer."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/DC/source TypeAndQuantityNode |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label TypeAndQuantityNode "TypeAndQuantityNode"))

(DEFCONCEPT TrainTrip (?X)
  :DOCUMENTATION "A trip on a commercial train line."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label TrainTrip "TrainTrip"))

(DEFCONCEPT Number (?X)
  :DOCUMENTATION "Data type: Number."
  :AXIOMS (DataType Number)
  :AXIOMS (/XMLNS/RDFS/label Number "Number"))

(DEFCONCEPT CableOrSatelliteService (?X)
  :DOCUMENTATION "A service which provides access to media programming like TV or radio. Access may be via cable or satellite."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label CableOrSatelliteService "CableOrSatelliteService"))

(DEFCONCEPT Service (?X)
  :DOCUMENTATION "A service provided by an organization, e.g. delivery service, print services, etc."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Service "Service"))

(DEFCONCEPT ParcelDelivery (?X)
  :DOCUMENTATION "The delivery of a parcel either via the postal service or a commercial service."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label ParcelDelivery "ParcelDelivery"))

(DEFCONCEPT PublicationVolume (?X)
  :DOCUMENTATION "<p>A part of a successively published publication such as a periodical or multi-volume work, often numbered. It may represent a time span, such as a year.</p>

<pre><code>  &lt;br/&gt;&lt;br/&gt;See also &lt;a href=\"http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html\"&gt;blog post&lt;/a&gt;.
</code></pre>
"
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/DC/source PublicationVolume |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (/XMLNS/RDFS/label PublicationVolume "PublicationVolume"))

(DEFCONCEPT SingleFamilyResidence (?X)
  :DOCUMENTATION "Residence type: Single-family home."
  :SUBSET-OF House
  :AXIOMS (/XMLNS/RDFS/label SingleFamilyResidence "SingleFamilyResidence"))

(DEFCONCEPT LegalService (?X)
  :DOCUMENTATION "A LegalService is a business that provides legally-oriented services, advice and representation, e.g. law firms.</p>

<p>As a <a class=\"localLink\" href=\"http://schema.org/LocalBusiness\">LocalBusiness</a> it can be described as a <a class=\"localLink\" href=\"http://schema.org/provider\">provider</a> of one or more <a class=\"localLink\" href=\"http://schema.org/Service\">Service</a>(s)."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label LegalService "LegalService"))

(DEFCONCEPT Intangible (?X)
  :DOCUMENTATION "A utility class that serves as the umbrella for a number of 'intangible' things such as quantities, structured values, etc."
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/RDFS/label Intangible "Intangible"))

(DEFCONCEPT VoteAction (?X)
  :DOCUMENTATION "The act of expressing a preference from a fixed/finite/structured set of choices/options."
  :SUBSET-OF ChooseAction
  :AXIOMS (/XMLNS/RDFS/label VoteAction "VoteAction"))

(DEFCONCEPT OrganizationRole (?X)
  :DOCUMENTATION "A subclass of Role used to describe roles within organizations."
  :SUBSET-OF Role
  :AXIOMS (/XMLNS/RDFS/label OrganizationRole "OrganizationRole"))

(DEFCONCEPT Enumeration (?X)
  :DOCUMENTATION "Lists or enumerations—for example, a list of cuisines or music genres, etc."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Enumeration "Enumeration"))

(DEFCONCEPT Date (?X)
  :DOCUMENTATION "A date value in <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 date format</a>."
  :AXIOMS (/XMLNS/RDFS/label Date "Date")
  :AXIOMS (DataType Date))

(DEFCONCEPT GovernmentOffice (?X)
  :DOCUMENTATION "A government office&#x2014;for example, an IRS or DMV office."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label GovernmentOffice "GovernmentOffice"))

(DEFCONCEPT DataFeedItem (?X)
  :DOCUMENTATION "A single item within a larger data feed."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label DataFeedItem "DataFeedItem"))

(DEFCONCEPT MusicGroup (?X)
  :DOCUMENTATION "A musical group, such as a band, an orchestra, or a choir. Can also be a solo musician."
  :SUBSET-OF PerformingGroup
  :AXIOMS (/XMLNS/RDFS/label MusicGroup "MusicGroup"))

(DEFCONCEPT DateTime (?X)
  :DOCUMENTATION "A combination of date and time of day in the form [-]CCYY-MM-DDThh:mm:ss[Z|(+|-)hh:mm] (see Chapter 5.4 of ISO 8601)."
  :AXIOMS (DataType DateTime)
  :AXIOMS (/XMLNS/RDFS/label DateTime "DateTime"))

(DEFCONCEPT Role (?X)
  :DOCUMENTATION "Represents additional information about a relationship or property. For example a Role can be used to say that a 'member' role linking some SportsTeam to a player occurred during a particular time period. Or that a Person's 'actor' role in a Movie was for some particular characterName. Such properties can be attached to a Role entity, which is then associated with the main entities using ordinary properties like 'member' or 'actor'.</p>

<p>See also <a href=\"http://blog.schema.org/2014/06/introducing-role.html\">blog post</a>."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Role "Role"))

(DEFCONCEPT ItemList (?X)
  :DOCUMENTATION "A list of items of any sort&#x2014;for example, Top 10 Movies About Weathermen, or Top 100 Party Songs. Not to be confused with HTML lists, which are often used only for formatting."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label ItemList "ItemList"))

(DEFCONCEPT CompoundPriceSpecification (?X)
  :DOCUMENTATION "A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. \"electricity\" or \"final cleaning\")."
  :SUBSET-OF PriceSpecification
  :AXIOMS (/XMLNS/DC/source CompoundPriceSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label CompoundPriceSpecification "CompoundPriceSpecification"))

(DEFCONCEPT BusinessEntityType (?X)
  :DOCUMENTATION "<p>A business entity type is a conceptual entity representing the legal form, the size, the main line of business, the position in the value chain, or any combination thereof, of an organization or business person.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#Business</li>
<li>http://purl.org/goodrelations/v1#Enduser</li>
<li>http://purl.org/goodrelations/v1#PublicInstitution</li>
<li>http://purl.org/goodrelations/v1#Reseller</li>
</ul>
"
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label BusinessEntityType "BusinessEntityType")
  :AXIOMS (/XMLNS/DC/source BusinessEntityType |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT Action (?X)
  :DOCUMENTATION "An action performed by a direct agent and indirect participants upon a direct object. Optionally happens at a location with the help of an inanimate instrument. The execution of the action may produce a result. Specific action sub-type documentation specifies the exact expectation of each argument/role.</p>

<p>See also <a href=\"http://blog.schema.org/2014/04/announcing-schemaorg-actions.html\">blog post</a> and <a href=\"http://schema.org/docs/actions.html\">Actions overview document</a>."
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/DC/source Action |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_ActionCollabClass|)
  :AXIOMS (/XMLNS/RDFS/label Action "Action"))

(DEFCONCEPT LiveBlogPosting (?X)
  :DOCUMENTATION "A blog post intended to provide a rolling textual coverage of an ongoing event through continuous updates."
  :SUBSET-OF BlogPosting
  :AXIOMS (/XMLNS/RDFS/label LiveBlogPosting "LiveBlogPosting"))

(DEFCONCEPT WebSite (?X)
  :DOCUMENTATION "A WebSite is a set of related web pages and other items typically served from a single web domain and accessible via URLs."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label WebSite "WebSite"))

(DEFCONCEPT TVClip (?X)
  :DOCUMENTATION "A short TV program or a segment/part of a TV program."
  :SUBSET-OF Clip
  :AXIOMS (/XMLNS/RDFS/label TVClip "TVClip"))

(DEFCONCEPT Audience (?X)
  :DOCUMENTATION "Intended audience for an item, i.e. the group for whom the item was created."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Audience "Audience"))

(DEFCONCEPT CommunicateAction (?X)
  :DOCUMENTATION "The act of conveying information to another person via a communication medium (instrument) such as speech, email, or telephone conversation."
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label CommunicateAction "CommunicateAction"))

(DEFCONCEPT TradeAction (?X)
  :DOCUMENTATION "The act of participating in an exchange of goods and services for monetary compensation. An agent trades an object, product or service with a participant in exchange for a one time or periodic payment."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label TradeAction "TradeAction"))

(DEFCONCEPT GameServerStatus (?X)
  :DOCUMENTATION "Status of a game server."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label GameServerStatus "GameServerStatus"))

(DEFCONCEPT EmergencyService (?X)
  :DOCUMENTATION "An emergency service, such as a fire station or ER."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label EmergencyService "EmergencyService"))

(DEFCONCEPT TechArticle (?X)
  :DOCUMENTATION "A technical article - Example: How-to (task) topics, step-by-step, procedural troubleshooting, specifications, etc."
  :SUBSET-OF Article
  :AXIOMS (/XMLNS/RDFS/label TechArticle "TechArticle"))

(DEFCONCEPT Brand (?X)
  :DOCUMENTATION "A brand is a name used by an organization or business person for labeling a product, product group, or similar."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/DC/source Brand |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label Brand "Brand"))

(DEFCONCEPT DeliveryChargeSpecification (?X)
  :DOCUMENTATION "The price for the delivery of an offer using a particular delivery method."
  :SUBSET-OF PriceSpecification
  :AXIOMS (/XMLNS/RDFS/label DeliveryChargeSpecification "DeliveryChargeSpecification")
  :AXIOMS (/XMLNS/DC/source DeliveryChargeSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT Invoice (?X)
  :DOCUMENTATION "A statement of the money due for goods or services; a bill."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Invoice "Invoice"))

(DEFCONCEPT PriceSpecification (?X)
  :DOCUMENTATION "A structured value representing a price or price range. Typically, only the subclasses of this type are used for markup. It is recommended to use <a class=\"localLink\" href=\"http://schema.org/MonetaryAmount\">MonetaryAmount</a> to describe independent amounts of money such as a salary, credit card limits, etc."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/DC/source PriceSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label PriceSpecification "PriceSpecification"))

(DEFCONCEPT FinancialService (?X)
  :DOCUMENTATION "Financial services business."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label FinancialService "FinancialService"))

(DEFCONCEPT CivicStructure (?X)
  :DOCUMENTATION "A public structure, such as a town hall or concert hall."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label CivicStructure "CivicStructure"))

(DEFCONCEPT VisualArtwork (?X)
  :DOCUMENTATION "A work of art that is primarily visual in character."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label VisualArtwork "VisualArtwork")
  :AXIOMS (/XMLNS/DC/source VisualArtwork |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_VisualArtworkClass|))

(DEFCONCEPT EducationalOrganization (?X)
  :DOCUMENTATION "An educational organization."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label EducationalOrganization "EducationalOrganization"))

(DEFCONCEPT Residence (?X)
  :DOCUMENTATION "The place where a person lives."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label Residence "Residence"))

(DEFCONCEPT AutomotiveBusiness (?X)
  :DOCUMENTATION "Car repair, sales, or parts."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label AutomotiveBusiness "AutomotiveBusiness"))

(DEFCONCEPT RestrictedDiet (?X)
  :DOCUMENTATION "A diet restricted to certain foods or preparations for cultural, religious, health or lifestyle reasons."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label RestrictedDiet "RestrictedDiet"))

(DEFCONCEPT MedicalOrganization (?X)
  :DOCUMENTATION "A medical organization (physical or not), such as hospital, institution or clinic."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label MedicalOrganization "MedicalOrganization"))

(DEFCONCEPT GeoShape (?X)
  :DOCUMENTATION "The geographic shape of a place. A GeoShape can be described using several properties whose values are based on latitude/longitude pairs. Either whitespace or commas can be used to separate latitude and longitude; whitespace should be used when writing a list of several such points."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/DC/source GeoShape |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|)
  :AXIOMS (/XMLNS/RDFS/label GeoShape "GeoShape"))

(DEFCONCEPT GeoCoordinates (?X)
  :DOCUMENTATION "The geographic coordinates of a place or event."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label GeoCoordinates "GeoCoordinates"))

(DEFCONCEPT TransferAction (?X)
  :DOCUMENTATION "The act of transferring/moving (abstract or concrete) animate or inanimate objects from one place to another."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label TransferAction "TransferAction"))

(DEFCONCEPT Rating (?X)
  :DOCUMENTATION "A rating is an evaluation on a numeric scale, such as 1 to 5 stars."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Rating "Rating"))

(DEFCONCEPT AlignmentObject (?X)
  :DOCUMENTATION "An intangible item that describes an alignment between a learning resource and a node in an educational framework."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/DC/source AlignmentObject |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_LRMIClass|)
  :AXIOMS (/XMLNS/RDFS/label AlignmentObject "AlignmentObject"))

(DEFCONCEPT Reservation (?X)
  :DOCUMENTATION "Describes a reservation for travel, dining or an event. Some reservations require tickets. </p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, restaurant reservations, flights, or rental cars, use <a class=\"localLink\" href=\"http://schema.org/Offer\">Offer</a>."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Reservation "Reservation"))

(DEFCONCEPT DigitalDocumentPermissionType (?X)
  :DOCUMENTATION "A type of permission which can be granted for accessing a digital document."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label DigitalDocumentPermissionType "DigitalDocumentPermissionType"))

(DEFCONCEPT Permit (?X)
  :DOCUMENTATION "A permit issued by an organization, e.g. a parking pass."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Permit "Permit"))

(DEFCONCEPT FinancialProduct (?X)
  :DOCUMENTATION "A product provided to consumers and businesses by financial institutions such as banks, insurance companies, brokerage firms, consumer finance companies, and investment companies which comprise the financial services industry."
  :SUBSET-OF Service
  :AXIOMS (/XMLNS/RDFS/label FinancialProduct "FinancialProduct")
  :AXIOMS (/XMLNS/DC/source FinancialProduct |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|))

(DEFCONCEPT Thing (?X)
  :DOCUMENTATION "The most generic type of item."
  :AXIOMS (/XMLNS/RDFS/label Thing "Thing"))

(DEFCONCEPT TVEpisode (?X)
  :DOCUMENTATION "A TV episode which can be part of a series or season."
  :SUBSET-OF Episode
  :AXIOMS (/XMLNS/RDFS/label TVEpisode "TVEpisode"))

(DEFCONCEPT BedDetails (?X)
  :DOCUMENTATION "An entity holding detailed information about the available bed types, e.g. the quantity of twin beds for a hotel room. For the single case of just one bed of a certain type, you can use bed directly with a text. See also <a class=\"localLink\" href=\"http://schema.org/BedType\">BedType</a> (under development)."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label BedDetails "BedDetails")
  :AXIOMS (/XMLNS/DC/source BedDetails |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFCONCEPT MusicRelease (?X)
  :DOCUMENTATION "A MusicRelease is a specific release of a music album."
  :SUBSET-OF MusicPlaylist
  :AXIOMS (/XMLNS/DC/source MusicRelease |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#MBZ|)
  :AXIOMS (/XMLNS/RDFS/label MusicRelease "MusicRelease"))

(DEFCONCEPT LeaveAction (?X)
  :DOCUMENTATION "<p>An agent leaves an event / group with participants/friends at a location.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/JoinAction\">JoinAction</a>: The antonym of LeaveAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/UnRegisterAction\">UnRegisterAction</a>: Unlike UnRegisterAction, LeaveAction implies leaving a group/team of people rather than a service.</li>
</ul>
"
  :SUBSET-OF InteractAction
  :AXIOMS (/XMLNS/RDFS/label LeaveAction "LeaveAction"))

(DEFCONCEPT CreativeWorkSeason (?X)
  :DOCUMENTATION "A media season e.g. tv, radio, video game etc."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label CreativeWorkSeason "CreativeWorkSeason"))

(DEFCONCEPT Specialty (?X)
  :DOCUMENTATION "Any branch of a field in which people typically develop specific expertise, usually after significant study, time, and effort."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label Specialty "Specialty"))

(DEFCONCEPT LoanOrCredit (?X)
  :DOCUMENTATION "A financial product for the loaning of an amount of money under agreed terms and charges."
  :SUBSET-OF FinancialProduct
  :AXIOMS (/XMLNS/DC/source LoanOrCredit |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#FIBO|)
  :AXIOMS (/XMLNS/RDFS/label LoanOrCredit "LoanOrCredit"))

(DEFCONCEPT SendAction (?X)
  :DOCUMENTATION "<p>The act of physically/electronically dispatching an object for transfer from an origin to a destination.Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/ReceiveAction\">ReceiveAction</a>: The reciprocal of SendAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/GiveAction\">GiveAction</a>: Unlike GiveAction, SendAction does not imply the transfer of ownership (e.g. I can send you my laptop, but I'm not necessarily giving it to you).</li>
</ul>
"
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label SendAction "SendAction"))

(DEFCONCEPT BoardingPolicyType (?X)
  :DOCUMENTATION "A type of boarding policy used by an airline."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label BoardingPolicyType "BoardingPolicyType"))

(DEFCONCEPT EntertainmentBusiness (?X)
  :DOCUMENTATION "A business providing entertainment."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label EntertainmentBusiness "EntertainmentBusiness"))

(DEFCONCEPT Language (?X)
  :DOCUMENTATION "Natural languages such as Spanish, Tamil, Hindi, English, etc. Formal language code tags expressed in <a href=\"https://en.wikipedia.org/wiki/IETF_language_tag\">BCP 47</a> can be used via the <a class=\"localLink\" href=\"http://schema.org/alternateName\">alternateName</a> property. The Language type previously also covered programming languages such as Scheme and Lisp, which are now best represented using <a class=\"localLink\" href=\"http://schema.org/ComputerLanguage\">ComputerLanguage</a>."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Language "Language"))

(DEFCONCEPT SoftwareApplication (?X)
  :DOCUMENTATION "A software application."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label SoftwareApplication "SoftwareApplication"))

(DEFCONCEPT Flight (?X)
  :DOCUMENTATION "An airline flight."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Flight "Flight"))

(DEFCONCEPT Book (?X)
  :DOCUMENTATION "A book."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Book "Book"))

(DEFCONCEPT MapCategoryType (?X)
  :DOCUMENTATION "An enumeration of several kinds of Map."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label MapCategoryType "MapCategoryType"))

(DEFCONCEPT CourseInstance (?X)
  :DOCUMENTATION "An instance of a <a class=\"localLink\" href=\"http://schema.org/Course\">Course</a> which is distinct from other instances because it is offered at a different time or location or through different media or modes of study or to a specific section of students."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label CourseInstance "CourseInstance"))

(DEFCONCEPT OrderItem (?X)
  :DOCUMENTATION "An order item is a line of an order. It includes the quantity and shipping details of a bought offer."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label OrderItem "OrderItem"))

(DEFCONCEPT ProgramMembership (?X)
  :DOCUMENTATION "Used to describe membership in a loyalty programs (e.g. \"StarAliance\"), traveler clubs (e.g. \"AAA\"), purchase clubs (\"Safeway Club\"), etc."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label ProgramMembership "ProgramMembership"))

(DEFCONCEPT Energy (?X)
  :DOCUMENTATION "Properties that take Energy as values are of the form '&lt;Number&gt; &lt;Energy unit of measure&gt;'."
  :SUBSET-OF Quantity
  :AXIOMS (/XMLNS/RDFS/label Energy "Energy"))

(DEFCONCEPT GameServer (?X)
  :DOCUMENTATION "Server that provides game interaction in a multiplayer game."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label GameServer "GameServer"))

(DEFCONCEPT SteeringPositionValue (?X)
  :DOCUMENTATION "A value indicating a steering position."
  :SUBSET-OF QualitativeValue
  :AXIOMS (/XMLNS/DC/source SteeringPositionValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|)
  :AXIOMS (/XMLNS/RDFS/label SteeringPositionValue "SteeringPositionValue"))

(DEFCONCEPT MovieSeries (?X)
  :DOCUMENTATION "A series of movies. Included movies can be indicated with the hasPart property."
  :SUBSET-OF CreativeWorkSeries
  :AXIOMS (/XMLNS/RDFS/label MovieSeries "MovieSeries"))

(DEFCONCEPT DataType (?X)
  :DOCUMENTATION "The basic data types such as Integers, Strings, etc."
  :SUBSET-OF CONCEPT
  :AXIOMS (/XMLNS/RDFS/label DataType "DataType"))

(DEFCONCEPT DeliveryMethod (?X)
  :DOCUMENTATION "<p>A delivery method is a standardized procedure for transferring the product or service to the destination of fulfillment chosen by the customer. Delivery methods are characterized by the means of transportation used, and by the organization or group that is the contracting party for the sending organization or person.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#DeliveryModeDirectDownload</li>
<li>http://purl.org/goodrelations/v1#DeliveryModeFreight</li>
<li>http://purl.org/goodrelations/v1#DeliveryModeMail</li>
<li>http://purl.org/goodrelations/v1#DeliveryModeOwnFleet</li>
<li>http://purl.org/goodrelations/v1#DeliveryModePickUp</li>
<li>http://purl.org/goodrelations/v1#DHL</li>
<li>http://purl.org/goodrelations/v1#FederalExpress</li>
<li>http://purl.org/goodrelations/v1#UPS</li>
</ul>
"
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/DC/source DeliveryMethod |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label DeliveryMethod "DeliveryMethod"))

(DEFCONCEPT FoodEstablishmentReservation (?X)
  :DOCUMENTATION "A reservation to dine at a food-related business.Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label FoodEstablishmentReservation "FoodEstablishmentReservation"))

(DEFCONCEPT OrderStatus (?X)
  :DOCUMENTATION "Enumerated status values for Order."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label OrderStatus "OrderStatus"))

(DEFCONCEPT Ticket (?X)
  :DOCUMENTATION "Used to describe a ticket to an event, a flight, a bus ride, etc."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Ticket "Ticket"))

(DEFRELATION hasPart (?X ?Y)
  :DOCUMENTATION "Indicates a CreativeWork that is (in some sense) a part of this CreativeWork."
  ;:BINARY-RELATION TRUE
  :AXIOMS (domainIncludes hasPart CreativeWork)
  :AXIOMS (inverseOf hasPart isPartOf)
  :AXIOMS (/XMLNS/DC/source hasPart |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_bibex|)
  :AXIOMS (rangeIncludes hasPart CreativeWork)
  :AXIOMS (/XMLNS/RDFS/label hasPart "hasPart"))

(DEFCONCEPT WebPage (?X)
  :DOCUMENTATION "A web page. Every web page is implicitly assumed to be declared to be of type WebPage, so the various properties about that webpage, such as <code>breadcrumb</code> may be used. We recommend explicit declaration if these properties are specified, but if they are found outside of an itemscope, they will be assumed to be about the page."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label WebPage "WebPage"))

(DEFCONCEPT Landform (?X)
  :DOCUMENTATION "A landform or physical feature.  Landform elements include mountains, plains, lakes, rivers, seascape and oceanic waterbody interface features such as bays, peninsulas, seas and so forth, including sub-aqueous terrain features such as submersed mountain ranges, volcanoes, and the great ocean basins."
  :SUBSET-OF Place
  :AXIOMS (/XMLNS/RDFS/label Landform "Landform"))

(DEFCONCEPT CommentAction (?X)
  :DOCUMENTATION "The act of generating a comment about a subject."
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label CommentAction "CommentAction"))

(DEFCONCEPT FoodEstablishment (?X)
  :DOCUMENTATION "A food-related business."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label FoodEstablishment "FoodEstablishment"))

(DEFCONCEPT WebApplication (?X)
  :DOCUMENTATION "Web applications."
  :SUBSET-OF SoftwareApplication
  :AXIOMS (/XMLNS/RDFS/label WebApplication "WebApplication"))

(DEFCONCEPT PropertyValue (?X)
  :DOCUMENTATION "A property-value pair, e.g. representing a feature of a product or place. Use the 'name' property for the name of the property. If there is an additional human-readable version of the value, put that into the 'description' property.</p>

<p>Always use specific schema.org properties when a) they exist and b) you can populate them. Using PropertyValue as a substitute will typically not trigger the same effect as using the original, specific property."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/DC/source PropertyValue |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|)
  :AXIOMS (/XMLNS/RDFS/label PropertyValue "PropertyValue"))

(DEFCONCEPT GenderType (?X)
  :DOCUMENTATION "An enumeration of genders."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label GenderType "GenderType"))

(DEFCONCEPT AskAction (?X)
  :DOCUMENTATION "<p>The act of posing a question / favor to someone.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/ReplyAction\">ReplyAction</a>: Appears generally as a response to AskAction.</li>
</ul>
"
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label AskAction "AskAction"))

(DEFCONCEPT PlanAction (?X)
  :DOCUMENTATION "The act of planning the execution of an event/task/action/reservation/plan to a future date."
  :SUBSET-OF OrganizeAction
  :AXIOMS (/XMLNS/RDFS/label PlanAction "PlanAction"))

(DEFCONCEPT ReservationStatusType (?X)
  :DOCUMENTATION "Enumerated status values for Reservation."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label ReservationStatusType "ReservationStatusType"))

(DEFCONCEPT OfferCatalog (?X)
  :DOCUMENTATION "An OfferCatalog is an ItemList that contains related Offers and/or further OfferCatalogs that are offeredBy the same provider."
  :SUBSET-OF ItemList
  :AXIOMS (/XMLNS/RDFS/label OfferCatalog "OfferCatalog"))

(DEFCONCEPT BuyAction (?X)
  :DOCUMENTATION "The act of giving money to a seller in exchange for goods or services rendered. An agent buys an object, product, or service from a seller for a price. Reciprocal of SellAction."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label BuyAction "BuyAction"))

(DEFCONCEPT Place (?X)
  :DOCUMENTATION "Entities that have a somewhat fixed, physical extension."
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/RDFS/label Place "Place"))

(DEFCONCEPT Blog (?X)
  :DOCUMENTATION "A blog."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Blog "Blog"))

(DEFCONCEPT PlaceOfWorship (?X)
  :DOCUMENTATION "Place of worship, such as a church, synagogue, or mosque."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label PlaceOfWorship "PlaceOfWorship"))

(DEFCONCEPT ExerciseAction (?X)
  :DOCUMENTATION "The act of participating in exertive activity for the purposes of improving health and fitness."
  :SUBSET-OF PlayAction
  :AXIOMS (/XMLNS/RDFS/label ExerciseAction "ExerciseAction"))

(DEFCONCEPT Event (?X)
  :DOCUMENTATION "An event happening at a certain time and location, such as a concert, lecture, or festival. Ticketing information may be added via the <a class=\"localLink\" href=\"http://schema.org/offers\">offers</a> property. Repeated events may be structured as separate Event objects."
  :EQUIVALENT-RELATION |http://purl.org/dc/dcmitype/Event|
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/RDFS/label Event "Event"))

(DEFCONCEPT Quantity (?X)
  :DOCUMENTATION "Quantities such as distance, time, mass, weight, etc. Particular instances of say Mass are entities like '3 Kg' or '4 milligrams'."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Quantity "Quantity"))

(DEFCONCEPT InteractionCounter (?X)
  :DOCUMENTATION "A summary of how users have interacted with this CreativeWork. In most cases, authors will use a subtype to specify the specific type of interaction."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label InteractionCounter "InteractionCounter"))

(DEFCONCEPT UserComments (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF UserInteraction
  :AXIOMS (/XMLNS/DC/source UserComments |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_rNews|)
  :AXIOMS (/XMLNS/RDFS/label UserComments "UserComments")
  :AXIOMS (supersededBy UserComments InteractionCounter))

(DEFCONCEPT APIReference (?X)
  :DOCUMENTATION "Reference documentation for application programming interfaces (APIs)."
  :SUBSET-OF TechArticle
  :AXIOMS (/XMLNS/RDFS/label APIReference "APIReference"))

(DEFCONCEPT ItemAvailability (?X)
  :DOCUMENTATION "A list of possible product availability options."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label ItemAvailability "ItemAvailability"))

(DEFCONCEPT GamePlayMode (?X)
  :DOCUMENTATION "Indicates whether this game is multi-player, co-op or single-player."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label GamePlayMode "GamePlayMode"))

(DEFCONCEPT RsvpAction (?X)
  :DOCUMENTATION "The act of notifying an event organizer as to whether you expect to attend the event."
  :SUBSET-OF InformAction
  :AXIOMS (/XMLNS/RDFS/label RsvpAction "RsvpAction"))

(DEFCONCEPT HomeAndConstructionBusiness (?X)
  :DOCUMENTATION "A construction business.</p>

<p>A HomeAndConstructionBusiness is a <a class=\"localLink\" href=\"http://schema.org/LocalBusiness\">LocalBusiness</a> that provides services around homes and buildings.</p>

<p>As a <a class=\"localLink\" href=\"http://schema.org/LocalBusiness\">LocalBusiness</a> it can be described as a <a class=\"localLink\" href=\"http://schema.org/provider\">provider</a> of one or more <a class=\"localLink\" href=\"http://schema.org/Service\">Service</a>(s)."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label HomeAndConstructionBusiness "HomeAndConstructionBusiness"))

(DEFCONCEPT CookAction (?X)
  :DOCUMENTATION "The act of producing/preparing food."
  :SUBSET-OF CreateAction
  :AXIOMS (/XMLNS/RDFS/label CookAction "CookAction"))

(DEFCONCEPT Suite (?X)
  :DOCUMENTATION "A suite in a hotel or other public accommodation, denotes a class of luxury accommodations, the key feature of which is multiple rooms (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/Suite_(hotel)\">http://en.wikipedia.org/wiki/Suite_(hotel)</a>).
<br /><br />
See also the <a href=\"/docs/hotels.html\">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>."
  :SUBSET-OF Accommodation
  :AXIOMS (/XMLNS/RDFS/label Suite "Suite")
  :AXIOMS (/XMLNS/DC/source Suite |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFCONCEPT CreateAction (?X)
  :DOCUMENTATION "The act of deliberately creating/producing/generating/building a result out of the agent."
  :SUBSET-OF Action
  :AXIOMS (/XMLNS/RDFS/label CreateAction "CreateAction"))

(DEFCONCEPT Vehicle (?X)
  :DOCUMENTATION "A vehicle is a device that is designed or used to transport people or cargo over land, water, air, or through space."
  :SUBSET-OF Product
  :AXIOMS (/XMLNS/RDFS/label Vehicle "Vehicle"))

(DEFCONCEPT Mass (?X)
  :DOCUMENTATION "Properties that take Mass as values are of the form '&lt;Number&gt; &lt;Mass unit of measure&gt;'. E.g., '7 kg'."
  :SUBSET-OF Quantity
  :AXIOMS (/XMLNS/RDFS/label Mass "Mass"))

(DEFCONCEPT PaymentMethod (?X)
  :DOCUMENTATION "<p>A payment method is a standardized procedure for transferring the monetary amount for a purchase. Payment methods are characterized by the legal and technical structures used, and by the organization or group carrying out the transaction.</p>

<p>Commonly used values:</p>

<ul>
<li>http://purl.org/goodrelations/v1#ByBankTransferInAdvance</li>
<li>http://purl.org/goodrelations/v1#ByInvoice</li>
<li>http://purl.org/goodrelations/v1#Cash</li>
<li>http://purl.org/goodrelations/v1#CheckInAdvance</li>
<li>http://purl.org/goodrelations/v1#COD</li>
<li>http://purl.org/goodrelations/v1#DirectDebit</li>
<li>http://purl.org/goodrelations/v1#GoogleCheckout</li>
<li>http://purl.org/goodrelations/v1#PayPal</li>
<li>http://purl.org/goodrelations/v1#PaySwarm</li>
</ul>
"
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label PaymentMethod "PaymentMethod")
  :AXIOMS (/XMLNS/DC/source PaymentMethod |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT PostalAddress (?X)
  :DOCUMENTATION "The mailing address."
  :SUBSET-OF ContactPoint
  :AXIOMS (/XMLNS/RDFS/label PostalAddress "PostalAddress"))

(DEFCONCEPT Answer (?X)
  :DOCUMENTATION "An answer offered to a question; perhaps correct, perhaps opinionated or wrong."
  :SUBSET-OF Comment
  :AXIOMS (/XMLNS/RDFS/label Answer "Answer")
  :AXIOMS (/XMLNS/DC/source Answer |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_QAStackExchange|))

(DEFCONCEPT CollectionPage (?X)
  :DOCUMENTATION "Web page type: Collection page."
  :SUBSET-OF WebPage
  :AXIOMS (/XMLNS/RDFS/label CollectionPage "CollectionPage"))

(DEFCONCEPT VideoGame (?X)
  :DOCUMENTATION "A video game is an electronic game that involves human interaction with a user interface to generate visual feedback on a video device."
  :SUBSET-OF Game
  :SUBSET-OF SoftwareApplication
  :AXIOMS (/XMLNS/RDFS/label VideoGame "VideoGame"))

(DEFCONCEPT EngineSpecification (?X)
  :DOCUMENTATION "Information about the engine of the vehicle. A vehicle can have multiple engines represented by multiple engine specification entities."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label EngineSpecification "EngineSpecification")
  :AXIOMS (/XMLNS/DC/source EngineSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#Automotive_Ontology_Working_Group|))

(DEFCONCEPT OpeningHoursSpecification (?X)
  :DOCUMENTATION "A structured value providing information about the opening hours of a place or a certain service inside a place.</p>

<p>The place is <strong>open</strong> if the <a class=\"localLink\" href=\"http://schema.org/opens\">opens</a> property is specified, and <strong>closed</strong> otherwise.</p>

<p>If the value for the <a class=\"localLink\" href=\"http://schema.org/closes\">closes</a> property is less than the value for the <a class=\"localLink\" href=\"http://schema.org/opens\">opens</a> property then the hour range is assumed to span over the next day."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label OpeningHoursSpecification "OpeningHoursSpecification")
  :AXIOMS (/XMLNS/DC/source OpeningHoursSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT Apartment (?X)
  :DOCUMENTATION "An apartment (in American English) or flat (in British English) is a self-contained housing unit (a type of residential real estate) that occupies only part of a building (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/Apartment\">http://en.wikipedia.org/wiki/Apartment</a>)."
  :SUBSET-OF Accommodation
  :AXIOMS (/XMLNS/RDFS/label Apartment "Apartment")
  :AXIOMS (/XMLNS/DC/source Apartment |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|))

(DEFCONCEPT ProductModel (?X)
  :DOCUMENTATION "A datasheet or vendor specification of a product (in the sense of a prototypical description)."
  :SUBSET-OF Product
  :AXIOMS (/XMLNS/RDFS/label ProductModel "ProductModel")
  :AXIOMS (/XMLNS/DC/source ProductModel |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT Photograph (?X)
  :DOCUMENTATION "A photograph."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Photograph "Photograph"))

(DEFCONCEPT UseAction (?X)
  :DOCUMENTATION "The act of applying an object to its intended purpose."
  :SUBSET-OF ConsumeAction
  :AXIOMS (/XMLNS/RDFS/label UseAction "UseAction"))

(DEFCONCEPT Clip (?X)
  :DOCUMENTATION "A short TV or radio program or a segment/part of a program."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Clip "Clip"))

(DEFCONCEPT EmployeeRole (?X)
  :DOCUMENTATION "A subclass of OrganizationRole used to describe employee relationships."
  :SUBSET-OF OrganizationRole
  :AXIOMS (/XMLNS/RDFS/label EmployeeRole "EmployeeRole"))

(DEFCONCEPT LocationFeatureSpecification (?X)
  :DOCUMENTATION "Specifies a location feature by providing a structured value representing a feature of an accommodation as a property-value pair of varying degrees of formality."
  :SUBSET-OF PropertyValue
  :AXIOMS (/XMLNS/DC/source LocationFeatureSpecification |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label LocationFeatureSpecification "LocationFeatureSpecification"))

(DEFCONCEPT Integer (?X)
  :DOCUMENTATION "Data type: Integer."
  :SUBSET-OF Number
  :AXIOMS (/XMLNS/RDFS/label Integer "Integer"))

(DEFCONCEPT PerformanceRole (?X)
  :DOCUMENTATION "A PerformanceRole is a Role that some entity places with regard to a theatrical performance, e.g. in a Movie, TVSeries etc."
  :SUBSET-OF Role
  :AXIOMS (/XMLNS/RDFS/label PerformanceRole "PerformanceRole"))

(DEFCONCEPT DataFeed (?X)
  :DOCUMENTATION "A single feed providing structured information about one or more entities or topics."
  :SUBSET-OF Dataset
  :AXIOMS (/XMLNS/RDFS/label DataFeed "DataFeed"))

(DEFCONCEPT OwnershipInfo (?X)
  :DOCUMENTATION "A structured value providing information about when a certain organization or person owned a certain product."
  :SUBSET-OF StructuredValue
  :AXIOMS (/XMLNS/RDFS/label OwnershipInfo "OwnershipInfo")
  :AXIOMS (/XMLNS/DC/source OwnershipInfo |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT MobileApplication (?X)
  :DOCUMENTATION "A software application designed specifically to work well on a mobile device such as a telephone."
  :SUBSET-OF SoftwareApplication
  :AXIOMS (/XMLNS/RDFS/label MobileApplication "MobileApplication"))

(DEFRELATION subOrganization (?X ?Y)
  :DOCUMENTATION "A relationship between two organizations where the first includes the second, e.g., as a subsidiary. See also: the more specific 'department' property."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label subOrganization "subOrganization")
  :AXIOMS (inverseOf subOrganization parentOrganization)
  :AXIOMS (rangeIncludes subOrganization Organization)
  :AXIOMS (domainIncludes subOrganization Organization))

(DEFCONCEPT InviteAction (?X)
  :DOCUMENTATION "The act of asking someone to attend an event. Reciprocal of RsvpAction."
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label InviteAction "InviteAction"))

(DEFRELATION containsSeason (?X ?Y)
  :DOCUMENTATION "A season that is part of the media series."
  ;:BINARY-RELATION TRUE
  :SUBSET-OF hasPart
  :AXIOMS (domainIncludes containsSeason RadioSeries)
  :AXIOMS (rangeIncludes containsSeason CreativeWorkSeason)
  :AXIOMS (domainIncludes containsSeason TVSeries)
  :AXIOMS (domainIncludes containsSeason VideoGameSeries)
  :AXIOMS (/XMLNS/RDFS/label containsSeason "containsSeason"))

(DEFCONCEPT Airport (?X)
  :DOCUMENTATION "An airport."
  :SUBSET-OF CivicStructure
  :AXIOMS (/XMLNS/RDFS/label Airport "Airport"))

(DEFCONCEPT RealEstateAgent (?X)
  :DOCUMENTATION "A real-estate agent."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label RealEstateAgent "RealEstateAgent"))

(DEFCONCEPT TravelAction (?X)
  :DOCUMENTATION "The act of traveling from an fromLocation to a destination by a specified mode of transport, optionally with participants."
  :SUBSET-OF MoveAction
  :AXIOMS (/XMLNS/RDFS/label TravelAction "TravelAction"))

(DEFCONCEPT SportsTeam (?X)
  :DOCUMENTATION "Organization: Sports team."
  :SUBSET-OF SportsOrganization
  :AXIOMS (/XMLNS/RDFS/label SportsTeam "SportsTeam"))

(DEFCONCEPT Text (?X)
  :DOCUMENTATION "Data type: Text."
  :AXIOMS (DataType Text)
  :AXIOMS (/XMLNS/RDFS/label Text "Text"))

(DEFCONCEPT RentalCarReservation (?X)
  :DOCUMENTATION "A reservation for a rental car.</p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label RentalCarReservation "RentalCarReservation"))

(DEFCONCEPT SportsOrganization (?X)
  :DOCUMENTATION "Represents the collection of all sports organizations, including sports teams, governing bodies, and sports associations."
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label SportsOrganization "SportsOrganization"))

(DEFCONCEPT Distance (?X)
  :DOCUMENTATION "Properties that take Distances as values are of the form '&lt;Number&gt; &lt;Length unit of measure&gt;'. E.g., '7 ft'."
  :SUBSET-OF Quantity
  :AXIOMS (/XMLNS/RDFS/label Distance "Distance"))

(DEFCONCEPT SocialMediaPosting (?X)
  :DOCUMENTATION "A post to a social media platform, including blog posts, tweets, Facebook posts, etc."
  :SUBSET-OF Article
  :AXIOMS (/XMLNS/RDFS/label SocialMediaPosting "SocialMediaPosting"))

(DEFCONCEPT Episode (?X)
  :DOCUMENTATION "A media episode (e.g. TV, radio, video game) which can be part of a series or season."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label Episode "Episode"))

(DEFCONCEPT RentAction (?X)
  :DOCUMENTATION "The act of giving money in return for temporary use, but not ownership, of an object such as a vehicle or property. For example, an agent rents a property from a landlord in exchange for a periodic payment."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label RentAction "RentAction"))

(DEFCONCEPT Offer (?X)
  :DOCUMENTATION "An offer to transfer some rights to an item or to provide a service — for example, an offer to sell tickets to an event, to rent the DVD of a movie, to stream a TV show over the internet, to repair a motorcycle, or to loan a book.</p>

<p>For <a href=\"http://www.gs1.org/barcodes/technical/idkeys/gtin\">GTIN</a>-related fields, see <a href=\"http://www.gs1.org/barcodes/support/check_digit_calculator\">Check Digit calculator</a> and <a href=\"http://www.gs1us.org/resources/standards/gtin-validation-guide\">validation guide</a> from <a href=\"http://www.gs1.org/\">GS1</a>."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label Offer "Offer")
  :AXIOMS (/XMLNS/DC/source Offer |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsTerms|))

(DEFCONCEPT UserInteraction (?X)
  :DOCUMENTATION "UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use <a class=\"localLink\" href=\"http://schema.org/Action\">Action</a>-based vocabulary, alongside types such as <a class=\"localLink\" href=\"http://schema.org/Comment\">Comment</a>."
  :SUBSET-OF Event
  :AXIOMS (supersededBy UserInteraction InteractionCounter)
  :AXIOMS (/XMLNS/RDFS/label UserInteraction "UserInteraction"))

(DEFCONCEPT PaymentStatusType (?X)
  :DOCUMENTATION "A specific payment status. For example, PaymentDue, PaymentComplete, etc."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label PaymentStatusType "PaymentStatusType"))

(DEFCONCEPT VideoGameSeries (?X)
  :DOCUMENTATION "A video game series."
  :SUBSET-OF CreativeWorkSeries
  :AXIOMS (/XMLNS/RDFS/label VideoGameSeries "VideoGameSeries"))

(DEFCONCEPT ChooseAction (?X)
  :DOCUMENTATION "The act of expressing a preference from a set of options or a large or unbounded set of choices/options."
  :SUBSET-OF AssessAction
  :AXIOMS (/XMLNS/RDFS/label ChooseAction "ChooseAction"))

(DEFCONCEPT ItemListOrderType (?X)
  :DOCUMENTATION "Enumerated for values for itemListOrder for indicating how an ordered ItemList is organized."
  :SUBSET-OF Enumeration
  :AXIOMS (/XMLNS/RDFS/label ItemListOrderType "ItemListOrderType"))

(DEFCONCEPT OrderAction (?X)
  :DOCUMENTATION "An agent orders an object/product/service to be delivered/sent."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label OrderAction "OrderAction"))

(DEFCONCEPT PayAction (?X)
  :DOCUMENTATION "An agent pays a price to a participant."
  :SUBSET-OF TradeAction
  :AXIOMS (/XMLNS/RDFS/label PayAction "PayAction"))

(DEFCONCEPT Store (?X)
  :DOCUMENTATION "A retail good store."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label Store "Store"))

(DEFCONCEPT MusicAlbum (?X)
  :DOCUMENTATION "A collection of music tracks."
  :SUBSET-OF MusicPlaylist
  :AXIOMS (/XMLNS/RDFS/label MusicAlbum "MusicAlbum"))

(DEFCONCEPT InformAction (?X)
  :DOCUMENTATION "The act of notifying someone of information pertinent to them, with no expectation of a response."
  :SUBSET-OF CommunicateAction
  :AXIOMS (/XMLNS/RDFS/label InformAction "InformAction"))

(DEFCONCEPT PropertyValueSpecification (?X)
  :DOCUMENTATION "A Property value specification."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/DC/source PropertyValueSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_ActionCollabClass|)
  :AXIOMS (/XMLNS/RDFS/label PropertyValueSpecification "PropertyValueSpecification"))

(DEFCONCEPT DeliveryEvent (?X)
  :DOCUMENTATION "An event involving the delivery of an item."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label DeliveryEvent "DeliveryEvent"))

(DEFCONCEPT ImageObject (?X)
  :DOCUMENTATION "An image file."
  :EQUIVALENT-RELATION |http://purl.org/dc/dcmitype/Image|
  :SUBSET-OF MediaObject
  :AXIOMS (/XMLNS/RDFS/label ImageObject "ImageObject"))

(DEFCONCEPT Organization (?X)
  :DOCUMENTATION "An organization such as a school, NGO, corporation, club, etc."
  :SUBSET-OF Thing
  :AXIOMS (/XMLNS/RDFS/label Organization "Organization"))

(DEFCONCEPT LocalBusiness (?X)
  :DOCUMENTATION "A particular physical business or branch of an organization. Examples of LocalBusiness include a restaurant, a particular branch of a restaurant chain, a branch of a bank, a medical practice, a club, a bowling alley, etc."
  :SUBSET-OF Place
  :SUBSET-OF Organization
  :AXIOMS (/XMLNS/RDFS/label LocalBusiness "LocalBusiness"))

(DEFCONCEPT GiveAction (?X)
  :DOCUMENTATION "<p>The act of transferring ownership of an object to a destination. Reciprocal of TakeAction.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/TakeAction\">TakeAction</a>: Reciprocal of GiveAction.</li>
<li><a class=\"localLink\" href=\"http://schema.org/SendAction\">SendAction</a>: Unlike SendAction, GiveAction implies that ownership is being transferred (e.g. I may send my laptop to you, but that doesn't mean I'm giving it to you).</li>
</ul>
"
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label GiveAction "GiveAction"))

(DEFCONCEPT Report (?X)
  :DOCUMENTATION "A Report generated by governmental or non-governmental organization."
  :SUBSET-OF Article
  :AXIOMS (/XMLNS/RDFS/label Report "Report"))

(DEFCONCEPT WinAction (?X)
  :DOCUMENTATION "The act of achieving victory in a competitive activity."
  :SUBSET-OF AchieveAction
  :AXIOMS (/XMLNS/RDFS/label WinAction "WinAction"))

(DEFCONCEPT Duration (?X)
  :DOCUMENTATION "Quantity: Duration (use <a href=\"http://en.wikipedia.org/wiki/ISO_8601\">ISO 8601 duration format</a>)."
  :SUBSET-OF Quantity
  :AXIOMS (/XMLNS/RDFS/label Duration "Duration"))

(DEFCONCEPT HealthAndBeautyBusiness (?X)
  :DOCUMENTATION "Health and beauty."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label HealthAndBeautyBusiness "HealthAndBeautyBusiness"))

(DEFCONCEPT TaxiReservation (?X)
  :DOCUMENTATION "A reservation for a taxi.</p>

<p>Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use <a class=\"localLink\" href=\"http://schema.org/Offer\">Offer</a>."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label TaxiReservation "TaxiReservation"))

(DEFCONCEPT FoodEvent (?X)
  :DOCUMENTATION "Event type: Food event."
  :SUBSET-OF Event
  :AXIOMS (/XMLNS/RDFS/label FoodEvent "FoodEvent"))

(DEFCONCEPT DatedMoneySpecification (?X)
  :DOCUMENTATION "A DatedMoneySpecification represents monetary values with optional start and end dates. For example, this could represent an employee's salary over a specific period of time. <strong>Note:</strong> This type has been superseded by <a class=\"localLink\" href=\"http://schema.org/MonetaryAmount\">MonetaryAmount</a> use of that type is recommended"
  :SUBSET-OF StructuredValue
  :AXIOMS (supersededBy DatedMoneySpecification MonetaryAmount)
  :AXIOMS (/XMLNS/RDFS/label DatedMoneySpecification "DatedMoneySpecification"))

(DEFCONCEPT ReplaceAction (?X)
  :DOCUMENTATION "The act of editing a recipient by replacing an old object with a new object."
  :SUBSET-OF UpdateAction
  :AXIOMS (/XMLNS/RDFS/label ReplaceAction "ReplaceAction"))

(DEFCONCEPT PaymentChargeSpecification (?X)
  :DOCUMENTATION "The costs of settling the payment using a particular payment method."
  :SUBSET-OF PriceSpecification
  :AXIOMS (/XMLNS/RDFS/label PaymentChargeSpecification "PaymentChargeSpecification")
  :AXIOMS (/XMLNS/DC/source PaymentChargeSpecification |http://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#source_GoodRelationsClass|))

(DEFCONCEPT ReservationPackage (?X)
  :DOCUMENTATION "A group of multiple reservations with common values for all sub-reservations."
  :SUBSET-OF Reservation
  :AXIOMS (/XMLNS/RDFS/label ReservationPackage "ReservationPackage"))

(DEFCONCEPT LodgingBusiness (?X)
  :DOCUMENTATION "A lodging business, such as a motel, hotel, or inn."
  :SUBSET-OF LocalBusiness
  :AXIOMS (/XMLNS/RDFS/label LodgingBusiness "LodgingBusiness"))

(DEFCONCEPT LendAction (?X)
  :DOCUMENTATION "<p>The act of providing an object under an agreement that it will be returned at a later date. Reciprocal of BorrowAction.</p>

<p>Related actions:</p>

<ul>
<li><a class=\"localLink\" href=\"http://schema.org/BorrowAction\">BorrowAction</a>: Reciprocal of LendAction.</li>
</ul>
"
  :SUBSET-OF TransferAction
  :AXIOMS (/XMLNS/RDFS/label LendAction "LendAction"))

(DEFCONCEPT MediaObject (?X)
  :DOCUMENTATION "A media object, such as an image, video, or audio object embedded in a web page or a downloadable dataset i.e. DataDownload. Note that a creative work may have many media objects associated with it on the same web page. For example, a page about a single song (MusicRecording) may have a music video (VideoObject), and a high and low bandwidth audio stream (2 AudioObject's)."
  :SUBSET-OF CreativeWork
  :AXIOMS (/XMLNS/RDFS/label MediaObject "MediaObject"))

(DEFRELATION containsPlace (?X ?Y)
  :DOCUMENTATION "The basic containment relation between a place and another that it contains."
  ;:BINARY-RELATION TRUE
  :AXIOMS (/XMLNS/RDFS/label containsPlace "containsPlace")
  :AXIOMS (rangeIncludes containsPlace Place)
  :AXIOMS (inverseOf containsPlace containedInPlace)
  :AXIOMS (domainIncludes containsPlace Place))

(DEFCONCEPT House (?X)
  :DOCUMENTATION "A house is a building or structure that has the ability to be occupied for habitation by humans or other creatures (Source: Wikipedia, the free encyclopedia, see <a href=\"http://en.wikipedia.org/wiki/House\">http://en.wikipedia.org/wiki/House</a>)."
  :SUBSET-OF Accommodation
  :AXIOMS (/XMLNS/DC/source House |https://www.w3.org/wiki/WebSchemas/SchemaDotOrgSources#STI_Accommodation_Ontology|)
  :AXIOMS (/XMLNS/RDFS/label House "House"))

(DEFCONCEPT ListItem (?X)
  :DOCUMENTATION "An list item, e.g. a step in a checklist or how-to description."
  :SUBSET-OF Intangible
  :AXIOMS (/XMLNS/RDFS/label ListItem "ListItem"))
